# Builds the main branch. This project is not published — see build.gradle.kts.
name: Build main

on:
  push:
    branches:
      - main

jobs:
  build:
    uses: xemantic/.github/.github/workflows/build-gradle.yml@main
    secrets: inherit
    with:
      gradle_args: build
