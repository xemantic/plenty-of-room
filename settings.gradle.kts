rootProject.name = "plenty-of-room"

// The library module: the part of this repository whose value is not the corpus.
// See LIBRARY.md for what goes in it and why; the corpus (studies, gpd/, tools/) stays here.
include("origami-engine")
