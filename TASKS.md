# TASKS — the queue

Live state of the loop.
Priority rule: **process blockers (`P-*`) outrank science tasks, and cheap bounds outrank expensive calculations.**
This overrides the default pull toward whatever has the best ROI.

Status vocabulary: `TODO` · `IN PROGRESS` · `BLOCKED` · `DONE` · `KILLED` (branch abandoned, with reason).

IDs: `T*` are the eight tasks of the problem definition §6, `P*` are process tasks we raised ourselves.
The `Leaf` column is the NDI `simulation-task-map` ID the work traces to.

## Process blockers

| ID | Task | Status | Notes |
|---|---|---|---|
| P-1 | GPD loop skeleton: `SESSION-PROMPT.md`, `JOURNAL.md`, `TASKS.md`, `gpd/{tasks,results,claims,challenges}` | DONE | Iteration 1 |
| P-2 | Locked units and constants module, plus the machine-readable result envelope every task writes through | DONE | Iteration 1 — `src/main/kotlin/Physics.kt`, and the `StudyResult` envelope that carries units, conventions, validity and every run parameter alongside the numbers |
| P-3 | PEG material parameter sheet: monomer size, Kuhn length, χ(T, salt), excluded volume, mass density, with provenance per number and a `derived`/`cited` flag | **DONE** (iteration 2) | Claim `C-0002`. `a = 0.35 nm` closed (derived + fitted); the measured osmotic EOS adopted; χ named as *not determined* and split off as `P-6`. Raised `CH-0001`. |
| P-4 | Volume-fraction bookkeeping: locate the crossover for *this* layer rather than quoting φ ≈ 0.2–0.3 | **DONE** (iteration 2) | Answered, and the answer inverted the question: the binding crossover is the **dilute→semidilute** one at φ# ≈ 0.026, approached from *below*, not the semidilute→concentrated one. The layer sits at φ/φ# = 1.08–1.23. `C-0002`. |
| P-5 | Decide and defend the brush-regime criterion (`Σ ≥ 5` vs `Σ > 1`) | **CLOSED** (iteration 3) | **`L₀/R₀ ≥ 1` is exactly vacuous, not merely weak**: it admits **all 183** points of the Gen-1 sweep, including a layer at `Σ = 0.063` with 22 nm grafting spacing — asserted as a test in `C-0016`. **`Σ = πR₀²σ ≥ 1` is the only criterion that bounds anything**, and it owns the lower edge of every surviving window. **Four brush criteria have now failed in this project**, each a convention asked to do a measurement's work: `Σ ≥ 5` failed thermodynamically (`CH-0001`) and geometrically (`CH-0003`), and `L₀/R₀ ≥ 1` — adopted by `T-1c` to replace it — failed twice over, first as unable to bound from below (`C-0011`) and then as vacuous (`C-0016`). |
| P-7 | Build isolation for concurrent agents: `-PbuildDirectory=<dir>` so parallel runs of one checkout stop racing on `build/test-results` | **DONE** (iteration 3) | Raised as a process blocker mid-iteration: four agents sharing one working tree could not get an authoritative `./gradlew test`, and the failure (`EOFException`, `NoSuchFileException` on the in-progress results binary) looks like a broken test rather than a broken harness. `build-*/` is git-ignored. |
| P-10 | Verification isolation at high agent concurrency: `-PbuildDirectory` alone is not sufficient | **DONE** (iteration 3) | Raised by `T-8`, which lost **fourteen** full-suite attempts to it. `P-7`'s per-agent build directory is necessary but not sufficient — the Gradle **project lock**, `~/.gradle` and the Kotlin daemon are still shared, and the incremental compiler's session state races, producing `NoClassDefFoundError` on classes nobody touched. Fixed by [`tools/verify.sh`](tools/verify.sh), which runs the suite on an isolated copy of the tree (`--committed` archives `HEAD` instead, which is what the coordinator needs before pushing). |
| P-12 | Fold the multi-agent harness workarounds into `tools/` | **DONE** (iteration 4) | Taken **first** in a parallel iteration, overriding its own low ROI ranking, because `SESSION-PROMPT.md` puts process blockers above cheap wins and three concurrent agents were about to be started. [`tools/study.sh`](tools/study.sh) runs one study on an **isolated copy** and copies back only the changed `gpd/results/` files — `-PbuildDirectory` isolates `build-*/test-results` but **not** `build-*/classes`, and `T-1d`'s sweep is 33 minutes of that exposure. [`tools/verify.sh`](tools/verify.sh) gains `--drop <pkg>`, removing a package left mid-TDD by another agent — the **third** cause of `NoClassDefFoundError`, raised by `C-0015`. |
| P-11 | Collapse the two `χ` transfer conventions into one derived map | The source's SCF free energy read, replacing the 0.089 gap between the ratio and offset transfers with a derived relation | TODO — **low, non-blocking**. Raised by `C-0013`. The Supporting Information (SCF equations) is paywalled at ACS and absent from the free NIST copy of the body. |
| P-13 | The electrode material and its potential of zero charge — a **specification** gap, not a modelling one | TODO — **low cost, high leverage** | Raised by `C-0021`. §1 says "patterned electrode" and never says of what: metal against oxide is **2.6×** on the one hold-down that cannot be designed away (`C-0021`). And a contact potential of **0.9–5.1 mV** supplies the entire thermal-scale hold-down, which no §3 parameter fixes. **One question to Kazik, not a calculation** — carried to the open-questions section below. |
| P-14 | The charge presented by the **cut rim** of a DNA-origami sheet | TODO — **medium** | Raised by `C-0022`, where the declared falsifier fired: taking the rim from uncharged to the face areal density moves the fitted edge depth from −0.291 to −0.158, a **1.85×** bracket. The tile's charge is volumetric and the surface it is smeared onto is a convention, so both readings are defensible and neither is sourced. Cheaper than it looks — a geometry question about helix ends, not a measurement. |
| P-16 | `tools/verify.sh --drop` at FILE granularity | **DONE** (iteration 5) | Taken **first**, ahead of every science task, because three agents were about to be started against one checkout. `--drop-file <repo-relative path>` added to [`tools/snapshot.sh`](tools/snapshot.sh) and wired into both [`tools/verify.sh`](tools/verify.sh) and [`tools/study.sh`](tools/study.sh), with `--drop <pkg>` demoted to the fallback it should always have been. Both deleting helpers now carry executable tests — [`tools/test-snapshot.sh`](tools/test-snapshot.sh), 19 checks — which is the discipline the Kotlin side already runs under, applied to the harness that protects it; `drop_files` additionally refuses an absolute path, a `..` escape and a directory argument, none of which a *package* name could express. **A second harness fix travelled with it**: `kotlin.daemon.jvmargs=-Xmx3g` moved into the checkout's own `gradle.properties`, so every snapshot inherits the documented cure for the daemon OOM rather than each agent remembering to patch its copy. |
| P-15 | Repair `bracketedRoot`'s Illinois sign test | **DONE** (iteration 5) — claim `C-0031`, raises `CH-0043` | **There were two defects and the reported one is not the one that was costing anything.** The product sign test is real — and it also has a second half `C-0019` did not name, the *entry* test accepting a bracket with no root — but a product underflows only once **both** factors are below `1.5e-154`, and a direct probe of the unrepaired routine on residuals spanning 30 decades escapes **zero** times. What was active on **every** call is that **the Illinois halving was unconditional**: Dowell & Jarratt (read directly) halve only when the *older* endpoint is retained, and halving every step deflates both residuals so they interpolate to the midpoint — the method silently became **bisection while still paying for a secant**, 52 evaluations against bisection's ~52 on `x²−2` where correct Illinois takes **11**. The doc comment's "eightfold saving" was the whole justification for the method and it was delivering a *loss*. The two are coupled: the halving is the **amplifier** of the underflow. Diagnosis closes as a **theorem** — a secant through opposite-signed ordinates is a convex combination, so an escape *proves* a lost bracket. **Re-run and diff: no verdict, no window edge and no quoted figure moves anywhere.** Movement is one of exactly three things — one ulp (`T-1c`: 11 281 of 11 336 fields at a median `4.3e−16`), the SCF's own declared `HEIGHT_TOLERANCE = 1e-6` (`T-1f`: 589 fields, median `9.0e−7`), or a quantity that is **identically zero** (`T-1c`'s `equilibriumStiffness` at `L₀`, `\|value\| ≤ 2.0e−11 pN/nm`; `T-16`/`T-23`'s reproduction residuals at `~1e−9`). |
| P-17 | The three remaining product-based sign tests | TODO — **low, and deliberately ranked rather than fixed** | Raised by `C-0031`. `FluctuationCorrectionStudy.kt:332,339`, `CoupledStandoffJoint.kt:276` and `StandoffBaseJoint.kt:224` all test a sign as a product. **None is reachable**: in each the fixed factor is `O(1)`, and a product underflows only when *both* factors are tiny. They are not changed because changing code that produces published results costs a re-run and a diff of everything downstream, and that price is only worth paying for a defect that can fire. The reachability analysis is in `C-0031` Part 3 so the next agent inherits the ranking rather than the alarm. |
| P-18 | Round result files to the precision the answer is DETERMINED to | TODO — **medium, and it touches every result file in the repository** | Raised by `CH-0043`. The tree rounds to **nine** significant digits; `SelfConsistentFieldLayer.heightAtPressure` carries `HEIGHT_TOLERANCE = 1e-6`, so anything downstream of a solved height is printed three digits past what it is pinned to. The file is then **reproducible without being determinate**: a byte-for-byte diff certifies that nothing perturbed the iteration — a property of the *path* — and any solver improvement becomes indistinguishable from a physics change and has to be re-adjudicated by hand, which is what `P-15` cost. Two directions: carry the rounding **down** to the determined precision (free, costs only the appearance of precision) or carry `HEIGHT_TOLERANCE` **up** to the printed one (honest, costs compute). Neither done in `P-15` because both re-emit every result file. |
| P-8 | Mg²⁺/PEG coordination constant in water | TODO — medium | Raised by `C-0005`. PEG's ether oxygens coordinate cations — the mechanism behind PEO polymer electrolytes — and this is the **only** mechanism that could flip the sign of the §4(c) answer, since `T-6`'s partitioning bound counts exclusion only. **`P-6` searched independently and confirms the number does not exist in accessible literature**: the mechanism is stated for the right system in water, with MgCl₂ in the salt list, but no constant, and the quantitative multivalent-cation/PEO NMR work is in **methanol**. Needs a paywalled pull or an experiment. |
| P-9 | **The effective `χ` of a *grafted* PEG layer is not the bulk one** — bound it, or declare `C-0002`'s bulk equation of state inapplicable to a brush | **DONE** (iteration 3) — **(a) inapplicable**; `C-0002`'s bulk equation of state **stands** | Claim `C-0013`, challenge `CH-0012`. **`χ ≈ 0.60` is not in the source.** Its fits are 0.789/0.852 on a scale whose *own* theta is **0.696** — the 0.60 was `1.2 × ½`, the ratio transferred onto the wrong axis, and the paper's disclaimer forbidding exactly that step is on the same page as the number. The system is an **air/D₂O Langmuir monolayer under *lateral* compression**, not a solid-grafted layer under normal compression. Its grafting densities **do** overlap the Gen-1 window, so the task closed on the *parameter*, not on system mismatch. Bounded independently at **`\|Δχ\| ≤ 0.053`** from normal-compression fits on grafted PEG at 1.5–2.5× the Gen-1 density: −11.4 % to +4.3 % in stiffness, inside `C-0003`'s own ±22 % bracket. |
| P-6 | `χ(T, salt)` and the Mg²⁺ salting-out coefficient for PEG/water at 2–10 mM | **DONE** (iteration 3) | Claim `C-0007`, challenge `CH-0006`. **The buffer does not reach the layer's mechanics**: the mobile-ion channel is exactly zero, and the solvent-quality channel is ≤ 0.4 % of the modulus over 2–10 mM. The Mg²⁺ coefficient is **not determinable and probably not well posed** — `θ(c)` shows *minima* for Group II chlorides, and PEG forms no binodal with MgCl₂ at all, so no ATPS-derived coefficient can exist. Bounded by a threshold instead: MgCl₂ would need `k_s ≥ 92.8 K/M`, 1.35× above the ceiling any PEO salt reaches. |

## Science tasks

| ID | Task | Acceptance (abridged — full text in `gpd/tasks/`) | Leaf | Status |
|---|---|---|---|---|
| T-1 | Stiffness of the polymer layer under the tile | Number with stated model, parameters, validity range; sensitivity to grafting density reported | A2.1 | **DONE** (iteration 1) — claim `C-0001`, **challenged by `CH-0001`**: numbers stand as *lower bounds*, validity range corrected, `m = 3` excluded |
| T-1c | Layer response from a **crossover-valid** free energy, not a fixed osmotic exponent | Stiffness and stroke re-derived with `m_eff(φ)` from `C-0002`; the Alexander-de Gennes height relation either justified at φ/φ# ≈ 1 or replaced; `N(L₀)` no longer resting on the failed premise | A2.1 | **DONE** (iteration 3) — claim `C-0003`, raises `CH-0002`, **resolves `CH-0003`**, **closes `P-5`**. The height relation is **replaced**: `L₀` is exactly linear in `N`, and `σ^(1/3)` holds only for a two-body interaction while des Cloizeaux gives `σ^(5/13)`. `N(L₀)` was 5–88 % too short. |
| T-1d | Numerical SCF density profile for the Gen-1 layer | Whether the 10 nm window exists, decided by a profile whose premise is met | A2.1 | **DONE** (iteration 3) — claim `C-0011`, raises `CH-0010`. **The 10 nm window EXISTS**: `σ ∈ [0.0116, 0.2601] nm⁻²`, **22.4× wide** against strong stretching's 3.5× and the box models' *empty*, robust across all three interaction laws and across two decades of the load that defines `L₀`. **7 nm is not empty either** (`[0.0296, 0.0496]`, 1.7× wide) — which contradicts a finding carried since `C-0001`. 5 nm remains empty and the ~10 nm desired stroke is still unreachable everywhere. |
| T-1b | Free-energy functional of the compressed layer, and the `(48/35)N/g` blob-count identity | Closed form implemented and the identity verified as a test, not as an argument | A2.1 | **KILLED** (iteration 3) — absorbed into `T-1c`, which built the free energy. The blob-count identity it existed to verify is **moot**: the layer is not a blob stack (0.06 thermal blobs per chain, a 1.47-blob Alexander stack), so verifying an identity of a picture we have refuted would be spending effort to confirm a premise we no longer hold. |
| T-2 | Feasible design window in (grafting density, height, chemistry) | Non-empty region satisfying §4(a)–(d) simultaneously, or a proof of emptiness naming the binding constraint | A2.1 | **DONE** (iteration 3) — claim `C-0016`, raises `CH-0015`. **P1 non-empty at 10 nm (`σ ∈ [0.0116, 0.2601] nm⁻²`, 22.4× wide) and 7 nm (`[0.0296, 0.0496]`); empty at 5 nm by a 13.3× crossing** of coil overlap against the 3 nm stroke. Both edges at both heights owned by exactly those two constraints; **§4(c) and §4(d) bind nothing at any of 183 grid points**. **P2 is NOT CLOSED in either direction: the deciding axis is the output-coupling stiffness (`T-16`), which no claim in this programme supplies.** Height convention **FORCE-ONSET** — the polymer to order is **1.1–3.3 kDa**, against ~8–9 kDa in the first-moment convention. |
| T-3 | Stroke and blocking force vs bias, incl. ionic screening | Stroke ≥ ~3 nm and force ≥ 100 pN at ≤ 2 V, or a demonstration it is unreachable | A2.2 | **DONE** (iteration 3) — claim `C-0012`, raises `CH-0011`. **Reachable, and the operating point it is reachable at is not one the device can be held at.** 100 pN blocking at 0.065–0.699 V and 100 pN *at* a 3 nm stroke at 0.082–0.368 V — all inside `CH-0007`'s ~1 V boundary with 5–12× margin. But **`k_eff < 0` at the loaded operating point at 7 and 10 nm** (428 of 810 state points), and the *free* operating point leaves three upstream validity ranges at once above **~0.1 V**. The usable bias window is **0.02–0.1 V, not 0–2 V**. The ~10 nm desired stroke is unreachable inside validity everywhere. |
| T-4 | Electrostatic softening and pull-in: does `k_eff = k_brush + k_es` reach zero? | Max usable bias with margin, or a demonstration the osmotic divergence removes the instability | new | **DONE** (iteration 4) — claim `C-0018`, raises `CH-0017`. **Both branches answered, each for a different load line: a ceiling belongs to a `(bias, load line)` pair.** For the **coupled** device (`C-0017`'s 33.333 pN/nm) the usable bias is **0.097–0.425 V**, set by `C-0002`'s `φ = 0.2` at 43 of 54 states and by **pull-in only at 10 nm / 2 mM**, where it is **0.130–0.184 V against an operating bias of 0.128–0.180 — a margin of 1.007–1.032**, i.e. `C-0017`'s 1.19–1.42× *stiffness* reserve is under 3 % on the *bias* axis. **0.5 mM removes the fold entirely** (1.29–2.36×). At 5 nm the operating point is past `φ = 0.2` before any instability (margin < 1 at 15 of 18); at 7 nm / 10 mM the fold stroke is **1.92–2.68 nm, shallower than §3's 3 nm**. The **unloaded** tile has no pull-in at 49 of 54 states — §6's second branch, true of the free tile and nothing else — and its ceiling is **0.085–0.595 V, not 0.02–0.1 V**. A **dead load** has no stable compressed equilibrium at any bias wherever it folds (25 of 25 folds at zero stroke), its ceiling being exactly `C-0008`'s blocking bias. `C-0005`'s 1.46 nm band and `CH-0007`'s 1 V bind **nowhere**; `T-11`'s 1.23 V is 2.9× above the largest ceiling. |
| T-5 | Load distribution across the origami | Peak per-load-path force against the 35–60 pN disassembly band; distributed and concentrated attachment treated separately | A1.2 | **DONE** (iteration 3) — claim `C-0006`. Minimum load paths: **3** to stay under 35 pN, **11** under 10 pN — but **55** for dishing below 10 % of stroke, and the tile holds only 43.7 independent patches. A rigid anchor saturates at 18.3 pN however large the tile. |
| T-5b | Deflected shape of the tile under actuation load | Deformation amplitude against the stroke; rigid-plate assumption upheld or rejected | A8.2 | **DONE** (iteration 3) — claim `C-0006`, raises `CH-0005`. **Rigid-plate assumption REJECTED.** Dishing is 0 % (uniform load, exactly), 27 % (edge taper), 50 % (4 anchors), 369 % (one lever), 26 % (thermal, 1.27 nm RMS). |
| T-9 | Crossover hinge constant `k_θ` for a single-layer sheet, from oxDNA — **and, at no extra cost, the crossover's vertical/axial compliance** | A value with an uncertainty, replacing a fitted model input whose `1/100` is borrowed from CanDo's *nick* softening | new | TODO — **PROMOTED to high by `C-0015`**: its *vertical-compliance* half now decides whether the registration design rule exists at all, since the whole lever is the distance-to-nearest-crossover curve and that curve rests on the crossover being rigid in `z`. Was: **re-scoped by `C-0009`, kept at medium**. *Less* urgent as a check on `C-0006`/`C-0009`: every ratio there is flat to under 3 % across the whole admissible `α`. *More* urgent as a **force-budget** input, because the peak per-path force rises with `k_θ` and that number now sits within a factor of 2 of an unzip allowable. Extend it to produce the **crossover's vertical compliance**, which `C-0009` models as a rigid constraint and which is the single uncited assumption under it. Also — was **downgraded to low-medium** by `C-0010`, which shows a 2× change in `D_⊥` moves the tile's fluctuation by only 2.5 %, because the shape modes are foundation-dominated at `ℓ/L ≈ 0.2–0.5`. It remains the largest open premise under `C-0006`, but it blocks nothing in `T-8`. Was: **medium**. The single largest open premise under `C-0006`: everything about `D_⊥` inherits it. Cost estimate from the iteration that raised it: 2–5 k nucleotides, µs-scale umbrella sampling on 8 cores, **days not weeks — it fits this box**. Needs `g++`/`cmake` (installed under `P-7`). A **third** deliverable is now attached: the crossover's **in-plane shear** stiffness `k_s`, `C-0020`'s single undetermined input, which no accessible source gives in any form. **`C-0028` promotes it from least urgent to decisive**: the recommended standoff base's whole restraint *is* `k_s d²/2`, so the buckling margin falls below one somewhere in its sweep — **the first verdict in this programme that `k_s` moves**. `C-0030`'s coupling then buys ~4× in it (the crossing moves from `k_s/2`–`k_s/8` to `k_s/8`–`k_s/32`), so it is one notch less urgent than `C-0028` left it — but `C-0029` finds **`α` moves a verdict too**, the first time in this programme, so `T-9` now blocks two claims. |
| T-10 | Discrete-lattice (beam-and-hinge grillage) check of the tile, replacing the continuum plate | The plate reduction upheld or rejected; local force concentration at an anchor resolved | new | **DONE** (iteration 3) — claim `C-0009`, raises `CH-0008`. **Split verdict: the plate is upheld for smooth loads and rejected for point-coupled ones**, and its error *changes sign* with how the load meets the sheet — it understates concentrated and thermal dishing by 12–38 % and 11–20 %, and **overstates** anchored and edge-taper dishing by 1–16 %. Peak per-crossover force at a discrete anchor is **5.63 pN nominal and 11.54 pN worst-case**, a **2.3–7.6× concentration** on `C-0006`'s equal-share figure, and the worst case **reaches the 10–15 pN unzip allowable**. No `C-0006` verdict moves. Superseded note — was: **medium**. Raised because `ℓ_⊥/p = 0.26–0.52 < 1` across the whole sweep, so the continuum reduction is marginal by its own criterion, and because the plate model cannot resolve the one number `T-5` had to decline. |
| T-6 | Validity boundary of mean-field screening at 2 mM Mg²⁺ | Quantified deviation from mean-field, with the boundary stated | A7.4 | **DONE** (iteration 3) — claim `C-0005`, raises `CH-0004`. Mean field is **uncontrolled across the whole 5–10 nm working range** (deviation 123–214 % for Mg²⁺) yet **qualitatively safe** there: correlation attraction needs a gap under 1.46 nm and the layer holds the tile 3.4–6.8× outside it. Controlled PB only above 12.9 nm. |
| T-3a | 1-D nonlinear Poisson-Boltzmann profile in the actual 2:1 buffer, tile + electrode as one system | Its own `σ_eff` for `T-3`, replacing the symmetric-`z:z` ceiling `C-0005` could only quote as an order of magnitude | A7.4 | **DONE** (iteration 3) — claim `C-0008`, raises `CH-0007`, **resolves `CH-0004`**. The 2:1 first integral was derived, not adapted; `C-0005`'s ceiling is confirmed as a ceiling and is 24 % high, by exactly `6 − 3√3`. The tile is **charge-saturated**, so the charge ambiguity `C-0005` could not resolve moves `σ_eff` by only 7 %. |
| T-6b | Size-modified (Bikerman) PB at the electrode | The 0.197 V point-ion boundary sharpened, or replaced | new | **Downgraded to low** — its Bikerman half was folded into `T-3a` (the point-ion model is exactly the `n_max → ∞` limit, so it cost one function and became an executable limiting case). What remains is the **Stern capacitance**, which `CH-0007` makes load-bearing for the diffuse-drop→applied-bias mapping. |
| T-3b | 2-D Poisson-Boltzmann solve of the tile edge | The lateral load non-uniformity `T-5b` needs, as a profile rather than a parameter | A7.4 | **DONE** (iteration 4) — claim `C-0022`, raises `CH-0025` and `CH-0026`. **§4(g) CLOSES, and the edge has the opposite sign to the one three claims carried.** The rim *gains* load: `(depth, width) = (−0.303, 8.94 nm)` against `C-0006`'s assumed `(+0.50, 4.00 nm)`, the finite tile acting as one **1.65 nm larger on every side** and carrying **+14.7 %** more total force than a 1-D pressure over its footprint (+4.9 % to +19.2 % over the box, **+25.8 %** at 20 nm). Dishing **32.1 % of the stroke** (21–44 % over the foundation sweep), so the **lever/sensor split is 32 %, irreducible** — replacing `C-0012`'s 11 %–369 %, which was a statement about the coupling. Per-load-path forces from the edge are **50× below the unzip allowable**, so no `C-0015` force and no `C-0016` window edge moves adversely. Centre-line reproduces `T-3a` to 0.03–0.14 % at 21/21 points. |
| T-3c | 3-D corner solve of the tile, replacing the two-mapping bracket | The corner's contribution to the total force and to the dishing, against the 1.8-point (40 nm) / 7.2-point (20 nm) bracket `C-0022` leaves | A7.4 | TODO — **low-medium**. Raised by `C-0022`. Worth what the bracket is wide, and no more: at the Gen-1 footprint that is 1.8 percentage points of total force against a mean-field error of 123–214 %. It becomes worth more if the programme ever considers a tile below ~30 nm, where the bracket widens to 7 points and the whole edge correction reaches 26 %. |
| T-3d | Whether the PEG layer moves the edge **ratio**, not just the force | The taper depth and width re-solved with `C-0005`'s partitioning medium in the gap | A7.4 | TODO — **low**. Raised by `C-0022`. The layer amplifies the 1-D force by 1.15–1.60× (`C-0008`); `PoissonBoltzmannEdge` already accepts a `GapMediumProfile` and the sweep does not use one, so this is a parameter change and a re-run, not new code. |
| T-11 | The aqueous electrochemical window: is 1–2 V applicable at all in MgCl₂? | The usable bias ceiling set by water electrolysis (1.23 V thermodynamic), against §3's 2 V | new | TODO — **downgraded to low** by `C-0012`: every threshold in the programme is now below 0.7 V and the usable window is 0.02–0.1 V, so 1.23 V never binds. Was: low–medium. `C-0008` notes that because the force **saturates** in bias, the answer barely moves its conclusions — *"but that is luck, not an argument."* |
| T-12 | **Lateral confinement of the tile** | An anchoring scheme delivering `k_lat ≥ 0.4602 pN/nm`, or a demonstration that none is available | A1.2 | **DONE** (iteration 3) — claim `C-0014`, raises `CH-0013`. **PASS: two schemes work, and the interesting result is why the obvious one fails.** A vertical strut has the anisotropy *inverted* — it fails by 40–160×, costs 96–99 % of the stroke, and is **destabilised by its own duty** (a column at its Euler load has exactly zero lateral stiffness). What works is a load path lying *in* the surface, or the ssDNA tether `C-0010` dismissed. **Its `L_min` table and its 7.6× column are superseded by `CH-0021`** — consume `C-0020`'s table instead, **with the tether's alignment quoted**. Superseded note — was: **HIGH**, and cheap (hours). `C-0010` shows the layer's lateral restoring stiffness is **exactly zero by symmetry**, not merely small, so the tile diffuses **62.8 nm in one 1 kHz period** — 21× the §6 predicate and 1.6 tile widths. §3 specifies nothing that confines it. This is a second **topological** axis on `T-2`'s window, beside `C-0006`'s distributed-coupling constraint. |
| T-14 | Crossover phase and anchor registration as a design variable | The peak per-load-path force as a function of crossover column count and anchor placement | A8.2 | **DONE** (iteration 3) — claim `C-0015`, raises `CH-0014`. **The lever is registration (×1.43–1.60), not crossover count** — `C-0009`'s 19 % count effect is 0.3–3.4 % once registration is controlled, **and its sign flips** (7 columns is the *better* layout). The governing variable is the attachment's **distance to the nearest crossover**, and the duplex shear runs the *opposite* way along it. **Flatness needs 45 attachments as 3 × 15, not 64 as 8 × 8** — `C-0009` searched the square diagonal of a two-parameter space on a 25.6×-anisotropic sheet. **One attachment row per duplex zeroes the per-path force exactly.** |
| T-17 | "One attachment row per duplex" as an output-coupling scheme | The exact-zero per-path force costed against `T-12`'s lateral confinement and `T-13`'s hold-down, and its survival under `T-3`'s edge taper and thermal excitation quantified | A8.2 | **DONE** (iteration 4) — claim `C-0026`, raises `CH-0033` and `CH-0034`. **There was one scheme, not two: `C-0015`'s 3 × 15 IS one attachment row per duplex** (rows on the duplex axes to `3.6e−15 nm`), so `C-0017`'s `K2` and `C-0023`'s `E3`/`E5` are already it. Costed against `C-0022`'s **solved** edge profile the exact zero becomes **0.150 pN** (0.332 worst of 21 states, 0.105–0.166 over the `k_f` sweep) — **6.8 % of the 2.222 pN static share and 67× below unzip**. Attachment scatter is the non-uniformity a builder controls, **0.883 pN per unit relative amplitude**, overtaking the edge at ε = 17 %; and it is **exactly zero** if the scatter alternates along the helices instead of across them. **Even at ε = 0.99 (0.86 pN) or the worst grid in the sweep (2.42 pN) the crossover path is 4–12× clear** — it never becomes binding, and the binding constraint stays the static share `100/n`. **Thermal excitation is not a member of that family at all**: it is `√(k_BT k_v)`, present under a uniform load, scheme-independent, and it **diverges as `√k_link`** — `T-9`. One grid discharges all three duties (lateral 60×, yaw 19×, hold-down 72×); **rows set by the load path, columns by flatness**. The zero is retired as a headline and kept as a free **20.2×**. |
| T-45 | The assembly tolerance of a 45-path DNA coupling | The relative stiffness scatter a staple-designed attachment array can be guaranteed to, against `C-0026`'s 0.883 pN per unit amplitude and its 17 % break-even | A1.2 | TODO — **medium, and cheap**. **`C-0060` (`T-122`, iteration 10) supplies the THRESHOLD the measurement would be graded against**: `C-0058`'s flat design loses `T-5b`'s verdict at **34.6 %** relative scatter (31.6 % with `C-0017`'s total held), **2.04×** `C-0026`'s break-even and **0.52×** the 66.7 % at which the two stiffness populations merely *overlap* — so flatness binds at half the amplitude the ordering does, and a small scatter *improves* the dishing. It also finds that `C-0026`'s **build rule reverses** on a non-uniform coupling ([`CH-0073`](gpd/challenges/CH-0073-the-along-helix-scatter-rule-reverses-on-a-non-uniform-coupling.md)). What is still missing is the measurement itself. `C-0026` reports the sensitivity per unit amplitude precisely so a measured or specified tolerance can be substituted without re-running, but nothing accessible gives the stiffness spread of nominally identical hybridised staple extensions. It is also the only non-uniformity a *builder* controls, and `C-0026`'s build rule — **let the scatter alternate along the helices, not across them, where it restores exactly zero** — is worth stating in a design document whatever the number is. |
| T-46 | Attacking the irreducible edge dishing in the ELECTROSTATICS rather than in the coupling | A counter-electrode geometry that flattens `C-0022`'s rim enhancement, or a demonstration that 0.149–0.32 of the stroke is a floor | A7.4 | TODO — **medium**. `CH-0034` closes the attachment-count axis: 45 → 225 attachments buys 6.9 percentage points and then saturates above the 10 % tolerance. The residual is a rim collar 8.9 nm wide that no interior attachment reaches, so the only remaining axes are the field and the tile plan-form — an inset or guarded counter-electrode, a rounded rim, or a larger tile (which `C-0022` shows costs +6.3 % instead of +14.7 %). This is what `C-0022`'s "it cannot be designed away" is actually a statement about. |
| T-47 | Is there any load in the Gen-1 stack structured on the duplex pitch? | A bound on across-helix load non-uniformity at the 2.69 nm scale, or a demonstration that none of the §1 stack produces one | A7.4 | TODO — low-medium. Everything in `C-0026` is 0.15–0.33 pN because `C-0022`'s collar is 8.9 nm wide against a 40 nm tile — smooth on the duplex scale. A load structured *on* the 2.69 nm pitch would restore far more, and it is the one way `C-0026`'s verdict fails. Candidates: a patterned electrode, the tile's own charge corrugation, and grafting-density fluctuation in the PEG layer. Cheap to bound, and it closes the last named failure route of `C-0026`. |
| T-25 | **Re-synthesise the design window against iteration 4's results** | `C-0016`'s window and `C-0017`'s verdict re-run against `C-0018`–`C-0026`, or a statement that no edge moves | A2.1 | **DONE** (iteration 4) — claim `C-0027`, raises `CH-0035` and `CH-0036`. **One edge moves, and it moves outward**: the 10 nm upper edge `0.26015 → 0.28854 nm⁻²` (one grid step; window **22.36× → 24.80×**). The 7 nm window is unchanged **to the last digit** — `C-0019` widens it one step and `CH-0024` narrows it one. **No owner changes**; 5 nm still empty, crossing widened 13.32× → 24.80×. **`C-0017`'s margin IMPROVES**: `C-0019` alone 1.11–1.25×, `CH-0026` alone 1.34–1.67×, together **1.231–1.528×** against a published 1.194–1.424× — the two are the same size and opposite sign, so `C-0019`'s ≥ 1.07× was half a correction. **`CH-0026`'s direction is backwards for a force-pinned operating point** (`CH-0035`). **`C-0018`'s 1.007–1.032 stands**, its movement unresolved (fold tangent −2.5 to +4.0 pN/nm). **`CH-0024` costs 0.6–0.9 %, not 2–13 %** (`CH-0036`), because `CH-0027` removed the tethers that caused it — worth **4 grid steps** at 10 nm and 3 at 7 nm, where the tethered reading is 1.230× wide. **Seven of eleven axes do not resolve in `σ`, and axis (i) LEFT the window.** **0.5 mM recommended as NOMINAL.** |
| T-60 | **The 2-D field on the equilibrium path, not at six sampled gaps** | The collar multiplier `μ(h)` and its gradient along the coupled equilibrium path, resolving whether `C-0018`'s pull-in bias moves | A7.4 | **DONE** (iteration 5) — claim `C-0033`, raises `CH-0051`. **`d ln μ/dh` is a derivative now: 0.01763–0.02011 nm⁻¹ at the 10 nm fold gaps**, converged to **0.11 %** in the mesh and **1.6 %** over a 6× range of difference step, against `C-0027`'s 0.0133–0.0226 (70 %) differenced across gaps `T-3b` visited at *different* biases. `μ` is a function of the gap to **0.29 % / 0.56 %** over three biases, and `T-3b` is reproduced at its own points to **2e−7**. **The collar-only tangent at `C-0018`'s own fold is `+2.60` to `+4.99 pN/nm`, STRICTLY positive — at a pinned force it is exactly `|F_es| d ln μ/dh`, so its sign is the gradient's sign with no computation** — and every fold moves to a deeper stroke. At 10 nm / 2 mM the margin **rises at all six models**, to **1.021–1.028** where pull-in still binds, and **at four of six pull-in stops binding at all** (the ceiling changes owner to `C-0002`'s φ = 0.2; those branches end on the field at 7.9–8.7 nm of stroke). **But the direction is not universal**: the margin is a ratio of two biases at two different gaps, so it moves with the sign of `3 nm − s_fold`, and at 7 nm / 10 mM — fold shallower than §3's stroke — it **falls 0.9–3.5 %**. `C-0027`'s `≥ 1.108–1.134` is **not a lower bound** (`CH-0051`): it froze the pull-in bias, which falls 7.1–8.0 % itself. The 8–9 % fall in `V*` is **independently reproduced at 8.5–9.9 %**. |
| T-62 | Propagate the finite-tile correction to the 7 nm / 10 mM folds | `C-0018`'s five 7 nm / 10 mM folds re-read with the edge correction, which `T-25` could not reach | A2.2 | **DONE** (iteration 5) — **closed by `C-0033`/`T-60`**, which re-solves those folds rather than transferring a `dV/dF` it has no record for. **The margin FALLS there, 0.9–3.5 %** — the opposite direction to 10 nm / 2 mM, from the same positive gradient, because the fold is *shallower* than §3's 3 nm. And the second test still fails: the fold stroke stays at **1.98–2.47 nm** with the collar carried, so §3's target stroke remains on the unstable side of the fold. |
| T-63 | **Is 0.5 mM MgCl₂ acceptable as the Gen-1 nominal buffer?** | A specification answer from NDI, not a calculation | A2.2 | TODO — **HIGHEST non-process item in the queue, and it is a question rather than a task.** **SIX** independent routes now recommend it (`C-0012` force, `C-0016` bias window, `C-0017` stability floor, `C-0018` usable bias, `C-0027` corrected margin, and now **`C-0032`**). At 0.5 mM the pull-in fold **does not exist**, the corrected stability margin is **2.16–9.87×** against 1.23–1.53× at §3's 2 mM, and it is the only margin in the programme that clears `C-0005`'s own 123–214 % mean-field error. It costs **nothing** (`C-0007`: the layer is buffer-independent to ≤ 0.4 %). **`C-0032` raises it from an improvement to a REQUIREMENT for the surviving coupling**: with `C-0030`'s realised strain-*softening* law the 10 nm / 2 mM bias margin collapses to **1.0000–1.0019** — the device sits *on* its own fold — and the fold's stroke walks back through §3's 3 nm target at two of six layer models. At 0.5 mM every predicate clears (1.44–5.93× on stiffness, 1.038–2.327× on bias). The escape route is priced and fails: the adverse mounting is 42.4–61.0 pN/nm, past `C-0023`'s 40 pN/nm ceiling at **0 of 8** lengths. **Carried to the open questions for Kazik.** |
| T-18 | Does a layout rule derived on a 40 nm tile transfer? | The along-helix registration separated from position-in-tile, on §3's 70 × 100 nm test tiles | A8.2 | TODO — low-medium. The Gen-1 tile is only **3.7 unit cells wide along the helices**, and the along-helix periodicity residual is 4.7–17.7 % against 0.3–1.9 % across them, so `C-0015`'s extrema are extrema over placements *on this tile*. |
| T-1e | Invert `N` on the **first-moment** thickness `2⟨z⟩` as well as the force-onset height | The definitional part of `CH-0010`'s chain-length gap separated from the physical part exactly, rather than by scaling | A2.1 | TODO — **medium, and cheap**; the machinery exists. `C-0011` reports `N(10 nm) = 62.1` against `C-0003`'s 224.8–374.3, and states honestly that **most** of that gap is the height convention (a 10 nm *first-moment* thickness would need `N ≈ 190–210` by scaling). What is **not** definitional is 78 pN against zero. |
| T-1f | Bound the mean-field fluctuation corrections at `φ ≈ 0.01` | The correction bounded, or declared unbounded with the missing method named | A2.1 | **DONE** (iteration 4) — claim `C-0019`, raises `CH-0019` and `CH-0020`. **Both: unbounded perturbatively, bounded non-perturbatively.** `Gi = \|ΔΠ\|/Π_MF = 0.30–1.71` across the window and **1.30 at the design point's mean `φ`** — the one-loop term exceeds the term it corrects and would drive `Π_int` negative, so **no bound exists inside the expansion**, exactly as `C-0005` reports for the electrostatics. But `K → 0` is a *computable floor* (the disjoining pressure is conformational), so destroying the interaction entirely costs **`k_brush` −9.4 % at 10 nm, −5.1 % at 7 nm** and buys **+2.0 % of stroke**. **`C-0016`'s windows WIDEN** (13.4 % / 1.8 %) and **`C-0017`'s margin goes ≥ 1.19× → ≥ 1.07×, still above one — but `CH-0019`: that margin sits inside the ELECTROSTATIC expansion and nothing here narrows it.** `C-0003`'s `k ∝ K^(1/(m+1))` **does not transfer** — measured 0.0647 against 0.3077. |
| T-50 | **Beyond-mean-field treatment of the actuated tile-electrode gap** | The direction and size of the correlation correction for *oppositely* charged 2:1 walls at 2–7 nm, or a demonstration that no method reaches it | A7.4 | TODO — **HIGH. The last unbounded exposure on the Gen-1 critical path.** Raised by `CH-0019`: `C-0017`'s 1.19–1.42× margin sits inside `C-0005`'s 123–214 % **electrostatic** one-loop correction, and `T-1f` has now shown that the *polymer* correction is a different expansion worth only −9.4 % on `k_brush`. So nothing else in the queue can establish the 10 nm verdict. `C-0005` costs primitive-model MC at **1–3 weeks of wall clock** and states that `Ξ = 17–24` has no systematic theory at all. **The alternative to running it is to adopt leaf `A2.2`'s 0.5 mM operating point, where `C-0017`'s margin is 2.09–8.65× and clears its own uncertainty.** |
| T-51 | Field-theoretic simulation of the grafted layer | A fluctuation-corrected density profile and disjoining pressure, replacing `C-0019`'s bracket with a number | A2.1 | TODO — **low. Named and costed, not warranted.** `C-0019` bounds the layer response at ≤ 9.4 % without it. Complex-Langevin sampling of the Edwards functional integral is exact in the fluctuations and does not expand about the saddle point; costed at **weeks** for a sweep over grafting density. The published near-substitute is **paywalled with no repository copy** — a candidate for the open questions for Kazik. |
| T-52 | Self-consistent chain swelling through the profile | `α(z)` rather than a uniform `b_eff`, and the second-order Fixman term | A2.1 | TODO — low-medium. `C-0019` applies the conformational correction as a *uniform* effective segment length, but `ξ` runs 2.5–4.2 nm across one profile so `α` should vary with `z`. And `z = 0.32` at the top of the design space is outside Yamakawa's own `\|z\| < 0.15` band for the first-order form. Both push the same channel, currently worth ≤ 2.2 % of `k_brush`. |
| T-16 | **Minimum output-coupling stiffness** — what a DNA-origami lever can actually deliver | The coupling stiffness a lever can supply at the tile, against what the operating point demands | A8.2 | **DONE** (iteration 3) — claim `C-0017`, raises `CH-0016`. **The requirement is 33.333 pN/nm, fixed by §3 alone** (`= 100 pN / 3 nm`, preload-free) — *not* the 5–277 pN/nm range, which was a stability boundary read at biases the device does not use. Read at the located operating bias the stability floor is **0 at 5 and 7 nm** and **23.4–27.9 pN/nm at 10 nm** (2 mM). Scheme **K2** — 45 attachments on `C-0015`'s own 3 × 15 flatness grid, each a 5 nm duplex standoff in series with a **13 nt tuned ssDNA spacer** — supplies it at **2.22 pN per path** against a 10 pN allowable. **`C-0016`'s P2 closes NON-EMPTY at 7 and 10 nm.** Margin at 2 mM is only 1.19–1.42× against `C-0005`'s 123–214 % mean-field error, so the verdict is **not excluded, never established**; 0.5 mM buys 6×. |
| T-15 | The in-plane (membrane) load path into the tile, by shear lag | The in-plane force-concentration factor, replacing `C-0009`'s out-of-plane one used as a bound | A8.2 | **DONE** (iteration 4) — claim `C-0020`, raises `CH-0021`. **The factor is 1, and it is bought by a design rule.** A lateral tether collects **nothing** (`C-0010`'s exact zero) and the in-plane **sharing length is 65 nm against a 40 nm tile**, so **aligned with the helices `η = 1.0000` exactly**, at all 480 placements and over four decades of the one undetermined input — `L_min` at the **desired 10 nm stroke falls 93.3 → 33.5 nm** and the assembly from ~227 to ~107 nm. **But the stand-in was NOT conservative when misaligned**: the worst of 7200 placements gives `L_min = 115.9 nm`. **Layout is worth ×1.0000 here against `C-0015`'s ×1.43–1.60.** The declared falsifier **fired** — an oblique tether's moment is reacted by the crossovers as an axial couple, `η` → 2.33, bounded and saturating. The footprint incompatibility **changes currency**: the minimum-length tether's normal preload is `n A √(2A/S)` = **54.9 pN, stroke-independent, 55 % of the §3 target**. |
| T-19 | The attachment's entry topology — what a tether actually bonds to | The peak per-load-path force for a tether bonded across two duplexes, or onto a crossover, against the one-point-on-one-duplex model | A8.2 | **DONE** (iteration 4) — claim `C-0024`, raises `CH-0029`. **`C-0020`'s headline survives, as arithmetic.** It rests on **cut equilibrium**, not on the one-point model: on a `D`-duplex tile `η ≥ 1/D = 0.0667` for **any** topology (`A_eff ≤ 720 pN`, an edge clamp), and an `m`-duplex bond **enters at exactly `1/m`** — over 3840 designs (every width × position × 32 phases) the peak exceeds `1/m` by ≤ **4.7 %**. **The halving is exact to 4 %**, the compliant/rigid split limits bracketing it to 11.4 %. **It costs nothing in the crossover path — it pays ×1.88**, and that path never becomes binding. **Bonding onto a crossover ≡ a two-duplex bond.** **The footprint is worth nothing**: over the complete phase sweep the worst 8 bp footprint is `η = 1.0000`, its apparent relief being load shed past the first crossover column. **The design content is in the JOINT**: Strunz's own constants give **18.8 pN at 8 bp, 34.8 at 16, 47.1 at 30** against the flat 48 in use, with a **14.3 bp split break-even**. `L_min(10 nm)` = 27.7 nm for a split 32 bp staple (not ~24 — the joint binds first), 39.4 nm for an unsplit 16 bp one. |
| T-35 | Does a staple domain inside an origami sheet rupture like a free oligonucleotide? | The shear rupture force of a hybridised domain flanked by crossovers, against Strunz's free-oligo scaling, or a demonstration that no measurement exists | A8.2 | TODO — **medium-high, and it is the way `C-0024` would fail**. Every per-path shear allowable in this programme traces to an AFM measurement on **free oligonucleotides** on a PEG linker. `C-0024`'s whole joint argument — the 18.8/34.8/47.1 pN ladder and the 14.3 bp split break-even — assumes that transfers. A domain inside a sheet is flanked by crossovers and neighbours, and whether that raises or lowers its rupture force is established nowhere. `T-9`'s oxDNA could bound it. |
| T-36 | The bonded length as a first-class design variable across the programme | Every claim that consumes a "48 pN" or "10 pN" per-path allowable re-read with the domain length it actually implies | A8.2 | TODO — **medium, and cheap**. `CH-0029` shows the allowable is a function of bonded length; `C-0009`'s crossover path, `C-0014`'s tethers, `C-0017`'s 45 coupling paths and `C-0021`'s hold-down all quote it length-free. Unzip is length-independent and shear is not, so the correction is uneven and cannot be applied as one factor. |
| T-37 | Where on the tether-length axis should the design sit? | The tether length chosen against preload, stroke cost and footprint together, rather than at `L_min` | A1.2 | TODO — medium. `C-0024` shows `L_min` is a *corner*: the preload there is 25–186× `C-0021`'s requirement, and the length that delivers exactly the wanted hold-down at the 10 nm stroke is 116.6 nm. `T-13`'s hold-down, `T-12`'s confinement and `T-2`'s footprint are three constraints on **one** scalar, and nothing has yet optimised it. |
| T-21 | The semidilute→concentrated crossover for **this** layer, replacing `C-0002`'s cited 0.2–0.3 band | The `φ` at which the des Cloizeaux exponent stops being the one the layer is entitled to, derived rather than read off a band | A2.1 | **DONE** (iteration 5) — claim `C-0036`, raises `CH-0048` against `C-0007` and `CH-0049` against `C-0002`. The crossover is a **one-parameter family** `φ_c(n) = (v_K/b³)n^(−1/2)`; the member that ends the des Cloizeaux exponent is **`φ** = 0.0041–0.0319`**, *below* the layer's own resting `φ = 0.033–0.133`, so **the des Cloizeaux window is EMPTY at 18 of 18 Gen-1 chains** (`φ**/φ* = √(N_K/g_T) < 1`, exactly) and the exponent never starts. **Nothing in the family lands in 0.2–0.3**; the two constructions that do are the same textbook expression read on a monomer (0.203, 0.257). Read as a *regime* ceiling the derived number is **LOWER** than 0.2 and the device is **worse**: at 0.141 the coupled margin falls 0.563–2.464 → 0.168–1.660 and 20 coupled states drop below unity instead of 15. But `C-0018` consumes it as a **support** ceiling, and on that axis it is the equation of state's fitted range, `φ = 0.49–0.63`, where the crossover binds at **0 of 162** and `C-0005`'s 1.46 nm band takes over at 99 — **saturating above `φ ≈ 0.4`, so resolving this number further buys nothing**. `C-0018`'s 1.007–1.032 pull-in margin, `C-0016`/`C-0027`'s window edges and `C-0017`'s coupling do **not** move. |
| T-22 | Dynamic pull-in: does a bias step carry the tile past a fold a quasi-static ramp stops at? | The fold re-evaluated against `C-0004`'s drainage corner and the tile's inertia, or a demonstration that the quasi-static reading holds below a stated frequency | A2.2 | TODO — low-medium. Raised by `C-0018`, which is static throughout. `C-0004`'s corner is 91 kHz–2.3 MHz, so the quasi-static reading is right below ~10 kHz; above that nothing in the programme covers a bias step. |
| T-20 | Membrane–bending coupling on a dished tile | Whether the in-plane and out-of-plane load paths stay decoupled once the tile is not flat | A8.2 | TODO — low-medium. `C-0020` decouples them exactly at linear order for a **flat** sheet, and `C-0006`/`C-0009`/`C-0015` all reject the rigid-plate assumption: the tile dishes by 26–369 % of the stroke. The coupling is second order in the slope and its **direction is not established**. It is the way `C-0020` would fail. |
| T-13 | Where the tile sits at zero bias | Whatever holds the tile down, named and quantified | new | **DONE** (iteration 4) — claim `C-0021`, raises `CH-0023` and `CH-0024`. **The §3 stack has NO zero-bias resting position — undefined, not large: 0 of 18 states return an equilibrium.** What *is* unavoidably there — van der Waals (0.245–28.1 pN over the electrode bracket) and the residual field (0.078–0.404 pN) — gives a **stable but NOT CONFINING** equilibrium: a `1/h³` force has a bounded potential, so the well is **0.2–5.7 `k_BT`** and **0 of 54 states confine**. Same for the device as specified (layer + `K2` + vdW + field): **1.40–5.37 `k_BT`, 0/18**. **`C-0014`'s eight substrate tethers close it**: `h₀` = **4.62–9.78 nm**, well **30.6–73.4 `k_BT`**, 18/18, RMS **0.360–0.501 nm** broadband and **0.019–0.041 nm** in band, for **0.07–0.38 nm** of stroke. **`C-0017`'s `K2` supplies EXACTLY ZERO** — 99.6 % of its compliance is ssDNA, which carries no compression — but the two tasks are one variable: **`F_down = (k_c − 33.333)·3 nm`**. The requirement is a **force**, `k_BT/3 nm = 1.3806 pN`, not a stiffness. |
| T-23 | A **two-sided** compliant DNA coupling — an antagonistic spacer pair, or a bending hinge in place of a stretched chain | An element that carries load in both directions at ≤ 40 pN/nm, or a demonstration that DNA offers none | A8.2 | **DONE** (iteration 4) — claim `C-0023`, raises `CH-0027`. **Three exist, and the requirement they were wanted for dissolves.** A **transverse duplex flexure** (span 24.61 nm = 72 bp, ends free to draw in), a **crossover-hinge flexure** (arm 4.11 nm = 12 bp, 92.5 % of its compliance in the hinge) and an **antagonistic ssDNA pair** (45 × 13 nt up against one 68 nt down limb) all place at 33.333 pN/nm exactly, stay linear, and put 2.2–3.4 pN per path against a 10 pN unzip allowable. But **two-sidedness changes the currency**: the potential above `L₀` becomes quadratic, so the requirement is `k_BT/σ² = 0.4602 pN/nm`, which §3's own mandate exceeds **72.4× unpreloaded** — `F_req = k_req·σ` identically. On `C-0021`'s own balance the tetherless device goes from **1.4–5.4 `k_BT`, 0/18 confining** to **959–7582 `k_BT`, 18/18**, RMS 2.56–12.98 → 0.217–0.352 nm, so **`C-0014`'s eight substrate tethers leave the design**. The preload branch is priced and rejected: it is a **mounting offset**, i.e. a length, and the requirement asks for 0.0409 nm against a 0.34 nm base-pair quantum that delivers 9.3× too much. One base pair of offset buys 0.5–1.1 % of `C-0018`'s bias margin; the buffer still buys 6×. |
| T-30 | The origami joint at a flexure's end — does it draw in, and does it clamp? | The flexure's end condition and axial restraint fixed by a joint model rather than bracketed | A8.2 | **DONE** (iteration 4) — claim `C-0025`, raises `CH-0031`. **Both brackets survive as LIMITS of one joint, and `C-0023`'s restrained reading is the one realised.** `c(ρ) = 192(ρ+2)/(ρ+8)` is exactly 48 and 192 at its limits, so the "4×" bracket is a **continuum** — and every joint that **supports** the beam sits at `c = 83.2–191.7`, the **upper 76 %**. The axial bracket is decided by **anisotropy, not softness**: every covalent motif is isotropic, so a **direct crossover** gives span 44.03 nm, tangent **79.18 pN/nm — 1.98× past the 40 pN/nm ceiling — at every one of `k_s`'s four decades** and 61.0 pN of beam tension at §3's desired stroke. **`C-0023`'s 2 nt ssDNA hinge is falsified** (`CH-0031`): a flexible link has no direction, so it buys 1.30 nm of draw-in for **1.30 nm of transverse dead band**, 43 % of the stroke. **A double nick IS a crossover**, to the last digit. **The draw-in factor is 2.4 only at the endpoints** — interior minimum exactly **9/4 at ρ = 8**. **The design: 45 flexures, span 31.64 nm = 93 bp, on 8 nm = 24 bp NORMAL standoffs** (anisotropy `Sℓ²/3EI` = 102×), tangent **37.39 pN/nm**, tension 0.37 / 3.83 pN at 3 / 10 nm; window ℓ = 7–10 nm. `CH-0029` inverted: the restrained reading asks 86.7 pN, **past the 68.1 pN saturation — no bonded length carries it**. **`T-13` still closes.** |
| T-40 | Is a duplex standing normal to a single-layer sheet buildable, and what holds its base? | The base joint of `C-0025`'s normal standoff, modelled as one of its own motifs, against the clamp it assumes | A8.2 | **DONE** (iteration 4) — claim `C-0028`, raises `CH-0037`, `CH-0038`. **The base is not a clamp, the motif is not in the literature, and the window is re-cut rather than narrowed.** All three of `C-0025`'s standoff constants are the `ρ_b → ∞` limit of a **series**; at a crossover base `ρ_b = 0.18–0.59`, leaving **32.0 / 13.6 / 32.0 %** of them. A softer base **relieves** the compliance ceiling (tangent 33.8–38.5 pN/nm over the whole 3–10 nm range) and **destroys** stability: a single-crossover base **buckles at all eight lengths**. A pinned base with a free head is a **MECHANISM**, `P_c = 0` exactly — a corner the "factor of exactly 4" bracket does not contain — and **the standoff's sway IS the flexure's draw-in**, so the held-head reading is unavailable. What closes it is **orientation**: two crossovers laid *across* the flexure give **261.2 pN·nm/rad** against **27.06** laid along, **9.65× free**. **Design: span 31.06 nm = 91 bp on 8 nm standoffs with a two-crossover favourable base**, tangent **36.51 pN/nm**, buckling **1.41×** free-head, window **`ℓ = 7–9 nm`**. **`CH-0037`**: `C-0025`'s duty is the *mandate* secant, 1.27–1.70× low. **LITERATURE: the motif is NOT established** — every published out-of-plane base is a **pin**, and rigidity comes from **triangulation**. Fields et al.'s measured duplex buckling implies `L_p = 41.7 nm`, **25 % below CanDo**, so every `P_c` here is optimistic. **`k_s` now moves a verdict** — the first time in this programme. |
| T-65 | Does the standoff's off-diagonal compliance take the buckling margin below one? | The coupled 2 × 2 tip compliance of a standoff carrying the beam's end moment and its inward tension together, instead of two independent springs | A8.2 | **DONE** (iteration 4) — claim `C-0030`, raises `CH-0041` and `CH-0042`. **No: it takes it 1.55× ABOVE, and `C-0028`'s bound was right while its sign argument was wrong in both halves.** The dropped off-diagonal is not a compliance but a **draw-in the joint SUPPLIES** — the beam's end moment tilts the head inward, `Φδ` per end, **first order in the stroke against a second-order demand**, and **3.09×** larger than it at the design point. So the beam is in axial **COMPRESSION** over `0 < s < 9.9 nm` (`C-0023`'s cable term changes sign), the duty at 10 nm falls **5.113 → 3.313 pN**, and the free-head margin rises **1.41 → 2.18×** (CanDo) and **1.06 → 1.64×** (measured). Against a *net* demand the joint is **2.06× STIFFER**, not softer. **`c₀ ≡ c(ρ)` exactly**; the *effective* `c` becomes a function of the **stroke** (124.4 at 3 nm, 92.2 at 10). **But `Φδ` is odd where `e(δ)` is even, so the law is no longer ODD and the sign is a MOUNTING CHOICE**: favourable gives window **`ℓ = 5–10 nm`**, adverse gives 42–61 pN/nm and **no window at any length**. New: the flexure has its own buckling condition (`P7`, 3.1–4.2×), and the favourable mounting costs a **clearance** — §3's desired 10 nm stroke unreachable at any `ℓ ≤ 10 nm`. Design: **span 31.82 nm = 94 bp on 8 nm standoffs, tangent 25.23 pN/nm, margin 2.18×**. Maxwell-Betti asserted between two independently integrated off-diagonals, departure `0.0`. |
| T-75 | Which body carries the standoffs | The mounting sense of the flexure fixed by a buildable argument rather than left open | A8.2 | **DONE** (iteration 5) — claim `C-0035`, raises `CH-0045` and `CH-0046`. **SETTLED as a determination, and it was never a free binary.** The midspan is tied to one body and the ends stand on the other, so the deflection **is** the change in their separation: `dδ/ds = (v_base − v_driven)/n`, exactly `±1`, **with no length in it**. The sign is a **PRODUCT of two binaries** — base body **and** standoff normal — of which `C-0030` named one: of the four mountings exactly two are favourable, **one with each body and one with each normal**, so "which body" predicts the sign no better than a coin. Equivalently: favourable ⟺ the tie **crosses the base plane** ⟺ the flexure is **outboard** ⟺ the standoff is in **COMPRESSION**. **The survivor is unique — `Su`: standoff bases on the OUTPUT SUPERSTRUCTURE, standoffs pointing AWAY from the tile, flexure outboard above it, each midspan tied back DOWN through it to the tile.** The other favourable mounting (`Td`) puts the flexure inside the actuation gap: **37–85 % of the layer's own volume**, and at or below the electrode at §3's 5 and 7 nm layers, 0 of 9 admissible. Both adverse mountings fail `C-0023`'s ceiling **and** cannot place §3's effort point — inboard it cannot come closer to the tile than `ℓ`, and §3 allows **5 nm at the 10 nm layer on both readings of its band**. **The pre-bow escape is real, priced and rejected 12 of 12**: 4.08–16.66 nm of rise (12–49 bp) for a **150–225 pN preload = 1.5–2.25× §3's whole target force**, and **CLOSED outright at the desired stroke**. **The tile now carries no out-of-plane element at all — only `C-0015`'s 45 tie attachments.** |
| T-76 | Does a strain-SOFTENING coupling still satisfy `C-0017`'s stability condition? | `C-0018`'s pull-in and fold analysis re-run on a coupling whose tangent has an interior minimum BELOW its placed secant | A8.2 | **DONE** (iteration 5) — claim `C-0032`, **resolves `CH-0042` as UPHELD**, raises `CH-0047`. **NO at 2 mM, YES at 0.5 mM, and that is a design decision.** The placement clause is discharged **exactly** — the operating bias `V*` is identical across all four load lines at **144 of 144** comparisons, departure `0.0` — so the whole margin loss is in the ceiling. At 10 nm / 2 mM the pull-in bias falls only 0.7–1.8 %, but the **fold's own STROKE walks back from 3.41–4.13 nm to 2.80–3.17 nm**, through §3's 3 nm target at **two of six models**, and the bias margin collapses **1.007–1.032 → 1.0000–1.0019**: the device sits *on* its fold. `Q2` fails 6 of 6 (22.875 against a 23.41–27.91 floor). **`C-0017`'s theorem is confirmed where its premise holds** — the strain-*stiffening* decoupled reading loses **0 of 54** states and *raises* the margin to 1.020–1.774. **The escape fails: the adverse mounting is 42.38–61.04 pN/nm, 1.06–1.53× past `C-0023`'s 40 pN/nm ceiling at 0 of 8 standoff lengths.** At **0.5 mM** every predicate clears (1.44–5.93× on stiffness, 1.038–2.327× on bias), with one model acquiring a fold the mandate lacks. **A third route lands 2 % short**: `ℓ = 5 nm` raises the tangent minimum to 27.30 and clears five of six models; `ℓ = 4 nm` would clear all six and is excluded by `C-0030`'s `P4`. |
| T-77 | The flexure as a beam-column | The compression the coupled joint imposes carried into the beam's own bending, instead of bounded against a braced eigenvalue | A8.2 | TODO — medium. `C-0030`'s `P7` passes at 3.1–4.2×, but the compression is carried only through `C-0023`'s chord membrane term, whose implied critical load is `12EI/L²` against the exact `π²EI/L²` — 22 % optimistic. A genuinely new failure mode: the decoupled reading puts the beam in tension at every stroke and cannot state it. |
| T-78 | What sits under the flexure's midspan | The favourable mounting's clearance ceiling settled, or shown to be a design choice | A8.2 | **DONE** (iteration 5) — claim `C-0035`. **The ceiling is REAL, and it is not a property of an unspecified body — it is a property of the only topology that works.** The favourable sense is *defined* by the driven body lying on the far side of the standoff base plane, so the standoff-carrying body sits under the midspan **by construction**, whichever body it is. Nor can it be imperforate: the tie has to cross it at exactly the place the midspan descends toward, so **the aperture the tie needs and the clearance the midspan needs are one feature**. Priced from `C-0025`'s beam integrated once more, `w(u)/δ = (24u + 12ρu² − 16(2+ρ)u³)/(8+ρ)`: at §3's **acceptable 3 nm** stroke the cost is **exactly zero for `ℓ ≥ 6 nm`** — no penetration, no slot; at §3's **desired 10 nm** and `ℓ = 8 nm` the midspan goes **4.69 nm** past the contact plane and the beam demands a slot **18.37 nm long, 57.7 % of its own span, 54 bp**, which over 45 paths is **2223 nm² = 1.39× the tile's whole footprint**. **The escape from the ceiling is a hole bigger than the device.** An irreducible floor remains at every stroke: `C-0023`'s two-sidedness makes the tie a duplex, so 45 duplex-omission holes = **326 nm², 20.4 % of the footprint**. Whether the superstructure may be perforated is the one input this task cannot supply → **open question 6**, and `T-95`. |
| T-79 | A large-rotation two-spring elastica for `E5`'s arm | The arm placed on a composition that is exact in BOTH the rotation and the end condition | A8.2 | **DONE** (iteration 6) — claim `C-0039`, raises `CH-0053`. **The two compositions do NOT bracket the arm, and the desired stroke does not survive the geometry.** The elastica's vanishing-load limit reproduces `C-0034`'s `c(ρ_n, ρ_f)` at all four textbook corners and over a 25-point interior to **1.7e−14** — which pins the field equation, both boundary conditions and every sign at once — and on that solver `E5a16` places at **12.7198 nm = 37.4 bp, 1.79 % OUTSIDE the 11.028–12.496 nm bracket**. `C-0034`'s premise *"two errors run opposite ways"* is false: both are corrections to the same linear beam and both **stiffen** it. **The placement clause is discharged exactly (33.3333 pN/nm secant) and the compliance clause is not** — tangent **36.44 pN/nm at 3 nm** (inside the 40 pN/nm ceiling, 8.9 % to spare) and **264.2 pN/nm at 10 nm, 6.6× past it**, with the **secant** there already **69.94 = 2.10× the mandate** and the assembled force 699 pN against §3's 100. **The usable stroke inside the ceiling is 3.877 nm**, and `clears 10 nm` is **false at 0 of 34 placements and 0 of 25 sensitivity points** — including `C-0040`'s buildable hinge counts (1–6) and `C-0041`'s 15 paths, where the arm is 8.40 nm and the stroke is out of *geometric* reach. Cause: the arm is capped at 13.65 nm, so 10 nm is **≥ 73 % of its own contour**, and the exact draw-in is **5.34 nm = 15.7 bp** (42 %) against `C-0029`'s quoted 0.095. **An inextensible arm has no axially-held reading at all** — held, it must stretch, ≥ 299 pN, 4.6× past the 65 pN nicked ceiling. §3's **acceptable** stroke is untouched. |
| T-80 | Can `E5`'s arm end as a singly nicked continuation of a tile duplex? | A strand routing for the arm's far end, or the statement that only the two-terminus couple is available | A8.2 | TODO — medium. `C-0034` prices `A4` (one intact backbone, `k_θ` = 683 pN·nm/rad) as the motif whose **kinematics** match a guide — a nick preserves the helix axis, which is what *guided* means — and it takes the cap from 13.43 to **15.18 nm** and the realised `c` from 7.4 to **11.1**. But it needs the arm collinear with a tile duplex at its far end while the arm arrives inclined, which is `T-67`'s closure question at the other end of the element. Not answered by `C-0034`, which adopts the conservative two-terminus reading. |
| T-81 | Does a 16-crossover hinge line exist on a 40 nm tile? | The hinge count `C-0023`, `C-0029` and `C-0034` all take as given, checked against `C-0015`'s lattice | A8.2 | **DONE** (iteration 6) — claim `C-0040`, raises `CH-0054`. **NO, at every one of the 32 phases and in BOTH directions of the sheet.** A hinge line carries **four** crossovers, at every phase; the other parity carries **three** at the 22 seven-column phases and four at the 10 eight-column ones — and **the phases that maximise a hinge line are exactly `C-0015`'s ten centro-symmetric ones**, which nothing forced. Sixteen needs **163.2 nm = 4.08 tiles** of collinear interface, or **33 duplexes = 88.8 nm = 2.22 tiles** transversally — and the transverse line's crossovers are dihedral springs about the **wrong axis**, so `TASKS.md`'s own guess fails twice. Absolute geometric ceiling **six**; per flexure at 45 paths **one or two**; whole-tile inventory **49–56** against a demand of **720**, **12.86×**. **Sixteen crossovers CAN be assembled — four interfaces of four — but interfaces compose in SERIES: `n_eff = n_i·3(2m−1)/(m(2m+1))`, so they are worth 2.333 of hinge, 14.6 % of their count, and 45 such flexures give 16.03 pN/nm, 2.08× too soft.** Re-priced: the stroke needs **10** crossovers (12 inside the ceiling) against **3** for the ceiling at the acceptable stroke, so the window in hinge count is **`3 ≤ n ≤ 6`**. **At four the arm places at 7.748 nm — §3's acceptable 3 nm clears at 36.58 pN/nm, the desired 10 nm is out of geometric reach; at the one or two a flexure can own, the tangent is 42.0–54.1 pN/nm, past `C-0023`'s own ceiling.** The dominant compliance term changes back to the **hinge** (78 % at four, 95 % at one). Continuum control: the quantisation is worth −18 %/+9 % where the assertion is out **4.35×**, so it is a **density** fact, not a lattice one. Premise swept: 16 bp mis-reading → 8, honeycomb → 6, both → 12; **no reading reaches sixteen**. |
| T-99 | Does a coupling of FEWER, LONGER flexures close where 45 short ones do not? | The `(path count, hinge count, hinge-line length)` trade swept, or the statement that no point of it reaches §3's desired stroke | A8.2 | **DONE — [`C-0046`](gpd/claims/C-0046-fewer-longer-flexures.md), and the answer is NO at every point.** The trade is **degenerate on its own axis**: the arm the crossover inventory can place is `δ/sinθ` with `θtanθ = kδ²/(N k_θ)`, a function of the **product** `n·h` alone — **5.387 nm at all eight splits of `n·h = 56`** — so crossovers cannot be converted into arm length by moving them between hinges. What breaks the tie is `C-0034`'s far anchorage, which is **per-flexure**: the placed arm **grows** with the path count (6.903 nm at `(14,4)`, 8.231 at `(28,2)`, **9.973 at `(56,1)`**), so *fewer, longer* is the **wrong direction**. **0 of 31 points reach §3's desired stroke at the standing placement and 0 of 29 at its own, at BOTH readings of `C-0023`'s ceiling** — so `T-107` cannot move it. Best point `(56,1)`: arm 9.973 nm = 29.3 bp, tangent 38.17 pN/nm at 3 nm, **usable stroke 3.312 nm**, spending **100 %** of the tile's crossovers. Only **3** of 31 points clear even §3's acceptable stroke, all at `h = 1`. Raises [`CH-0059`](gpd/challenges/CH-0059-the-desired-stroke-placement-is-below-the-stability-floor.md). |
| T-110 | What does spending 80–100 % of the tile's crossovers on HINGES do to the sheet? | `C-0009`'s `D_⊥`, `C-0015`'s flatness and `C-0006`'s load distribution re-run on a sheet whose interfaces have been converted into free hinge lines | A8.2 | **DONE** (iteration 8) — claim [`C-0054`](gpd/claims/C-0054-consumed-crossover-sheet.md), raises [`CH-0066`](gpd/challenges/CH-0066-the-surviving-designs-consume-the-sheets-own-connectivity.md). **The two uses are EXCLUSIVE at the site** — `C-0040`'s own definition of a hinge line (one interface, one *pair of bodies*) and of `k_θ` as the *interhelical dihedral* spring settle it — **and the binding constraint is then not stiffness but TOPOLOGY.** A connected sheet needs one retained crossover on each of its 14 interfaces, so the hinge budget is **42 of 56 = 75.0 %** (35 of 49 at the other phases) against `C-0046`'s **80–100 %**: all three surviving designs sever the tile, into **≥ 4, ≥ 9 and exactly 15** pieces. **`D_⊥` does not degrade, it collapses** — the interfaces bend in **series**, so the honest rigidity is a harmonic mean and is **exactly zero** at the first empty interface where the smeared continuum still reports 0.667 pN·nm and an anisotropy of 128 instead of unbounded. Under `C-0022`'s solved load on the 3 × 15 grid: **0.218 intact → 0.242 at the ceiling (+11 %) → 0.465 at 45 (2.13×)** — **the step is at severance, not at consumption**, and the lattice/plate ratio flips 0.87 → 1.62 exactly there. The peak per-load-path force falls to **exactly zero** because a deleted path cannot be overloaded. **`C-0010`'s insensitivity is right and is the wrong channel**: the smeared plate moves 31 % where the lattice moves **117 %**, because a crossover is two elements and the *vertical link* is a constraint no plate can represent. The generous "a hinge IS an attachment" reading is **3.3× worse** at the same count. **The branch survives at `39 ≤ n ≤ 42`** — `C-0046`'s unresolved `34 < n ≤ 45` threshold is **39** — delivering 3.005–3.063 nm, and none of `C-0046`'s three designs is in it. **Converges with `C-0053` (`T-116`), filed the same iteration from the plan view: 42 is what the counting permits, 25 is what the geometry delivers, and the two bounds compose.** Opens `T-119` (the one falsifier: a junction site the single-layer sheet does not use) and `T-120`. |
| T-111 | A plan view for `E5a` — does the crossover-hinge flexure array pack? | `C-0041`'s blocking-digraph pipeline re-run on a body with no standoff and no tie | A8.2 | TODO — **medium-high.** `C-0041` solves the packing for the **standoff** flexure and gets 15; `E5a` has neither a standoff nor a tie, so that count does not transfer and no packing verdict for it exists. It matters because `C-0046`'s surviving design needs **45–56 paths** and would fail §3's *acceptable* stroke at 15 (2.469 nm). The pipeline is already written and the element is a plain `span × d` rectangle hinged on an interface, so this is cheap. **`C-0054` narrows the target: the count to pack is now 39–42, not 45–56.** **ANSWERED by [`C-0053`](gpd/claims/C-0053-hinge-arm-array-packing.md) (`T-116`, concurrent, iteration 8), which is the same question**: the bodies pack (0 clashes at every orientation, `arm + d = 11.82` under the 13.33 column pitch), the **hinge lattice** places **43** (three per interior row, two per edge row — exact, construction meets bound), and the host keeps every duplex bonded only up to **25** arms, because an arm is a length of duplex **cut out of** its own row and buries 11–13 further crossovers. `C-0053`'s 25 sits below `C-0054`'s 42 by exactly those two terms, so **the count to pack is 25, not 39–42**. What is left of `T-111` is the *stiffness* of that body, which is `T-120`. |
| T-119 | Can a flexure hinge be built on a junction site the single-layer sheet does not use? | A literature answer, or the statement that the motif does not exist — the one way `C-0054`'s exclusivity premise fails | A8.2 | **DONE** (iteration 9) — claim [`C-0055`](gpd/claims/C-0055-unused-junction-site.md), raises [`CH-0068`](gpd/challenges/CH-0068-the-hinge-inventory-is-not-the-sheets-own.md). **Answer (a): the site exists, it is published geometry, and `C-0054`'s pigeonhole falls — while `CH-0066`'s conclusion survives for a different reason.** Ke et al. (*JACS* **131**:15903, 2009, **read directly**): *"each double helix has up to four nearest neighbors … Every 8 bp, that staple strand is positioned to cross over to one of its four neighbors … adjacent helices share crossovers every 32 bp"*, and *"crossovers in i and iii … are parallel to the xz-plane, while … ii and iv … to the yz-plane"*. A single-layer sheet builds on **one of the two orthogonal plane families**; `8 bp × 33.75°/bp = 270.0°` **exactly**, so the empty family points **out of the sheet plane**. **The unused site is in BETTER register than the used one** — 4.286° against 8.571° at the preferred 10.5 bp/turn, exactly 2:1, because the departure is linear in the offset. **Census: 161–176 junction sites, of which the sheet builds 49–56 — it occupies 27.8–33.1 %, under a third at every phase**, and the in-plane half reproduces `C-0015`'s 56/49 as a **set equality** at all 32 phases from an independent construction. **So exclusivity AT A SITE is upheld and the INVENTORY premise is false**: the ceiling is not `56 − 14 = 42` with a severed tile but **52 upward hinges at `C-0054`'s own phase and 60 at the best, with all 56 interface crossovers retained and the sheet in ONE piece at every count**. **But the design count only moves `C-0053`'s 25 → 34**, because the escape and its price are the same fact — an upward line belongs to **one** duplex, so nothing empties *and* its roots sit at 10.88 nm against an arm demanding 11.82, three arms per row falling to two at `arm > 8.19 nm` (`n > 34`). **§3's 45 still does not place on a 40 nm tile — 30 do — for the root pitch, not for severance; at a 49.25 nm edge 45 places with the host intact.** New phase trade: the ten phases maximising the upward inventory (0–2, 14–18, 30, 31) are **disjoint** from `C-0015`'s ten that maximise the interface one. **The motif is half-published**: the site and a crossover on it are the elementary step of square-lattice multilayer origami, but a **free lever** held by one crossover there was **NOT FOUND** in 62 recorded EuropePMC queries across fifteen families, every published origami hinge is an **ssDNA** connection (2023 review, read directly), and Ke et al. name the **8 bp staple break** it forces as a *"significantly lower yield"* cost. Opens `T-121`. |
| T-120 | Is a sheet held together by ONE crossover per interface still a plate? | `C-0009`'s discreteness criteria re-derived at the connectivity ceiling | A8.2 | **DONE** (iteration 9) — claim [`C-0056`](gpd/claims/C-0056-connectivity-ceiling-plate.md), raises [`CH-0069`](gpd/challenges/CH-0069-the-plate-fails-before-the-connectivity-does.md). **NO, and it stops being one FIVE crossovers before it stops being connected.** The depleted lattice is parametrised by **one** number through `p_eff = p N/N_ret` and `D_⊥ = k_θ d/p_eff`, both exact at `N_ret = N`, so every criterion is `C-0009`'s published number times an exact power of `N_ret` and **each therefore INVERTS**. At the ceiling an anchor's influence patch holds **0.694** crossovers against 3.93 intact, and **inverted rather than evaluated the patch criterion crosses one at 37.3 SPENT — inside `C-0054`'s own `39 ≤ n ≤ 42` window.** The other two criteria are useless here: `ℓ_⊥/d` reads **1.061**, above one, and never fires; `ℓ_∥/p_eff` demands **67.2** crossovers, more than the sheet owns, so it is **degenerate** — it fails on the intact sheet at every foundation but the softest. **`ℓ_⊥/d` cannot tell depletion from a stiffer foundation at all** (both divide `D_⊥/k_f`; the ceiling's 1.061 IS `C-0009`'s `k_f×4` corner, exactly), where the other two separate the same pair by `2√2`. **A criterion is not a model**, so a third model — fifteen uncoupled beams on one foundation — was run beside the plate: **the four thresholds fall in a strict order, 28 (point compliance) < 37.3 (criterion) < 42 (connectivity) < 45 (smooth load), 1.61× apart.** `C-0009`'s smooth/point split **survives and becomes a change of model class**: the plate is out 14.9 % on `C-0022`'s solved load at the ceiling and **33–79 % under a concentrated lever**, and the point compliance varies **2.53×** over one cell where the plate says 1.14× — a quantity the plate does not have. `C-0006`'s load distribution stands (the hinges carry `9e−4` of the energy **intact**); **`C-0010`'s insensitivity survives and strengthens** (3.07 % → **0.99 %** at the ceiling) while the amplitude rises 1.71×, so it is insensitivity to the *rigidity* and not to the *connectivity*; `C-0047`'s 0.218/0.695 and `CH-0034`'s saturation are unchanged. **Found and bracketed: `C-0054`'s `SPREAD` tie-break clusters all 14 survivors into the two lowest columns** — a `staggeredRetention` is 1.68× better in the distance from an anchor to the nearest surviving crossover at identical count and density. |
| T-121 | What do 34 duplexes stacked ABOVE the tile do to it? | `C-0009`'s grillage, `C-0006`'s flatness and `C-0010`'s variance re-run on a sheet carrying `C-0055`'s upward arm array | A8.2 | **DONE** (iteration 10) — claim [`C-0061`](gpd/claims/C-0061-stacked-arm-sheet.md), raises [`CH-0074`](gpd/challenges/CH-0074-the-flat-distribution-lives-on-stations-no-placement-supplies.md). **They do EXACTLY NOTHING to the sheet's rigidity, and the reason is the same sentence that limited them to 34.** A body attached to a structure at **one** point and otherwise free has a **zero Schur complement** there — its rigid-body motions span everything one crossover can impose — so 34 arms covering **46.3 %** of the tile's plan move `C-0009`'s peak dishing, peak crossover force and peak duplex shear by `5e−9` relative, and that residual is the arm regularisation and vanishes **linearly** with it over four decades. **The single tie is forced, not chosen**: an upward site belongs to one duplex, so its pitch is the bare 32 bp = **10.88 nm**, against an arm of **8.164 nm** at 34 paths and **9.131 nm even at §3's 45**. The bracket has **three** steps and none is large — one tie adds nothing; **two ties add a TORSION BAR and nothing else** (5.832 pN·nm/rad, **0.112 %** of the dishing, because two points determine a line); the arm's own `EI` needs **three** ties, i.e. 21.76 nm of arm; the 19.3× axially coupled second layer is outside both the motif and the kinematics. **Mass: 1.21× on a quality factor of `5e−4`.** **Drag is the only channel that moves anything and it is an upper bound** — the arms are on the `+z` face, so `C-0004`'s squeeze film is untouched **by construction**, and what they add is **9.1 %** of the total drag: the nominal corner goes 91.2 → **82.9 kHz** and the **worst** §4(d) margin 22.81× → **20.73×**, still discharged. **`C-0010`'s `σ_RMS ≤ 3.0 nm` is IDENTICALLY unchanged broadband** — equipartition sees no drag and no mass — with the in-band amplitude up **4.9 %**. **What the array really changes is where the coupling ENTERS**: on `C-0055`'s own 34 roots a uniform coupling dishes **0.4156** of the stroke against 0.2182 on `C-0015`'s 3 × 15 and **0.3079** free — **1.35× worse than no coupling at all** — and `C-0058`'s flat rim rule reaches only **0.1649** there against its published 0.0753 (`CH-0074`). Two smaller results: **`C-0055`'s placement is not centro-symmetric** (centroid at `x = −8.80 nm`) and reflecting alternate rows is free and worth 0.4156 → 0.3558; and the peak crossover force is **8.3×** larger at the arm roots than on the inset grid. Opens `T-125` and `T-126`. ~~TODO — **high, and it is the largest open item `C-0055` leaves.**~~ `C-0055` moves the `E5a` array off the sheet's own crossovers and onto the unoccupied upward azimuth, which costs the host **no crossover and no duplex length** — so every table in `C-0054` (computed on *consumed* interfaces) simply does not apply, and nothing replaces them. But 34 arms of 8.16 nm are **278 nm of duplex added out of plane at one interhelical distance**, i.e. mass, drag and rigidity in a direction no model in this programme contains: `C-0009`'s grillage is a single sheet, `C-0006`'s plate is 2-D, and `C-0010`'s variance budget has no out-of-plane member. The cheap bound first — the arms' plan footprint is 0.46 of the tile and their added second moment about the sheet's mid-plane is `A d²`, which is knowable before any solve. Also unresolved: the **8 bp staple break** Ke et al. name as a yield cost has a published sign and no published size at 34 arms per tile, and the caDNAno scaffold route that visits them (35.7 % of M13 is a ledger, not a route). |
| T-122 | Can a 5:1 per-path coupling stiffness ratio be BUILT? | `C-0030`/`C-0023`'s element catalogue asked for two stiffnesses on one tile, or the statement that a coupling can only be uniform | A8.2 | **DONE** (iteration 10) — claim [`C-0060`](gpd/claims/C-0060-buildable-stiffness-ratio.md), raises [`CH-0073`](gpd/challenges/CH-0073-the-along-helix-scatter-rule-reverses-on-a-non-uniform-coupling.md) against `C-0026`'s build rule. **YES on the stiffness, NO on the placement — and the qualifier `C-0058` needs is about the ARRAY, not the ratio.** The declared falsifier — *one quantum of the design parameter taking the ratio out of the flat window* — **did not fire at any of fourteen element/level pairs**: the coarsest quantum is **19.1 %** of a level's own stiffness (an 11 bp hinge arm), the finest **1.0 %** (a 99 nt ssDNA limb), against a flat ratio window this task **measures rather than cites** at **3.5 ≤ R ≤ 20**, i.e. 471 % wide — quantisation is **25× finer than the requirement**, where `C-0023`'s mounting-offset quantum was **8.3× coarser** than its own. **The structural reason is one line**: a preload is a *length* and a stiffness is a *power* of a length, so the same 0.34 nm enters divided by 87 base pairs rather than by one. All seven settings reach both levels (realised ratios **4.667–5.144**) and **all fourteen built designs are flat**, 0.0715–0.0815 against `T-5b`'s 0.10. **A second cheap bound turned out to be load-bearing**: rounding the two *levels* independently misses `C-0017`'s mandate by **0.40–5.44 %** — a placement error, not a rounding nuisance — and **trimming individual paths by ONE base pair** (legitimate because the mandate is an equality on a **sum**) takes the worst to **`1.3e−4`** in ≤ 18 moves, at the price of 3–4 distinct staple lengths instead of two. **`T-45` is answered as a THRESHOLD**: the built design loses flatness at **34.6 %** relative scatter (31.6 % with the total held), **2.04×** `C-0026`'s 17 % break-even and **0.52×** the 66.7 % at which the two populations merely *overlap* — so **flatness binds at half the amplitude the ordering does**, and small scatter *helps*. **What fails is the array**: `k ∝ span^(−3)` makes the soft level **1.71×** the longer member, so `C-0030`'s interior span is **52.36 nm** on a 40 nm tile and places **0** of 45; `E3f` 15, `E5n1` 30, `E5n2` 15, `C39` 0 — six of seven — and only `E4` places 45, at the second ground `C-0023` already excluded it for. **A small free result: the best one-parameter ratio at `C-0058`'s own collar is 7, not 5** (0.0653 against 0.0753, a further 13.4 %), and it is no harder to build. Opens `T-127` (does a MIXED-span array pack?). ~~TODO — **high, and it is the largest open item `C-0058` leaves.**~~ `C-0058` makes the Gen-1 tile flat for the first time by giving the 34 rim stations 5× the stiffness of the 11 interior ones at `C-0017`'s unchanged total — but every distribution in it assumes an independently specifiable linear spring per station, which no claim has ever had to supply. The cheap bound is one line: `C-0030`'s flexure goes as `span^−3`, so 5× is a **1.71× span ratio** (21.44 → 12.5 nm at the same section), which `C-0041`'s packing has to admit *and* `C-0049`'s per-path allowable has to survive at the shorter, stiffer end. Also to check: `CH-0042`'s strain-softening is a **different curve per span**, so two spans do not stay in a 5:1 ratio over the stroke, and `C-0058`'s springs are linear. **If the answer is no, `CH-0071` stands only with the qualifier "for buildable couplings" and `CH-0034`'s floor returns.** |
| T-123 | Is ANY distribution flat at every one of `C-0022`'s solved states? | `C-0058`'s minimax re-run with a real optimiser, more starts and the placement freed, or a bound saying it cannot be | A8.2 | **DONE** (iteration 11) — claim [`C-0064`](gpd/claims/C-0064-robust-distribution.md), raises [`CH-0077`](gpd/challenges/CH-0077-five-solved-states-are-four-devices.md) against `C-0058`'s Deliverable 4 and `CH-0071`'s condition 2. **NO at the five states, YES over every operating RANGE — and the requirement itself was mis-posed.** A real minimax — log-sum-exp smoothing with continuation, **analytic gradients through the Woodbury solve**, conjugate gradients on the log-weights, **42 starts**, `C-0058`'s own optimiser as a polish — reaches **0.1254** of the stroke against `C-0058`'s 0.1587 (−21.0 %) and stops, still **1.25×** `T-5b`'s 0.10. **The search is not the limit**: 16 starts to 42 moves it by **exactly zero**, the homotopy is worth 8.4 %, and **two states are active at the optimum**. At a *single* state the same optimiser is **2.54× better** than `C-0058` (0.0214 against 0.0544), so every state is easy alone — the difficulty is entirely in sharing. **The obstruction is a SIGN.** Of the 31 non-empty subsets, **every one of the 14 that puts the 2 nm state with a 10 nm state fails** (0.1086–0.1254) and **every one of the other 17 is flat** (0.0090–0.0799), including everything-but-2-nm at **0.0799** and 2 nm paired with its OWN device's rest state at **0.0620**. The 2 nm state's free-tile dishing field is **anti-parallel** to every other's — cosine **−0.943 to −1.000**, exactly **−1.000** against 10 mM, where all six other pairs run **+0.949 to +0.997** — and `C-0022` says why in a row nobody had used: it is the **only one of its 21 solved states** whose finite tile carries *less* total force than a 1-D pressure over the footprint (**−3.91 %**). A distribution that flattens an edge *enhancement* deepens an edge *deficit*. **But the five states are FOUR DEVICES** — three buffers' rest states at a 10 nm layer, plus the rest and *held* states of a **5 nm** layer, of which the 2 nm one is `C-0022`'s own *"held at 3 nm stroke"* row and no state of the 10 nm device at all. Over the range each device traverses (one buffer, one layer, one bias, `L₀` → `L₀ − s`, both ends solved by `C-0022` at 0.192 V for the design device) the minimax is **0.0373** (`C-0018`'s placed 2 mM device), **0.0435** (`C-0032`'s 0.5 mM), **0.0620** (the 5 nm device) and **0.0504** (10 mM) — **all four inside `T-5b`'s 0.10**, both endpoints active at each optimum, and two interpolated intermediate gaps move the first by **exactly zero**. **A real buildability failure is reported**: the 45-parameter optimum's **quantisability is not a well-posed quantity** — its optimal set is a MANIFOLD (2 active constraints, 44 free directions), and two builds differing only in decision precision returned equally good points (0.0373 and 0.0372) whose spreads were **26.0×** and 7.9× and whose two-level projections were **0.0899** and **0.1002**, on opposite sides of `T-5b`'s line. **What answers it is that a constrained family must be searched IN, not projected ONTO**: `C-0058`'s own two-level rim × 5 rule is flat over the whole traversed range of both 10 nm devices at **0.0753** and **0.0683**, 24.9 % better than the projected optimum and already priced buildable by `C-0060`. **Cost**: 4.243 pN per path at 3 nm (2.36× clear of the 10 pN unzip allowable, inside `C-0049`'s 4.50 and *outside* its 1.35 at the desired 10 nm), 1.106 pN worst crossover (9.0× clear), 1.366 pN duplex shear, and **+91 %** on `C-0014`'s thermal force — the largest single cost of the non-uniformity. **The cheap bound did not fire**: the per-state least-squares floor is 0.00321, 31× below the tolerance, so the five-state negative is a *"not found at a large budget"* and not a theorem. **Conditional on placement**: every station is `C-0026`'s 3 × 15, and `C-0063` (`T-125`) resolves `CH-0074` on a *different* station set where `C-0058`'s rim rule REVERSES SIGN — so nothing here transfers, and **whether `C-0063`'s flat placement is flat over a RANGE is now the largest open item** (it reports 0.0706 at one state). Opens `T-129`. ~~TODO~~ **medium-high.** `C-0058` finds a distribution flat at the design point (0.0544–0.0753 of the stroke) whose worst state is 0.187, and a minimax over all five solved states that reaches only **0.1587** — marginally *above* `CH-0034`'s 0.149. That is a *"not found"*, not a *"does not exist"*: the search is a coordinate descent from three starts at fixed stations. Three things are untried — a genuine minimax formulation (the objective is a max of convex-ish surfaces and the `C-0058` surrogate makes an exchange algorithm cheap), the **placement** as a variable rather than `C-0026`'s grid, and the force-space least-squares bound evaluated **per state** to see whether a common flat point is excluded at all. **The uniform coupling is already flat at the two compressed states**, so what is wanted is a distribution that does not spoil them. |
| T-124 | Re-derive the truss branch's junctions on the torsion-feasible set | `C-0029`'s, `C-0042`'s and `C-0052`'s routings re-optimised over `C-0057`'s feasible placements, with the mechanical consequences carried through — or the statement that no feasible placement supports them | A8.2 | **DONE** (iteration 10) — claim [`C-0059`](gpd/claims/C-0059-torsion-feasible-routing.md), raises [`CH-0072`](gpd/challenges/CH-0072-the-alignment-is-not-free-on-the-torsion-feasible-set.md). **A split verdict: two of the three junctions survive on the feasible set and the third does not.** `C-0057`'s census re-derives to **departure 0** (3 546 / 1 855 and 280 / 137 of 69 120). The **single junction** closes at a chord of **90.0° — exactly the flexure axis**, `cos²ψ = 1.0000`, at a 0.643 nm binding link in the window's *interior*, 7 of the 120 best-aligned. The **pair** closes at every separation 6–12 bp but its alignment stops being free — 33°/**69°**/57°/**6°**/**6°**/33°/33° against `C-0042`'s 0.00° everywhere — so its own **7 bp row is the worst of the seven** and, at 69°, is **not representable at all** under `C-0037`'s two-axis base; **9–10 bp** delivers 6.0°. The **trio does not close**: 750 of 1 800 crossbar lattices are reach-feasible and none of the 24 best-aligned survives, 134 junction solves, 0 closing. **And `C-0029`'s recommended scaffold excursion cannot be aligned** — 1 of 120 at 39.0° against independent staples' 7 at 0.0°. **The mechanics is nearly unchanged** (1.81–2.45 / 1.36–1.84 against `C-0052`'s 1.81–2.43 / 1.36–1.83), because the binding misalignment is the leg's own quantised twist budget and not the chemistry. **The cheap bound written to bind did not** — the reach-feasible set occupies 118 of 120 azimuths — and that falsifier was declared in advance. Leaves `T-127`. |
| T-127 | Does a torsion-feasible trio exist on the crossbar at all? | `C-0059`'s trio search deepened — more lattices, more candidate azimuths per junction, the crossbar length and row pitch freed — or a bound saying the cap cannot be routed | A8.2 | **DONE** (iteration 11) — claim [`C-0062`](gpd/claims/C-0062-crossbar-trio-existence.md), raises [`CH-0075`](gpd/challenges/CH-0075-the-cap-floor-is-read-off-a-stage-that-failed.md). **THE TRIO EXISTS, at every one of the 21 admissible `(crossbar, row)` configurations, and `C-0059`'s negative was a property of its budget and not of the geometry.** The cheap bound is the whole answer: the three junctions are conditionally independent given the lattice, they close on **21.1 % / 20.0 % / 27.8 %** of lattices marginally, so `C-0059`'s **24** solved lattices were worth **0.28** trios — a null result was the expected one. Deepened to **49 857 lattices solved and 149 789 junction solves** (**1 039×** and **557×** `C-0059`'s 48 and 269, 299 578 link closures) the crossbar carries **609** closing trios: **4–13 per configuration** of 750–904 reach-feasible, **151 of 11 834** and **167 of 11 874** in the two 25 920-lattice depth runs. **Restricted to `C-0059`'s own budget this task also finds nothing**, and `C-0059`'s class still returns 750 / 24 / **134** / 0 / 6.0° at departure 0. The budget is affordable only because **`C-0057`'s reach bound is a PER-ASSIGNMENT proof of exclusion** — 12 % of the 32 assignments survive it, a **17.2×** speedup with **0 disagreements** against `bestLinkClosure`. **39 of the 44 published trios also close on `C-0057`'s 180-step verdict grid.** Two smaller results: **`C-0052`'s own 13 bp crossbar is the worst of the three lengths at its own row** (6 closures at 57° against 13 at 21° for 14 and 15 bp, and overhang is mechanically free); and **the closing count is a density on a continuum, not a converged number** — refining the phase 45 → 180 takes it 2 → 18, so at `C-0059`'s own resolution this task finds 0 too. **The mechanics does not move**: recomputed at ONE row pitch throughout and at the alignment a *closing* trio delivers, the best representable design is the **10 bp row at both ends** (base 6.0°, cap **27.0°**, 17 bp crossbar, 4.08 nm leg) at **2.45 / 1.84**, against `C-0059`'s own best 2.45 / 1.84 — **a 4.5× worse cap floor for a third-decimal change**, which is `C-0059`'s insensitivity finding tested rather than assumed. `C-0042`'s **7 bp** and `C-0037`'s **8 bp** rows stay **NOT REPRESENTABLE** (base 69.0° and 57.0°). `CH-0075`: `C-0059`'s table reads the row pitch at 7 bp for the cap and 9 bp for the base, and its cap floor is the best *reach-feasible* alignment of a stage that returned no closure. ~~TODO — **high, and it is the item that decides the truss branch.**~~ **high, and it is the item that decides the truss branch.** `C-0059` finds **0 closing trios in the 24 best-aligned of 750 reach-feasible lattices** at two candidate azimuths per junction, against a single junction that closes exactly aligned and a pair that closes at every separation. That is a *"not found within the budget"*, and the budget is thin: 24 of 750 lattices, 134 junction solves. What is untried is the crossbar **length** (only 13 and 15 bp were swept), the **row pitch** (only 7 bp, while `C-0059` moves the *sheet* row to 9–10), more azimuth candidates per junction, and the six-target distinctness as a search variable rather than a filter. **If it comes up empty at a real budget, `C-0048`'s cap has no routing and the truss branch closes** — which §7 wants early. |
| T-125 | Sweep the row phases of `C-0055`'s upward arm array | The flatness of the best 34-root placement under `C-0022`'s solved load, against `T-5b`'s 10 % and against `C-0058`'s 0.0753 | A8.2 | **DONE** (iteration 11) — claim [`C-0063`](gpd/claims/C-0063-upward-root-placement.md), **resolves [`CH-0074`](gpd/challenges/CH-0074-the-flat-distribution-lives-on-stations-no-placement-supplies.md)**, raises [`CH-0076`](gpd/challenges/CH-0076-the-mirrored-placement-is-on-the-other-face-of-the-sheet.md). **The upward array makes the Gen-1 tile FLAT, and it does it with EQUAL SPRINGS.** The best of **1 144 858** evaluated placements dishes **0.0706** of the stroke under `C-0022`'s solved load — inside `T-5b`'s 0.10, **4.36× better than no coupling at all** (0.3079), **5.9×** better than `C-0055`'s own placement (0.4156), and better than `C-0058`'s **0.0753** on a 3 × 15 grid no placement supplies — at `C-0017`'s unchanged total, with **no distribution at all**. **Two cheap bounds did the work.** The count vector is forced: `3a + 2(15 − a) = 34` gives **exactly four rows of three**. And a placement can be centro-symmetric only where `2c ≡ 0 (mod 10.88 nm)`, which is **exactly 2 of the 32 phases — 8 and 24**, both inside `C-0015`'s eight-column ten; the winner is at **24**, is centro-symmetric, and its centroid is `0.000 nm` against `C-0055`'s `−8.80`. It came from an **exhaustive** enumeration of **361 584** centro-symmetric placements, which beat the descent at its own phase (0.0706 against 0.0917). **The flat set is `C-0015`'s ten**: all ten eight-column phases reach 0.077–0.092 and only one of the other twenty-two gets inside the convention — a **fifth** independent construction landing there. **Every phase beats no coupling**, so `C-0061`'s dishing *source* is a property of its greedy scheduler, not of the lattice. **`C-0058`'s rim rule REVERSES SIGN here** — 0.0706 uniform against 0.2214 at its own ×5 — and the 34-parameter optimum wants a peak ratio of **1.30** for a further 13.9 % (0.0608), against a least-squares floor of 0.0031. **Cost unchanged in kind**: 2.30 pN in the worst path solved (2.94 pN on the mandate secant), 3.4× clear of the 10 pN unzip allowable, 0.35 pN of `C-0014` thermal force, 1.25 pN in the worst crossover (8.3× the 3 × 15's, as `C-0061` warned, still 8× clear). **`CH-0076`**: `C-0061`'s *"free, one line"* mirrored placement is **not on the upward lattice** — reflection maps a row's `EAST` sites exactly onto its `WEST` ones, so 16 of its 34 arms hang **into the grafted layer** — and its 0.4156 is read on a **phase-8 host** while the array is at phase 0, which is worth **38.9 %** (0.5771 on its own seven-column host). ~~TODO — **high, and it is the cheapest open item on the branch.**~~ `C-0061` found `C-0055`'s own placement is **not centro-symmetric** — its scheduler fills every row greedily from the low-`x` end and points every arm the same way, so the coupling centroid sits at `x = −8.80 nm` on a tile running −20 to +20 — and that reflecting the odd rows, **one line of code, free, on the same column lattice**, takes the dishing from 0.4156 to 0.3558 of the stroke and the rim × 5 design from 0.3419 to 0.2250. That is **more than `C-0058`'s whole rim rule buys on the unreflected set**. `C-0055`'s own gate-3 finding is that the per-row problems are **independent**, so the sweep is a product of per-row phases and is cheap; nobody has run it. The falsifier is that the best placement still misses `T-5b`'s 0.10, in which case `CH-0074` hardens from *"this station set is not flat"* to *"no station set the upward lattice can supply is flat"*. |
| T-126 | Does the arm slab clear `C-0035`'s tie-down path? | A plan-and-section answer, or the statement that the only buildable mounting and the only buildable hinge array cannot coexist | A8.2 | **DONE** (iteration 12) — claim [`C-0066`](gpd/claims/C-0066-arm-slab-tie-clearance.md), raises [`CH-0079`](gpd/challenges/CH-0079-a-tie-grid-is-a-registration-and-an-armed-tile-has-none.md). **They CAN share one face, and there is exactly one registration at which they do — the ties must land ON the arms.** **The section decides the plan**: the tie has to reach the **tile**, so its clear column runs from the tile's top face at `z = +1.0` to the standoff base plane and strictly contains the arm slab (**1.69–3.69 nm** at rest, **1.69–6.69 nm** swept at 3 nm), which makes every plan overlap **level-independent** — `C-0041`'s Fact A in a new place, and the reason the **0.664** area fraction settles nothing. **Every regular grid then fails**: on `C-0063`'s phase-24 placement `C-0015`'s own 3 × 15 puts **30 of 45** ties inside an arm at the published arm senses and **26** at the best of them, because **8 of the 15 rows have no choice of sense at all** (the tile edge forces every row rooted at `±16.32`); two columns give **24 of 30**, one column **10 of 15**; and **no rigid translation of a two- or three-column grid clears every row** over 400 001 offsets, the one-column grid clearing in four windows the nearest **6.785 nm off the tile centre-line**. **But the room is there** — the array leaves **108** places a tie could stand against the 45 demanded, and the poorest row holds five — **so what refuses them is REGISTRATION, not size, and `T-102` is not what this wants.** The escape (snap each tie to the feasible `x` nearest its column, worst displacement **4.332 nm**) is **nearly free and not worth taking**: it costs **+1.7 %** of dishing (0.2182 → **0.2219**) because the displacement is *along* the helices where `C-0026`'s registration is *across* them, and neither set is flat, while the design that **is** flat needs no tie grid at all (`C-0063`'s 34 roots, **0.0706**, coupling entering at the hinges). **The one free registration is the arms' own tips**: 34 ties on the arm ends clear every neighbouring arm by **2.7156 nm** against 1.345 demanded — the **root pitch minus the arm**, which as a free-standing gap clears a 2.69 nm duplex by **0.0256 nm** and fails at the 2.73 nm square-lattice value. **The sweep runs the FAVOURABLE way** (the plan projection is `√(L²−s²)`, so the swept envelope is the rest footprint identically and a static plan view is conservative), and §3's **desired 10 nm does not change the clearance — it removes the element**, corroborating `C-0050` from a plan view. ~~TODO — **medium-high, and it is the one thing `C-0061` states without solving.**~~ `C-0035` found exactly one buildable mounting: standoff bases on the **output superstructure**, standoffs pointing **away** from the tile, the flexure **outboard** of its own ground, and each midspan tied back **down** through that ground to the tile. `C-0055`'s upward arms occupy a slab **1.69–3.69 nm above the sheet surface over 46.3 % of the plan**, on the same `+z` side. `C-0061` shows the arms cost the actuation gap nothing — they are on the dry face, and every number in `C-0035`'s occupancy table is untouched — but the tie-down path crosses exactly the space they occupy, and `C-0035`'s own note that *"the tile now carries no out-of-plane element at all"* is no longer true of a design that adopts the escape. `CH-0055`'s 326 nm² tie aperture and its **18-component** severance verdict are the other half of the same geometry. |
| T-128 | Does a MIXED-span flexure array pack where a uniform one does not? | A plan-view answer for `C-0060`'s two levels laid out together — long members where there is room and short where there is not — against `C-0041`'s 15 and `C-0058`'s 45, or the statement that the mixture cannot help | A8.2 | TODO — **high, and it is the only route by which `C-0058`'s flat design becomes buildable.** `C-0060` shows the 5:1 *stiffness* ratio is free and the *array* is not: `k ∝ span^(−3)` makes the soft level **1.71×** the longer member, so a 45-station mixed array priced at the longer span places **0** on `C-0030`'s element and 15 on `C-0023`'s. But `C-0041`'s and `C-0053`'s packing sweeps evaluate **one length for the whole array**, and the design here is 34 short members on the rim and 11 long ones in the interior — exactly the geometry a uniform sweep cannot represent, and the long ones sit where the tile is emptiest. Also open in the same breath: whether the two levels may be **different elements** (a hinge arm at the interior, a flexure at the rim), which `C-0060` names and nothing forbids. |
| T-131 | **Reconcile `ANSWERS.md` against the claims, line by line** | Every number and verdict in `ANSWERS.md` traced to a standing claim, with each superseded statement corrected or explicitly marked superseded — or a list of the statements that cannot be traced | — | **DONE** (iteration 12) — claim [`C-0067`](gpd/claims/C-0067-answers-reconciliation.md), **raising no challenge**. **84 statements adjudicated: 63 TRACE, 16 had DRIFTED, 2 are UNTRACEABLE, 4 completeness gaps closed.** The arithmetic is near-perfect — **414 of 415** numeric tokens appear in the corpus and the one that does not is `42.4`, a rounding of `C-0032`'s `42.38` — so **the drift is in the STATUS of answers, not in the value of numbers**. Worst kind, three instances: entries of *"What we cannot answer"* that the programme had **answered** and left standing for up to **seven iterations** (`T-60`→`C-0033`, `T-75`/`T-78`→`C-0035`, `T-76`→`C-0032`/`C-0049`) — **a deliverable that under-claims is as wrong as one that over-claims and is harder to catch**, because a reviewer checks assertions and not disclaimers. Most consequential single correction: §1's *"the 32 % split is **irreducible**, no coupling choice can remove it"* is **false** — `C-0058` reaches **0.0753** by distributing the same total and `C-0063` **0.0706** with **equal** springs by placement, against 0.3079 uncoupled. Also corrected: *"seven of eleven axes"* → **ten of twelve** (`C-0051`); *"`P2` closed by `C-0017`"* → closed for the affine mandate and **failing** for the realised coupling (`C-0032`); *"45 needed for flatness"* → `CH-0034`'s saturation reading; *"`T-1f` is the binding uncertainty"* → **`T-50`** (`CH-0019` — `T-1f` bounds the *polymer* expansion); *"twenty-nine challenges"* → **69**; *"two paywalled papers"* → **one**; the 90° routing's own routing withdrawn by `C-0057`/`CH-0070`; the mounting sense settled by `C-0035`; the 40 pN/nm ceiling withdrawn by `C-0049` with the verdict surviving on `C-0035`'s ground. **Five of the six specification questions were not asked in the deliverable at all** and are now a table with thresholds. **No claim was found to be wrong** — every disagreement was the synthesis misreporting the corpus, which is why nothing is challenged. Tooling: [`tools/trace-answers.py`](tools/trace-answers.py) + **22 checks** in [`tools/test-trace-answers.py`](tools/test-trace-answers.py), retained so the next pass costs one command. **Left undone: the window is still unsynthesised against iterations 8–11** (`C-0052`–`C-0064`, `CH-0065`–`CH-0077`) — recorded in `ANSWERS.md` as a coverage statement rather than assumed away. |
| T-130 | Do `C-0062`'s closing trio lattices PLACE 34 times? | The closing lattices carried into `C-0053`'s plan view, or the statement that a trio which closes in isolation cannot be repeated | A8.2 | **DONE** (iteration 12) — claim [`C-0065`](gpd/claims/C-0065-crossbar-array-placement.md), raises [`CH-0078`](gpd/challenges/CH-0078-the-base-floor-is-a-minimum-over-a-coordinate-the-array-pins.md). **YES — all 44 of `C-0062`'s recorded trios place 34 times, and the plan view is not what the composition costs.** Three cheap bounds settle the plan first: 34 truss blocks cover **0.385** of the footprint, the widest crossbar demands **9.49 nm** against the **10.88 nm** station pitch (`C-0053`'s 45-path arm demands 11.82 and does *not* clear it), and — **the bound that fixes the shape of the answer** — every one of `C-0063`'s 34 stations is the **SAME helical phase of its OWN host duplex**, 24 bp from that duplex's `NORTH` plane, because adjacent rows are phase-shifted by exactly the 16 bp their sites are offset by. **So the register is ONE question, not thirty-four, and the count is quantised at 0 or 34.** It comes out 34: 0 overlaps, 0 leg clashes, one level, at every trio. **What the composition finds is a REGISTER.** A leg's *base* sits only where the host's backbone offers one — **17 of 89** axial positions do — and **no row pitch closes at the station itself**, so the array translates **0.17 nm (9 bp) to 3.91 nm (6 bp)**, and **no crossover phase can absorb it** because the phase moves the station and the backbone together (a gate test). Pinning that coordinate re-reads the floor `C-0059` measured and `C-0062` built its table on: the **10 bp** row — `C-0062`'s own recommendation — reads **57.0°** against its published **6.0°** (**9.5×**), past the **45°** at which `C-0037`'s `TwoLinkBase` cannot represent a base at all (`CH-0078`); the 6.0° exists 2.55 nm away, at the price of the rim and the flatness. **The row an array can build is 9 bp at 18.0°**, and it keeps the tile flat — **0.0780** of the stroke against `C-0063`'s 0.0706 at 34 nominal stations, inside `T-5b`'s 0.10, with splitting one station into two leg bases costing 6.1 % and the register a further 4.2 %. **17 of the 44 trios survive every clause at once** (place 34, close on the verdict grid, representable base, flat tile) — the 9, 11 and 12 bp rows. **The flexure is the other story and is reported apart**: with `C-0030`'s 27.41 nm span the same array covers **1.84×** the footprint, needs **7 levels** and places **12 of 34** — `C-0041`'s standing obstruction, independent of every trio. Also recorded: this task's prompt quoted *"93 of 5 940 … at 3.00°"*, numbers that appear nowhere in `C-0062` and that `JOURNAL.md` already records as retracted first-report text. Opens `T-132`, `T-133`. ~~TODO~~ |
| T-132 | Does the leg's own length budget survive the PINNED base misalignment? | `C-0052`'s `chordPairMisalignment(m)` re-composed against `C-0065`'s registered base misalignment at one global leg length, over the whole 12–26 step envelope — or the statement that no leg length serves all 34 instances | A8.2 | **DONE** (iteration 13) — claim [`C-0070`](gpd/claims/C-0070-pinned-leg-budget.md), raises [`CH-0082`](gpd/challenges/CH-0082-a-truss-has-two-legs-and-the-design-table-gives-it-one-base.md), discharges [`CH-0078`](gpd/challenges/CH-0078-the-base-floor-is-a-minimum-over-a-coordinate-the-array-pins.md)'s item 2 and **upholds it**. **YES — the budget survives, at one leg length for all 34 instances, and the shared-length clause costs NOTHING.** Cheap bound 1 decides the shape: `C-0065`'s phase-class congruence carries from the station to the **68 leg bases**, which fall into exactly **2 classes of 34** presenting **1** distinct `(low leg, high leg)` pair at every row pitch — so *"one length for 34 instances"* collapses to *"one length for two legs"*, and the declared falsifier did not fire. What pinning actually does is make the base **OVERSPEND** `C-0052`'s budget rather than spend it exactly: a free rotation always opposes the budget's sense and a pinned one need not, so `ψ_base + ψ_cap ≥ |m τ − 90°|` **always**, and at the recommended design the two ends spend **81.13°** of a **45.13°** budget (**1.80×**). **And nothing fails** — at the 9 bp row **all 15** leg lengths of the 12–26 envelope are representable and pass all nine predicates, at margins **1.815–2.443** (CanDo) and **1.364–1.836** (Fields). The best shared length is **12 steps, 4.08 nm**, at **every** surviving row; at 9 bp it carries **2.443 / 1.836**, *better* than `C-0062`'s own 2.410 / 1.812 there (a worse cap moves couple into the plane that governs) and **0.13 % below** the 2.446 of the 10 bp row the array cannot build. **`C-0065`'s 17 of 44 and its 9 bp / 18.0° recommendation are unchanged** — the tightening is zero. One new constraint appears and does not bind: a truss has **two** legs pinned at **two** azimuths (9.0° and 18.0° at 9 bp), so their cap chords differ by that angle at every length, giving a floor `|fold(δ_A−δ_B)|/2` — 4.5° here, but **28.5°** at the 6 and 10 bp rows against `C-0062`'s composed 9.0° and 27.0° (`CH-0082`). One sensitivity moves the verdict: a **0.5 nm lateral seat** leaves the 9 bp row not representable at any leg length. ~~TODO~~ |
| T-133 | Is there an output element that does not lie in the plan? | An element whose 34 instances place at one level on `C-0063`'s stations, or the statement that the truss branch's output stage is refused by the plan view at every span | A8.2 | **DONE** (iteration 13) — claim [`C-0069`](gpd/claims/C-0069-output-element-placement.md), raises [`CH-0081`](gpd/challenges/CH-0081-a-rigid-root-demands-a-longer-arm-than-the-plan-admits.md) against the truss branch's output stage. **NO element out of the plan exists, and none is needed — the plan was never what refused the flexure.** Of an **11-row catalogue**, **3 place** all 34 at one level and **2 survive every clause**: `C-0055`/`C-0063`'s own **hinge-rooted arm** (**8.16439 nm**, 34 of 34, one level, 0.463 of the footprint, tangent minimum **30.03 pN/nm** over `[0, 3]` clearing **6 of `C-0017`'s six** 2 mM floors, 2.941 pN per path, two-sided) and a **standoff-headed crank** (`C-0028`'s `B2` base + a pinned tip, **5.331 nm**, 1.53× shorter, but only **4 of 6** floors, a compression member at 2.45× of Euler margin, and a second undemonstrated motif). **Three closed-form bounds settle the element space before any packer runs.** The plan budget on **every** 34-root placement is `pitch − d` = **8.19 nm** exactly (four rows of three are forced by `3a + 2(15−a) = 34`; the bisected ceiling agrees to `<1e−9`). The two-support flexure family's **shortest possible** member is `(48 EI/k)^(1/3)` = **22.41 nm — 2.74×** the budget, so it is refused **at every span, every end joint and every placement**, which is strictly stronger than `C-0065`'s 12 of 34. And the normal direction supplies only **axial** stiffness: `S/k` = **1122 nm**, 112× `C-0017`'s whole envelope. **What refuses the flexure is its END CONDITION**: `c` is 48–192 supported twice and ≤ 12 supported once, and the span is `c^(1/3)`. The other escapes fail too — a **fold** shrinks the budget (28 of 34 at the contour where the straight element places 34), a second **level** is reached only by a vertical member whose clash is level-independent (`C-0041`), and a **single strand** is compact (6.04 nm) and places and is **one-sided**, measured at negative argument. **The budget reduces to one inequality, `c ≤ 2.3416`, and both of the arm's joints sit inside it by 2–3 %**: at a one-crossover root the tip may be no stiffer than **79.68** and `C-0034`'s `A2` is **78.24**; at an `A2` tip the root may be no stiffer than **13.93** and one crossover is **13.53**. Neither joint was chosen for that reason and **the design has no margin on either**. The window has a **floor** as well, `1.5×` the stroke = 4.50 nm — a pinned tip at a one-crossover root asks for 3.591 nm and is **refused by its own kinematics, short enough to place and too short to work**. **Three sensitivities close the 0.0256 nm margin**: the 2.73 nm square-lattice interhelical distance (18 of 34), the top of Chen et al.'s own fitted `α` bracket (30 of 34), and §3's 45 paths (24 of 34) — so **34 is the count the plan budget prefers**, not a rounding of 45. `C-0065`'s 27.4119472 nm span and its **12 of 34** both reproduce at departure `0.0`. ~~TODO~~ |
| T-134 | **Does a tolerance model erase the branch's two knife edges?** | A scatter model for the plan positions, applied to `C-0069`'s 0.0256 nm arm margin and `C-0066`'s 0.0256 nm tie clearance, with the placed count as a distribution rather than a point — or the statement that no defensible scatter model exists and both margins are unquotable | A8.2 | TODO — **high, and it is the first thing to run against [`C-0069`](gpd/claims/C-0069-output-element-placement.md).** Two claims now carry a headline that sits inside **0.03 nm** — 0.075 of a base-pair rise — and both are computed on `C-0041`'s hard-body plan model at **nominal** positions, which has no thermal excursion, no assembly tolerance and no out-of-plane bow in it. `C-0069`'s own sensitivity table already shows what a 0.04 nm move costs: at the **2.73 nm** square-lattice interhelical distance the arm places **18 of 34** rather than 34. **The two quantities are not independent** — both are `pitch − arm` on the same lattice — so a single scatter model settles both, and it is the one axis on which the whole `C-0055`/`C-0063`/`C-0069` design is a coin toss rather than a design. Nothing in this programme has ever modelled scatter; the cheap bound is the *equipartition* excursion of a duplex end held by one crossover (`C-0009`'s `k_θ` and `k_s`, `σ = √(k_BT/k)`), which needs no new physics and can only be compared against 0.0256 nm. |
| T-129 | Is `C-0063`'s flat placement flat over a RANGE? | `T-123`'s multi-state minimax re-run on `C-0055`/`C-0063`'s 34 upward roots, over the states each device traverses, against `T-5b`'s 10 % — or the statement that the placement is a single-state result | A8.2 | **DONE** (iteration 13) — claim [`C-0068`](gpd/claims/C-0068-range-robust-placement.md), raises [`CH-0080`](gpd/challenges/CH-0080-the-equal-spring-advantage-belongs-to-the-ten-nanometre-layer.md) against `C-0063`'s Deliverable 2. **YES for the device the programme placed, and only for a 10 nm layer.** With **34 equal springs** and `C-0017`'s unchanged total, `C-0063`'s placement dishes **0.0789** over the whole range `C-0018`'s placed 2 mM device traverses (gaps 10 → 7 nm at its own 0.192 V, both ends solved by `C-0022`), **0.0853** at `C-0032`'s 0.5 mM and **0.0896** at 10 mM — **all three inside `T-5b`'s 0.10**, against **0.0706** at the single state. **So the one-state verdict travels, and what the range costs is the margin**: 1.42× becomes **1.12×**, spent at the *compressed* end. **The exception is the 5 nm device** — the one whose range owns `C-0022`'s 2 nm state: equal springs dish **0.2000**, 2.0× outside, and are **worse than no coupling at all at both of its states** (0.1104 against a free 0.0638 at 5 nm; 0.2000 against 0.1648 at 2 nm), where the same equal springs on the 3 × 15 grid are inside it at 0.0796. **A distribution recovers all four** (0.0291 / 0.0365 / **0.0565** / 0.0382) at peak ratios of only **1.72–2.32**, far inside what `C-0060` prices — so this is a scope correction, not an infeasibility, and it is `CH-0080`. **The placement is vindicated twice, and the phase turns out to be what the LAYER selects**: re-enumerating the centro-symmetric family **exhaustively under a range objective** (361 584 placements, each priced under two objectives) finds **0 of 198 288** at phase 24 better than `C-0063`'s own, which *is* the range argmin — while under the **5 nm** device's range **nothing at phase 24 clears** (best 0.1169) and a **phase-8** placement does with equal springs (**0.0895**), reading 0.2416 at the 10 nm design state. Two interpolated intermediate gaps move the design reading by **exactly zero**, and the tightest range's 41/81/161 sampling spread is 0.08 %. **The cheap bound did not fire** (per-state floor 0.0056, 18× below) and `C-0064`'s sign instrument explains it: a 10 nm device's own two ends run **+0.9969 to +0.9998**, while the 5 nm device's two states run **−0.9427** — a device whose range is anti-parallel to itself. `C-0064`'s subset dichotomy transfers in **direction, not exactness**: 12 of the 14 mixed subsets fail (against 14 of 14 on the grid). ~~TODO — **high, and it is the cheapest open item on the branch.**~~ `C-0063` makes the Gen-1 tile flat at **0.0706** of the stroke with **equal springs** on a placement the upward lattice actually supplies, which resolves `CH-0074` — but it reports that at **one** state, `C-0022`'s 2 mM / 10 nm / 0.192 V design point, which is the same state `C-0058` was tuned at. `C-0064` (`T-123`) is the reason that is not enough: a flatness verdict at one state does not travel, the uniform coupling on the 3 × 15 grid moves from 0.2182 to 0.0710 across `C-0022`'s five, and the 2 nm state's edge effect has the **opposite sign** to every 10 nm state's. Everything needed exists: `coupling.MultiStateSurrogate` prices every state of one distribution on `n + S` load cases and `minimaxStiffnessDistribution` optimises the worst of them, both model-agnostic; `anchoring.UpwardRootPlacement` supplies the stations. Three questions in one run — is `C-0063`'s **winning placement** flat over the 2 mM device's traversed range; if not, does a **different phase** of the same lattice do better (the phase sweep is over a *placement*, and `C-0063` shows all ten eight-column phases reach 0.077–0.092 at one state); and does the **34-root** array need a distribution after all, given `C-0058`'s rim rule reverses sign there and `C-0063`'s own 34-parameter optimum wants a peak ratio of only 1.30. The falsifier is that the best placement misses `T-5b`'s 0.10 over the range, in which case the flat Gen-1 tile is a single-state result and `CH-0074` reopens on a new axis. |
| T-112 | **Which device does §3's DESIRED clause ask for?** | A specification answer from NDI, not a calculation | A8.2 | TODO — **it is a question, not a task, and it is the third this branch has raised** (after `T-95` and `T-102`). `CH-0059`: every claim reads the desired stroke on a coupling **placed for the acceptable one**, `k_c = 33.333 pN/nm`, which must then deliver **333 pN** (and `C-0039` finds the real element needs **699**). `C-0017`'s own arithmetic says the desired clause's coupling is `k_c = 100/10 = **10 pN/nm**` — a different device, which `C-0046` shows the flexure **can** build (arms 11.4–18.1 nm, 12 of 29 points clearing every element clause) and which `C-0017`'s **stability floor refuses at 2.34–2.79×**. Composing the two clauses caps the stroke at **3.58–4.27 nm at 100 pN** whatever the coupling is made of. **Carried to the open questions for Kazik as item 8.** |
| T-100 | The fan flexure, priced at §3's ACCEPTABLE stroke | The one hinge topology `C-0040` shows to be buildable, taken through `C-0023`'s predicates | A8.2 | TODO — medium. `C-0040` derives `n_eff = n_i·3(2m−1)/(m(2m+1))` for a raft hinged on `m` parallel lines and finds a 4 × 4 fan buildable on the tile (32.64 nm of line per interface) at 16.03 pN/nm, 2.08× too soft for the mandate at 45 paths. Nothing has priced it at the acceptable stroke, where the mandate is the same but the geometric demand is 3 nm against a 9.415 nm lever. It is the only hinge topology in the programme that is *buildable as counted*. |
| T-95 | May the output superstructure be perforated under each flexure midspan? | A specification answer from NDI, not a calculation | A8.2 | TODO — **it is a question, not a task, and it is worth §3's DESIRED stroke.** Raised by `C-0035`/`T-78`. The favourable topology needs the tie to cross the standoff base plane, so the superstructure needs **45 duplex-omission holes = 326 nm², 20.4 % of the tile footprint, at every stroke**. That is the floor. Buying §3's **desired** 10 nm stroke at `ℓ = 8 nm` needs those holes widened into **18.37 × 2.69 nm slots — 2223 nm², 1.39× the footprint**. The threshold: an imperforate superstructure caps the stroke at `ℓ − 2.69 nm`, i.e. **5.31 nm at the recommended `ℓ = 8 nm`**, which covers §3's acceptable clause and nothing more. **Carried to the open questions for Kazik as item 6.** |
| T-96 | Does the surviving mounting survive `T-31`'s array packing? | 45 flexures of ~32 nm span placed in plan, with their 45 apertures, on a body the size of the tile | A8.2 | **DONE** (iteration 6) — claim `C-0041`, challenge `CH-0055`. **It does not, at EITHER stroke, and it does not fail on area.** The cheap bound is 2.59× the footprint with the standoff feet — exactly the size that invites *"stack it in three levels"*, and `C-0017`'s envelope has room for three (`ℓ = 5.78 / 7.82 / 9.86 nm`). **Stacking buys nothing**: a standoff runs from the superstructure to its own beam plane and a tie from that plane to the tile, so any two vertical members share a height range whatever levels their beams sit at — the clash is **level-independent**. Two lattice facts meet nowhere: the grid's **across-helix pitch IS one duplex**, so beams in adjacent rows are tangent at zero tilt and mutually bury each other's ties at any other; and its **along-helix pitch, 13.33 nm, is under the span plus a duplex, 34.51 nm**, so a row's beams bury each other's standoff **feet**. **0 of 720 orientations, at any level count, on any body** — for 3 × 15 and for 2 × 15. The tile carries **exactly fifteen**: 1 × 15, span **21.44 nm = 63 bp**, feasible at **exactly one** of 720 orientations and that one supplied by the sheet's own helix direction. **The two clauses differ in KIND** — at 3 nm the binding variable is the **path count** (45 → 15, free: 6.67 pN/path against 10 pN, 2.16× buckling margin, tangent 25.49 pN/nm); at 10 nm the count is bounded **below at 29** by the same allowable and **above at 15** by the packing, so the window is **empty** and the binding variable is the **footprint**: **≥ 2330 nm², 1.44×, 1.20× in edge**. And the tie apertures are not 45 holes but `m` collinear slots that **sever** the superstructure into **18 components** at every one of the 32 phases — cured by **8 bp of stagger**. |
| T-101 | Is a 15-attachment scheme flat under the SOLVED load? | The dishing of a 1 × 15 grid under `C-0022`'s solved profile, against `T-5b`'s 10 % convention | A8.2 | **DONE** (iteration 7) — claim `C-0047`, raises `CH-0060`. **NO, and it is not `CH-0034`'s saturation — it is a LOSS.** 1 × 15 dishes **0.695 of the stroke**, **7.0×** `T-5b`'s convention, against 0.218 at 3 × 15 and 0.223 at 8 × 8; the criterion does not saturate below 45, and 15 sits **4.7× above** `CH-0034`'s 0.149 floor. **Below three columns a coupling is a NET DISHING SOURCE** — the free, uncoupled tile dishes 0.308, so 1 × 15 is **2.26× worse than no coupling at all**, break-even at exactly the three columns `C-0041` shows cannot be built. The **cheap bound** saw it in four operations: the along-helix Winkler bending length is **12.83 nm** against a 40 nm column pitch (3.12) and a 2.69 nm row pitch against 5.71 nm across (0.47). Orientation is worth 1.08× in dishing and **16× on the crossover path**, and the packing forces the good angle. The 8 bp stagger costs +2.19 % of the dishing and **breaks `C-0015`'s exact zero at first order** (0.389 pN, `CH-0060`); swept as a *design variable* it buys **45 %** back at ±13.60 nm — the bending length to 6 % — and still does not reach the tolerance. `C-0022`'s 32.1 % split is **unaffected**; what moves is the sag term, 32 % of the dishing at 3 × 15 → **79 %** at 1 × 15. |
| T-113 | Can a NON-UNIFORM coupling stiffness buy back the edge dishing? | The dishing of a 1 × 15 and a 3 × 15 grid with the rim springs stiffened, against `T-5b`'s 10 % convention | A8.2 | **DONE** (iteration 9) — claim [`C-0058`](gpd/claims/C-0058-non-uniform-coupling.md), raises [`CH-0071`](gpd/challenges/CH-0071-the-saturation-floor-is-a-property-of-the-equal-spring-family.md) against `CH-0034`. **YES at three columns, NO at one, and it is the first time anything in this programme has made the Gen-1 tile flat.** At 45 paths, redistributing `C-0017`'s **same** 33.3333 pN/nm — no extra stiffness, no extra attachments — takes the dishing from **0.2182** of the stroke to **0.0753** under a one-parameter rule (*the 34 stations within 6.7 nm of an edge carry 5× the other 11*) and **0.0544** under a 45-parameter optimisation, both inside `T-5b`'s 0.10, where `CH-0034`'s **count** axis saturates at 0.149. So **0.149 is the saturation of the EQUAL-SPRING family, not a rim floor** (`CH-0071`). **The cost is a force and it is affordable at 45 paths**: 2.762 pN per path at the 3 nm stroke against the 10 pN unzip allowable (3.62× clear), 0.784 pN in the worst crossover, 1.13 pN of duplex shear, and +24 % on `C-0014`'s thermal force — which is **linear** in a path's share, not its square root. **The per-path ceiling costs nothing at 45 paths** (capped and uncapped optima coincide) and binds at 15. **At `C-0041`'s 1 × 15 the axis fails**: 13.0 % admissible improvement, still 6.0× the tolerance and **1.96× worse than no coupling at all** — fifteen springs on one line can reweight a placement, not move it. **But flatness bought this way is owed at ONE state**: the same design is flat at three of `C-0022`'s five solved states and dishes 0.187 at the 2 nm gap where the *uniform* coupling dishes 0.071, and a minimax over all five reaches only 0.1587. Two smaller results: the break-even moves from three columns to **two**, and the lattice/plate excess **changes sign with the distribution** (−8 % uniform, +24 % at the flat design). Opens `T-122` (is a 5:1 per-path stiffness ratio buildable?) and `T-123` (is any distribution flat at every state?). ~~TODO — **medium-high, and it is the last unexplored axis `C-0047` leaves.**~~ Every attachment in the corpus is an *equal* spring: `C-0017`'s mandate fixes the **total** 33.3333 pN/nm and nothing upstream requires it to be shared equally. `C-0047` shows the dishing at fifteen paths is 79 % the coupling's own sag and that `C-0022`'s rim collar is where the load actually is, so stiffening the two rim rows (or the rim *stations* of a multi-column grid) at constant total is free of every standing claim and has never been run. The cheap bound is that the load is 1.3–1.9× the interior value inside an 8.9 nm collar, so the matched stiffness distribution is knowable before the solve. **It cannot rescue 1 × 15** — a single column has no rim stations along `x` — **but it is the only remaining way to attack `CH-0034`'s 0.149 floor**, which no attachment *count* touches. |
| T-114 | Does the 54 bp buildable stagger survive connectivity, packing and the load path? | `C-0041`'s union-find and orientation sweep re-run at ±9.18 nm, or the statement that only 8 bp is admissible | A8.2 | TODO — **medium.** `C-0047` finds the stagger is a **design variable** worth 22 % of the dishing at its buildable optimum, 54 bp, against the 8 bp `C-0041` chose for connectivity alone. But `C-0041` checked severance, plan-view fit and the 720-orientation sweep **at 8 bp only**, and a 6.75× larger stagger moves every tie aperture, every standoff foot and every beam centre. Three things need re-running and none is expensive: the union-find (does a large stagger still leave one component, and does it leave *more* material intact?), the plan-view blocking (beams at alternating `x` are no longer collinear, which may **relieve** `C-0041`'s Fact B), and `C-0047`'s own 2.58 pN crossover force against the 10 pN unzip allowable at every one of `C-0022`'s 21 states. **If the large stagger relieves Fact B it is not only a flatness gain — it is a second column by another name**, and `C-0047`'s break-even says two columns is still worse than none while three is better. |
| T-102 | May §3's tile grow by 1.44× in area? | A specification answer from NDI, not a calculation | A8.2 | TODO — **it is a question, not a task, and it is the second one this branch has raised** (after `T-95`). `C-0041` finds the flexure array's minimum body is `n(L + d)d` **exactly**, carrying no aspect ratio, and that §3's **desired** 10 nm stroke needs **≥ 2330 nm² = 1.44× the Gen-1 footprint, 1.20× in edge** — because the path count is bounded **below at 29** by the unzip allowable and **above at 15** by the packing, so the window on a 40 × 40 nm tile is **empty**. `C-0022` prices the growth **favourably**: a larger tile costs **+6.3 %** at the rim instead of +14.7 %. It also relieves `T-99`'s hinge-line inventory in the same direction, since a bigger tile has more interface line. **Carry to the open questions for Kazik as item 7.** |
| T-103 | Yaw and lateral confinement with a SINGLE attachment column | `C-0014`'s and `C-0017`'s by-products re-evaluated at 1 × 15 | A1.1 | TODO — low-medium. `C-0041` puts every attachment on one line at `x = 0` (or ±1.36 nm staggered), so the yaw stiffness `Σ k_i r_i²` loses the whole of the 3 × 15 grid's `x` arm. `C-0017` reports 70× of spare in the lateral condition and `CLAUDE.md` records that **yaw and translation are the same condition on the budget radius**, margins standing as `(r_anchor/r_budget)²` — so this is expected to close comfortably, and it is unchecked. |
| T-106 | The truss cap as a solved body, not a series spring | The cap's own geometry and rotational compliance, and `C-0037`'s frame couple recomputed on it | A8.2 | **DONE** (iteration 7) — claim `C-0048`, raises `CH-0061`. **The cap cannot be the flexure and cannot be a spring.** `C-0042`'s own two functions settle the geometry before any solve: the steric floor puts two legs `≥ 2R` apart and a leg is seated only within `R` of a seat's axis, so a duplex laid ACROSS the row seats **neither** (line contact **0.000 nm** at 6–16 bp, against 2.000 along it) — and the counting theorem says it again, one link per leg. **The cap is a separate crossbar duplex, 13 bp = 4.38 nm, hosting THREE 90° junctions, six links, its axis one radius above the leg heads.** Its *bending* is the small term (`12EI/w`, 8.93× the couple, so the frame couple falls only 74.18 → **71.31** pN·nm/rad at 7 bp — but 42.5 % at 16 bp, because it goes as `1/w` where the couple goes as `w²`); its torsion 0.1 %; its **height** raises `C12` and shortens the legs 1 nm. **What moves the answer is that `C-0037` applies `C-0029`'s counting theorem to the head's AXIAL path and takes its ROTATION as infinite** — 78.24 / 13.53 on a conserved 91.76, a **ceiling** on the head restraint, not a series correction. So the cap junction's azimuth is a **third** instance of one rank-one budget and, unlike the leg row's, it has **no free corner**: laid along the flexure axis the free plane caps at 6.20 pN and **no** separation to 16 bp crosses; laid across it, **7 bp** crosses again. **`C-0042`'s seven base pairs stands and its reason does not — the crossing is bought at the cap, not at the row.** All nine predicates PASS over `h = 5–10 nm` on both rigidities, at **1.95 / 1.46** against `C-0037`'s 2.79 / 2.10: the cap costs **30 %** of the margin, 38 % of the draw-in supply and 35 % of the tangent headroom, and `k_s`/32 now fails on **both** rigidities. Opens `T-109` (three junctions on one crossbar). |
| T-117 | Do THREE 90° junctions close on one crossbar duplex? | `C-0042`'s closure search at the other end of the same legs: two leg heads from below and the flexure's own end from the side, on a **lone** 13 bp seat with no lattice neighbours | A8.2 | **DONE** (iteration 8) — claim `C-0052`, raises `CH-0067`. **THEY CLOSE**, at `C-0048`'s own 13 bp and at every length to 19 bp and every row from 6 to 12: six links inside the measured `[0.60, 0.70]` nm step, **zero unpaired nucleotides**, six distinct targets, seat contacts 1.83–1.96 nm against `C-0042`'s 1.60 floor, and the wanted chord alignment at **0.00°** on the converged grid — and it still closes at **14 of 15** integer leg lengths with the legs' azimuths *carried up from `C-0042`'s base* rather than searched. Two cheap bounds were not formalities: the axial **rim** leaves exactly **0.02 nm** per end at every row width (`2R` is 5.88 rises and `ceil` buys 6 — and *"13 bp = 4.38 nm"* is a demand and a length, 13 bp being 4.42), and the **flexure's end passes a leg's surface at 0.249 nm**, tighter than the 0.54–0.69 nm gaps packed origami keeps, with the row width the only lever (0.184 at 6 bp, 0.714 at 12). **The finding is that a LEG IS ONE BODY WITH TWO JUNCTIONS**: its base and cap chords differ by `m × 33.74°`, so `C-0048`'s recommended 7.00 nm leg (21 steps) is **78.5°** from the azimuth pair its design is written on — and on the wrong side at 10.67, 10.5 **and** 10.4 bp/turn, with 13 steps the only length surviving all three. Only the *difference* is quantised (rotating the leg trades the two misalignments one for one), so **every** quantised length still passes all nine predicates at **1.81–2.43 / 1.36–1.83** — and at 21 steps the forced pair is **better** than the recommended one (2.20/1.66 against 1.95/1.46), because `C-0048` chose its cap azimuth on the **free** plane while the **loaded** one governs. The crossbar is a duplex: **31 `k_BT`** sequence-averaged and **17** at the worst sequence at 2 mM MgCl₂ on Huguet et al.'s measured magnesium nearest-neighbour set, and overhang is mechanically free because `12EI/w` and `4C/w` carry the **row**. |
| T-107 | Is `C-0023`'s 40 pN/nm compliance ceiling required at the DESIRED stroke, or only at the working point? | The ceiling re-derived from `C-0017`/`C-0018` with the stroke it is read at named | A8.2 | **DONE** (iteration 7) — claim `C-0049`, challenge `CH-0062`. **It is owed at the PLACEMENT stroke, 3 nm, and nowhere else** — 40 is exactly `1.2 x (100 pN / 3 nm)`, a **declared linearity tolerance on the placement discharge** that carries its stroke inside it, and the *same* construction at §3's desired clause is **12 pN/nm, not 40**. **Neither `C-0017` nor `C-0018` contains an upper bound on a coupling tangent**: placement is an EQUALITY on the secant, stability a FLOOR on the tangent, and `C-0032` measures a stiffer tangent *raising* `C-0018`'s pull-in margin. What binds beyond the working point is `C-0006`'s **per-path unzip allowable**, a bound on a FORCE, so as a stiffness it is `n·allowable/s` and **tightens as 1/s**: 150 → 45 pN/nm at 45 paths, 50 → **15** at `C-0041`'s buildable 15. `C-0039`'s `E5a16` secant at 10 nm (69.94) is past both, so the miss moves from **6.6x on a declared tolerance to 1.55x on a cited allowable** and **no verdict moves**. **And the same answer settles `CH-0047`/`T-76b`**: a device placed at 3 nm traverses `[0, 3]` and never occupies the 4.556 nm stroke `CH-0042`'s minimum is taken at — read at the placement stroke `C-0030`'s flexure is 25.227 pN/nm and clears **4 of `C-0017`'s 6** 2 mM floors, where 22.875 clears **none**. |
| T-108 | Is §3's DESIRED 10 nm stroke reachable by any coupling this programme has? | A synthesis over every element in the catalogue, or the plain statement that 10 nm is out of reach and 3 nm is not | A8.2 | **DONE** (iteration 7) — claim `C-0050`. **NO, and the bound that settles it contains no coupling at all.** The stroke is `s = L₀ − h`, so **`s < L₀` identically**, and §3 names no layer taller than 10 nm: a 10 nm stroke on a 10 nm layer IS `h = 0`. Best over 66 states — **kinematic** `L₀ − Nσv₀` **9.790 nm** (1.02x short), **validity** `L₀ − Nσv₀/0.2` (`C-0002`'s crossover, which is `C-0018`'s OWN binding bias ceiling at 10 nm) **8.959 nm** (1.12x), **dead-load at §3's 100 pN 7.424 nm** (1.35x) — and a coupling can only *reduce* the last, because `C-0017`'s own gate-2 theorem makes the delivered stroke monotone decreasing in the coupling stiffness. So the free stroke is the supremum over **every** coupling, and `T-108` is not a search. Catalogue: **0 of 14 rows clear at the desired stroke, 3 of 14 at the acceptable one** (`E3a` at 15 paths, `C-0023`'s linear `E5`, and **`E5a1`**). The telling row is `E5`, which clears every **coupling-side** predicate at 10 nm and fails on the reach. **Established: unreachable ON §3's OWN STACK** — stronger than *with this catalogue*, weaker than *in physics*. The escape is a **taller layer, 16.6–26.1 nm** → `T-115`. §6 task 3 is **unaffected**: its predicate is the ≥ 3 nm one. |
| T-118 | Re-synthesise the design window and the standing findings against iterations 5–7 | `C-0027`'s window and verdicts re-run against `C-0031`–`C-0050`, or a statement that no edge moves — with the **axis** each new constraint lives on named | A2.1 | **DONE** (iteration 8) — claim `C-0051`, raises `CH-0063` and `CH-0064`. **NOT ONE WINDOW EDGE MOVES** — 0 of 6, 0 grid steps, no owner change — and the reason is the finding: of the twenty claims of iterations 5–7 **exactly one** (`C-0036`) carries a quantity that is a function of `σ` at all, and it says itself it reaches the design only through `C-0018`'s bias ceiling. `C-0050`'s two NEW `σ`-resolved ceilings — kinematic `L₀ − Nσv₀` and validity `L₀ − Nσv₀/φ_c` — **could** narrow and do not, by 1.71–3.11× at all 61 grid points of both windows. **An axis LEAVES the acceptance stack** (`C-0049` withdraws `C-0023`'s 40 pN/nm, which is `1.2 ×` a *placement* mandate) and a **path count** replaces it. **The one thing that moved a verdict is a composition nobody had made**: `C-0033`'s collar (+2.61 to +4.99 pN/nm) and `C-0032`'s realised softening (−9.21 to −10.29) both move `C-0018`'s 10 nm / 2 mM fold and neither carries the other — composed exactly at `C-0018`'s own fold, where the baseline tangent vanishes by construction, the total is **−8.40 to −11.06 pN/nm at 6 of 6 models**, so the collar recovers **27–49 %** of what the element costs and `C-0032`'s **1.0000–1.0019** is the standing statement (`CH-0063`). And `C-0050`'s bound 3 is read on a layer **4.15× denser** than the window's own at its upper edge (`CH-0064`). **The deliverable is no longer a window**: §3's acceptable clause is delivered, its desired one is refused kinematically, and what is left is a **height plus five specification questions** of which only `T-115` can buy the desired stroke. |
| T-115 | May the polymer layer be taller than 10 nm — 17–26 nm? | A specification answer from NDI, not a calculation | A2.1 | TODO — **it is a question, not a task, and it is the FOURTH this programme has raised** (after `T-95`, `T-102` and `T-63`), and the only one that can buy §3's **desired** stroke. `C-0050` shows the stroke is the layer's own compression, so `s < L₀ ≤ 10 nm` on §3's stated heights and the desired clause asks for `h = 0`. The layer height at which the 100 pN dead-load stroke reaches 10 nm is **16.63–26.12 nm** over `C-0003`'s six models at `σ` = 0.024 nm⁻². §3's own tile row already allows the effort point to sit *"~20–25 nm above the electrode"*, so the geometry is not absurd — but a 17–26 nm layer moves `C-0002`'s crossover, `C-0005`'s screening validity, `C-0007`'s drainage and `C-0012`'s bias, and **nothing in this programme has evaluated one**. **Carried to the open questions for Kazik.** |
| T-116 | Does a 45-arm hinge-line array have a plan view? | 45 `E5a1` arms of 9.13 nm placed in plan on a 40 x 40 nm tile, with their 45 crossovers, or the statement that it does not pack | A8.2 | **DONE** (iteration 8) — claim `C-0053`, raises `CH-0065`. **NO — and `C-0041`'s answer does not transfer, so the reason is new.** Both of `C-0041`'s obstructions **dissolve**: `E5a1` owns **no vertical member** (0 blocking pairs, 0 clashes, at every orientation) and `arm + d = 11.82 nm` sits **under** the 13.33 nm column pitch where `C-0041`'s 34.51 sat 2.59× over it. As **bodies** the 45 arms pack — 0.685 of the footprint, 2 of 720 orientations, exactly `θ = 0` and `θ = π` (a rooted arm has a direction; `C-0041`'s centred beam does not). **What refuses them is the hinge lattice**: a hinge is a crossover, a row can only root on its **two bounding interfaces**, so roots sit at **5.44 nm** interior and **10.88 nm** on an edge row against an arm needing **11.82** — three arms per interior row, two per edge row, **43 of 45**, and 43 is **exact** (the construction meets the independent per-row bound). The best phases are `C-0015`'s **ten centro-symmetric** ones, for the third time. **And the two missing arms are the least of it**: an arm is *cut out of* the host, so 43 arms take **65.4 %** of its duplex length, spend **43** crossovers and bury **13** more — **56 of 56, exactly 100 %** — leaving **zero** surviving crossovers and **no bonded component at all**. Self-consistent count **43**; the count that leaves the host whole is **25** (15 duplexes bonded at 25, 14 at 30, 8 at 35, 3 at 40, none at 42), so the threshold is **45 → 25, 1.80×**. 45 never place on **any** of five swept axes (the double-nick reading of the root gives 30). One thing improves: the lattice carries **three** columns, exactly `C-0047`'s break-even, so this array is **not** a net dishing source where `C-0041`'s 1 × 15 is 2.26× worse than nothing. |
| T-109 | Which joint supplies `E5`'s draw-in at the acceptable stroke? | A named joint with a compliance, or the statement that the flexure's own ends must give | A8.2 | TODO — medium. `C-0039` puts the exact arc-length demand at **0.383 nm = 1.1 bp** at §3's acceptable stroke, 4.0× `C-0029`'s quoted 0.095 nm, and shows there is **no axially-held alternative** for an inextensible arm: held, it must stretch and carries ≥ 30 pN at 3 nm. `C-0025` prices such a demand and `C-0035` shows a *standoff* can supply one; `E5` has no standoff. |
| T-97 | Can TWO 90 degree junctions close on ONE sheet duplex 6-8 bp apart? | A closure search with a second standoff seated on the same duplex, or a statement that the pair does not fit | A8.2 | **DONE — [`C-0042`](gpd/claims/C-0042-paired-perpendicular-junction.md), PASS, and the risk `C-0037` named as its largest does not exist.** The pair fits at **every** separation from the **6 bp = 2.04 nm steric floor** to 12 bp, both links of both junctions inside the measured `[0.60, 0.70]` nm step with **zero unpaired nucleotides**, four distinct targets, one shared seat (`Σ(Δy)² = 0`) and 24 of 32 crossover phases free — **and the azimuth costs exactly nothing**: both chords come out at **0.00°** from the flexure axis, on both groove conventions, on the strict seat-duplex-only reading and at the duplex's own axis. The cheap bound written to bind did not: a **screw image** would rotate the second chord by `n × 33.74°`, i.e. **89.9° at `C-0037`'s recommended 8 bp**, and it fails because a standoff must stand *normal* to the sheet. What does bind is new: `L2a6`/`L2a8` are identical only in the **loaded** plane, and **7 bp** is the smallest row whose free plane has crossed above it — 9.77 pN and margin 2.79, `C-0037`'s `L2a8` numbers, **0.68 nm narrower**. Raises [`CH-0056`](gpd/challenges/CH-0056-the-azimuthal-quantum-belongs-to-the-sheet.md). |
| T-98 | Standoff-and-truss against `E5a16`: which branch should Gen-1 actually take? | A stated comparison on premises, not on numbers, with the open items of each branch counted | A8.2 | TODO — **high**. `C-0037` reopens the standoff branch at §3's desired stroke but does **not** argue it should be taken. `C-0034`'s `E5a16` needs **no 90 degree junction, no standoff, no truss and no cap**, and clears the same stroke on a motif every published origami already has; the truss branch clears it on **two** junctions nobody has built, a cap nobody has designed, and 180 standoffs whose plan view is `T-96`. Both are `A8.2` answers and the programme has to say which one it recommends.
| T-76a | The standoff length as a STABILITY variable, and the unzip allowable that shuts the door | Whether any point of `C-0030`'s window clears the 2 mM stability floor at all six layer models | A8.2 | TODO — **medium-high, and it is one study.** `C-0032` finds the favourable mounting's tangent minimum **non-monotone in `ℓ`**: 22.87 pN/nm at the recommended 8 nm, **27.30 at `ℓ = 5 nm`** (the bottom of `C-0030`'s window) — which clears five of the six 2 mM floors and misses the sixth, 27.91, by **2.2 %**. `ℓ = 4 nm` reaches **28.71** and clears all six, and is excluded by `C-0030`'s `P4` alone: the beam's own tension at the 10 nm desired stroke against `C-0006`'s 10 pN unzip allowable. So 2 mM closes on a 2 % margin against one layer model, gated by an allowable two claims upstream. Sweep the `(ℓ, span, path count)` trade rather than `ℓ` alone — more paths make each flexure longer (`C-0023`) and lower its tension. If it opens, §3's own buffer is recovered and `T-63` stops being load-bearing for this branch. |
| T-76b | Which range is a coupling's stability tangent minimised over? | A stated convention, and the two readings shown to differ only where they must | A8.2 | TODO — low-medium. `CH-0047`. `CH-0042` prescribed `min_s k_tangent` over `[0, 10 nm]`; at zero stroke the requirement is **identically zero**, so a strain-stiffening element's minimum there is its bending stiffness alone and the reading ranks a 44.8 pN/nm coupling below a 22.9 pN/nm one. `C-0032`'s result file already emits both readings, so this is a convention decision plus a re-read, not a re-run. The honest quantity is `min_s [k_tangent(s) − \|k_eff(s)\|]`, whose zero **is** the fold. |
| T-66 | A triangulated standoff — the only rigid out-of-plane mounting the literature actually shows | Whether two or three duplexes in a truss can carry the flexure's end shear without buckling, and what draw-in release survives | A8.2 | **DONE** (iteration 6) — **covered by `T-72`**, claim `C-0037`. **Two legs carry it, three are better and unnecessary, and the draw-in release that survives is 77 % of the single standoff's.** A cross row's critical load is `2 x` the single standoff's *restrained-axis* load — 9.77 pN against a 3.50 pN duty, **2.79x on CanDo's rigidity and 2.10x on Fields et al.'s** — and it buckles at an **18.27 nm** stroke. Three legs across reach 3.52x; a **triangle** and a **rectangle** reach 16.4 and 25.9 pN and **fail anyway**, on `P8` and `P4`, because they put frame couple in the LOADED plane and the supply collapses to 0.95 and 0.66 of the demand. The literature's own *"set of double-helical spacers"* is **exactly two**, 39 bp each, **one covalent link per end** (Pumm et al.'s SI strand table, counted by hand here) — so the published rigidity is a **frame couple made of ball joints**, which is this claim's mechanism, already built.
| T-67 | Does a 90° scaffold or staple routing between a sheet duplex and a normal standoff exist at all? | A concrete strand routing, or a statement that it does not exist and the branch must be re-planned | A8.2 | **DONE** (iteration 4) — claim `C-0029`, raises `CH-0039` and `CH-0040`. **It exists, and it is the base's arithmetic that fails, not its chemistry.** Both links close covalently at **0.600 nm** inside the measured `[0.60, 0.70] nm` step, **zero unpaired nucleotides**, optimum a **scaffold excursion** (out at strand 1 bp 9, back at bp 10, chord −87.8°). But a duplex END has **two strand termini**, so a base has at most **two links** and a lever arm ≤ **1.0 nm**: `C-0028`'s `B2` needs 1.345 nm and its 261.2 pN·nm/rad is **3.34× over the hard ceiling of 78.24**. Two links lie on a chord ⇒ **one restrained axis**, and about the chord only 13.53 = `B1`; **a column buckles about its softest axis, so `P6` fails at every length** and **the standoff branch closes at §3's desired stroke** (it stands at the acceptable 3 nm, ℓ = 5–10 nm). The **90° exit is not set by the routing** — the azimuth is, quantised at 33.74°/bp and worth ≤ 8.4 %; the polar tilt is free, and one-sided sterics leave 30° of rocking. **`E5` as filed also fails, twice on geometry**: a `δ = r sin θ` law at **46.9°** (`t/s` = 1.549) and an arm capped by the mandate at **9.77 nm < 10 nm** at any hinge count. **What survives is `E5g16` — a 12.24 nm = 36 bp GUIDED arm on 16 crossovers**, tangent 33.68/38.68 at 3/10 nm, 2.04 pN per crossover, **no member in compression and no 90° junction anywhere**. |
| T-70 | What holds `E5g16`'s guided arm, and does `c = 12` survive its own anchorage? | The second anchorage's rotational compliance, and the arm cap `(c n EI/k)^(1/3)` recomputed on the realised `c` | A8.2 | **DONE** (iteration 5) — claim `C-0034`, raises `CH-0044`. **`c = 12` does NOT survive — the realised factor is 6.28–7.36 — and the cap does not need it to.** The end condition is a continuum, **`c(ρ) = 12(1+ρ)/(4+ρ)`**, exactly 3 at a free far end and 12 at a guided one and exactly **6 at `ρ = 2`**; and because **`ρ_far = k_far r/EI` carries the ARM**, the cap is a **fixed point**, `r = (c(k_far r/EI) n EI/k)^(1/3)`, so a longer arm buys its own guidance. **Every TWO-link anchorage clears §3's desired 10 nm stroke** — 13.43 nm at the arm's own duplex-end couple, 15.18 at a singly nicked continuation, 15.44 at a two-crossover clamp, and **10.97 nm even about the CHORD**, the axis the counting theorem leaves free (and which here is unloaded, because the arm carries no compression). **A ONE-link anchorage is exactly the cantilever, 9.77 nm, and fails** — and that is Rothemund's own observed failure. Design: **`E5a16`, an 11.03–12.50 nm = 32–37 bp arm on 16 crossovers**, tangent 33.56/36.78 at 3/10 nm, 1.83 pN per crossover, anchorage couple 16.7 pN per link = **7.3 bp** on `CH-0029`'s ladder. **The dominant compliance has changed sides: the ARM carries 58.5 %, not the hinge.** `CH-0044`: `c = 12` and `C-0023`'s series composition cannot both be right — the composition is exact **iff the far end carries no moment**, and `E5g16` read on its own `c = 12` is **1.64× over-placed**. |
| T-71 | A backbone-torsion check of `C-0029`'s closed routing | Whether the dihedrals of the scaffold-excursion junction close, not merely the phosphate distances | A8.2 | **DONE** (iteration 9) — claim `C-0057`, raises `CH-0070`. **They do NOT close at any of the three reported optima, and the reason is that the OBJECTIVE is blind, not that the junction is impossible.** `C-0029`'s single junction closes **0 of 4** links, `C-0042`'s pair **1 of 8**, `C-0052`'s trio **2 of 6**. Four of the eighteen are excluded by a **closed-form reach bound** — they close at *no* torsion: `C-0042`'s two 7 bp legs would need an `O3′–P` bond of **0.2517 / 0.2460 nm** against a covalent **0.16022 ± 0.00191**. The rest fail on **population**: `C-0029`'s binding link needs **ε = −22.9°** (0.015 % of 13 084 measured linkages) and its second **β = 27.4°** (**0 of 15 457** residues). **But a torsion-feasible placement EXISTS in `C-0029`'s own space and nobody was looking**: of **69 120** placements, 3 546 close on distance, **1 855** survive the reach bound and **18 of the 100 solved** close at torsion level — at gaps near **0.690 nm**, the *interior* of the window a residual-minimising search never visits. The scaffold excursion is **12.7× rarer** (280 covalent, 1 of 100 closing). Every constant is measured here from **876** X-ray DNA-only PDB entries and reproduces Parkinson's restraint library to ≤ 1.6° and Svozil's BI to ≤ 7.1°; the `[0.60, 0.70]` nm window this programme cites has **no primary source** and measures **0.607 / 0.664**. |
| T-72 | Re-price `T-66`'s triangulated standoff as a **stability** remedy, not a rigidity one | Whether a second standoff restores the free axis at an affordable cost in sway | A8.2 | **DONE** (iteration 6) — claim `C-0037`, raises `CH-0050`, **covers `T-66`**. **YES, and the trade does not exist in the form it was posed.** The draw-in lives on `(u_x, phi_y)` and the buckling that closed the branch lives on `(u_y, phi_x)`; a frame couple is a **rank-one tensor on the leg offsets**, so for a two-leg row at azimuth `theta`, `Sigma x^2 = (w^2/2)cos^2 theta` and `Sigma y^2 = (w^2/2)sin^2 theta` — **conserved, and at 90 degrees the loaded plane inherits EXACTLY ZERO**. Two legs laid **ACROSS** the flexure axis take the adopted critical load **1.46 -> 9.77 pN (6.71x)**, move the governing plane from free to loaded — restoring `C-0029`'s restrained-axis reading, which is exactly what `C-0029` said a truss would have to do — and cost **23 % of the draw-in supply** (2.90x the demand, against 3.75x) and **3.5 % of the tangent** (26.09 against a 40 pN/nm ceiling). **The window is `l = 5-10 nm`, all nine predicates, on BOTH rigidities**, where the single standoff fails `P6` at every length (0.40-0.57) and buckles at a 3.05-7.02 nm stroke. **Laid ALONG the axis the same two legs still fail `P6`** and lose 63 % of the supply — worse on every axis at once. **The cost is the leg COUNT, not the leg SPACING**: `L2a6`/`L2a8`/`L2a12` are bit-identical in the loaded plane. Unanticipated: **`P9`**, an off-square row reacts part of the head moment as an axial **couple** and overloads its outermost leg — it fails at **12 of 12** azimuths below 90 degrees, so the exact cross row is **uniquely** optimal. Design: **`L2a8` — two 8 nm = 24 bp legs at 8 bp = 2.72 nm across the flexure axis, span 33.43 nm = 98 bp, tangent 26.09 pN/nm, 180 standoffs = 45 % of a tile footprint**.
| T-68 | Can the single-layer sheet react the standoff's base moment? | The base couple carried into `C-0009`'s grillage instead of grounded on a rigid sheet | A8.2 | TODO — medium. `C-0028` treats the sheet beyond the base crossovers as rigid; `C-0009` says it is not, and any compliance there lowers `k_θ_base` — into a range where `P6` already fails. |
| T-41 | The cantilever joint's off-diagonal compliance | `c` and `S_eff` re-derived with the joint as a 2 × 2 flexibility matrix rather than two independent springs | A8.2 | **DONE — superseded by `T-65` / `C-0030`**, which solves exactly this at the standoff level. Its premise ("it **softens** the joint, so the compliance verdict is conservative") is **falsified**: `S_eff` is **2.06× stiffer** against a net demand and **negative** over 99 % of the stroke, and it is `c` that moves — by +34 % at the placement point, as a function of the stroke rather than a constant. |
| T-42 | A pre-bowed flexure, and what a built-in rise is worth | The draw-in demand measured from the BUILT shape rather than the straight one, priced in peak tension and in stroke | A8.2 | TODO — medium. The demand is `g δ²/L` from the *unstressed* configuration; building the beam with a rise `δ₀` at mid-stroke makes it `g\|δ² − δ₀²\|/L`, worth up to **2×** in peak tension at both §3 strokes and therefore in the standoff length the joint needs. `C-0025` names it and does not price it. Base-pair-quantised like `C-0023`'s mounting offset. |
| T-43 | The two draw-in demands differ by 1.13–1.20× and only one produces the tension | The cable term's implied `2δ²/L` reconciled with the beam shape's `2.25–2.40 δ²/L` | A8.2 | TODO — low-medium, and it is a model-consistency debt inherited from `C-0023`. The membrane force is built on straight half-span chords while the draw-in demand is integrated over the bending shape; the gap is 1.13–1.20×. `C-0025` reports both and sizes on the larger. A single consistent large-deflection form would close it and would move every span by a few per cent. |
| T-31 | Does a flexure array on a common superstructure stay as compliant as independent leaf springs? | The array stiffness against `n` independent beams, with crossover coupling between neighbours | A8.2 | TODO — medium, and **narrowed by `C-0041`/`T-96`**: the 45-path array it was asked about **has no plan view**, so at that count the question is moot. It returns at **15 paths**, where the beams are one duplex apart, free, and in one level — whether they should be crossed over to each other is then a live design choice, and the compliance **ceiling** is still the binding side so independence is *not* the conservative assumption. |
| T-32 | The dishing a **two-sided** coupling causes | The tile's flatness under a coupling that loads it in both directions | A8.2 | TODO — low-medium. Every flatness result in the programme (`C-0006`, `C-0009`, `C-0015`) is for a coupling that only pulls. A two-sided element loads the tile *upward* at some attachments and *downward* at others during a thermal excursion, which `C-0006`'s uniform-load exact-rigidity result does not cover. |
| T-33 | Is the lever's own load path two-sided? | The superstructure's ability to react a downward push, budgeted as `C-0017` budgets its bending | A8.2 | TODO — **medium**. **A coupling that can push is only as two-sided as the path behind it.** `C-0023`'s whole verdict assumes the lever is vertically *and* laterally grounded — `C-0017`'s assumption, now load-bearing in a new direction. If the superstructure cannot react a downward push, `C-0021`'s force requirement returns and `C-0014`'s tethers come back with it. |
| T-24 | Does PEG bridge a DNA-origami face? | The adsorption energy per chain against `C-0021`'s 0.002–0.009 `k_BT` threshold, or a demonstration that no accessible measurement resolves it | new | TODO — **medium, and it is a shared premise**. `C-0021` shows hundredths of a `k_BT` per chain would supply the whole hold-down, so "PEG does not adsorb to DNA" cannot be read as "bridging contributes nothing". `P-8`'s missing Mg²⁺/PEG constant is the mechanism that would flip it — **and it would take `C-0010`'s exact lateral zero with it**. |
| T-7 | Poroelastic drainage time vs thickness and volume fraction | Bounded, with the conditions under which it would constrain ≥ 1 kHz stated | new | **DONE** (iteration 3) — claim `C-0004`, raises `CH-0003`. **Not binding**: 91 kHz at the nominal design point, 22.6 kHz at the §3 worst case, 5.6 kHz under a composite worst case. §4(d) is discharged. |
| T-7b | Electro-osmotic drag on the squeeze flow: a streaming potential opposes drainage in a porous layer under a biased electrode | Bounded, or shown to be below the 22× margin `C-0004` leaves | new | TODO — **downgraded to low** by `C-0005`, which supplies a ~10⁻³ suppression **as an argument, not a verified coefficient**: the PEG layer is neutral, only 12–17 % of its thickness carries net space charge, and counterion domination raises the local conductivity 12–23×, shorting out the streaming potential. Still needs the tile's hydrodynamic zeta and `T-7`'s Brinkman length. |
| T-8 | Tile positional variance at 300 K | σ_RMS ≤ 3.0 nm for the nominal Gen-1 tile | A1.2 | **DONE** (iteration 3) — claim `C-0010`, raises `CH-0009`. **PASS at the operating point** on the declared acceptance quantity (area RMS 0.87–0.96 nm, 3.1–3.4× margin; in band below 1 kHz, 0.069–0.110 nm). Two qualifications travel with it: the tile's **worst point exceeds 3.0 nm in every state softer than the working point** (3.13–4.38 nm), and the **lateral coordinate is not part of the PASS** (`T-12`). Leaf `A1.2` is only **partly** discharged — no simulated ensemble and no 95 % CI, stated as not discharged rather than approximated. Superseded note — was: **re-scoped, promoted, and UNBLOCKED by T-1c** (σ_RMS at the working point is 0.13–0.30 nm across the new bracket, and must be quoted from the crossover-valid stiffness, not `C-0001`'s). It must consume `C-0006`, **not** `C-0001`: the 0.28 nm figure is the *piston mode alone*, and the total point fluctuation is **1.37 nm** nominal, **2.24 nm** at the soft end of the sweep — 46–75 % of the 3.0 nm predicate rather than 9 %. Still passing, but the margin is gone. Two further constraints: leaf `A1.2` demands a **simulated** σ_RMS with a **95 % CI** from a coarse-grained ensemble, not an analytic bound; and `C-0004` supplies the noise bandwidth (91 kHz corner) rather than leaving it assumed. |

## Entry points

| Study | Task | Emits |
|---|---|---|
| `./gradlew study -Pstudy=brush.BrushStiffnessStudyKt` | `T-1` | `gpd/results/T-1-layer-stiffness.json` |
| `./gradlew study -Pstudy=material.PegMaterialStudyKt` | `P-3` | `gpd/results/P-3-peg-material-parameters.json` |
| `./gradlew study -Pstudy=poroelastic.PoroelasticDrainageStudyKt` | `T-7` | `gpd/results/T-7-poroelastic-drainage.json` |
| `./gradlew study -Pstudy=structure.TileLoadDistributionStudyKt` | `T-5` | `gpd/results/T-5-load-distribution.json` |
| `./gradlew study -Pstudy=structure.TileFlatnessStudyKt` | `T-5b` | `gpd/results/T-5b-tile-flatness.json` |
| `./gradlew study -Pstudy=electrostatics.MeanFieldValidityStudyKt` | `T-6` | `gpd/results/T-6-mean-field-screening-validity.json` |
| `./gradlew study -Pstudy=material.SolventQualitySaltStudyKt` | `P-6` | `gpd/results/P-6-solvent-quality-vs-salt.json` |
| `./gradlew study -Pstudy=brush.CrossoverLayerStudyKt` | `T-1c` | `gpd/results/T-1c-crossover-valid-layer-response.json` (~3.5 min) |
| `./gradlew study -Pstudy=electrostatics.NonlinearPbProfileStudyKt` | `T-3a` | `gpd/results/T-3a-nonlinear-pb-profile.json` |
| `./gradlew study -Pstudy=structure.TilePositionalVarianceStudyKt` | `T-8` | `gpd/results/T-8-tile-positional-variance.json` |
| `./gradlew study -Pstudy=structure.DiscreteLatticeTileStudyKt` | `T-10` | `gpd/results/T-10-discrete-lattice-tile.json` |
| `./gradlew study -Pstudy=material.GraftedChiStudyKt` | `P-9` | `gpd/results/P-9-grafted-chi.json` |
| `./gradlew study -Pstudy=anchoring.LateralConfinementStudyKt` | `T-12` | `gpd/results/T-12-lateral-confinement.json` |
| `./gradlew study -Pstudy=actuator.StrokeAndBlockingForceStudyKt` | `T-3` | `gpd/results/T-3-stroke-and-blocking-force.json` |
| `./gradlew study -Pstudy=brush.ScfDensityProfileStudyKt` | `T-1d` | `gpd/results/T-1d-scf-density-profile.json` (~33 min) |
| `./gradlew study -Pstudy=structure.CrossoverRegistrationStudyKt` | `T-14` | `gpd/results/T-14-crossover-phase-and-registration.json` (~3 min) |
| `./gradlew study -Pstudy=window.DesignWindowStudyKt` | `T-2` | `gpd/results/T-2-design-window.json` (~2 s) |
| `./gradlew study -Pstudy=coupling.OutputCouplingStudyKt` | `T-16` | `gpd/results/T-16-output-coupling-stiffness.json` |
| `./gradlew study -Pstudy=structure.InPlaneLoadPathStudyKt` | `T-15` | `gpd/results/T-15-in-plane-shear-lag.json` (~35 s) |
| `./gradlew study -Pstudy=actuator.MaximumUsableBiasStudyKt` | `T-4` | `gpd/results/T-4-maximum-usable-bias.json` (~7 min) |
| `./gradlew study -Pstudy=anchoring.ZeroBiasRestingPositionStudyKt` | `T-13` | `gpd/results/T-13-zero-bias-resting-position.json` (~40 s) |
| `./gradlew study -Pstudy=structure.EntryTopologyStudyKt` | `T-19` | `gpd/results/T-19-attachment-entry-topology.json` (~90 s) |
| `./gradlew study -Pstudy=anchoring.TwoSidedCouplingStudyKt` | `T-23` | `gpd/results/T-23-two-sided-coupling.json` |
| `./gradlew study -Pstudy=electrostatics.TileEdgeLoadProfileStudyKt` | `T-3b` | `gpd/results/T-3b-tile-edge-load-profile.json` |
| `./gradlew study -Pstudy=anchoring.FlexureEndJointStudyKt` | `T-30` | `gpd/results/T-30-flexure-end-joint.json` |
| `./gradlew study -Pstudy=coupling.OneRowPerDuplexStudyKt` | `T-17` | `gpd/results/T-17-one-row-per-duplex.json` |
| `./gradlew study -Pstudy=brush.FluctuationCorrectionStudyKt` | `T-1f` | `gpd/results/T-1f-mean-field-fluctuation-corrections.json` |
| `./gradlew study -Pstudy=anchoring.StandoffBaseJointStudyKt` | `T-40` | `gpd/results/T-40-standoff-base-joint.json` |
| `./gradlew study -Pstudy=window.WindowResynthesisStudyKt` | `T-25` | `gpd/results/T-25-window-resynthesis.json` (~10 s) |
| `./gradlew study -Pstudy=anchoring.CoupledStandoffJointStudyKt` | `T-65` | `gpd/results/T-65-coupled-standoff-joint.json` |
| `./gradlew study -Pstudy=anchoring.PerpendicularJunctionStudyKt` | `T-67` | `gpd/results/T-67-perpendicular-junction-routing.json` |
| `./gradlew study -Pstudy=anchoring.FlexureMountingSenseStudyKt` | `T-75` / `T-78` | `gpd/results/T-75-flexure-mounting-sense.json` (~3 s) |
| `./gradlew study -Pstudy=anchoring.GuidedArmAnchorageStudyKt` | `T-70` | `gpd/results/T-70-guided-arm-anchorage.json` |
| `./gradlew study -Pstudy=stability.SofteningCouplingStabilityStudyKt` | `T-76` | `gpd/results/T-76-softening-coupling-stability.json` (~8 min) |
| `./gradlew study -Pstudy=anchoring.TriangulatedStandoffStudyKt` | `T-72` / `T-66` | `gpd/results/T-72-triangulated-standoff.json` (~2 s) |
| `./gradlew study -Pstudy=crossover.ConcentratedCrossoverStudyKt` | `T-21` | `gpd/results/T-21-concentrated-crossover.json` (~70 s) |
| `./gradlew study -Pstudy=actuator.CollarEquilibriumPathStudyKt` | `T-60` | `gpd/results/T-60-collar-on-the-equilibrium-path.json` (~10 min) |
| `./gradlew study -Pstudy=anchoring.HingeLineCensusStudyKt` | `T-81` | `gpd/results/T-81-hinge-line-census.json` (~1 s) |
| `./gradlew study -Pstudy=anchoring.FlexureArrayPackingStudyKt` | `T-96` | `gpd/results/T-96-flexure-array-packing.json` (~5 s) |
| `./gradlew study -Pstudy=anchoring.PairedPerpendicularJunctionStudyKt` | `T-97` | `gpd/results/T-97-paired-perpendicular-junction.json` (~90 s) |
| `./gradlew study -Pstudy=anchoring.TwoSpringElasticaStudyKt` | `T-79` | `gpd/results/T-79-two-spring-elastica.json` (~2 min) |
| `./gradlew study -Pstudy=coupling.SingleColumnFlatnessStudyKt` | `T-101` | `gpd/results/T-101-single-column-flatness.json` (~45 s) |
| `./gradlew study -Pstudy=anchoring.FlexureCountHingeTradeStudyKt` | `T-99` | `gpd/results/T-99-flexure-count-hinge-trade.json` (~6 min) |
| `./gradlew study -Pstudy=anchoring.TrussCapStudyKt` | `T-106` | `gpd/results/T-106-truss-cap.json` (~20 s) |
| `./gradlew study -Pstudy=synthesis.DesiredStrokeReachStudyKt` | `T-107` / `T-108` | `gpd/results/T-108-desired-stroke-reach.json` (~2 min) |
| `./gradlew study -Pstudy=window.SecondResynthesisStudyKt` | `T-118` | `gpd/results/T-118-window-resynthesis-two.json` (~5 s) |
| `./gradlew study -Pstudy=structure.ConsumedCrossoverSheetStudyKt` | `T-110` | `gpd/results/T-110-consumed-crossover-sheet.json` (~80 s) |
| `./gradlew study -Pstudy=anchoring.HingeArmArrayPackingStudyKt` | `T-116` | `gpd/results/T-116-hinge-arm-array-packing.json` (~40 s) |
| `./gradlew study -Pstudy=anchoring.CrossbarJunctionTrioStudyKt` | `T-117` | `gpd/results/T-117-crossbar-junction-trio.json` (~15 min — 89 closure searches on a 360 × 8 × 5 crossbar grid) |
| `./gradlew study -Pstudy=anchoring.BackboneTorsionStudyKt` | `T-71` | `gpd/results/T-71-backbone-torsion-closure.json` (~9 min — 36 inverse-kinematic closures plus a 69 120-placement torsion census). Its measured constants come from `gpd/data/T-71-bdna-backbone-survey.json`, regenerated by `python3 tools/T-71-bdna-backbone-survey.py --resolution 2.3 --limit 900 --rmsd 0.45` (~40 min, network) and `python3 tools/T-71-emit-kotlin-constants.py` |
| `./gradlew study -Pstudy=anchoring.UnusedJunctionSiteStudyKt` | `T-119` | `gpd/results/T-119-unused-junction-site.json` (~60 s) |
| `./gradlew study -Pstudy=coupling.NonUniformCouplingStudyKt` | `T-113` | `gpd/results/T-113-non-uniform-coupling.json` (~4 min — 120 rim-sweep states and 13 optimisations over up to 45 per-path stiffnesses) |
| `./gradlew study -Pstudy=structure.ConnectivityCeilingPlateStudyKt` | `T-120` | `gpd/results/T-120-connectivity-ceiling-plate.json` (~70 s — 216 three-model states, 22 registration solves) |
| `./gradlew study -Pstudy=structure.StackedArmSheetStudyKt` | `T-121` | `gpd/results/T-121-stacked-arm-sheet.json` (~20 s — 18 augmented 1059-degree-of-freedom solves, 21 distribution solves) |
| `./gradlew study -Pstudy=coupling.BuildableStiffnessRatioStudyKt` | `T-122` | `gpd/results/T-122-buildable-stiffness-ratio.json` (~25 s — 7 enumerated ladders, 21 ratio-sweep states, 16 assembled flatness solves, 24 bisected scatter thresholds) |
| `./gradlew study -Pstudy=anchoring.TorsionFeasibleRoutingStudyKt` | `T-124` | `gpd/results/T-124-torsion-feasible-routing.json` (**~55 min** — 13 enumerations of `C-0057`'s 69 120-placement grid, and ~2 000 inverse-kinematic link closures ranked by chord alignment at three scales) |
| `./gradlew study -Pstudy=anchoring.UpwardRootPlacementStudyKt` | `T-125` | `gpd/results/T-125-upward-root-placement.json` (**~6 min** — **1 144 858 placements** evaluated through one Woodbury influence bank per phase: 783 274 by descent over all 32 phases, 361 584 by exhaustive enumeration of the centro-symmetric family) |
| `./gradlew study -Pstudy=anchoring.CrossbarTrioExistenceStudyKt` | `T-127` | `gpd/results/T-127-crossbar-trio-existence.json` (**~24 min** on six threads — **49 857 lattices solved** of 113 040 enumerated over 37 configuration sweeps, **149 789 junction closure solves** and **299 578 inverse-kinematic link closures**, affordable only because `C-0057`'s reach bound prunes 88 % of the assignments) |
| `./gradlew study -Pstudy=coupling.RobustDistributionStudyKt` | `T-123` | `gpd/results/T-123-robust-distribution.json` (**~18 min** — a 13-state Woodbury surrogate over `C-0015`'s 45 stations, 31 subset minimaxes, 10 pairwise, 4 operating ranges, ~42 starts each on a smoothed minimax with analytic gradients, 8 assembled lattice-and-plate solves) |
| `./gradlew study -Pstudy=anchoring.ArmSlabClearanceStudyKt` | `T-126` | `gpd/results/T-126-arm-slab-clearance.json` (~5 s — the plan interleave of 34 arms and 45 ties at every feasible arm sense, a 400 001-offset sweep of the rigid grid translation at three column counts, and 5 assembled flatness solves) |
| `./gradlew study -Pstudy=anchoring.CrossbarArrayPlacementStudyKt` | `T-130` | `gpd/results/T-130-crossbar-array-placement.json` (~55 s — a 89-position base register field at 0.17 nm steps over one host duplex (241 pruned junction closure solves), 44 array placements of 34 instances each through `C-0053`'s packer, and 10 assembled flatness solves under `C-0022`'s solved load) |
| `./gradlew study -Pstudy=anchoring.PinnedLegBudgetStudyKt` | `T-132` | `gpd/results/T-132-pinned-leg-budget.json` (~125 s — an 89-position register of **signed** base chord azimuths over one host duplex (255 pruned junction closure solves, every closer retained), 7 leg-base phase-class censuses, three compositions of each of 7 row pitches, 105 per-length `capDesign` solves, and 6 sensitivity registers) |
| `./gradlew study -Pstudy=anchoring.RangeRobustPlacementStudyKt` | `T-129` | `gpd/results/T-129-range-robust-placement.json` (**~12 min** — a 13-state multi-state influence bank over every candidate root at phase 24, 4 operating-range minimaxes at 24 starts, 31 subset minimaxes at 12, **361 584 centro-symmetric placements re-enumerated exhaustively under two range objectives**, 6 banks for the convergence sweeps and one 45-station bank for `C-0064`'s reproductions; a re-run differs in **28 lines of 1 423**, all inside the 31 subset minimaxes, with every other section byte-identical) |
| `./gradlew study -Pstudy=anchoring.OutputElementPlacementStudyKt` | `T-133` | `gpd/results/T-133-output-element-placement.json` (~190 s — a closed-form census of DNA's five compliance mechanisms, 11 candidate elements each placed at `C-0063`'s 34 stations through `C-0053`'s packer and graded on every standing clause, 4 bisections of `C-0039`'s exact elastica for the end-restraint window, 2 orientation sweeps of 720 samples, a 7-point fold sweep and 8 sensitivity registers; re-run through `tools/study.sh` and diffed **byte-for-byte identical**) |

Add `-PbuildDirectory=<dir>` to any Gradle command when more than one agent is working this checkout (`P-7`),
and use [`tools/verify.sh`](tools/verify.sh) for an authoritative full-suite run — at four or more concurrent
agents the shared project lock defeats `-PbuildDirectory` on its own (`P-10`).
Run a study through [`tools/study.sh`](tools/study.sh) rather than `./gradlew study` whenever another agent is
working the checkout: `-PbuildDirectory` does **not** isolate `build-*/classes`, so a sibling build can delete
the classes out from under a multi-minute run (`P-12`).
Both scripts take `--drop <pkg>`, which removes a package another agent has left mid-TDD from the copy.

## Ordering rationale

NDI names Tasks 1 and 2 as the starting point, and the dependency structure agrees:
T-1 is the cheapest thing in the programme and T-3, T-4, T-7 and T-8 all consume its output.

T-8 is the next-cheapest — equipartition against the stiffness T-1 produces — and it is a *falsifier*:
a layer compliant enough to actuate may be too compliant to hold position at 300 K.
That tension is worth exposing early, so T-8 is promoted above its position in the problem definition's numbering.

P-3 and P-4 were raised as blockers rather than niceties because T-1's answer is only as good as its premises,
and the problem definition (§2, second caveat) says exactly that:
*"Where that crossover sits for our layer decides which exponent we are entitled to."*
That judgement was vindicated harder than expected: P-3 found the premise **violated**, not merely unchecked,
and the violation propagates into the chain length T-1 derives, not just into the exponent it applies.
Hence `T-1c` now sits above `T-2` for the same reason `P-3` sat above it before —
`T-2`'s deliverable is either a window or a *proof of emptiness*, and neither can rest on a failed premise.

## Standing findings that constrain everything downstream

- **Stiffness is not a single number at the resting height.** The SCF form has zero stiffness at first
  contact; the scaling form does not. Any downstream task quoting "the layer stiffness" must quote it
  at a stated compression. (`C-0001`, surprise S-1.)
- **The 10 nm design window exists, and so does a 7 nm one.** Under a *solved* density profile the 10 nm window
  is `σ ∈ [0.0116, 0.2601] nm⁻²` — **22.4× wide**, against strong stretching's 3.5× and the box models'
  *empty* — and 7 nm gives `[0.0296, 0.0496]`. **"Empty at 5 nm and 7 nm" is withdrawn**; it was carried from
  `C-0001` through three iterations. 5 nm is still empty and the ~10 nm desired stroke is still unreachable
  everywhere, which is `C-0001`'s one headline to survive a third independent model. (`C-0011`.)
- **The two profile models agreed with each other because they share a defect, not because they bracket an
  answer.** Neither the Alexander box nor strong stretching contains the chain's *entropic resistance to
  confinement*, and against an absorbing wall that term is not a correction — it is the **whole** disjoining
  pressure. Where both models say the tile floats free, the solved layer holds **78 pN**. (`C-0011`.)
- **The Gen-1 layer's height is a coil height, not a brush height.** `L₀ ∝ N^(0.49–0.64)`, not `N¹`, and
  `L₀ ≈ 1.6–2.9 R₀` everywhere — the resting height is set by the tail of a nearly-ideal coil, not by an
  osmotic balance. Every scaling height relation in the programme had assumed the opposite. And an SCF layer
  **has no resting height at all** unless one is defined: `P = 0` only asymptotically, so `L₀` is a convention,
  and a hundred-fold change in the defining load moves `N` by 2.5×. (`C-0011`, `CH-0010`.)
- **No part of the layer is semidilute — it sits *below* `φ#`, not above it.** `φ/φ# = 0.138` at the design
  point, peaking at 0.378. This reverses the direction `C-0003` asserted, and it puts the layer exactly where
  the fitted `αφ^(9/4)` limb is **least** constrained by data. (`C-0011`, `CH-0010`.)
- **The *bulk* solution is in the dilute→semidilute crossover, with `m_eff = 1.66–1.92` — but that is a
  bulk quantity and it does not transfer to the layer.** **The grafted layer's own osmotic exponent is
  2.00–2.56**, because the `φ lnφ/N` term that bends the bulk exponent below 9/4 is the translational
  entropy of whole chains, which grafting removes. So `m < 2` is what is excluded, not `m = 9/4`.
  (`C-0002`, `C-0003`, `CH-0002`.)
- ~~**`C-0001`'s strokes are lower bounds, and its window a lower bound on its own width.** Every
  correction found in P-3 makes the layer softer — exponent down, prefactor ×0.751, excluded volume ×0.230.~~
  **WITHDRAWN by `CH-0002`.** The direction is wrong. The height relation — which `CH-0001` had itself
  identified as the unrepairable part — is the larger effect and runs **stiffer**: stiffness at first
  contact is 33–87 % *higher*, not 19 % lower, and the stroke bracket **straddles** `C-0001` at every
  height. Concluding a direction from the corrections one happens to have is a distinct failure mode from
  getting a correction wrong, and this is an instance of it.
- **PEG in water is a *marginal* solvent, and the Gen-1 chains are not swollen at all.** The measured `A₂`
  gives an excluded volume of **12.25 Å³** against a 60.4 Å³ monomer, so the thermal blob is 1222 Kuhn
  segments — **167 kDa** — while the whole design space is 60–375 monomers, i.e. **0.02–0.10 of one blob**.
  Every blob-based statement about this layer was about a structure it does not have. (`C-0003`.)
- **The Alexander-de Gennes unity prefactor is worth 6.6× in excluded volume** — `L₀ = N a^(5/3)σ^(1/3)` is
  reproduced exactly by a two-body box layer at `v = 81.0 Å³`, against the measured 12.25 Å³. Quote it as a
  scaling, never as a number. (`C-0003`.)
- **The layer response is only weakly sensitive to the interaction strength**: `k ∝ K^(1/(m+1))` and
  `N ∝ K^(−1/(m+1))` exactly, because the chain length a specified height demands moves against the
  interaction. A **16×** change in `K` moves the stroke only from 5.81 to 4.38 nm. This bounds `P-9`'s
  exposure, and it says not to spend effort narrowing `Π_int` before checking this exponent. (`C-0003`.)
- **A compliant brush and a semidilute brush may be mutually exclusive for PEG.** Reaching the
  des Cloizeaux domain needs σ = 0.99 nm⁻² at 10 nm and 3.96 nm⁻² at 5 nm — the latter closer than one
  Kuhn diameter, so unrealisable; the former melt-like and ruled out by §4(a) for stiffness. (`C-0002`.)
- **The §2 chain-tension caveat is discharged.** 4.2 pN per chain at the design point against a ~30 pN
  threshold, and structurally incapable of reaching it by grafting density alone. (`C-0002`.)
- **§4(d) poroelasticity is discharged as a non-constraint, with its boundary named.** 91 kHz at the
  nominal design point, 22.6 kHz at the §3 worst case, 5.6 kHz under a composite worst case — against a
  1 kHz requirement. Drainage is a **footprint** problem, not a thickness problem (`τ ∝ L²`, `h` cancels),
  and a **denser** layer drains faster, so the binding direction is dilution. The design would have to
  leave the poroelastic model's own domain of validity before poroelasticity could bind. (`C-0004`.)
- **The buffer does not reach the layer's mechanics.** The mobile-ion channel is **exactly zero** by a
  conservation argument — ideal excluded salt gives a free energy strictly linear in φ, which `Π = φf′ − f`
  annihilates — despite carrying **3.5× the layer's own osmotic pressure** at 10 mM. The solvent-quality
  channel is ≤ 0.4 % of the modulus over 2–10 mM; it would take a salt 1.35× stronger than any in the PEO
  literature to reach 1 %. **But the layer-local Mg²⁺ is 33–66 mM, not 2–10 mM, and goes as `1/h`** — a
  ≤ 1.7 % stroke-dependent stiffness term, and the only positive-feedback term anywhere downstream. (`C-0007`.)
- **`χ` for PEG/water is 0.372 at 300 K, measured — not the 0.45 that was cited, which has no primary source
  at all** (the 0.44 in circulation is *polystyrene in toluene*). And `χ` carries a lattice-site convention
  worth a factor of **2.010**, the exact analogue of `C-0002`'s three meanings of `a`. (`C-0007`.)
- ~~**A bulk `χ` is not a brush `χ`, and that gap is 239× everything else in this section.**~~
  **CORRECTED by `C-0013`/`CH-0012`. A bulk `χ` and a brush `χ` differ by at most 0.053, and the 0.23 that was
  feared was a units error.** `χ ≈ 0.60` for a grafted PEO layer was `1.2 × ½` assembled from an abstract —
  the source's own fits are 0.789/0.852 on a scale whose theta is **0.696**, for an air/water Langmuir
  monolayer under *lateral* compression, and the paper forbids that transfer explicitly. Normal-compression
  osmotic stress on grafted PEG **denser** than the Gen-1 window gives `χ_eff = 0.346–0.424` against a bulk
  0.372 — worth −11.4 % to +4.3 % of the stiffness, a fifth of the model bracket already carried.
  **What remains open** is that no compression measurement exists *inside* the Gen-1 window, so the bound
  comes from above and assumes monotonicity in grafting density. (`C-0013`, `CH-0012`.)
- **Mean-field screening is uncontrolled across the whole working range, and qualitatively safe across it.**
  The deviation is 123–214 % for Mg²⁺ at 5–10 nm gaps, so PB is not merely inaccurate there but outside the
  control of its own expansion; yet correlation attraction needs a gap under 1.46 nm, which the layer never
  allows. Both edges of the boundary miss the working gap — one 3.4× below, one 1.3× above. **`Ξ ∝ q³`: the
  divalence does this, not the surface charge** (Na⁺ at the same surface gives `Ξ = 3.0` against 24). (`C-0005`.)
- ~~**Point-ion PB at the electrode dies above ~0.197 V of diffuse-layer drop — 10× below the §3 ≤ 2 V target.**~~
  **CORRECTED by `CH-0007`.** The 0.197 V is a **diffuse-layer drop**, not an applied bias, and the two differ
  by the compact-layer drop — which takes 66 % of 0.1 V and 88 % of 2 V, because the electrode charge is
  exponential in `ψ_d` while the compact term is linear in it. The boundary is therefore at **≈ 1.0 V of
  applied bias**, so §3's 2 V ceiling exceeds it by 1.2×, not by 10×. Comparing a `ψ_d` threshold against a
  §3 bias without the Stern series is the error. (`C-0005`, `C-0008`, `CH-0007`.)
- **The output coupling has two requirements and only one of them contains physics.** Stability fixes a lower
  bound; **placement** fixes the value, because the force delivered over a stroke is `k_c·Δs` — so §3's own
  100 pN and 3 nm give **33.333 pN/nm by arithmetic**. Placement is written on the coupling's **secant** and
  stability on its **tangent**, so a strain-stiffening element discharges both with one part and the
  tangent/secant ratio is free stability margin. **The stiffness was never the constraint** — 45 duplexes in
  tension are 148× too stiff; the design problem is spending stiffness as *compliance*, and the dominant
  compliance term is a 13 nt ssDNA spacer at 99.6 % of the load path. (`C-0017`, `CH-0016`.)
- **Normal stabilisation and lateral confinement want the *same* anchors, not opposite ones**, because the two
  requirements differ by **72.4×** (33.3 against 0.46 pN/nm). An anchor sized on the normal condition delivers
  lateral confinement with 70× to spare; one sized on the lateral condition delivers 0.4 % of the normal.
  (`C-0017`.)
- **Three of the five axes this programme discovered do not resolve in grafting density at all.** Flatness,
  the usable bias window and the output-coupling stiffness are height-level or topological, so they cannot
  *narrow* a window — only *close a height*. **A `(σ, L₀)` window is the wrong shape for the Gen-1 decision**,
  and a constraint that cannot narrow is invisible to an intersection. (`C-0016`.)
- **`L₀/R₀ ≥ 1` is exactly vacuous, not weak** — it admits all 183 points of the sweep, including `Σ = 0.063`
  at 22 nm spacing. Coil overlap owns the lower edge of every surviving window. (`C-0016`.)
- **The unzip exceedance `C-0015` found is unreachable inside the window** — the solved layer's foundation
  multiplier is 0.823–1.605 and `C-0015` entered the band only at ×0.25 of the foundation sweep. A loosening
  neither claim could see alone. (`C-0016`.)
- **Static stability wants the thin layer, whose window is empty; the window, the stroke and the force trade
  all want the thick one.** All three pull the same way and stability pulls against all three. **That
  inversion, not any single number, is the Gen-1 design problem.** (`C-0016`.)
- **The §6 task 3 target is reachable, and the operating point it is reachable at is not one the device can be
  held at.** 100 pN *at* a 3 nm stroke needs only 0.082–0.368 V — but at the loaded operating point
  **`k_eff < 0` at 7 nm and 10 nm**, so the §6 target requires an output coupling that supplies **5–72 pN/nm of
  its own stiffness**, comparable to the whole layer at first contact. And the *free* operating point leaves
  three upstream validity ranges at once above ~0.1 V, so **the usable bias window is 0.02–0.1 V, not 0–2 V**.
  (`C-0012`.)
- **The blocking force is not the peak output force — it understates it by up to 20×.** `dW/dh = k_eff`
  exactly, so wherever the field softens the layer the characteristic *rises* with stroke. (`C-0012`.)
- **`k_es` changes sign at 0.55–1.58 nm, and that is what arrests the collapse.** §6 task 4's second branch is
  nearly right, but the arresting mechanism is **electrostatic, not osmotic**. (`CH-0011`.)
- **The two halves of §6 task 3 run in opposite directions with layer height** — blocking force gets 10×
  harder from 5 to 10 nm while stroke gets 10× easier — so a single "bias needed" figure hides both.
  Static stability then opposes both: 5 nm is stable and 10 nm is not. (`C-0012`.)
- **100 pN at ≤ 2 V is reachable on the electrostatics alone, with room** — 0.067 V at 5 nm, 0.113 V at 7 nm,
  0.679 V at 10 nm in 2 mM buffer, all comfortably inside the point-ion boundary. But **the actuator is
  voltage-saturated above ~0.5 V**: a factor of 8 in bias buys 1.9× in force, so §3's 2 V ceiling is almost
  irrelevant to what the device can do. (`C-0008`.)
- **The force's decay length is a fourth number, and the only bias-dependent one** — 1.8–2.8 nm at the working
  gap, rising to the bulk `λ_D` in the far field, and `λ_D/2` at zero bias against `λ_D` under bias. None of
  `CH-0004`'s three lengths is it. (`C-0008`, resolving `CH-0004`.)
- **§4(c)'s sign reversal now carries a force: the layer *amplifies* `F_es` by 1.15–1.60×**, largest at 10 mM
  and under compression. And **finite ion size *raises* the force by up to 56 %**, so point-ion PB is a
  **lower** bound on `|F_es|`, not an upper one. (`C-0008`.)
- **§4(c) has the sign backwards: the polymer layer *protects* the field.** It admits only 52–77 % of the bulk
  salt, so the local Debye length is 1.14–1.39× **longer** inside the layer, and lengthens further under
  compression. The dielectric-decrement mechanism §4(c) names is 3.9 % at φ ≈ 0.03 — the layer is 97 % water.
  The bound is one-sided (exclusion only); cation coordination by PEG's ether oxygens could flip it (`P-8`). (`C-0005`.)
- **"The Debye length" is three different numbers here, and all three are right in their own place** — 3.93 nm
  in bulk buffer, 0.84–1.18 nm in the counterion-dominated gap, 4.5–5.5 nm inside the PEG layer. Substituting
  one for another is `CH-0004`. (`C-0005`.)
- **Lateral confinement is decided by anchor *orientation*, not material — and it puts a footprint constraint
  on the device that scales with the stroke.** For any flexible link crossing the layer, `k_lat/k_norm` is the
  secant over the tangent of its force-extension law, which convexity bounds at **≤ 1**: a through-layer path
  costs at least as much normal stiffness as it buys laterally. A rigid vertical strut is 40–160× worse still,
  costs 96–99 % of the stroke, and is **destabilised by the load it exists to resist**. What works is a path
  lying *in* the surface — but such a tether converts stroke into tension geometrically, so
  **`L_min = δ√(Sn/2A)`**: 28 nm of tether for §3's acceptable 3 nm stroke, **93 nm for the desired 10 nm**,
  i.e. a ~100 nm assembly around a 40 nm tile, rising to ~230 nm. **Lateral confinement and the desired stroke
  are incompatible at a fixed footprint — not in physics.** (`C-0014`.)
- **Over-stiffening an anchor is not free.** The per-anchor thermal force is `√(k_BT k)/N`, so a 120×-margin
  scheme puts 29 pN on a load path where the minimum design puts 2.6 pN — past the unzip allowable. (`C-0014`.)
- **The layer confines the tile in one direction only.** Its lateral restoring stiffness is **exactly zero by
  symmetry** — a laterally homogeneous grafted layer under a non-adsorbing tile has a translation-invariant
  free energy — so the tile diffuses 62.8 nm in one 1 kHz period, 21× the σ_RMS predicate. And it exerts **no
  upward force above `L₀`**, so at zero bias the tile is unconfined in *both* directions. Whatever holds it
  down is not in the §3 stack. (`C-0010`; `T-12`, `T-13`.)
- **A variance without a bandwidth is the `f → ∞` limit, and here that is 13× the in-band number.** Only
  0.55–3.07 % of the tile's variance lies below 1 kHz, so the in-band σ_RMS is 0.07–0.77 nm where the
  broadband figure is 0.87–4.38 nm — and the predicate passes in band even where it fails broadband. (`C-0010`.)
- **The stroke and the noise are set by different stiffnesses, and the gap is a factor of three.** The secant
  (16.6–26.1 pN/nm) sets the stroke; the tangent at the working point (47.7–64.1) sets the fluctuation.
  Substituting one for the other is a 1.6× error in amplitude. Relatedly, **actuating the tile quiets it** —
  the piston RMS falls 4.2× from unbiased to the working point — but that is one-sided, because `k_es < 0`
  runs the other way. (`C-0010`.)
- **The tile is not a rigid plate.** `ℓ/L = 0.14–0.64`; it is rigid only for a perfectly uniform load,
  where it is rigid *exactly*, whatever its rigidity. Any concentrated coupling, discrete anchor, load
  non-uniformity or thermal excitation dishes it by 26–369 % of the stroke. A point-coupled lever and an
  area-averaging charge sensor therefore **do not measure the same displacement** — they differ by 26 % of
  the stroke. §4(g)'s own test for abandoning the rigid-plate picture is met. (`C-0006`, `CH-0005`.)
- **The output coupling must be near-continuous, but the count is 45 and the "more attachments than load
  paths" reading is withdrawn.** Searched over grid *shapes* rather than counts — the sheet is 25.6× stiffer
  along the helices than across them, so the square diagonal was the wrong slice — flatness needs **45
  attachments as 3 × 15**, i.e. **0.80 per crossover**, not `C-0009`'s 64 as 8 × 8 and not `C-0006`'s 55.
  Stronger: **one attachment row per duplex zeroes the peak per-load-path force exactly**, by the same
  symmetry that makes a uniform load dish nothing — and it is just as fragile, since any load non-uniformity
  restores it in proportion. (`C-0006`, `C-0009`, `C-0015`, `CH-0014`.)
- **A load-path contour average is not a peak.** A rigid anchor is carried by its **two nearest crossovers and
  essentially nothing else**, so the equal-share figure understates the peak by **2.3–7.6×**. The worst case
  reaches the 10–15 pN unzip allowable — but `C-0015` shows **layout nearly closes it**: at the design point
  every layout is clear of the band, and at the soft end the best registration removes 4.60 pN of a 4.65 pN
  excursion, landing 0.5 % inside. The lever is the attachment's **distance to the nearest crossover**
  (×1.43–1.60), not the crossover count, whose effect is 0.3–3.4 % **with the opposite sign** to `C-0009`'s.
  For a *concentrated* coupling, layout alone decides whether one attachment sits above or below the 48 pN
  duplex-shear allowable. (`C-0009`, `C-0015`.)
- **A discretisation is not automatically a relaxation.** The continuum plate errs in **both** directions and
  the sign is set by how the load meets the sheet, not by the discreteness: softer under a point load
  *entering* it, stiffer under a point *reaction* and a smooth taper, because a 2 nm duplex cannot bend across
  its own diameter and the continuum lets it. `C-0006` asserted the softening direction universally.
  (`C-0009`, `CH-0008`.)
- **The 35–60 pN band is not a per-load-path allowable.** It is a *whole-cross-section* disassembly force for
  a 6–8-helix tube at 5.5 pN/s, and a DNA rupture force without a loading rate is not a material constant.
  Per path, use single-duplex shear (~48–65 pN) or unzip (10–15 pN), with 65 pN a hard ceiling. (`C-0006`.)
- **The layer is ~1.5 blobs tall.** `L₀/s = (Σ/π)^(5/6)` identically, so the conventional `Σ = 5` onset
  buys 1.47 blobs and a ten-blob stack needs `Σ ≈ 50`. This is a *geometric* failure of the same
  convention `CH-0001` failed thermodynamically, and the two are inverse powers of the same `Σ`.
  Strong-stretching theory is also outside its own premise here: `L₀/R_F = 1.17–1.25`. (`CH-0003`.)

## Open questions for Kazik

**1. Two paywalled PDFs would close the only genuinely missing measurements in `P-6` and `P-8`.**
This is an **access** limit, not a compute limit — ACS, Elsevier, Springer and IOP all refuse an automated fetch,
and Crossref/EuropePMC serve only the abstracts (which is how the bound in `C-0007` was built at all).

- **Boucher & Hines, *J. Polym. Sci. Polym. Phys. Ed.* 14:2241 (1976)** — the one study that measured Group II
  chlorides against PEO, and so the only source for the θ-versus-[MgCl₂] curve *including the minima* that make
  a linear salting-out coefficient ill-posed in the first place. Paywalled and pre-digital; only the abstract
  is reachable, via Crossref.
- ~~**Lee et al., *J. Phys. Chem. B* 116:7367 (2012)**~~ — **STRUCK. The body was obtained free**, from NIST's
  public repository, because two coauthors are NIST staff. Worth recording as a general lesson: **Unpaywall
  and OpenAlex both report this paper `closed` and both are wrong.** Check the authors' institutions for
  federal or institutional repositories before declaring a body unreachable.
- Any PEG/PEO salt study **below 50 mM**. `C-0007` found none at all — every cloud-point and aqueous-two-phase
  paper works at 0.1–3 M, which is two orders of magnitude above the Gen-1 buffer and exactly where a
  non-monotonic `θ(c)` would have its structure. This may simply not exist rather than be paywalled.

Neither is blocking: `C-0007` bounds the effect they would pin at ≤ 0.4 % of the layer modulus, and states the
bound as a falsifiable threshold rather than a guess. They would convert a bound into a number.

**2. What is the electrode made of, and where is its potential of zero charge?**
Raised by `C-0021` as `P-13`, and it is a **specification** gap rather than a modelling one, so no amount of
calculation closes it.
§1 says *"patterned electrode"* and never says of what.
Metal against oxide is **2.6×** on the van der Waals hold-down — the one term no design can remove — which is
larger than the DNA Hamaker constant (1.17× after the square root), larger than retardation, and larger than
the polymer in the gap.
And zero *applied* bias is not zero *charge*: a contact potential of **0.9–5.1 mV**, below anything a bench
would call zero, supplies the **entire** thermal-scale hold-down by itself.
A better calculation of the wrong material is not an improvement, so the answer is bracketed and handed back
rather than chosen.

**3. Should Gen-1 be specified at 0.5 mM MgCl₂ rather than §3's 2 mM?**
This is the **single largest open design decision in the programme**, it is a specification question rather
than a calculation — which is why it is here rather than in the queue (it is `T-63`) — and as of iteration 5
it has stopped being an improvement and become a **requirement**.
**Six independent routes recommend it**: `C-0012` on the force clause, `C-0016` on the bias window,
`C-0017` on the stability floor, `C-0018` on the usable bias, `C-0027` on the corrected margin,
and `C-0032` on the realised coupling law.
At 0.5 mM the pull-in fold **does not exist** (0 of 6 states, against 6 of 6 at 2 mM), and the corrected
stability margin is **2.16–9.87× against 1.23–1.53×** — the only margin in this repository that clears
`C-0005`'s own 123–214 % mean-field error.
It costs **nothing**: `C-0007` puts the layer's buffer sensitivity at ≤ 0.4 %, so the window edges, the chain
length, the stroke and the chemistry are all unchanged.

**What changed the character of the question is `C-0032`.**
Every earlier route made 0.5 mM the *comfortable* choice while 2 mM remained defensible.
`C-0017`'s free stability margin was banked on the coupling being strain-**stiffening**,
and `C-0030` has since established that the realised coupled-standoff flexure strain-**softens**
(`t/s` falls 1.095 → 0.757).
Re-running `C-0018`'s fold analysis on that law over 216 states,
the 10 nm / 2 mM bias margin collapses from 1.007–1.032 to **1.0000–1.0019** —
the device is placed *on* its own fold —
and the fold's stroke walks back from 3.41–4.13 nm to 2.80–3.17 nm,
**through §3's own 3 nm target at two of six layer models**.
At 0.5 mM every predicate clears, at 1.44–5.93× on stiffness and 1.038–2.327× on bias.
The obvious escape is priced and fails: the adverse mounting is 42.4–61.0 pN/nm,
past `C-0023`'s 40 pN/nm compliance ceiling at **0 of 8** lengths.
A third route — a shorter standoff — lands **2.2 % short** at ℓ = 5 nm and is excluded by the
10 pN unzip allowable at ℓ = 4 nm.

**The alternative is `T-50`, costed at 1–3 weeks of primitive-model Monte Carlo for a regime `C-0005` reports
has no systematic theory at all.**

**4. Two queued tasks now exceed what one session can run, and one of them is on the critical path.**

- **`T-50` — the beyond-mean-field treatment of the actuated gap — is the last unbounded exposure on the
  Gen-1 critical path**, and `CH-0019` has just established that nothing else in the queue can reach it.
  `C-0017`'s 10 nm coupling margin is 1.19–1.42× and sits inside `C-0005`'s 123–214 % **electrostatic**
  one-loop correction; `T-1f` bounded the **polymer** correction at ≤ 9.4 % and showed it is a different
  expansion acting on the other term of `k_eff`, so it does not help. `C-0005` costs primitive-model Monte
  Carlo at **1–3 weeks of wall clock** and records that `Ξ = 17–24` has no systematic theory at all.
  **There is a free alternative and it is already in the programme**: adopt leaf `A2.2`'s 0.5 mM operating
  point, where the same margin is 2.09–8.65× and clears its own uncertainty. The decision is whether Gen-1
  is specified at 0.5 mM — a design choice, not a calculation.
- **`T-9`** (the crossover hinge constant from oxDNA) is unchanged: 2–5 k nucleotides, µs-scale umbrella
  sampling on 8 cores, **days not weeks**. It fits the machine; it does not fit inside one session.
  Its urgency rose again this iteration — `C-0026` shows the crossover's *thermal* force is `√(k_BT k_v)`
  and **diverges** in the programme's own lattice, so it is bracketed at 2.78–115.8 pN and only `T-9`
  turns that into a number.

Both are flagged so the decision to start them is yours rather than made by accident.

**5. Nothing else has needed more compute than this box provides.** `T-9` (crossover hinge constant from oxDNA)
is the first queued item that would run for *days* rather than minutes — costed at 2–5 k nucleotides and
µs-scale umbrella sampling on 8 cores. It fits the machine; it does not fit inside one session.
Flagged so the decision to start it is yours rather than made by accident.

**6. May the output superstructure be perforated under each flexure midspan — 45 duplex-omission holes as a
floor, and 45 slots one and a half times the tile's own footprint if §3's *desired* 10 nm stroke is wanted?**
Raised by `C-0035` (`T-78`), and it is a **specification** question about a body §1 and §3 never describe,
so no amount of calculation closes it.
The favourable mounting — the only one of four that is buildable at all — requires the flexure's midspan tie
to cross the plane its own standoffs stand on.
So the superstructure needs **45 duplex-sized holes, 326 nm², 20.4 % of the tile footprint**, at every stroke;
that is the floor and it is not optional.
**The bracket:** an **imperforate** superstructure caps the stroke at `ℓ − 2.69 nm` — **5.31 nm** at the
recommended 8 nm standoff, which delivers §3's *acceptable* 3 nm clause and nothing more.
**The threshold:** §3's *desired* 10 nm stroke needs each hole widened into an **18.37 × 2.69 nm slot**,
totalling **2223 nm² — 1.39× the whole 40 × 40 nm tile footprint** — or a standoff of `ℓ ≥ 12.7 nm`, which is
outside `C-0017`'s envelope.
Either half is settled by one sentence about what the superstructure is.
It is `T-95`.

**7. May §3's tile grow by 1.44× in area — 2330 nm² instead of 1600 — to buy the *desired* 10 nm stroke?**
Raised by `C-0041` (`T-96`), and like item 6 it is a **specification** question, not a calculation.
The flexure array's minimum body is `n(L + d)·d` exactly, and it carries **no aspect ratio**:
growing the tile across the helices adds rows, growing it along them adds columns, and the product is the same.
On the specified 40 × 40 nm tile the path count is bounded **below at 29** by the 10 pN unzip allowable at the
desired stroke and **above at 15** by the plan-view packing, so **the window is empty** — the two clauses of §3
differ in kind rather than in degree.
**The threshold:** a tile of **≥ 2330 nm², 1.44× the Gen-1 footprint and 1.20× in edge** — for instance
29.9 × 78.0 nm, or the same area at any other aspect.
**The price is favourable where it is usually feared:** `C-0022` finds a larger tile costs *less* at the rim,
**+6.3 %** instead of +14.7 %, because the fringing collar is a fixed 1.65 nm and scales as `1/L`.
It also relieves `T-99`'s hinge-line inventory in the same direction.
It is `T-102`.

**8. Which device does §3's *desired* clause ask for — a 33.3 pN/nm coupling travelling 10 nm, or a 10 pN/nm one?**
Raised by `C-0046` (`T-99`) as `CH-0059`, and it is the third **specification** question this branch has produced.
`C-0017` establishes that placement is arithmetic: `k_c = F/Δs`.
Every claim in the corpus has evaluated §3's *desired* 10 nm stroke on the coupling placed for its *acceptable*
3 nm one — `k_c = 33.3333 pN/nm` — which at full stroke must deliver **333 pN**, 3.3× what §3 asks for;
`C-0039` finds the real element needs **699 pN** there and reports it as *"seven times §3's own 100 pN"*.
The desired clause's own coupling is `k_c = 100 pN / 10 nm = **10 pN/nm**`, a **different device**.
`C-0046` builds it: arms of 11.4–18.1 nm that reach 10 nm inside `C-0023`'s compliance ceiling and inside the
10 pN unzip allowable, at **12 of 29** points.
**And `C-0017`'s stability floor refuses every one of them** — `|k_eff| = 23.41–27.91 pN/nm` at the 10 nm layer
in 2 mM, so a 10 pN/nm coupling is **2.34–2.79× below the floor**.
**The threshold, and it names no element at all:** composing the two clauses gives `δ ≤ F/|k_eff|`, i.e.
**3.58–4.27 nm at §3's 100 pN**.
So the answer decides *where* the desired stroke fails, not whether — and it is worth the 34-path floor that
three claims quote as though it were a property of the material.
It is `T-112`.

**9. May the polymer layer be TALLER than §3's 10 nm — 17 to 26 nm?**
Raised by `C-0050` (`T-108`), and it is the **fourth** specification question this programme has produced —
and, unlike items 6, 7 and 8, it is the only one that can buy §3's *desired* stroke at all.
The stroke is the layer's own compression, `s = L₀ − h`, so **`s < L₀` identically**:
on §3's stated heights of 5, 7 and 10 nm a ~10 nm stroke is the statement that the polymer is crushed to zero
thickness, and the actuator refuses it **1.02×** on the dry-thickness floor, **1.12×** on `C-0002`'s own
validity range and **1.35×** under §3's own 100 pN — all three **before any coupling exists**.
No output coupling can move this, because `C-0017`'s own theorem makes the delivered stroke monotone
*decreasing* in the coupling stiffness.
**The threshold:** the layer height at which the 100 pN dead-load stroke reaches 10 nm is **16.63–26.12 nm**
across `C-0003`'s six models at `σ` = 0.024 nm⁻².
§3's own tile row already allows the effort point to sit *"~20–25 nm above the electrode"*, so the geometry is
not absurd — but a 17–26 nm layer is a **different device**: `C-0002`'s concentrated crossover, `C-0005`'s
screening validity, `C-0007`'s drainage corner and `C-0012`'s bias all move with it, and nothing in this
programme has evaluated one.
**Items 6, 7 and 8 cannot substitute for it**: a bigger tile, a perforated superstructure and a softer
placement each unblock something the *coupling* needs, and none of them is a layer height.
It is `T-115`.
