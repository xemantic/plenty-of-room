# Challenges

A result that contradicts a standing claim is filed here as a challenge, with methodological grounds.
Claims in `../claims/` are never silently overwritten — §5 of the problem definition is explicit about this,
and it is the difference between a record and a rolling opinion.

A challenge names: the claim it contradicts, the result that contradicts it, the methodological grounds
(not merely a different number), and the resolution — which may be that the challenger is wrong.

| ID | Against | Grounds | Status |
|---|---|---|---|
| [`CH-0001`](CH-0001-semidilute-premise.md) | `C-0001`, its `Semidilute` validity bullet | The wrong crossover was checked, in reduced rather than physical volume-fraction units, using a coil-overlap criterion that is documented as insufficient **for this material** | **UPHELD in part** — `C-0001` stands, its numbers become bounds, `m = 3` is excluded |
| [`CH-0002`](CH-0002-corrections-do-not-all-soften.md) | `CH-0001`'s conclusion that every correction softens the layer | The height relation, which `CH-0001` itself named as the unrepairable part, is the larger effect and runs **stiffer**: 33–87 % higher at first contact, and the stroke bracket straddles | **UPHELD** — `C-0001`'s strokes are not lower bounds; that standing finding is withdrawn |
| [`CH-0003`](CH-0003-blob-stack-height.md) | `C-0001`, the Alexander-de Gennes height relation under it | The blob stack is `(Σ/π)^(5/6)` tall — 1.47 blobs at the `Σ = 5` convention — so the blob picture is being applied to a stack of one and a half, and strong stretching is outside its own premise at `L₀/R_F = 1.17–1.25` | **RESOLVED** by `C-0003`: the height relation is replaced, and the chains carry 0.06 thermal blobs — there is no blob structure to stack |
| [`CH-0004`](CH-0004-screening-decay-length.md) | the single-Debye-length reading of §1 and §3 | "The Debye length" is three different numbers — bulk buffer, counterion-dominated gap, and inside the polymer layer — and the error runs in the non-conservative direction for pull-in | **RESOLVED** by `C-0008`: the force's decay length is a *fourth* number, 1.8–2.8 nm at the working gap, and the only bias-dependent one |
| [`CH-0005`](CH-0005-rigid-tile-assumption.md) | `C-0001`, its `Rigid tile` validity bullet | The tile is rigid *exactly* under a uniform load at any rigidity, and dishes by 26–369 % of the stroke under every departure from uniformity, including 300 K | **UPHELD** — `C-0001`'s numbers stand for the case they model; its `σ_RMS` is one degree of freedom of many |
| [`CH-0006`](CH-0006-solvent-quality-bound.md) | `C-0002`, its ≤ 0.7 % of `τ` bound on the salt effect | Arithmetic 1.5× out, evaluated at the buffer concentration rather than the layer-local one, and the sign asserted rather than established | **Upheld in conclusion, overturned in construction** — no number in the parameter sheet moves |
| [`CH-0007`](CH-0007-point-ion-boundary-in-applied-bias.md) | this queue's reading of `C-0005`'s 0.197 V point-ion boundary | 0.197 V is a **diffuse-layer drop**, not an applied bias; the compact layer takes 88 % of 2 V, so the boundary is ≈ 1.0 V applied and §3's ceiling exceeds it by 1.2×, not 10× | **UPHELD** — the error was in the queue, not in `C-0005` |
| [`CH-0008`](CH-0008-plate-conservative-about-flatness.md) | `C-0006`'s claim that the continuum plate is conservative about flatness, and its `ℓ_⊥/p` validity criterion | The plate's error **changes sign** with how the load meets the sheet — it overstates anchored and edge-taper dishing by 1–16 % while understating concentrated and thermal dishing by 12–38 %; and `ℓ_⊥/p` pairs lengths from two different directions | Raised by `C-0009`; `C-0006` annotated, **no verdict moves** |
| [`CH-0009`](CH-0009-worst-point-is-not-the-centre.md) | `C-0006`'s thermal fluctuation table, its "at a point" column | The centre is the *fixed point of both tilts* and so the quietest place on the tile; a corner is exactly `√7` noisier for a rigid plate and 2.49× here, which moves the §6 task 8 verdict from 46 % of the predicate to 114 % | Raised by `C-0010`; `C-0006` annotated, no arithmetic changed |
| [`CH-0010`](CH-0010-brush-height-is-coil-height.md) | `C-0003`'s linear `L₀(N)`, its `N(L₀)`, and its `φ/φ#` | A solved profile gives `L₀ ∝ N^(0.49–0.64)`, not `N¹`; `N(10 nm) = 62.1` against 224.8–374.3; and `φ/φ# = 0.138`, i.e. the layer sits **below** the crossover, reversing the direction `C-0003` asserted | **UPHELD in substance and split** — most of the chain-length gap is the height *convention*, but 78 pN against zero is not definitional. `C-0003`'s stroke and secant stiffness survive |
| [`CH-0011`](CH-0011-electrostatic-stiffness-changes-sign.md) | `C-0008`'s statement that `k_es < 0` everywhere | `\|F_es(h)\|` peaks and then falls toward a sign change at 0.55–1.58 nm, so past the peak the electrostatics *stiffens* the layer — `C-0008`'s sweep started at 3 nm and never saw it | Raised by `C-0012`; `C-0008` annotated. It also answers §6 task 4's second branch: the collapse **is** arrested, but electrostatically rather than osmotically |
| [`CH-0012`](CH-0012-grafted-chi-number.md) | `C-0007`'s reading of the grafted `χ ≈ 0.60`, and the `P-9` premise built on it | The number is not in the source: its fits are 0.789/0.852 on a scale whose own theta is 0.696, for an air/water Langmuir monolayer under *lateral* compression, and the paper forbids the transfer explicitly | **UPHELD** — `P-9` closes as inapplicable and `C-0002`'s bulk equation of state stands |
| [`CH-0013`](CH-0013-entropic-tether-is-not-zero.md) | `C-0010`'s dismissal of an ssDNA tether as "≈ 0 at zero tension" | The geometry stretches it to the layer height whether or not it is taut, and there `F/L` *equals* the chain's own entropic constant — 0.124 pN/nm at a 10 nm gap, 27 % of the whole `A1.1` bound from one tether. The element `C-0010` dismissed is the cheapest scheme available | Raised by `C-0014`; `C-0010` annotated, no number changed |
| [`CH-0014`](CH-0014-layout-sampled-not-swept.md) | `C-0009`'s 19 % crossover-count effect, its four-point registration sample, and its 64-attachment flatness count | The layout space was sampled, not swept: the phase has period 32 bp not 16, registration is the real lever (×1.43–1.60) while the count effect is 0.3–3.4 % **with the opposite sign**, and flatness needs 45 attachments as 3 × 15 because the square diagonal is the wrong slice of a 25.6×-anisotropic sheet | Raised by `C-0015`; `C-0009` annotated in place, nothing overwritten |
| [`CH-0015`](CH-0015-usable-bias-window-is-unloaded.md) | `C-0012`'s 0.02–0.1 V usable bias window | It is a property of the **unloaded** actuator, which snaps to near-contact; the tile *held* at the §6 target sits at a 2–7 nm gap and `φ ≤ 0.09`, inside every upstream validity range. The two readings disagree by more than 2× and the device operates at the second | Raised by `C-0016`; both readings reported rather than one chosen |
| [`CH-0016`](CH-0016-coupling-requirement-is-quoted-off-operating-point.md) | `C-0012`'s output-coupling requirement table, and the same numbers in `C-0016`'s P2 | It is a **stability** threshold quoted where a **placement** condition decides the design, and it is evaluated at two grid biases that *bracket* the device's operating bias without sampling it — an interval `C-0012` itself names as an open question | **UPHELD on both grounds**, and its own "favourable direction" claim **struck**: at 10 nm the located floor is 1.5–4.4× *higher* than the column it replaces. No number in `C-0012` moves; what changes is what the table is a requirement *for* |
| [`CH-0017`](CH-0017-collapse-is-arrested-osmotically.md) | [`CH-0011`](CH-0011-electrostatic-stiffness-changes-sign.md) — its second statement, *"the instability is removed, but not by the osmotic divergence alone"*, and specifically *"the collapse **is** arrested, but by `k_es` reversing sign at 0.55–1.58 nm, not by the osmotic divergence"* as that sentence stands in `C-0012`, in `ANSWERS.md` §6 task 4 and in `TASKS.md`'s standing findings | methodological — a mechanism named from the *existence* of a feature rather than from a comparison against the alternative mechanism, and never tested against it. Both stoppers exist; only one of them is ever reached | raised. **`CH-0011`'s FIRST statement is upheld and is now asserted as four executable tests** on the real Poisson-Boltzmann solver. Only the mechanism clause is challenged, and no number in `CH-0011` or `C-0012` is disputed |
| [`CH-0019`](CH-0019-two-mean-field-expansions.md) | The standing rationale — recorded in `TASKS.md`'s `T-1f` row and repeated in `C-0011`'s "two largest exposures" framing — that bounding the **polymer** fluctuation correction is what decides whether `C-0017`'s 10 nm coupling margin survives | There are two mean fields in this programme, and the coupling margin sits inside the other one | **Upheld. The rationale identifies two different expansions.** `C-0017`'s own text is *not* at fault — it names `C-0005` correctly and calls the correction electrostatic. What is challenged is the inference drawn from it in the queue. |
| [`CH-0020`](CH-0020-thermal-blob-coarse-graining.md) | [`C-0003`](../claims/C-0003-crossover-valid-layer-response.md) — its *"the thermal blob is 1222 Kuhn segments, 3799 monomers, 167 kDa"* and the inference *"the whole Gen-1 design space is 0.02–0.10 of one thermal blob … blob arguments do not apply to Gen-1 chains at all"*; and `PegWater.thermalBlobKuhnSegments`, the code that produces it | The thermal-blob count is a convention, its excluded volume is coarse-grained linearly, and two errors nearly cancel | **Upheld in part, and it is the *inference* rather than the number that fails.** `C-0003`'s conclusion — the chains are unswollen, so Gaussian Kuhn elasticity and not blob elasticity — **stands**. What does not stand is the margin, the code that computes it, and the reading of a blob count as a measurement. |
| [`CH-0021`](CH-0021-in-plane-factor-is-not-out-of-plane.md) | [`C-0014`](../claims/C-0014-lateral-confinement.md) — its per-anchor force table, its `L_min = δ√(Sn/(2A))` design rule, and the `T-2` window constraint it derives from them | methodological — a factor transferred between two problems that have different load-path topologies, *and* transferred between two definitions that differ by the number of paths on a contour | raised. **`C-0014`'s verdict does not move** — a scheme still exists, the anisotropy theorem is untouched, and orientation is still what decides everything. What moves is the number the footprint constraint is written on |
| [`CH-0023`](CH-0023-placement-preload-sign.md) | [`C-0017`](../claims/C-0017-output-coupling-stiffness.md) — the `placementPreload` relation `R₀ = k_c s − W(s)` and the sentence that interprets its sign, in `coupling/CouplingRequirement.kt` | methodological — a sign convention stated in one direction and implemented in the other, in the one function that is the interface between `T-16` and `T-13` | raised. **No number, table or verdict in `C-0017` moves.** `placementPreload` appears in no study and in no result file; its only call sites are three assertions in `CouplingRequirementTest`, and every one of them passes with the returned value unchanged |
| [`CH-0024`](CH-0024-stroke-is-measured-from-a-height-the-tile-never-occupies.md) | [`C-0012`](../claims/C-0012-coupled-stroke-and-blocking-force.md) and [`C-0017`](../claims/C-0017-output-coupling-stiffness.md) — the stroke coordinate `s = L₀ − h`, and every stroke quoted on it; with [`C-0016`](../claims/C-0016-design-window.md) inheriting it through the window | methodological — a displacement quoted from an origin the device does not occupy, in the one state (zero bias) that the origin was supposed to describe | raised. **No verdict in `C-0012`, `C-0016` or `C-0017` moves** — the shortfall is inside every model bracket those claims already carry. What moves is the *statement* that §3's 3 nm is delivered, which becomes "2.62–2.93 nm is delivered" |
| [`CH-0025`](CH-0025-edge-taper-is-an-edge-enhancement.md) | [`C-0006`](../claims/C-0006-tile-load-distribution-and-flatness.md) (`T-5`/`T-5b`) and [`C-0009`](../claims/C-0009-discrete-lattice-tile.md) (`T-10`), specifically their `electrostatic-edge-taper` load case | methodological — an assumed load model, flagged as assumed by its own authors, replaced by a solve | the **sign** and the **width** of the taper. **No dishing verdict in either claim moves**, and no arithmetic in either is disputed. |
| [`CH-0026`](CH-0026-forces-are-footprint-integrated-one-dimensional-pressures.md) | [`C-0008`](../claims/C-0008-electrostatic-force-and-decay-length.md) (`T-3a`) and [`C-0012`](../claims/C-0012-coupled-stroke-and-blocking-force.md) (`T-3`), and through them [`C-0016`](../claims/C-0016-design-window.md), [`C-0017`](../claims/C-0017-output-coupling-stiffness.md) and [`C-0018`](../claims/C-0018-maximum-usable-bias.md) | methodological — a stated one-dimensional limitation, now quantified and found to be non-negligible and **one-signed** | a **multiplier on every force**, not a change to any mechanism. No mechanism, decay length, stiffness sign or stability verdict is disputed. |
| [`CH-0027`](CH-0027-hold-down-requirement-is-a-force-only-for-a-one-sided-stack.md) | [`C-0021`](../claims/C-0021-zero-bias-resting-position.md) — *"The scale everything is judged against"* (`F ≥ k_BT/3.0 nm = 1.380649 pN`, the **declared acceptance**), and the sentence **"A coupling can decide where the tile sits; it cannot be the thing that holds it there."** | methodological — an acceptance *currency* derived from a property of the stack that was being evaluated, and then applied as a property of the problem | raised. **No number, table or verdict in `C-0021` moves.** Every one of its 144 solved states is a one-sided stack; its two device rows are reproduced here on its own quadrature domain (1.391 against 1.40 `k_BT`, 5.371 against 5.37, 18/18 and 0/18 confining) |
| [`CH-0029`](CH-0029-the-48-pn-allowable-is-a-30-bp-number.md) | [`C-0006`](../claims/C-0006-tile-load-distribution-and-flatness.md)'s per-path allowable table **as consumed** by [`C-0009`](../claims/C-0009-discrete-lattice-tile.md), [`C-0014`](../claims/C-0014-lateral-confinement.md) and [`C-0020`](../claims/C-0020-in-plane-shear-lag.md) — specifically `C-0020`'s `A_eff = 48.00 pN`, its `L_min` table and the 54.9 pN preload that follows from it | methodological — a **length-dependent measured quantity used as a length-free material constant**, at a length no claim in the programme has ever specified | raised. **No verdict of `C-0006`, `C-0009`, `C-0014` or `C-0020` moves** — the correction is 1.35× on one path class and is dominated by the ×2 the same claim's own entry-topology result buys back. What moves is that `A_eff` acquires a **sequence-design argument** |
| [`CH-0031`](CH-0031-a-flexible-hinge-cannot-be-anisotropic.md) | [`C-0023`](../claims/C-0023-two-sided-coupling.md)'s remedy for the axial-restraint bracket — *"**A two-nucleotide single-stranded hinge at each end absorbs it**, and it is loaded along the beam's axis, not in the normal load path"* — and, through it, its `E3a` *ends free to draw in* column as a **buildable** reading rather than an idealisation | methodological — a joint sized against **one** of the three load components it has to carry, with the other two not written down | raised. **No number in `C-0023` moves** — its four spans (24.61, 39.07, 49.41, 54.91 nm), its two tangents (33.333, 91.13 pN/nm), its 8.08/86.7 pN tensions and its 0.88 nm draw-in demand are all reproduced here to ≤ 5.9e−4 — and the design that replaces the hinge lands **inside** its `E3a`/`E3b` bracket, not outside it |
| [`CH-0033`](CH-0033-thermal-excitation-is-not-a-load-non-uniformity.md) | [`C-0015`](../claims/C-0015-crossover-phase-and-registration.md)'s validity clause *"any load non-uniformity, thermal excitation or attachment-stiffness scatter restores a finite crossover force **in proportion to the non-uniformity**"*, and [`C-0017`](../claims/C-0017-output-coupling-stiffness.md)'s second named failure route, *"`T-17` finding that a real, non-uniform load restores a per-path crossover force large enough to matter, which would put `K2` back inside `C-0009`'s 2.3–7.6× concentration and take its 2.22 pN to 5.1–16.9 pN"* | methodological — **three quantities of different kinds put in one list**, and a **concentration factor applied to a force that never crosses the interface it concentrates on** | raised. **No verdict, number or table of `C-0015` or `C-0017` moves.** What moves is the *status* of one sentence in each |
| [`CH-0034`](CH-0034-flatness-count-saturates-under-the-solved-load.md) | [`C-0006`](../claims/C-0006-tile-load-distribution-and-flatness.md)'s *"the minimum number of load paths … dishing below 10 % of the stroke: 64"*, [`C-0009`](../claims/C-0009-discrete-lattice-tile.md)'s solved 64 and [`C-0015`](../claims/C-0015-crossover-phase-and-registration.md)'s **45 as 3 × 15**, **as design numbers** — i.e. as counts that make the Gen-1 tile flat, rather than as counts that make it flat *under a uniform load* | methodological — an **optimisation run against the one load case in which the objective is identically zero at infinite count**, then quoted against a load case in which it saturates at a non-zero floor | raised. **No count and no verdict of `C-0006`, `C-0009` or `C-0015` moves.** What moves is the sentence *"45 attachments make the tile flat"*, which becomes *"45 attachments exhaust what attachments can do"* |
| [`CH-0035`](CH-0035-the-edge-correction-cannot-reach-the-window-edge.md) | [`CH-0026`](CH-0026-forces-are-footprint-integrated-one-dimensional-pressures.md) — its two *directional* statements, not its measurement | methodological — a one-signed correction propagated to two clauses by the sign of the force rather than by which quantity each clause is a function of | raised. **No number in `C-0022` or `CH-0026` moves**, and the +14.7 % is reproduced from `T-3b`'s own file. What is challenged is the two-line consequence table |
| [`CH-0036`](CH-0036-a-correction-and-the-part-that-caused-it.md) | [`CH-0024`](CH-0024-stroke-is-measured-from-a-height-the-tile-never-occupies.md) — its **quoted bracket** and its status line, not its methodological point | methodological — a correction evaluated against a stack that a claim filed on the same day removed, and reported as a property of the device | raised. **`CH-0024`'s coordinate argument is upheld in full and is right.** What moves is the number attached to it, from 2–13 % to **0.6–0.9 %** at the design point, and the reason the number moved is worth more than the number |
| [`CH-0037`](CH-0037-the-buckling-duty-is-the-mandate-not-the-element.md) | [`C-0025`](../claims/C-0025-flexure-end-joint.md)'s standoff-window table — its `buckling margin (pinned / guided)` column, `17.0 / 68.1×` down to `1.5 / 6.1×` — and the design summary line *"end shear per joint 1.111 pN at 3 nm, **3.70 pN at 10 nm**; standoff buckling 8.87 pN (pinned head) / 35.5 pN (guided)"* | methodological — **a load read off the placement condition rather than off the element that carries it**, in a programme whose own `CLAUDE.md` records four times that a secant and a tangent are different quantities | raised. **No other number in `C-0025` moves** — its span, `c`, tangent, beam tension, both buckling loads and all eight window rows are reproduced in `C-0028` to ≤ 1.2e−9 |
| [`CH-0038`](CH-0038-a-standoff-grounded-at-infinity.md) | [`C-0025`](../claims/C-0025-flexure-end-joint.md)'s `J5` normal standoff — `k_θ = EI/ℓ`, `k_a = 3EI/ℓ³`, `k_⊥ = S/ℓ`, anisotropy `Sℓ²/(3EI)` — and, through them, its catalogue row (28.75 / 1.348 / 137.5 / 102.0 at 8 nm), its design summary (*"transverse support 137.5 pN/nm … 186×"*), its `ℓ = 7–10 nm` window and its statement that the window is *"closed from both sides by different mechanisms … below, by `C-0023`'s compliance ceiling … above, by `C-0017`'s own 10 nm standoff envelope"* | methodological — **a member's stiffness quoted without the compliance of what it stands on**, which `C-0025` itself names as its own second open question and then uses unqualified in every downstream number | raised. **No number in `C-0025` fails to reproduce** — its `J5-8` span (31.6403748), `c` (95.6390226), tangent (37.3911226), beam tension (3.82799407), its eight window rows and both its buckling loads (8.8672227 / 35.4688908) are all recovered in `C-0028` to ≤ 1.2e−9 |
| [`CH-0039`](CH-0039-the-base-couple-needs-a-lever-arm-the-standoff-does-not-have.md) | [`C-0028`](../claims/C-0028-standoff-base-joint.md) (`T-40`) | **structural** — a counting theorem about strand termini, not a numerical disagreement | **high**: it removes the recommended base outright and moves the surviving window |
| [`CH-0040`](CH-0040-e5-is-a-small-rotation-law-at-47-degrees.md) | [`C-0023`](../claims/C-0023-two-sided-coupling.md) (`T-23`), and by inheritance `C-0025`'s and `C-0028`'s *"`E5` is untouched, and still the fallback"* | **geometric** — a tip cannot rise past its arm, and the arm is capped by the mandate | **high**: `E5` is the branch the whole standoff line falls back to |
| [`CH-0041`](CH-0041-the-standoff-supplies-the-draw-in-it-was-charged-for.md) | [`C-0025`](../claims/C-0025-flexure-end-joint.md) (open question 1 and its `S_eff`) and [`C-0028`](../claims/C-0028-standoff-base-joint.md) (its validity range, its `standoffTipCompliance` bound and its stated failure mode) | **methodological and numeric.** The dropped term is not small, it is not a correction to the term that was kept, and its argued sign is wrong in both halves | **UPHELD in the bound, OVERTURNED in the consequence.** No number in either claim fails to reproduce; what fails is the sentence both wrote about what the number means |
| [`CH-0042`](CH-0042-a-strain-softening-coupling-has-no-free-stability-margin.md) | [`C-0017`](../claims/C-0017-output-coupling-stiffness.md)'s tangent/secant theorem **as consumed by** [`C-0023`](../claims/C-0023-two-sided-coupling.md), [`C-0025`](../claims/C-0025-flexure-end-joint.md) and [`C-0028`](../claims/C-0028-standoff-base-joint.md) — and against the transfer of `C-0023`'s **oddness** through all three | **methodological.** A theorem stated for a strain-*stiffening* element is being applied to one that is now strain-*softening*, and a two-sidedness argument written on an odd law is being inherited by a law that is not odd | **UPHELD and RESOLVED** by [`C-0032`](../claims/C-0032-softening-coupling-stability.md) / [`T-76`](../tasks/T-76-softening-coupling-stability.md) (2026-08-14). **Both horns hold.** At 10 nm / 2 mM the softening line's bias margin collapses from `C-0018`'s 1.007–1.032 to **1.000–1.002** and the fold's own stroke walks back from 3.41–4.13 nm to **2.80–3.17 nm**, crossing §3's 3 nm target at two of six models; `min_s k_tangent` = 22.875 is below the 23.41–27.91 floor at 6 of 6. The **adverse mounting is 1.06–1.53× past `C-0023`'s ceiling at 0 of 8 standoff lengths.** What the two-horn framing did not contain: **0.5 mM clears every predicate at 1.44–5.93×**, and `ℓ = 5 nm` at 2 mM clears five of six. `C-0032` raises [`CH-0047`](CH-0047-a-tangent-minimum-over-zero-stroke-is-not-a-requirement.md) against this challenge's own `[0, 10]` reading of `min_s k_tangent` — **no verdict here moves** |
| [`CH-0043`](CH-0043-a-reproducibility-certificate-that-certifies-the-path.md) | [`C-0019`](../claims/C-0019-mean-field-fluctuation-corrections.md) — its **provenance line**, not any of its numbers | **methodological.** A byte-for-byte diff certifies that nothing perturbed the iteration, which is a property of the *path*. It was reported as a property of the *answer*, and the two differ here by three significant digits | **UPHELD, and no number of `C-0019` fails.** Every verdict, every window edge and every quoted figure stands. What fails is the sentence about what the diff established |
| [`CH-0044`](CH-0044-c-equals-twelve-and-the-series-composition-cannot-both-be-right.md) | [`C-0029`](../claims/C-0029-perpendicular-junction-routing.md), the `E5g16` design table and the `E5g` family it recommends | **methodological** — a boundary condition changed without changing the composition the boundary condition belongs to | **the design number moves, the verdict does not.** `E5g16`'s arm is 12.24 nm from a composition that is exact only at `c = 3`; solved on the boundary-value problem `c = 12` describes, the same geometry assembles to **54.61 pN/nm against the 33.3333 pN/nm mandate** and past its own **40 pN/nm** compliance ceiling at the **secant**. The corrected arm on the *realised* anchorage is **12.50 nm**, and every acceptance verdict `C-0029` reported still holds |
| [`CH-0045`](CH-0045-the-mounting-sense-is-not-a-free-binary.md) | [`C-0030`](../claims/C-0030-coupled-standoff-joint.md) — its Deliverable 4, its *"Still open"* item 1, and its sensitivity row `mounting: favourable → adverse` | **methodological** — a design variable named without the kinematic chain that determines it, and an emptiness quoted over a swept interval rather than as a threshold | **RAISED.** `C-0030`'s numbers all reproduce; its *framing* of the variable does not survive. |
| [`CH-0046`](CH-0046-a-standoff-in-tension-does-not-buckle.md) | [`C-0030`](../claims/C-0030-coupled-standoff-joint.md) — the adverse rows of its Deliverable 4 table (`margin, CanDo / Fields` = 2.53/1.90 … 0.75/0.57) and its sensitivity row `mounting: favourable → adverse` reported as `2.18 → 0.99` and `1.64 → 0.74` | **methodological** — a stability predicate evaluated on the *magnitude* of a member force whose *sign* the same model determines, and whose sign is opposite in half the cases tabulated | **RAISED, and it does not move a verdict** — the adverse mounting fails on `P3`, which owns that column. Filed because the number is quoted and would be read. |
| [`CH-0047`](CH-0047-a-tangent-minimum-over-zero-stroke-is-not-a-requirement.md) | [`CH-0042`](CH-0042-a-strain-softening-coupling-has-no-free-stability-margin.md)'s own prescription — *"the quantity to compare is not the tangent at the working point but its **minimum over the stroke**"* — as consumed by [`C-0030`](../claims/C-0030-coupled-standoff-joint.md)'s `minimumTangent` field and by [`C-0032`](../claims/C-0032-softening-coupling-stability.md)'s `Q2` | **methodological.** A minimum is being taken over a range whose lower endpoint carries **no requirement at all**, so for one whole class of elements the reported number is a property of the interval rather than of the coupling | **OPEN, and narrow.** No verdict in `C-0030` or `C-0032` moves — the softening element's minimum is **interior** and identical on both ranges. What moves is every *comparison* against a strain-stiffening element |
| [`CH-0048`](CH-0048-the-good-solvent-premise-was-checked-on-monomers.md) | [`C-0007`](../claims/C-0007-solvent-quality-vs-salt.md) — its `φ** = v/v₀ = 0.516` row and the gate-5 statement *"the good-solvent premise checked against the actual material — `φ/φ** = 0.06–0.14`, a factor of 7 of margin"* | **methodological** — a crossover defined on a *statistical segment* evaluated on a *monomer*, which is `C-0002`'s `a`-trap in a third costume | **RAISED.** Wrong by `b⁶/(v₀ v_K n_K²) = 16.17`, a factor **independent of the excluded volume**; corrected, `φ** = 0.0105–0.0319` and the layer sits at **0.9–2.2×** it at rest, i.e. the premise fails rather than holding. `C-0007`'s headline result and every other row stand, and the correction selects the mean-field end of a bracket `C-0007` already carries |
| [`CH-0049`](CH-0049-the-cited-band-is-a-reduced-density-and-the-fit-range-is-wrong.md) | [`C-0002`](../claims/C-0002-peg-material-parameters.md) — its *"the semidilute→concentrated one at 0.2–0.3"*, its *"fitted over 0–50 wt %"*, and the DOI it carries | **methodological + provenance** — a crossover quoted in the wrong convention on the wrong segment, a fitted range narrower than the data, and a citation that does not resolve | **RAISED, on three grounds.** No derived crossover lands in `0.2–0.3`; the two that do are `v_m/v₀ = 0.203` and `1 − 2χ = 0.257`, both monomer readings, and R&C's printed `φ** ≈ v/b³` is a **reduced** density, `0.257` reduced being `0.036` physical. The real fit range is **1.5–67.5 wt %** (`φ ≤ 0.63`), recovered from the source data. The DOI is `10.1021/jp806893a`. **`C-0002`'s parameter sheet, `α`, `φ#` and `m_eff` do not move** |
| [`CH-0051`](CH-0051-the-pull-in-bias-falls-too.md) | [`C-0027`](../claims/C-0027-window-resynthesis.md) — its **pull-in propagation** (`marginLowerBound` = `≥ 1.108–1.134`, and *"the operating bias falls, unambiguously"*), not its window, its axes, its coupling margins or its decomposition | **methodological** — a **ratio** of two biases corrected in one of its two arguments, with the other held at the value the correction was applied to change | **RAISED.** `C-0027`'s 8–9 % fall in `V*` is reproduced at 8.5–9.9 % by re-solving, but the pull-in bias falls 7.1–8.0 % too, so the solved margin is **1.021–1.028**, not `≥ 1.108`. And the direction is not universal: the margin moves with the sign of `3 nm − s_fold`, so at 7 nm / 10 mM the same collar makes it **0.9–3.5 % worse**. At four of six models pull-in stops binding altogether |
| [`CH-0054`](CH-0054-the-sixteen-crossover-hinge-line-does-not-exist.md) | [`C-0029`](../claims/C-0029-perpendicular-junction-routing.md)'s `E5g16` and [`C-0034`](../claims/C-0034-guided-arm-anchorage.md)'s `E5a16`, and [`C-0023`](../claims/C-0023-two-sided-coupling.md)'s free `hingeCount` | **a count on the lattice** — a hinge of `n` crossovers needs `(n − 1) × 32 bp` of **collinear interface**, and none of the three claims counted | **RAISED, and it moves a verdict rather than a number.** A 40 nm tile carries **four** at every one of the 32 phases; sixteen needs **163.2 nm** (4.08 tiles) or **33 duplexes** (2.22 tiles) transversally, and the transverse line restrains the wrong axis. Every upstream number reproduces at ≤ `3.8e−9`. At the count that exists the arm places at **7.748 nm** — §3's **acceptable** stroke clears at 36.58 pN/nm and the **desired** 10 nm is out of geometric reach; at the one or two a flexure can own at 45 paths the tangent is **42.0–54.1 pN/nm**, past `C-0023`'s own ceiling |
| [`CH-0055`](CH-0055-the-forty-five-path-array-is-not-a-placement.md) | [`C-0023`](../claims/C-0023-two-sided-coupling.md), [`C-0030`](../claims/C-0030-coupled-standoff-joint.md) and [`C-0035`](../claims/C-0035-flexure-mounting-sense.md) — the shared `Conditions` premise *"45 load paths on `C-0015`'s 3 × 15 grid"*, and `C-0035`'s 326 nm² tie-aperture **floor** | **methodological** — a design point carried as a condition through four claims without a plan view, and a connectivity question priced as an area | **RAISED, and it moves a design point rather than a number.** The 45-path array is unrealisable at **0 of 720 orientations, at any level count and on any body**: the grid's across-helix pitch **is** one duplex and its along-helix pitch is under the span plus a duplex, and stacking cannot separate two vertical members that share a height range whatever levels their beams sit at. The tile carries **15**. Every upstream number reproduces (`C-0030`'s span to `2.9e−5`, `C-0035`'s three areas to ≤ `1.2e−3`). And the tie holes are collinear, so they **sever** the superstructure into **18 components** at every one of the 32 phases — cured by **8 bp** of stagger |
| [`CH-0056`](CH-0056-the-azimuthal-quantum-belongs-to-the-sheet.md) | [`C-0029`](../claims/C-0029-perpendicular-junction-routing.md)'s cheap bound 3 — the 33.74 deg/bp azimuthal quantum and its *"the phase costs at most 8.4 %"* — as inherited verbatim by [`C-0037`](../claims/C-0037-triangulated-standoff.md)'s validity range | **a bound evaluated on the wrong body** — a lattice belonging to the **sheet's** phosphate positions applied to the **standoff's** base chord | **RAISED, and NO number in either claim moves.** A standoff is a *separate* duplex, so nothing fixes its helical phase and its rotation about its own axis is **continuous**: `C-0042`'s search lays both chords at **0.00 deg** from the flexure axis at every separation from 6 to 12 bp, on both groove conventions and at the seat duplex's own axis. Both claims were conservative and neither applied the projection, so the effect is a **design obligation** and not an arithmetic — the 8.4 % is not a reserve, and `C-0037`'s "best-phase" caveat can be struck. The quantum still binds *where a link may land*, one level down, as `C-0042`'s sheet-phase residual |
| [`CH-0059`](CH-0059-the-desired-stroke-placement-is-below-the-stability-floor.md) | the programme-wide **reading convention** for §3's *desired* stroke, carried by [`C-0023`](../claims/C-0023-two-sided-coupling.md), [`C-0029`](../claims/C-0029-perpendicular-junction-routing.md), [`C-0034`](../claims/C-0034-guided-arm-anchorage.md), [`C-0039`](../claims/C-0039-two-spring-elastica.md), [`C-0040`](../claims/C-0040-hinge-line-census.md) and [`C-0041`](../claims/C-0041-flexure-array-packing.md) | **methodological** — a design evaluated at a state it was not placed for; the sixth instance of *"quote a stiffness with its compression"*, applied to the acceptance clause | **RAISED, and NO number in any claim moves — what moves is what the numbers are OF.** §3's desired clause has never been *placed*: every claim reads it on the coupling placed for the **acceptable** stroke, `k_c = 33.3333 pN/nm`, which must then deliver **333 pN** (and `C-0039` finds the real element needs **699**). `C-0017`'s own arithmetic gives the desired clause `k_c = 100/10 = **10 pN/nm**`, a different device — which `C-0046` shows the flexure **can** build (arms 11.4–18.1 nm, **12 of 29** points clearing every element clause) and which `C-0017`'s **stability floor refuses at 2.34–2.79×**. Composing the two clauses caps the stroke at **3.58–4.27 nm at 100 pN**, naming no element. The concrete casualty is the **34-path floor**, quoted in three claims as a property of the material: it is `33.333 × 10/10`, and the same allowable gives **10** under the other reading. `T-112` |
| [`CH-0062`](CH-0062-the-buildable-hinge-count-clears-the-ceiling-on-the-elastica.md) | [`C-0040`](../claims/C-0040-hinge-line-census.md)'s verdict clause *"at the one or two a flexure can actually own, the tangent is 42.0–54.1 pN/nm and even the acceptable stroke fails `C-0023`'s own ceiling"*, and its design rows at `hingeCount` = 1–3 | **methodological** — a design graded on a placement pipeline that a sibling claim, filed in the same iteration, had already superseded; the two differ by **1.9× in the placed arm and 1.38× in the tangent**, and the difference is largest exactly where the verdict is taken | **RAISED, and it moves a verdict the FAVOURABLE way.** `C-0040`'s 4.765 nm / 54.113 pN/nm at one crossover is `C-0034`'s **series composition**, which [`CH-0053`](CH-0053-both-errors-run-the-same-way-and-the-desired-stroke-does-not-survive-them.md) supersedes; on `C-0039`'s own exact elastica the same design places at a **9.131 nm arm and 39.18 pN/nm — inside `C-0023`'s ceiling with 2.1 % of margin** — and `C-0039`'s own published table already contains that number. At 45 paths the crossover budget makes `h = 1` the only affordable count, so this is the design and not a corner. `C-0046` reaches the same conclusion independently (`(56, 1)` at **38.17 pN/nm**) without naming the disagreement. **The census, the ledgers, the fan law, the `n_eff` result and the desired-stroke verdict are untouched** |
| [`CH-0063`](CH-0063-the-collar-was-carried-onto-a-load-line-the-device-does-not-have.md) | [`C-0033`](../claims/C-0033-collar-on-the-equilibrium-path.md)'s `P4` — *"the margin rises at every one of the six models … it does not go below one at any model"* — and by inheritance [`CH-0051`](CH-0051-the-pull-in-bias-falls-too.md)'s relocation table | **methodological** — a correction carried onto the **affine mandate** load line while a sibling claim of the same iteration had established that the coupling the programme has is strain-**softening**; the second instance of `CLAUDE.md`'s *"never quote a margin corrected on one channel without checking whether the other channel's correction exists yet"*, and the first where the two are not the same size | **RAISED. Every number in `C-0033` reproduces and its scope does not survive.** At `C-0018`'s own fold the baseline coupled tangent vanishes by construction, so the three standing corrections compose **exactly**: `C-0033`'s collar `+2.605` to `+4.993`, `C-0019`'s fluctuation `−1.449` to `−5.807`, and `C-0030`'s realised softening `−9.207` to `−10.288 pN/nm`, giving **−8.398 to −11.062 pN/nm at 6 of 6 models with no straddle**. The collar recovers **27–49 %** of what the element costs. `C-0033`'s **1.021–1.028** is the affine mandate's margin; **`C-0032`'s 1.0000–1.0019 is the standing 10 nm / 2 mM statement.** `C-0033`'s `P1`, `P2` and `P3` are untouched, and 0.5 mM makes the whole composition vacuous |
| [`CH-0064`](CH-0064-the-validity-ceiling-is-read-on-a-layer-four-times-denser-than-the-window.md) | [`C-0050`](../claims/C-0050-desired-stroke-reach.md)'s **bound 3** and the validity clause that says its bounds *"inherit `C-0003`'s validity ranges in full"* | **methodological** — a licence re-checked at the design points a *previous* falsifier named rather than at the one the claim actually reads, the sixth instance of *"quote it with the state it was read at"* and the first applied to a **layer model** | **RAISED, and the verdict does not move.** At the top of `C-0027`'s own 10 nm window `C-0003`'s six trial-function models sit at `φ = 0.1505–0.2845` where `C-0011`'s solved layer — the layer every window edge in this programme is drawn on — sits at **0.0623–0.0686**, a factor of **4.15**, and the two **disagree about whether a `φ ≤ 0.2` ceiling exists at all** (2 of 6 models have none; the solved layer is 2.92× clear). `C-0050`'s answer is untouched because its **bound 2 is kinematic**, needs no crossover and is short by 1.02× on its own; what is not licensed is the number, at the one design point nobody had checked. Checking costs one multiplication: `φ = Nσv₀/h` and `T-1d` emits it |
| [`CH-0065`](CH-0065-the-hinge-arm-array-has-no-plan-view-either.md) | [`C-0050`](../claims/C-0050-desired-stroke-reach.md)'s catalogue row **`E5a1` at 45 paths** — *"none — every predicate clears"* — and its headline *"3 of 14 clear them all at the acceptable one"*; and, by inheritance, [`C-0039`](../claims/C-0039-two-spring-elastica.md)'s and [`C-0023`](../claims/C-0023-two-sided-coupling.md)'s *"45 load paths"* premise for the whole `E5` family | **methodological** — an **unevaluated** predicate reported as an absent one; the row carries `packingAssessed = false` and `C-0050`'s validity range says so, but its verdict line and its count do not | **RAISED, and `C-0050`'s bound is untouched and strengthened.** Assessed, the predicate fails twice. A hinge is a crossover, so an arm roots only where one is, and a row sees only its **two bounding interfaces** — 5.44 nm apart on an interior row, 10.88 on an edge row, against an arm needing **11.82** — so the tile places **43 of 45**, exactly (construction meets the independent per-row bound), at every one of the 32 phases and on all five swept axes. And an arm is **cut out of** its host: 43 arms take **65.4 %** of its duplex length, spend 43 crossovers and bury 13, **56 of 56**, leaving **zero** surviving crossovers and **no bonded component at all**. The count that leaves the host whole is **25**, so the threshold is **45 → 25 (1.80×)**, not 45 → 43. `C-0050`'s acceptable-stroke count becomes **2 of 14**, and the row that leaves is the only one it computed on the exact element. `C-0039`'s mechanics, `C-0040`'s census and `C-0041`'s own verdict all reproduce |
| [`CH-0066`](CH-0066-the-surviving-designs-consume-the-sheets-own-connectivity.md) | [`C-0046`](../claims/C-0046-fewer-longer-flexures.md)'s verdict clause *"the best point of the whole space is `(56, 1)`"*, its three surviving designs at 45/50/56 paths, and its *"the window's width is 1.24× in path count"* | **methodological** — a budget spent past the point at which the thing it is drawn from stops existing; the ledger `n·h ≤ 56` carries no reserve for the sheet's own connectivity | **RAISED, and NO number in `C-0046` moves — every one reproduces to ≤ `4.7e−5`. What moves is which of them is a design.** The interfaces of a single-layer sheet form a **path graph** on its 15 duplexes, so a connected sheet needs one retained crossover on each of its **14** interfaces and the hinge budget is `56 − 14 = **42**`, i.e. **75.0 %** (35 of 49 at the other 22 phases) against an admissible region of **80–100 %**. All three reported designs sever the tile — into **≥ 4, ≥ 9 and exactly 15** pieces. The best *buildable* point is **`(42, 1)`**, arm 8.882 nm, usable stroke **3.063 nm**, and the surviving window is **`39 ≤ n ≤ 42`** — which also resolves `C-0046`'s own open `34 < n ≤ 45` threshold at **39**. The correction costs **7.4 %** of the stroke and leaves a **2.1 %** margin on §3's acceptable clause. `C-0046`'s degeneracy result and its desired-stroke verdict are untouched. `T-119` is the one falsifier |
| [`CH-0067`](CH-0067-a-leg-is-one-body-and-its-two-chords-are-not-independent.md) | [`C-0048`](../claims/C-0048-truss-cap.md)'s recommended azimuth pair and leg length, and `C-0037`/`C-0042` insofar as they treat the leg length as a continuum | Raised by `C-0052` — the two ends of one rigid leg cannot present independently chosen chords: they differ by `m × 33.74°`, and 21 steps is 78.5° from the pair the design asks for, on all three twist readings | **RAISED. The design survives everywhere and the forced pair is better than the recommended one**, because the recommendation reads the plane that does not govern |
| [`CH-0073`](CH-0073-the-along-helix-scatter-rule-reverses-on-a-non-uniform-coupling.md) | [`C-0026`](../claims/C-0026-one-row-per-duplex.md)'s **build rule** — *"if they must differ, let the error be along the helix"* — and the reading of its scatter table that treats the along-helix pattern as harmless *simpliciter* | **methodological** — a direction shown to be a **symmetry of equal springs on one row per duplex**, carried as a build rule for `C-0058`'s non-uniform coupling, whose 3 × 15 grid makes the along-helix index the rim/interior index | raised by `C-0060` (`T-122`). **No number of `C-0026` moves** — its 0.883 pN per unit amplitude, its `3e−11 pN` exact zero and its 17 % break-even are reproduced or cited unchanged, and the rule survives on the **crossover-force** channel. What is added is a second channel that ranks the two patterns **oppositely**: the flatness verdict is lost at **31.6 %** along the helices against **69.8 %** across, a factor of 2.21 with `C-0017`'s total held |
| [`CH-0072`](CH-0072-the-alignment-is-not-free-on-the-torsion-feasible-set.md) | [`C-0042`](../claims/C-0042-paired-perpendicular-junction.md)'s *"the azimuth costs exactly nothing … both base chords come out at 90.0°, at every separation"* and its recommended **7 bp** row; and [`C-0029`](../claims/C-0029-perpendicular-junction-routing.md)'s *"a 90° routing exists **and it is a scaffold excursion**"* | **methodological** — a design variable priced on a search space larger than the feasible one: the alignment is free over the placements that satisfy the *distance* criterion, and the placements that also satisfy the *torsion* criterion are a sparse subset over which it is not | raised by `C-0059` (`T-124`). **The pair still closes at every separation 6–12 bp** — a stronger existence result than `C-0042` could give — but its worst chord runs **33°/69°/57°/6°/6°/33°/33°** instead of 0.00° everywhere, so its own **7 bp is the worst of the seven** at 69° (12.8 % of the couple, and *past the half right angle* where `C-0037`'s two-axis base cannot be represented at all) while **9–10 bp** delivers 6.0°. And the **scaffold excursion cannot be aligned**: 1 of 120 closing at a 39.0° chord against independent staples' 7 at **0.0°**. `CH-0056` is upheld — a free duplex's chord inherits no lattice phase — and its *consequence* is not, because feasibility is a relation between two bodies |
| [`CH-0079`](CH-0079-a-tie-grid-is-a-registration-and-an-armed-tile-has-none.md) | [`C-0035`](../claims/C-0035-flexure-mounting-sense.md)'s nominal-design row — *"what the tile carries: only the 45 tie attachments of `C-0015`'s 3 × 15 grid"* — and the premise beside it that *"the tile now carries no out-of-plane element at all"* | **conditions**, not arithmetic — the row is true of the tile `C-0035` had and false of the tile the programme now proposes. On `C-0063`'s 34-arm placement **26 of those 45 stations do not exist**, at the best of 2916 arm senses, and **no rigid translation** of the grid restores them | raised by `C-0066` (`T-126`). **No number in `C-0035` fails to reproduce** — its 325.62 nm² aperture floor, 5.31 nm clearance and 4.69 nm penetration are recomputed through its own library as this task's zero-arm limiting case. What falls is the availability of the stations, not the mounting determination |
| [`CH-0078`](CH-0078-the-base-floor-is-a-minimum-over-a-coordinate-the-array-pins.md) | [`C-0059`](../claims/C-0059-torsion-feasible-routing.md)'s base misalignment floors and [`C-0062`](../claims/C-0062-crossbar-trio-existence.md)'s design table built on them — the **6.0°** at the 9 and 10 bp rows and the *"best representable design is the 10 bp row at both ends"* verdict | **conditions**, not arithmetic — a floor is a minimum over the base's axial position on the host duplex, and an array **pins** that position at the 34 upward stations `C-0055` and `C-0063` supply. Read there, the 10 bp row is **57.0°** (9.5×) and past the 45° at which `C-0037`'s `TwoLinkBase` cannot represent a base at all; the 9 bp row is 18.0° | raised by `C-0065` (`T-130`). **No number in `C-0059` or `C-0062` fails to reproduce.** `C-0062`'s design table stands as a statement about a **lone** truss and is withdrawn as one about an **array**; the row an array can build is **9 bp at 18.0°** |
| [`CH-0080`](CH-0080-the-equal-spring-advantage-belongs-to-the-ten-nanometre-layer.md) | [`C-0063`](../claims/C-0063-upward-root-placement.md)'s **Deliverable 2** — *"it needs **no distribution at all**"* — as carried forward by `C-0065` and `C-0066` | **conditions**, not arithmetic — the sentence is a statement about a **10 nm layer's** load field. Over the traversed range of all three 10 nm devices the 34 equal springs are flat (**0.0789 / 0.0853 / 0.0896**), and against §3's **5 nm** layer the same 34 roots dish **0.2000** and are **worse than no coupling at all at both** of that device's states (0.1104 against a free 0.0638; 0.2000 against 0.1648) | raised by `C-0068` (`T-129`). **No number in `C-0063` fails to reproduce** — its 0.0706 to `2.9e−10` — and its placement is *strengthened*, being the argmin of the range objective as well (0 of 198 288 better). There are two remedies: a distribution at a peak ratio of **2.32** (0.0565), or a **different phase** — no centro-symmetric placement at phase 24 clears the 5 nm range (best 0.1169) while one at **phase 8** does with equal springs (**0.0895**), and it reads 0.2416 at the 10 nm design state |
| [`CH-0082`](CH-0082-a-truss-has-two-legs-and-the-design-table-gives-it-one-base.md) | [`C-0059`](../claims/C-0059-torsion-feasible-routing.md)'s `feasibleTrussDesign` and [`C-0062`](../claims/C-0062-crossbar-trio-existence.md)'s design table built on it — both take a truss's base misalignment as a **single scalar** shared by the two legs | **methodological** — a truss has **two** legs `w` base pairs apart on one duplex, and the register pins them at two *different* azimuths (−9.0° and −18.0° at the 9 bp row, 0.0° and +57.0° at the 10 bp). One leg length rotates both cap chords by the same `m τ`, so their difference is invariant under the length and floors the worse cap at `\|fold(δ_A − δ_B)\|/2` — a bound neither pipeline can express | raised by `C-0070` (`T-132`). **Not verdict-moving on the surviving set** — 4.5°, 19.5° and 16.5° at the 9, 11 and 12 bp rows, all below `C-0062`'s own cap floors — but **28.5°** at the 6 and 10 bp rows against its composed 9.0° and 27.0°, and both of those rows already fail another clause. **No number of `C-0059`'s or `C-0062`'s fails to reproduce** |
| [`CH-0081`](CH-0081-a-rigid-root-demands-a-longer-arm-than-the-plan-admits.md) | [`C-0048`](../claims/C-0048-truss-cap.md)'s, [`C-0062`](../claims/C-0062-crossbar-trio-existence.md)'s and [`C-0065`](../claims/C-0065-crossbar-array-placement.md)'s truss branch — the premise that a triangulated standoff array is a useful **foundation** for the Gen-1 output coupling | **methodological** — a truss cap is a **root**, and a bending element's plan length `(c EI/k)^(1/3)` is monotone increasing in *both* end restraints, so the truss's own virtue (a base that does not rotate) is what pushes the element it caps outside the plan budget | raised by `C-0069` (`T-133`). **`C-0065`'s placement result stands entirely** — 44 of 44 trios place 34 times, and its 27.4119472 nm span and **12 of 34** both reproduce at departure `0.0`. What falls is the output stage: against an **8.19 nm** budget the two-support flexure family's shortest possible member is **22.41 nm (2.74×)** and a rigid-root, free-tip arm is **9.247 nm (12.9 % over, 24 of 34)**, while the element that places needs a **compliant** root — one crossover at 13.53 against a ceiling of **13.93** |
| [`CH-0084`](CH-0084-the-measured-staple-incorporation-is-past-the-flatness-threshold.md) | [`C-0060`](../claims/C-0060-buildable-stiffness-ratio.md)'s tolerance verdict — *"the tolerance is a threshold, and it is generous"* — and through it [`C-0058`](../claims/C-0058-non-uniform-coupling.md)'s flat two-level distribution and [`C-0017`](../claims/C-0017-output-coupling-stiffness.md)'s mandate as an achievable **sum** | **a measurement exists and neither claim looked for it.** `C-0060` records `T-45` as *"still unmeasured"* and reasons about *"the 5 % a staple design might plausibly hold"*, an assumption with no source. Strauss et al., *Nat. Commun.* **9**:1600 (2018) map incorporation at single-staple resolution across all **168** staples of a Rothemund rectangle — **48–95 %, mean 84 %, worst at the edges** — and a missing staple **removes** a load path rather than perturbing it, so the population is Bernoulli and `σ_rel = √(f/(1−f))` | raised by `C-0072` (`T-134`). The mean gives **43.6 %** relative per-path scatter — **1.26×** `C-0060`'s 34.6 % threshold and **2.57×** `C-0026`'s break-even — the edge sites **104 %**, and the mean alone is a **16 %** shortfall on `C-0017`'s mandate, **2.9×** the worst rounding placement error `C-0060` spends a trim to remove. **The position dependence runs the wrong way for `C-0058`**, which puts 34 of its 45 stations on the rim and gives them the *stiff* level. `C-0060`'s stiffness and buildability results are **untouched and reproduce** |
| [`CH-0090`](CH-0090-the-scaling-estimate-uses-the-exponent-of-a-different-quantity.md) | [`C-0011`](../claims/C-0011-scf-density-profile.md) and [`CH-0010`](CH-0010-brush-height-is-coil-height.md) — their *"a layer whose `2⟨z⟩` is 10 nm would need `N ≈ 190–210`"*; and [`C-0003`](../claims/C-0003-crossover-valid-layer-response.md)'s reading of its own box-to-SST spread as **profile uncertainty** | **methodological** — an extrapolation is worth what its exponent is worth, and the exponent used is measured on a quantity the extrapolation is not about | raised by [`C-0077`](../claims/C-0077-first-moment-chain-length.md) (`T-1e`). **UPHELD on both heads, and neither claim's verdict fails.** The exponent of `L₀^F` in `N` (0.455–0.468) is a consistent **10 %** below the exponent of `2⟨z⟩` in `N` (0.501–0.520), and the formula uses the first where it needs the second; the band is also narrowed from the claim's own 0.49–0.64 to 0.50–0.55 with no derivation. **The exact inversion is `N = 175.08`, 7.713 kDa — OUTSIDE `190–210`**, and every reading at a measured exponent overstates it by 13.6–34.3 %, the exponent drifting upward over an extrapolation of 2.8× in `N`. Head 2: read on **one** functional the box and strong stretching agree to **0.76 %** where their own conventions put them **28.4 %** apart, so most of `C-0003`'s bracket's internal spread is a convention difference between its own two models. `C-0011`'s `N = 62.1`, window, stroke and stiffness are reproduced and untouched |
| [`CH-0091`](CH-0091-a-first-moment-ten-nanometre-layer-is-not-a-ten-nanometre-layer.md) | [`C-0016`](../claims/C-0016-design-window.md)'s **height-convention banner**, as carried by `C-0027`, `C-0051` and `ANSWERS.md` | **methodological**, with two arithmetic corrections — the banner presents two conventions as two readings of **one** layer; they are two different layers, and only one is inside §3 | raised by [`C-0077`](../claims/C-0077-first-moment-chain-length.md) (`T-1e`). **UPHELD, and `C-0016`'s window does not move — not one edge, not one owner, not one grid step.** A `2⟨z⟩ = 10 nm` layer puts its tile at **13.20–18.05 nm**, inside §3's 5–10 nm band at **0 of 61** grid points, so the window is in the right convention. The banner's *"8–9 kDa"* is **7.713 kDa** at the design point and **4.17–8.73 kDa** across the window, and its *"about four times"* is **2.585–2.843×** — that factor was the top of one range over the bottom of the other. Its *"1.71–2.16× smaller"* is correct (measured 1.710–2.065 over the window). The banner's **point** — that a convention mismatch is the likeliest way this window is misread — is upheld intact |

### Backfilled index — `CH-0050` onward (`P-26`, iteration 28)

**The index above stops at `CH-0091` and carried 66 rows against 124 files.** Three separate claims noticed in
passing — `C-0113` measured 65 of 111, `C-0122` re-measured it, `C-0128` reported it as a process note — and
none could act, because each was in the middle of something else. **An index that is half the corpus is worse
than no index**, because a reader who finds `CH-0100` in it will read `CH-0151`'s absence as meaning it does
not exist.

These rows are **generated from each challenge's own header fields** by
[`tools/check-challenge-index.py`](../../tools/check-challenge-index.py), deliberately rather than written by
hand: the 66 rows above carry prose a generator cannot reproduce, and **inventing some would be worse than
transcribing what each file already states about itself**. A backfilled row says where to look and what the
file claims; it does not paraphrase the argument. The checker's `--backfill` regenerates them, and
`tools/verify.sh` now fails if any challenge is unindexed.

| ID | Against | Grounds / raised by | Status |
|---|---|---|---|
| [`CH-0050`](CH-0050-the-truss-cost-was-priced-on-the-wrong-axis.md) | [`C-0028`](../claims/C-0028-standoff-base-joint.md) (open question 3, and the *"the sway IS the draw-in"* identity as applied to a truss) and [`C-0029`](../claims/C-0029-perpendicular-junction-routing.md) (the closing paragraph of *"the design that results"*, and its way 2 of failing) | **logical** — a frame couple is a rank-one tensor on the leg offsets, so it acts on **one** axis and is identically zero on the orthogonal one — **plus in-silico**, the whole of `C-0030`'s pipeline re-run on an assembled truss head | **RAISED.** Both claims' *identities* are upheld to the last digit; what is challenged is a **consequence** each of them states in one sentence and neither computes |
| [`CH-0060`](CH-0060-the-stagger-is-not-free.md) | [`C-0041`](../claims/C-0041-flexure-array-packing.md)'s Deliverable 4 — *"staggering the tie column is free of every upstream claim"* and *"leaves the beams comfortably inside the 40 nm edge … and moves nothing else: rows are disjoint strips, so a stagger cannot make two beams overlap"* | methodological — **a freedom established in one model (plan-view hard-body packing) asserted as a freedom in every model**. The reasoning offered for *"moves nothing else"* is a plan-geometry argument about beam overlap; the quantity it is asserted over includes the sheet's load paths, which no plan-view model can see | raised. **No count, orientation, span, packing verdict, area, severance result or allowable of `C-0041` moves.** What moves is one clause |
| [`CH-0061`](CH-0061-the-cap-is-a-body-and-its-junctions-are-counted-too.md) | [`C-0037`](../claims/C-0037-triangulated-standoff.md) — the cap's geometry and its head tie; and [`C-0042`](../claims/C-0042-paired-perpendicular-junction.md) insofar as it inherits both | **methodological** — a stiffness quoted without the body that supplies it, and a counting theorem applied to one of a joint's two load paths | — |
| [`CH-0068`](CH-0068-the-hinge-inventory-is-not-the-sheets-own.md) | [`C-0054`](../claims/C-0054-consumed-crossover-sheet.md) — its **cheap bound** `f_max = 1 − (D−1)/N = 0.750`, the **42-crossover ceiling** that follows, the *"every one of `C-0046`'s designs severs the tile"* verdict, and the `39 ≤ n ≤ 42` window | **methodological, and named in advance by the claim it challenges** | **OPEN** |
| [`CH-0069`](CH-0069-the-plate-fails-before-the-connectivity-does.md) | [`C-0054`](../claims/C-0054-consumed-crossover-sheet.md) (`T-110`) — specifically the reading of its `39 ≤ n ≤ 42` window as a region in which its **continuum companion** numbers still describe the sheet, and its *"the step is at severance, not at consumption"* | **methodological**: a validity range that was never checked, on a claim whose own *"Still open"* item 5 named the check and did not run it | **OPEN, and CONDITIONAL on [`CH-0068`](CH-0068-the-hinge-inventory-is-not-the-sheets-own.md)** — this challenge is written against `C-0054`'s window, which assumes crossovers are spent; `C-0055` argues they need not be, and if that is upheld the window and this challenge fall together. See the closing section. |
| [`CH-0070`](CH-0070-the-reported-optima-are-in-the-torsion-infeasible-set.md) | [`C-0029`](../claims/C-0029-perpendicular-junction-routing.md)'s reported routing, as inherited by [`C-0042`](../claims/C-0042-paired-perpendicular-junction.md) and [`C-0052`](../claims/C-0052-crossbar-junction-trio.md) | **methodological** — the objective the three searches minimise does not measure the quantity that decides the answer, and it drives the optimum toward the edge of a window where a backbone cannot be built | — |
| [`CH-0071`](CH-0071-the-saturation-floor-is-a-property-of-the-equal-spring-family.md) | [`CH-0034`](CH-0034-flatness-count-saturates-under-the-solved-load.md)'s **Ground 2** — the decomposition that puts `C-0022`'s edge enhancement at *"**0.149 of the stroke, floor**"*, marks it *"bought with attachments? **no** — it is a rim collar 8.9 nm wide"*, and concludes in Ground 3 that *"the attachment count is not an axis on which it can be attacked"* — and against the remedy sentence *"the residual dishing is 0.149 of the stroke and is **a property of the tile's rim, not of the coupling**"* | methodological — a **saturation measured inside one family** (all springs equal) quoted as a property of the **object**. Every point of `CH-0034`'s 45 → 225 table shares the total stiffness equally, which no upstream claim requires; `C-0017`'s mandate is an equality on a **sum** | raised. **No count, no table, no reproduction and no remedy of `CH-0034` moves.** What moves is the word *floor*, and the design-space conclusion drawn from it |
| [`CH-0074`](CH-0074-the-flat-distribution-lives-on-stations-no-placement-supplies.md) | [`C-0058`](../claims/C-0058-non-uniform-coupling.md) (`T-113`) — its *"YES at three columns"* verdict, and the licence that reading carries for the Gen-1 device | **a premise, not a number** — every number in `C-0058` reproduces here, including its 0.0753 to `5.8e−4` | **RESOLVED** (iteration 11) by [`C-0063`](../claims/C-0063-upward-root-placement.md) (`T-125`) — **and resolved from the other side than either party expected.** Sweeping the row phases of the *same* 34 upward roots gives a placement that dishes **0.0706** of the stroke with **34 equal springs** at `C-0017`'s unchanged total: inside `T-5b`'s 0.10, **4.36× better than no coupling at all**, and better than `C-0058`'s own 0.0753. So the stations *are* supplied — they were simply not the ones `C-0055`'s greedy scheduler chose, and the pathology recorded below is a property of that scheduler (centroid `x = −8.80 nm`), not of the upward lattice: **every one of the 32 phases has a placement that beats the free tile.** Two corrections travel with the resolution: the *"same 34 roots, alternate rows reflected"* row of the table below is **not on the upward lattice at all** — 16 of its 16 reflected odd-row roots are `WEST` (downward) sites, which is [`CH-0076`](CH-0076-the-mirrored-placement-is-on-the-other-face-of-the-sheet.md) — so the best *buildable* entry of that table was 0.2902 and not 0.1649; and `C-0058`'s **rim rule reverses sign** on the swept placement (0.0706 uniform against 0.2214 at its own ×5), the 34-parameter optimum asking for a peak ratio of only **1.30**. `C-0058`'s numbers all stand; what falls is the reading that Gen-1 flatness *depends* on a station set no placement supplies. |
| [`CH-0075`](CH-0075-the-cap-floor-is-read-off-a-stage-that-failed.md) | [`C-0059`](../claims/C-0059-torsion-feasible-routing.md)'s design table — *"the mechanics at the alignment feasibility gives"* — and specifically its composition of a **9 bp** base floor with a **7 bp** cap floor of **6.0°** | **a floor taken from a stage that failed, and a shared design variable read at two values.** `C-0059`'s own validity range declares the first — *"the cap floor is taken from the trio's best reach-feasible alignment (6.0°), because no trio closed … an optimistic reading of a stage that failed"* — and this challenge supplies what it could not: the alignment a trio that **does** close actually delivers. The second is not declared anywhere | — |
| [`CH-0076`](CH-0076-the-mirrored-placement-is-on-the-other-face-of-the-sheet.md) | [`C-0061`](../claims/C-0061-stacked-arm-sheet.md) (`T-121`), Deliverable 3 item 3 and the `ROOTS-MIRRORED` rows of its distribution table — and, through it, the *"the same, mirrored"* row of [`CH-0074`](CH-0074-the-flat-distribution-lives-on-stations-no-placement-supplies.md) | **a geometry, not a number** — every number in `C-0061` reproduces here, including its 0.4156, its 0.3558 and its centroid at −8.80 nm | **OPEN** |
| [`CH-0077`](CH-0077-five-solved-states-are-four-devices.md) | [`C-0058`](../claims/C-0058-non-uniform-coupling.md)'s **Deliverable 4** — *"A distribution is tuned to a **load**, and the load is an operating state"*, and its conclusion *"a flatness count now needs a load case **and an operating state**"* as applied to those five — and against [`CH-0071`](CH-0071-the-saturation-floor-is-a-property-of-the-equal-spring-family.md)'s **overturning condition 2**, *"A requirement that one distribution be flat at every operating state. No distribution found here is."* | **a premise, not a number.** Every number in `C-0058`'s Deliverable 4 reproduces here — 0.2182, 0.0753, 0.1867, 0.0710, 0.2551 to `≤ 5.8e−4` — and its five-state minimax is *improved on* rather than contradicted. What is challenged is that those five states are the set the requirement is owed over | raised. **No count, no table, no verdict and no remedy of `C-0058` moves.** What moves is the word *"every"* |
| [`CH-0083`](CH-0083-the-pull-in-verdict-is-quoted-for-a-load-line-the-recommendation-does-not-use.md) | [`C-0018`](../claims/C-0018-maximum-usable-bias.md)'s and [`C-0032`](../claims/C-0032-softening-coupling-stability.md)'s **coverage** — not against any number either of them reports. Specifically against the reading, carried in `ANSWERS.md`'s §6 table and in `C-0071`'s own §6 row, that §6 task 4 is discharged **for the Gen-1 device** | **`C-0032` is itself the proof that a fold does not transfer between load lines.** It re-ran `C-0018`'s 162 fold searches with one substitution — the affine `R = 33.333 s` replaced by `C-0030`'s nonlinear law — and **lost 7 of 54 states**, moved the pull-in bias by 0.7–1.8 %, and walked the fold's own stroke from 3.41–4.13 nm back to **2.80–3.17 nm, across §3's 3 nm target at two of six models**. A third law is therefore a third answer, and `C-0071`'s recommended element is a third law: it is **strain-stiffening** at the placement stroke, where `C-0030`'s is softening, and its tangent minimum over the traversed range is **30.03 pN/nm** where `C-0030`'s is 22.88 | — |
| [`CH-0085`](CH-0085-a-window-edge-quoted-at-a-tie.md) | [`C-0016`](../claims/C-0016-design-window.md) and [`C-0011`](../claims/C-0011-scf-density-profile.md) — their **four-figure rendering** of the 10 nm upper window edge, not any number either claim derives | **methodological.** A quantity quoted at four significant figures whose fifth figure is exactly `5` is not determined at four significant figures; which way it renders is decided by digits the file has no business printing | **UPHELD, and no number of either claim fails.** Every verdict, every window width, every constraint attribution and every five- and six-figure quotation stands unchanged |
| [`CH-0086`](CH-0086-a-plan-ceiling-is-a-property-of-a-placement-not-of-a-count.md) | [`C-0072`](../claims/C-0072-plan-tolerance-model.md)'s Deliverable 6 — its `length ceiling` column and the sentence *"Dissolve them and every row carries at most two; **the ceiling is then set by the tile edge, at 9.12 nm**"* — and, through it, [`C-0069`](../claims/C-0069-output-element-placement.md)'s own ceiling column, which is 8.19 nm in every row of a table whose subject is how the design changes with the count | **the quantity is a maximum over placements and both claims evaluate it at one placement.** A placement's plan ceiling is `min` over its rows of that row's own ceiling, and the rows are **independent**, so `max over placements of min over rows` is exactly `sup{L : Σ_r min(m, maximumRootedElementsInRow(r, L)) ≥ n}` — a bisection on a monotone capacity, using `C-0069`'s own library, exact and free of any solve. `C-0072`'s ceiling column is that quantity evaluated at the output of `rowsWithoutInteriorRoots`, a deterministic reduction rule chosen to dissolve rows of three and not to maximise a ceiling | — |
| [`CH-0087`](CH-0087-a-sensitivity-row-that-does-not-meet-the-mandate.md) | [`C-0069`](../claims/C-0069-output-element-placement.md)'s Deliverable 5 — *"the sensitivities, and the one axis that closes the margin"* — and specifically its note on the 15-path row, *"fewer paths shorten the arm; **the placement is unchanged because the count is what sets the stations**"* | **`C-0017`'s mandate is a stiffness on a SUM**, so a path count `n` sizes the element *and* counts the instances. Deliverable 5 changes the first and holds the second at 34, and it never states the total the resulting array delivers. Worse, the shortfall is not confined to the path-count rows: the delivered total is `N_placed · 33.3333/n`, so **any** row whose placed count differs from its path count misses the mandate by exactly that ratio — including the two rows the claim reports as plan failures | — |
| [`CH-0088`](CH-0088-the-weave-read-through-the-plan-margin-is-on-the-wrong-axis.md) | [`C-0072`](../claims/C-0072-plan-tolerance-model.md), Deliverable 7, *"The framing changes: 2.69 nm is a Bragg lattice constant, not a local distance"* — the table headed *"read through the plan model"* | **methodological** — a measured quantity carried into a slot that names it but does not mean it | **STANDS.** `C-0072`'s arithmetic is reproduced to `3.9e−4` (its own rounding) and its *conclusion from that arithmetic* is withdrawn |
| [`CH-0089`](CH-0089-the-collinear-clearance-is-a-girth-not-a-lattice-constant.md) | [`C-0066`](../claims/C-0066-arm-slab-tie-clearance.md) bound 4 (*"the only gap the lattice offers clears a duplex by 0.0256 nm"*) and [`C-0069`](../claims/C-0069-output-element-placement.md) `Q5` (*"the plan budget on every 34-root placement is `pitch − d` = 8.19 nm exactly"*), and through them [`C-0072`](../claims/C-0072-plan-tolerance-model.md)'s *"neither margin is quotable"* | **methodological** — one symbol used for three quantities, and the one with a measurement is not the one the number came from | **STANDS as a statement about what the 0.0256 nm is a margin AGAINST.** It does **not** assert that 0.898 nm is the true clearance: the honest outcome is that the exclusion width in this role is **unmeasured**, with a measured **floor** of 1.8173 nm and a counter-argument pointing the other way |
| [`CH-0092`](CH-0092-the-propagation-did-not-close.md) | [`C-0073`](../claims/C-0073-determined-precision-of-a-result-file.md) Part 4, *"a result file is an INPUT"* — specifically its closing statement that the propagation **closes** | **methodological** — the audit's instrument (a grep for a literal filename) cannot see the call it needed to find, and the claim's own declared falsifier is what it missed | **UPHELD, and the claim's verdict stands.** The two stale files are re-emitted here. **No verdict, no flag, no string other than one precision-carrying parameter, and no quoted figure of `C-0027`, `C-0051` or `T-63` moves.** `C-0073`'s conclusion is untouched; its *audit* is not. |
| [`CH-0093`](CH-0093-the-collinear-clearance-is-a-stacking-allowance-not-an-exclusion.md) | [`C-0053`](../claims/C-0053-hinge-arm-array-packing.md)'s footprint convention (*"a rooted element occupies `[root, root ± L]` and the next along the same row may start at `high + d`"*) and, through it, [`C-0069`](../claims/C-0069-output-element-placement.md)'s `Q5` budget `p − d = 8.19 nm` and [`C-0072`](../claims/C-0072-plan-tolerance-model.md)'s margin identity | **methodological** — a geometry substitution: the convention charges a *transverse* girth in an *axial* slot, and the two geometries' pair energies differ by 15× | **STANDS.** It is [`CH-0089`](CH-0089-the-collinear-clearance-is-a-girth-not-a-lattice-constant.md)'s own failure route 2 — *"a demonstration that `C-0053`'s footprint convention is charging something other than a body"* — answered, and answered in the direction that **widens** the margin. It does **not** assert that 1.3 nm is the right allowance; it asserts that the quantity is a **stacking range** and that the range is measured |
| [`CH-0094`](CH-0094-a-hard-body-width-is-a-threshold-and-no-plan-claim-states-one.md) | [`C-0041`](../claims/C-0041-flexure-array-packing.md), [`C-0053`](../claims/C-0053-hinge-arm-array-packing.md), [`C-0065`](../claims/C-0065-crossbar-array-placement.md), [`C-0066`](../claims/C-0066-arm-slab-tie-clearance.md), [`C-0069`](../claims/C-0069-output-element-placement.md) and [`C-0076`](../claims/C-0076-weave-exclusion-width.md) — every claim in the branch that writes *"a duplex in plan is a rectangle of width `d`"* | **methodological** — a quantity quoted without the state it is read at, which is this programme's own recurring failure mode; here the missing state is an **energy budget** rather than a load, a compression, a bandwidth, a loading rate or a lattice coordinate | **STANDS as a statement about what a plan width IS.** It does **not** overturn any placed count: `C-0079` finds **34 of 34** at every physically calibrated reading. What it removes is the licence to quote a plan margin without naming the budget it assumes |
| [`CH-0097`](CH-0097-the-rounding-site-table-names-the-wrong-studies.md) | [`C-0073`](../claims/C-0073-determined-precision-of-a-result-file.md)'s `roundingSites` table (`gpd/results/P-18-determined-precision.json`, field `roundingSites`), and the `P-19` ranking that quotes it | **methodological, and the same one as `CH-0092`** — the table's study sets were assembled by inspection, and inspection is what `CH-0092` established cannot produce a census of this repository by hand. The two challenges are the same defect on two different tables of one claim. | **RAISED. `C-0073`'s verdict is not affected** — direction (a) is right, the ninth digit is not worth buying, and every measured `determinedDigits` stands, because a determined precision is a property of the *solver*, not of the study list. What is corrected is **which studies each site governs**, and therefore what re-emitting one would cost. |
| [`CH-0098`](CH-0098-the-0-5-mM-requirement-is-quoted-for-a-withdrawn-coupling.md) | [`C-0032`](../claims/C-0032-softening-coupling-stability.md)'s **escalation** — *"as of iteration 5 it stopped being an improvement and became a requirement for the surviving coupling"* — as carried into [`DECISIONS-FOR-NDI.md`](../../DECISIONS-FOR-NDI.md) decision 1, into `TASKS.md`'s `T-63` row, and into `C-0071`'s conditional ledger. **Not against any number `C-0032` reports**, every one of which stands | `C-0032`'s escalation was measured on `C-0030`'s strain-**softening** coupled-standoff flexure, which [`CH-0081`](CH-0081-a-rigid-root-demands-a-longer-arm-than-the-plan-admits.md) and [`C-0069`](../claims/C-0069-output-element-placement.md) removed from the output role and which [`C-0071`](../claims/C-0071-output-element-recommendation.md) does not recommend. `C-0084` re-runs `C-0018`'s 54-state fold search with the **recommended** element's own law and finds **0 folds of 6** at the 10 nm layer in 2 mM, a bias margin of **1.3877–2.5764** against `C-0032`'s 1.0000–1.0019, and the binding ceiling changing owner from pull-in to `C-0002`'s `φ = 0.2` and the element model's own branch end | — |
| [`CH-0099`](CH-0099-a-ceiling-taxonomy-belongs-to-a-load-line-too.md) | [`C-0018`](../claims/C-0018-maximum-usable-bias.md)'s **candidate ceiling list** — `{static stability (pull-in), C-0002's φ = 0.2, CH-0007's point-ion 1.0 V}` — as re-used verbatim by [`C-0032`](../claims/C-0032-softening-coupling-stability.md) and, until it was extended, by `C-0084` itself. **Not against any number `C-0018` reports**: on `C-0018`'s own load lines the list is complete, and every one of its 162 rows stands | `C-0018` established the discipline that *"a ceiling belongs to a `(bias, load line)` pair, never to the bias alone"* (`CH-0015`) and then applied it to the **values** of the three candidates while holding the **membership** of the list fixed. The membership is load-line-dependent too. Every load line `C-0018` and `C-0032` ran — an affine mandate, a preloaded dead load, a midspan flexure — is defined at every stroke the layer admits. `C-0071`'s recommended element is an **inextensible rotating arm** whose law stops existing at 7.9197 nm of stroke, and `C-0039`'s shooting solve enumerates only its small-rotation branch. At 12 of `C-0084`'s 108 states the equilibrium path therefore ends on the **element model**, and the inherited list has no candidate for that | — |
| [`CH-0100`](CH-0100-the-collinear-margin-is-an-integer-not-a-residue.md) | [`C-0069`](../claims/C-0069-output-element-placement.md)'s `Q5` margin, [`C-0072`](../claims/C-0072-plan-tolerance-model.md)'s margin identity and its floor 1, and [`C-0071`](../claims/C-0071-output-element-recommendation.md)'s three `NONE` bands | **methodological** — a quantisation argument. The three claims compute a difference of three lengths, two of which are not lengths a design can draw | **STANDS.** It does not contradict any computation in the three claims: every number reproduces to `1.3e−7` or better. It contradicts the *reading* of the result — and in the direction that makes the margin **quotable**, which is the opposite of what `C-0072` concluded |
| [`CH-0101`](CH-0101-the-nominal-tile-width-is-not-a-buildable-raster-width.md) | `Gen1Tile.EDGE_X = 40.0` and, through it, every plan, flatness and load claim written on a 40.0 nm width — [`C-0006`](../claims/C-0006-tile-load-distribution-and-flatness.md), [`C-0015`](../claims/C-0015-crossover-phase-and-registration.md), [`C-0022`](../claims/C-0022-tile-edge-load-profile.md), [`C-0053`](../claims/C-0053-hinge-arm-array-packing.md), [`C-0063`](../claims/C-0063-upward-root-placement.md), [`C-0069`](../claims/C-0069-output-element-placement.md) | **methodological, and it is a specification gap as much as a modelling one** — the width is inherited from §3 as a nominal dimension and has never been checked against the design language that has to draw it | **STANDS as a statement about a SEAMLESS raster**, which is what `C-0086` recommends. It does **not** assert that 40.0 nm is unbuildable in general: the seamed case is not derived, and Rothemund himself offers an escape (*"one or two scaffold bases could be left unpaired"*) whose cost he introduced it to avoid |
| [`CH-0102`](CH-0102-the-forty-eight-per-cent-is-one-corner-of-one-hundred-and-sixty-eight.md) | [`C-0072`](../claims/C-0072-plan-tolerance-model.md)'s `T-45` deliverable and the [`CH-0084`](CH-0084-the-measured-staple-incorporation-is-past-the-flatness-threshold.md) it raises — specifically the composition table's row *"**Strauss, EDGE sites** \\| **0.48** \\| **0.52** \\| **104.1 %** \\| **6.12×** \\| **3.01×**"*, and the `TASKS.md` and `CLAUDE.md` phrasing *"48 % at the edges to 95 % in the centre"* read as an edge value | **the per-staple map was never read; only the abstract was.** Strauss et al.'s Supplementary Fig. 14 prints all 168 values. Their minimum, 47.9 % incorporation, is **one cell** — `P1`, the bottom-right **corner**. The perimeter's 52 cells average **77.5 %**, the interior's 116 average **86.2 %**, six perimeter cells are *above* the interior mean, and the four corners run 60.2, 69.9, 47.9 and **87.1 %**. An "edge value" of 48 % is 29.6 percentage points below what the edges measure | — |
| [`CH-0103`](CH-0103-the-path-count-recommendation-runs-against-fabrication.md) | [`C-0072`](../claims/C-0072-plan-tolerance-model.md)'s recommendation to reduce the array to 30 arms, [`C-0074`](../claims/C-0074-two-per-row-placement.md)'s recommended 30-root phase-8 placement and [`C-0075`](../claims/C-0075-path-count-consistency.md)'s self-consistent count table — specifically the reading that a **lower** path count is a strictly better design because it buys plan margin at no cost | **the count is a robustness variable and all three claims treat it as a geometry variable.** `C-0089` sweeps the path count under the measured dropout and finds the 90th-percentile dishing **monotone decreasing** in it: 0.85219673 at 15 paths, 0.668998495 at 30, 0.614243977 at 45, 0.61234699 at 60, 0.572257776 at 75 and 0.532748246 at 90, with equal springs at `C-0017`'s unchanged total. The per-path force falls as `1/n` (6.667 → 1.111 pN against the 10 pN unzip allowable), so **nothing else in the acceptance stack opposes a higher count**. `C-0072`/`C-0074`/`C-0075` move the other way | — |
| [`CH-0104`](CH-0104-a-reachable-floor-is-an-oracle-and-cannot-license-a-design.md) | [`C-0074`](../claims/C-0074-two-per-row-placement.md)'s *"the negative belongs to the equal springs, not to the station set: the least-squares floor over every phase-24 upward root is **0.00071**, 140× below the convention"*, and by inheritance the same construction in [`C-0058`](../claims/C-0058-non-uniform-coupling.md) (`InfluenceSurrogate.reachableDishingFloor`, *"the honest statement of how much room the search may have left"*) and in [`C-0087`](../claims/C-0087-position-dependent-staple-dropout.md)'s falsifier `F4` | **the floor optimises over force vectors, and a design chooses a STIFFNESS vector once.** `reachableDishingFloor` is an ordinary linear least squares over `ℝⁿ` at a station set: it asks *"what is the flattest field any set of support forces at these stations could leave?"*. A stiffness distribution does not choose forces — the forces are the solution of a coupled system, and under fabrication the **support set itself** is random. `C-0089` measures the gap between the two on the same station sets and the same 10 000 realisations: the p90 oracle floor is **0.00111473343** at 90 paths against a best achievable fixed distribution of **0.284537599**, and **0.00917** on `C-0063`'s own 34 roots against a best of **0.449584219** | — |
| [`CH-0105`](CH-0105-the-plan-budget-is-two-bounds-and-only-one-carries-the-tile-width.md) | [`C-0069`](../claims/C-0069-output-element-placement.md)'s *"the plan budget on **every** 34-root placement is exactly `pitch − d` = 8.19 nm"*, and through it [`C-0085`](../claims/C-0085-collinear-stacking-clearance.md)'s 8.19 → 8.84 nm widening and its `c ≤ 2.34166` → `2.94462` razor | **methodological** — a quantity quoted without the state it was read at, where the state is the **tile width**. `rootedLengthCeiling`, the bisection `C-0069` uses to support the claim, takes `edgeX` as an argument; the identification of its output with `pitch − d` is a coincidence of the width it was called at | **STANDS as a statement about the QUANTIFIER, not about the number.** Every figure in `C-0069` is correct at 40.0 nm, and on `C-0069`'s own rows the budget does not move even at 38.08 nm. What is challenged is *"on **every** 34-root placement"*, which is false at any width below 38.14 nm |
| [`CH-0106`](CH-0106-six-routes-are-three.md) | the sentence *"**Six independent routes recommend it**: `C-0012` on the force clause, `C-0016` on the bias window, `C-0017` on the stability floor, `C-0018` on the usable bias, `C-0027` on the corrected margin, and `C-0032` on the realised coupling law"*, as it stands in [`DECISIONS-FOR-NDI.md`](../../DECISIONS-FOR-NDI.md) decision 1, in [`ANSWERS.md`](../../ANSWERS.md) question 1 and in `TASKS.md`'s `T-63` row. **Not against any number any of the six claims reports**, every one of which stands | **arithmetic, and mechanically checkable.** Independence was asserted from the *names* of the clauses, not from the numbers behind them. `T-2`'s `biasClauses[].biasForHundredPiconewtonBlocking` is `T-3`'s own number at **15 of 15** `(height, buffer)` states, worst departure **0.0**, and `T-2`'s binding clause at 10 nm is literally *"force"* at every buffer. `T-25`'s `bufferComparison` carries `T-16`'s `stabilityMargin` extrema and `T-4`'s coupled `margin` extrema at **20 of 20** comparisons, worst departure **2.66e−8** — which is `T-25` printing eight significant digits where `T-16` prints nine. `C-0032`'s route was already withdrawn by [`CH-0098`](CH-0098-the-0-5-mM-requirement-is-quoted-for-a-withdrawn-coupling.md). **Six named routes are 1 withdrawn + 2 transfers + 3** | — |
| [`CH-0107`](CH-0107-the-branch-end-is-a-force-ladder-artefact.md) | [`C-0084`](../claims/C-0084-recommended-element-pull-in-fold.md) **Deliverable 2** — *"refusal 7.9197 nm, branch validity 7.9205 nm … **the branch ends by FOLDING, not by turning past a right angle**"* — and the boundary [`CH-0099`](CH-0099-a-ceiling-taxonomy-belongs-to-a-load-line-too.md) prices its **2.57–3.74×** inflation from. **Not against `C-0084`'s verdict**, which this claim strengthens, nor against any of its 108 fold rows | `C-0084` reads its ceiling off `C-0039`'s `TwoSpringElastica.forceForDisplacement`, which **doubles** a tip force from `0.5 δ k_small` until the stroke reaches its target, and off `stateAtForce`, which brackets the shooting parameter by **doubling from a seed** and runs Illinois inside whatever that produces. Both are exact while the far-end moment residual has **one** root. On this arm a second root appears at about **100 pN** of tip force — far below the right angle `C-0039`'s own KDoc warns about — and once it has, a doubling step can bracket the wrong one: the stroke reported at that force collapses, the force ladder never reaches its target, and the routine throws. **A doubling ladder does not report a branch end; it reports having lost the branch.** Continued instead by marching the near-end rotation and taking the **first** sign change in the force above the previous one, the same arm answers to **8.1610821 nm** with `max_s\\|φ\\| = 1.5707924 rad`, **0.999997 of `π/2` and still below it** | — |
| [`CH-0108`](CH-0108-the-flexure-bound-is-a-bound-on-a-path-count-not-on-a-family.md) | [`C-0069`](../claims/C-0069-output-element-placement.md) cheap bound 3, and [`CH-0081`](CH-0081-a-rigid-root-demands-a-longer-arm-than-the-plan-admits.md) statement 1 | **methodological** — a quantity quoted without the state it is read at, where the state is a **path count** | **OPEN** |
| [`CH-0109`](CH-0109-a-cancellation-and-a-robust-design-are-different-designs-only-within-one-topology.md) | [`C-0089`](../claims/C-0089-dropout-robust-placement.md) Deliverable 2 and its "unanticipated result 2", and the **amplification** column of its Bound 1 | **methodological** — a trade measured inside one family reported as a property of the object, and a ratio used where an absolute is meant | **OPEN** |
| [`CH-0111`](CH-0111-the-row-end-crossover-is-admitted-with-an-interior-crossover-s-stiffness.md) | [`C-0090`](../claims/C-0090-buildable-raster-width.md)'s headline **0.0621469105** and its framing of the end-of-row question as a **convention**; and [`C-0009`](../claims/C-0009-discrete-lattice-tile.md)'s uniform crossover element, which has one `k_θ` and one vertical link for every crossover on the sheet | **methodological** — a quantity quoted without the state it was read at, where the state is *which kind of object the element is*. `C-0009`'s `k_θ` was derived for a crossover with duplex on **both** sides of it; 14 of the 56 at this width have duplex on one side only, and the primary source says their strain is unresolved | **DISCHARGED, iteration 20, by [`C-0099`](../claims/C-0099-row-end-crossover-stiffness.md) (`T-164`) — the task this challenge itself queued, in the order this challenge itself ranked.** All four *what would settle it* items are answered: the sweep is run and is monotone (16 of 16), the vertical link taken separately **is** the whole of the reachable set, Rothemund's remedy is priced at `0.0030284749` of the stroke, and the oxDNA edge crossover is **recommended against**. Ground 2's bracket is **corrected** by [`CH-0115`](CH-0115-the-row-end-bracket-is-a-constraint-not-a-stiffness.md): only **2.85 %** of it is a stiffness. Filed as raised: **STANDS as a statement about the UNCERTAINTY, not about the verdict.** `C-0095`'s answer to *"may a crossover be drawn there"* is unaffected — that is a design rule and a folded structure. What is challenged is the **precision** of the number the admitted reading produces, and the claim that the two readings are alternatives rather than bounds |
| [`CH-0112`](CH-0112-a-lost-branch-is-recorded-as-a-fold-and-as-a-ceiling.md) | **(a)** [`C-0039`](../claims/C-0039-two-spring-elastica.md)'s placement table, at the two rows it records as *"FAILS the desired stroke: the arm folds before reaching it"*; **(b)** [`C-0084`](../claims/C-0084-recommended-element-pull-in-fold.md) Deliverable 2's census — *"at **12 of 108** states the branch ended there … and at **8** of those the branch-end bias is the binding ceiling"*; **(c)** [`C-0050`](../claims/C-0050-desired-stroke-reach.md)'s reach catalogue, at the two elastica rows whose refusal note says the arm folds; **and separately (d)** [`C-0046`](../claims/C-0046-fewer-longer-flexures.md)'s binding-constraint lists at two of its 60 trade rows. **Not** against any of those claims' verdicts, all of which survive and one of which is strengthened | `C-0092`/`CH-0107` established that `C-0039`'s **doubling** force ladder loses the small-rotation branch rather than reaching its end, and re-read the **12** states of the recommended device. `T-159` repairs the ladder in place — `forceForDisplacement` continues the branch, anchoring each force step's shooting root on the previous accepted one, and **refuses** rather than returning a root off the branch — and then re-runs **every** study that consumes the changed source. Three published records were written on the lost branch and none of them was in `C-0092`'s twelve | — |
| [`CH-0113`](CH-0113-the-fifty-three-site-ceiling-is-one-phase-s-inventory-not-the-lattice-s.md) | [`C-0093`](../claims/C-0093-shared-body-coupling.md)'s Deliverable 4 row *"ties the density fit demands, against the upward sites the lattice has — 252 demanded, 53 available"* and its **4.76× shortfall**, and by inheritance the same 53 wherever it is quoted as a lattice ceiling (`C-0066`, `ANSWERS.md` §1) | **a quantity quoted without the state it is read at** — the eighth instance in this project, and here the state is a **lattice phase**. The upward (`EAST`) inventory is a function of the crossover-column phase, and 53 is its value at one of the 32 | **OPEN** |
| [`CH-0114`](CH-0114-the-shared-body-s-tie-ceiling-is-stated-at-one-value-and-used-at-another.md) | [`C-0093`](../claims/C-0093-shared-body-coupling.md)'s **Bound 2** (*"the mandate arithmetic, which is the escape in one division"*) and the `sharedBodyPerStation` column of `mandatePlacementArithmetic` in `src/main/kotlin/coupling/SharedBodyCoupling.kt` | **an inherited bound applied outside the topology it was derived in**, and a claim whose stated cap and reported design disagree by 300× without the disagreement being named | **OPEN** |
| [`CH-0115`](CH-0115-the-row-end-bracket-is-a-constraint-not-a-stiffness.md) | [`CH-0111`](CH-0111-the-row-end-crossover-is-admitted-with-an-interior-crossover-s-stiffness.md)'s **Ground 2** — *"the two 'conventions' are a bracket, and it is a wide one"*, and its arithmetic *"`T-5b`'s 0.10 lies at 0.356 of the way from the admitted reading to the refused one … so a row-end crossover retaining less than roughly a third of an interior one's contribution would take the design out of the convention"*; and, through it, [`C-0090`](../claims/C-0090-buildable-raster-width.md)'s validity-range sentence that *"the end-of-row convention is a MODELLING CHOICE and both readings are carried"* | **methodological** — a one-parameter bracket read off a two-model comparison that moves more than one parameter. `CLAUDE.md` already records the general form of the error (*"a crossover is TWO elements and only one of them is `D_⊥`"*, and *"scaling `D_⊥` is not a model of removing crossovers"*); this is the same trap in reverse, and `CH-0111`'s own *"what would settle it"* item 2 names it without drawing the consequence | **STANDS, and it DISCHARGES the challenge it is raised against.** `CH-0111`'s question — *"is the row-end crossover as stiff as an interior one?"* — is answered in the only way that matters to the acceptance: **it does not matter**. Nothing in `C-0090`'s or `C-0095`'s verdicts moves; what moves is the *width of the exposure*, from 2.70925468× to **1.0487309×** (`0.0651753854/0.0621469105`) |
| [`CH-0116`](CH-0116-the-collar-split-is-a-mesh-node.md) | [`C-0022`](../claims/C-0022-tile-edge-load-profile.md)'s emission of the edge effect as a `(depth, width)` pair **plus** a rim residual, and specifically its treatment of the two as separately meaningful load terms — its *"solved edge effect, smooth term only"* dishing row and its `edgeForceFractionMinMargin`, which pairs the **global** zeroth moment with the **smooth-only** first moment | **methodological, and numerical** — a published quantity whose value is set by a discretisation choice that the claim does not name, does not converge, and does not report the convergence of. `C-0022`'s convergence table emits `centrelineLoad`, `taperDepth`, `taperWidth`, `chargeBalance` and `centrelineRouteSpread`. It does not emit `rimResidualPerUnitEdge` or `rimResidualDepth`, which are the two fields the split is carried in | **STANDS as a statement about the SPLIT, not about the collar.** Every dishing in this programme applies **both** terms through `coupling.edgeCollarPressure` and therefore integrates their sum, which is safe; no verdict anywhere moves. What is challenged is that either term alone is a number |
| [`CH-0118`](CH-0118-the-row-end-crossover-is-a-station-at-two-phases.md) | [`C-0090`](../claims/C-0090-buildable-raster-width.md)'s Deliverable 3, the sentences *"an end plane has an **even** index, and the upward azimuth needs `k ≡ 2b + 3 (mod 4)`, which is odd. **The row-end crossover can never be an upward site**, at any phase, so admitting it adds in-plane inventory and no stations"* | **a statement proved at the state it was read at and quoted as a theorem** — the ninth instance in this project, and here the state is again a *lattice coordinate*. The parity of the end plane's index is a function of the phase, and `C-0090` examined the two phases at which it is even | **OPEN** |
| [`CH-0119`](CH-0119-a-redundancy-slope-measured-over-searched-subsets-is-not-a-count-slope.md) | [`C-0098`](../claims/C-0098-shared-body-placement-and-distribution.md)'s **Deliverable 3** — the real-lattice redundancy fit, its slope **−0.376769756**, its *"2.08× shallower"* reading and the mechanism attributed to it | **a slope fitted through six placement-searched optima confounds the count with the tightness of the search at each count, and `C-0098` records the premise for this itself.** Its own validity range says *"the subset search is a first-improvement descent on an objective that ranks at ρ = 0.47, so the fixed-count rows are **upper bounds** on what the real lattice reaches at those counts, and the 34 → 45 non-monotonicity is a property of the search rather than of the lattice."* An upper bound whose tightness varies with the count is exactly what a slope cannot be read through. `C-0103` measures the size of the effect on an array at phase 24: **nested chains −0.740086889 and −0.704431429; the placement-searched family +0.0610348337**, i.e. **12.1×** apart in magnitude and opposite in sign | — |
| [`CH-0120`](CH-0120-an-influence-bank-assumes-a-lattice-with-no-self-load.md) | [`C-0058`](../claims/C-0058-non-uniform-coupling.md) (`InfluenceSurrogate`, `latticeInfluenceSurrogate`) and [`C-0063`](../claims/C-0063-upward-root-placement.md) (`UpwardRootInfluenceBank`) | [`C-0104`](../claims/C-0104-row-end-prestrain.md) (`T-172`) | **OPEN**, with a remedy already implemented and tested on the caller's side (`OrigamiGrillage.withoutPrestrain`), and **not** on the bank's |
| [`CH-0121`](CH-0121-two-of-the-three-phase-demand-sets-are-identical-not-disjoint.md) | [`C-0102`](../claims/C-0102-crossover-phase-selection.md)'s **title line** — *"at the buildable 38.08 nm the three sets go from **10 / 10 / 2** to **2 / 2 / 2** and stay **disjoint** — richest `{0, 16}`, eight-column and centro-symmetric `{8, 24}`"* | **a set-theoretic word used of three sets when it is true only of one against the other two.** At 38.08 nm `C-0102`'s own Deliverable 1 table gives *most columns* = `{8, 24}` and *centro-symmetric* = `{8, 24}`. Two sets that are equal are not disjoint; they have both their elements in common. The pair was never disjoint at 40.00 nm either — `C-0090` states that `C-0015`'s ten eight-column phases **collapse onto** `C-0063`'s two, i.e. `{8, 24}` was a **subset** of the ten — so the collapse takes a nested pair to an equal pair, and the only disjointness anywhere in the ledger is the richest set against the rest | **STANDS as a statement about the headline, not about the claim.** No re-run, no re-derivation, nothing withdrawn |
| [`CH-0122`](CH-0122-the-prestrain-threshold-is-a-secant-not-a-triangle-inequality.md) | [`C-0104`](../claims/C-0104-row-end-prestrain.md), Deliverable 1 and its `cheapBounds` record *"the prestrain at which C-0090's placement reaches T-5b's 0.10"* | [`C-0107`](../claims/C-0107-row-end-prestrain-value.md) (`T-182`) | **OPEN, and it does not overturn a verdict.** `C-0104`'s number remains a valid upper bound on the crossing, but by **convexity** and not by the inequality it names; the inequality it names gives **11.5188°**, which is 1.34× tighter and moves the threshold *away* from every candidate value |
| [`CH-0123`](CH-0123-a-count-term-and-a-phase-term-are-one-of-two-readings-of-one-journey.md) | [`C-0103`](../claims/C-0103-path-count-at-fixed-geometry.md)'s **Deliverable 2** and the third row of its verdict — *"the count term is +12.86 % and the phase term is −19.0 %, and the phase is the larger of the two"* — and the same sentence where it appears in its *"What this does to the standing claims"* row for `C-0074` | **a two-factor move has two splits and only one total, and `C-0103` reports one split as if it were the decomposition.** `C-0108` grades all four corners of the 2 × 2 under one ensemble per phase: count first gives **+12.8596328 %** then **−19.267547 %** (reproducing `C-0103` exactly), phase first gives **−11.4787354 %** then **+2.9293362 %**. The two orderings share a total of **−8.88564999 %** at a path disagreement of **0.0**, and differ by an interaction of **−8.79880284 %**. On the **search-free** family run at both phases the same construction gives **+10.8397678 %** / **−3.92538749 %** against **+1.92730307 %** / **+4.47532136 %** — the phase term is **adverse** in one reading and **favourable** in the other. And over the whole 32-phase × 6-count grid the **interaction carries more of the variation (9.79218189 %) than the phase main effect does (7.83610301 %)**, with a worst additive residual of **1.53234725×** the count main effect it splits | — |
| [`CH-0124`](CH-0124-the-four-layer-variant-is-a-mixed-state-not-a-bound.md) | [`C-0006`](../claims/C-0006-tile-load-distribution-and-flatness.md), the `four-layer honeycomb, rigid coupling` row of its variant table, and `structure/OrigamiSheet.kt`'s `InterlayerCoupling.RIGID` | methodological — a quantity presented as a bracket end is a mixture of the two ends, one taken in each direction | **OPEN** |
| [`CH-0125`](CH-0125-the-four-layer-brick-is-mis-specified-in-three-ways.md) | [`C-0093`](../claims/C-0093-shared-body-coupling.md), the `four-layer honeycomb brick` body of its tie ladder, and the statement *"a buildable body loses where the rigid limit wins"* | the body's three plate rigidities are each taken at a reading `T-191` shows to be wrong, and the three corrections do not share a sign | **OPEN** — the re-solve is owed and is **not** performed here |
| [`CH-0126`](CH-0126-a-tall-layer-breaks-section-3s-own-effort-point.md) | [`C-0050`](../claims/C-0050-desired-stroke-reach.md)'s escape section — the sentence *"§3's own tile-thickness row notes the effort point 'may sit ~20–25 nm above the electrode', so the geometry is not absurd"* — and the same sentence as it has been carried into `TASKS.md`, `ANSWERS.md` §6 row 2 and `DECISIONS-FOR-NDI.md` decision 2 | `ActuatorGeometry.effortPointHeight(L₀) = L₀ + tileThickness + leverAttachmentHeight`, with §3's own 10 nm tile. At §3's three layer heights and a 5 nm attachment that is **20 / 22 / 25 nm** — the §3 band reproduced at both ends, which is exactly why `CLAUDE.md` records the row as *forcing a constant attachment height and fixing it at 5 nm*. At 17–26 nm the same expression gives **32–41 nm**, and 27–36 nm at a zero-length attachment. The row `C-0050` cites as permission is the row that refuses | — |
| [`CH-0127`](CH-0127-the-tall-layer-escape-is-kinematic-not-actuated.md) | [`C-0050`](../claims/C-0050-desired-stroke-reach.md)'s *"What is established"* table, row 3: *"…unreachable **in physics**" — **NO, and it is false.** A taller layer delivers it"*, and the escape table's framing as *"the escape is a specification change, and it is priced"* | the row is established by a **root of the layer's own load**, `P(L₀ − 10 nm)·A = 100 pN` at `σ = 0.024 nm⁻²` — a statement about a **compression**, not about an **actuator**. `C-0110` reproduces all six of its heights to ≤ 2.0e−4 and then asks the question the root does not contain: the deepest gap across which the field delivers 100 pN is **13.6989 nm at 0.5 mM, 11.8724 at 1 mM and 10.1299 at 2 mM** (1.0 V; 1.23 V buys 0.05 nm more), every one of them **below** the bottom of the escape's own 16.63 nm. On the 96 tall device-B states the coupled equilibrium is admitted at **1**, and on the 96 device-A states — §3's *acceptable* clause — at **0**, all 96 refused because no bias holds the tile at a 3 nm stroke | — |
| [`CH-0128`](CH-0128-inverse-debye-length-called-with-a-bjerrum-length.md) | [`C-0021`](../claims/C-0021-zero-bias-resting-position.md) `M4` and [`C-0023`](../claims/C-0023-two-sided-coupling.md), specifically the **low end** of their van der Waals bracket | **methodological.** `MagnesiumChlorideBuffer.inverseDebyeLength`'s first parameter is a **temperature**, and both call sites pass a **Bjerrum length**. The resulting `κ` is 20.5× the physical one and `e^(−2κd)` saturates to `2e−23`, so the zero-frequency Hamaker term is not screened, it is removed. | **OPEN** — the repair is **not** performed here, because it moves two committed result files and belongs with their re-emission |
| [`CH-0129`](CH-0129-the-graded-field-is-compared-against-the-other-overall-sign.md) | [`C-0107`](../claims/C-0107-row-end-prestrain-value.md), Deliverable 4 and its *"Two readings, and they disagree about the verdict"* passage | [`C-0112`](../claims/C-0112-interior-crossover-prestrain.md) (`T-190`) | **OPEN, and it strengthens the claim it is against.** `C-0107`'s **0.0922622** is reproduced here at **1.1e−10** and its flat verdict stands; what does not stand is the pairing that explains it, and correcting the pairing makes the interior term **2.68×** larger than published |
| [`CH-0130`](CH-0130-the-overall-sign-of-the-corrugation-is-undetermined.md) | [`C-0107`](../claims/C-0107-row-end-prestrain-value.md), Deliverable 3 (*"the sign … is a lattice congruence"*) and Deliverable 4's single graded row | [`C-0112`](../claims/C-0112-interior-crossover-prestrain.md) (`T-190`) | **OPEN, and it is DISCHARGED on the number**: `T-190` reads the missing state at **0.0910197** of the free stroke, flat, so the verdict survives at both signs. What does not survive is the claim that the sign was *determined* |
| [`CH-0131`](CH-0131-t-157-was-re-emitted-before-its-own-input.md) | [`C-0092`](../claims/C-0092-large-rotation-arm-branch.md)'s margin-movement deliverable — its **`A5`** verdict clause *"the margins move by 1.0000–3.3380×"* — the `biasMarginInC0084` and `marginMovement` columns of its fold table, its findings line *"the bias margin … moves by a factor of 1.0000–3.3380×"*, and the derived readings *"1.0007–1.1620× … at the other four the point-ion boundary binds and the movement is 2.84–3.34×"*. And, as the cause, [`C-0101`](../claims/C-0101-re-emitting-what-the-repair-moved.md)'s re-emission of eleven files | **a consumer was re-emitted before its own producer, inside one commit.** `C-0101` (`399333a`) re-emitted `T-149` (284 fields moved) **and** `T-157` (14 fields moved). `LargeRotationArmBranchStudy.c0084Margins()` reads `gpd/results/T-149-recommended-element-fold.json` at run time, so the order decides the answer — and the committed `T-157` reproduces the **pre-`C-0101`** `T-149` margins **digit for digit at all twelve rows**, while a re-run today reproduces the current ones, also digit for digit. The residual has stood unread for six iterations | — |
| [`CH-0147`](CH-0147-the-recommended-tile-is-folded-from-a-different-scaffold.md) | [`C-0119`](../claims/C-0119-honeycomb-raster-width.md) §5, the row *"p8064 — designs i, iii, v, **including ours**"*, and its finding that *"the remainder is **1 344 nt** rather than 529 on the standard scaffold for this design"* | **supersession** — the statement was true of the tile `C-0119` was written on and stopped being true two claims later, without anything in `C-0119` becoming wrong on its own terms. `CLAUDE.md`'s third drift class: *"a SUPERSEDED STANDING VALUE reads `CITED`"* | **OPEN** |
| [`CH-0148`](CH-0148-the-remainder-and-the-sheet-do-not-condense-alike.md) | [`C-0086`](../claims/C-0086-seamless-scaffold-routing.md) Deliverable 3, the row *"the unpaired remainder at 112 bp … a **33.3 nm** ideal coil, carrying **1.66×** the sheet's own backbone charge"*, and the same phrase in its headline and in `gpd/results/T-151-scaffold-routing.json` | **methodological** — a ratio of two charges taken before the renormalisation that this repository applies to **both** of them, and applies at a factor that differs between them by 3.35–4.12× | **OPEN** |
| [`CH-0149`](CH-0149-the-format-defect-census-is-off-by-a-file-a-study-and-a-result.md) | [`C-0125`](../claims/C-0125-scaffold-remainder.md), its *"Spawned"* paragraph and the `T-195` row of `TASKS.md`: *"its sweep of `src/` finds **14 defects in 6 files from 5 studies, all committed**, with raw `%` conversions in **8 result files**"* | **arithmetic** — a census stated as a count, checkable in one pass, and wrong in all three of its counts. Plus **a tool defect**: one of the fourteen rows is the checker's, not the tree's | **OPEN** |
| [`CH-0150`](CH-0150-twelve-of-the-fourteen-were-visibly-broken-and-the-silent-failure-diagnosis-does-not-explain-them.md) | the diagnosis carried by [`C-0125`](../claims/C-0125-scaffold-remainder.md) and the `T-207` row of `TASKS.md`: *"**Java silently ignores extra arguments**, so the class does not throw — it emits a grammatical sentence with the wrong numbers in it"*, and `CLAUDE.md`'s own entry for this family | **generalisation from one instance** — a failure mode read off the single most alarming defect and attributed to the class. The cheap bound that separates the two costs one pass and is already a column of the checker's own output | **OPEN** |
| [`CH-0151`](CH-0151-an-oblique-helix-has-two-free-azimuths-not-one.md) | [`C-0122`](../claims/C-0122-honeycomb-station-lattice.md) — its census table (`15 × 4` → **90** stations, `10 × 6` → **60**), and the sentence that licenses it: *"**It fixes which helices carry the perpendicular root and which carry two oblique ones; it does not change the COUNT**, because every top-face helix has exactly one free direction either way"* (`HoneycombLattice.pointsDirectlyOut`'s KDoc, repeated in the claim's §5) | methodological — a **parity** correctly identified and then used to justify a **count** it does not support. The parity says *which* azimuths are free; the count needs *how many*, and the two sublattices differ in that number as well as in the direction | **raised.** The correction is **FAVOURABLE** and `C-0122`'s headline findings all survive it |
| [`CH-0152`](CH-0152-the-defect-was-read-tested-and-misdescribed-not-unread.md) | [`CH-0150`](CH-0150-twelve-of-the-fourteen-were-visibly-broken-and-the-silent-failure-diagnosis-does-not-explain-them.md)'s diagnosis, carried into [`C-0127`](../claims/C-0127-format-string-repair.md)'s headline: *"**This defect class damages the machine-readable artifact and spares the human one**"*, and *"the reason they survived is that **nobody re-reads a result file once the claim is written**"* | **a counter-instance found by `CH-0150`'s own repair.** `T-148`'s saturated statistic was computed correctly, emitted correctly, **unit-tested**, read, and **described in a sentence written around it** — and the sentence says the opposite of what the number means. Every clause of the *"unread"* answer is false of it, and no text gate can reach the class | **OPEN** |
| [`CH-0153`](CH-0153-a-statistical-power-gate-discharged-by-a-statistic-that-is-identically-zero.md) | [`C-0087`](../claims/C-0087-position-dependent-staple-dropout.md)'s gate-4 row — *"with the binomial standard error quoted beside every probability … and the binomial error to fall as `1/√n`"*, verdict `PASS` — and its `P3` predicate *"a distribution, not a point"* | **the convergence property is demonstrated at a state the study does not occupy.** The `1/√n` fall is tested at `p̂ = 0.5`; the sweep reports `p̂ = 1.0` at **25 of 60** cells and at **5 of 5** convergence sample counts, where `√(p̂(1−p̂)/n)` is identically zero and carries no `n` at all. **302 records in 7 files** carry the same degenerate statistic | **OPEN, and it moves no verdict** — a one-sided bound *strengthens* every failure reading it replaces |
| [`CH-0154`](CH-0154-the-rule-lives-once-was-true-of-one-package.md) | [`C-0129`](../claims/C-0129-result-file-hygiene.md)'s `T-209` deliverable — *"The rule is about a **record type**, so it now lives once, by name"* — and its census of what is left, *"199 fields in 27 files"* | **the mechanism and the census are both narrower than the rule, and three measurements say so.** A bare `departure` appears under **eleven** parents and `T-193`'s is in **volts**; `roundedForActuatorResult` has **no `digitsByKey` parameter at all**, so six files could not have obeyed the rule by any edit at their own emission sites; and the census keys on **one spelling of four**, 199 fields in 27 files against **601 in 63** | **UPHELD and repaired in the same task** |
| [`CH-0155`](CH-0155-the-degenerate-discharge-is-in-five-claims.md) | [`CH-0153`](CH-0153-a-statistical-power-gate-discharged-by-a-statistic-that-is-identically-zero.md)'s scope, and the four further claims carrying the identical gate-4 discharge: [`C-0089`](../claims/C-0089-dropout-robust-placement.md), [`C-0093`](../claims/C-0093-shared-body-coupling.md), [`C-0098`](../claims/C-0098-shared-body-placement-and-distribution.md), [`C-0103`](../claims/C-0103-path-count-at-fixed-geometry.md) | **the challenge is a population too, and the population is CLAIMS.** *"A binomial standard error beside every exceedance"* discharges the **statistical power** gate in five claims, and the statistic is exactly `0.0` at 25 of 60, 16 of 24, 16 of 39, 16 of 25 and 16 of 21 cells. `C-0129` measured the population of **records** and left the population of **discharges** unmeasured | **UPHELD, and repaired for all five** — no exceedance, percentile, boolean or verdict moves |
| [`CH-0156`](CH-0156-the-rim-charge-bracket-is-a-bigger-tile.md) | [`C-0022`](../claims/C-0022-tile-edge-load-profile.md) — its **falsifier 5** paragraph (*"Taking the rim from uncharged to the face areal density moves the fitted depth from −0.2906 to −0.1575, a factor of 1.85. **The two readings are both defensible** — the tile's charge is volumetric and the surface it is smeared onto is a convention"*), the validity-range line *"The rim charge is a 1.85× bracket on the depth"*, and the *"Still open"* item *"The rim charge is unsourced"* | **arithmetic, and it costs one division** — a charge-conservation ledger, checkable without a solver | **OPEN.** No `C-0022` verdict moves; what moves is the exposure fourteen downstream claims carry |
| [`CH-0157`](CH-0157-the-rim-sweep-varied-the-smaller-of-two-terms.md) | [`C-0022`](../claims/C-0022-tile-edge-load-profile.md) — the *method* of its falsifier 5, i.e. that varying `rimChargeDensity` alone measures the sensitivity of the collar to the rim convention, and the conclusion drawn from it that *"the rim charge … is load-bearing after all"* | **methodological** — a two-term perturbation swept on one term, where the unswept term is 1.41× the swept one and runs the other way; plus a **representation** failure in the quantity the sweep was read on | **OPEN.** It strengthens `C-0022`'s headline and withdraws its bracket |
| [`CH-0158`](CH-0158-the-admissible-width-list-assumes-an-uncorrected-twist.md) | [`C-0086`](../claims/C-0086-seamless-scaffold-routing.md) Deliverable 4 — *"At the 1.5-turn (16 bp) spacing this sheet is built on, three half-turns are 16 bp, so an odd number of half-turns is an **odd multiple of 16 bp**"* — and the 112 bp row it selects, carried by [`C-0090`](../claims/C-0090-buildable-raster-width.md), [`C-0104`](../claims/C-0104-row-end-prestrain.md), [`C-0107`](../claims/C-0107-row-end-prestrain-value.md) and [`CH-0101`](CH-0101-the-nominal-tile-width-is-not-a-buildable-raster-width.md) | **methodological** — a quantisation derived with one of its two inputs held at a value the same programme records as a defect. *"Three half-turns are 16 bp"* is true only at 33.75 °/bp, which is exactly what `C-0107`'s 17.15–24.98° row-end prestrain is a complaint about; at B-DNA's twist the admissible lengths are 16, 47, 79, **110**, 142 bp and **112 is not among them** | **OPEN.** Neither the theorem nor the arithmetic of `C-0086` moves; what moves is that *"the buildable widths"* is one set **per design twist**, and the programme has never chosen which twist the Gen-1 tile is built at |
| [`CH-0159`](CH-0159-a-zero-plan-clearance-cannot-survive-a-width-change.md) | [`C-0090`](../claims/C-0090-buildable-raster-width.md)'s arm ceiling — *"`C-0085`'s quantisation to **24 rises** makes the clearance exactly zero and restores every one of those numbers bit for bit"* — and [`C-0085`](../claims/C-0085-collinear-stacking-clearance.md)'s quantisation | **methodological** — a margin reported as *exactly zero* and read as sufficient, with no tolerance stated on the variable it is zero in. At the twist-corrected 110 bp row the 34-root centro-symmetric family is **empty** at 24 rises and needs **23** | **OPEN, and it costs one base pair rather than a verdict** |
| [`CH-0160`](CH-0160-a-stratified-robustness-argument-is-quantified-over-a-widths-own-strata.md) | [`C-0108`](../claims/C-0108-count-phase-interaction.md)'s per-stratum deliverable — *"THE INTERACTION IS NOT A HOST EFFECT, BECAUSE IT SURVIVES INSIDE EVERY STRATUM"* — and the `C-0102` row of its *"What this does to the standing claims"* table | **a quantifier over a census, and the census is a property of the tile WIDTH.** At `C-0086`'s buildable 38.08 nm three of `C-0102`'s four strata have no counterpart: the eight-column set is **{8, 24}** instead of ten, the *eight-column but not centro-symmetric* set is **empty**, and the richest-inventory set is **{0, 16}**. Re-stratified there the residual ratios are 0.29785241, 0.305403211 and **2.14777246** | **OPEN, and the conclusion is UPHELD on the new ground** — no `C-0108` number moves; what moves is which lattice *"every stratum"* ranges over |
| [`CH-0161`](CH-0161-the-guard-that-went-live-is-the-existence-not-the-value.md) | [`CLAUDE.md`](../../CLAUDE.md)'s two `CrossoverLayout.EDGE_MARGIN` entries, and specifically the instruction *"Sweep the guard (here 0.05 nm / half a rise / one rise: 0.32 % spread) rather than assuming it is still inert at the geometry you are running"* | **the guard's VALUE and the guard's EXISTENCE are different variables.** Swept over exactly those three values the guard leaves **one** lattice over all 32 phases of the 38.08 nm tile (worst station displacement **`0.0` nm**) and **two** at 40.00 nm, moving the column count at phases 6, 10, 22, 26. The recommended sweep is worth `0.0` refused and 0.4442352 % of a level admitted, against the **171 %** (0.0621469105 → 0.168371808) the entry is about | **OPEN, and it makes the entry stronger** — the general lesson survives and gains `C-0100`'s binary |
| [`CH-0162`](CH-0162-three-agreeing-runs-are-a-draw-not-a-verdict.md) | [`C-0131`](../claims/C-0131-departure-and-saturation-audits.md) §6: *"Three independent runs agree on `ranges[1]` to the last digit and the committed file disagrees with all three. So `gpd/results/T-129-range-robust-placement.json` at `HEAD` **is not reproducible from `HEAD`'s own code**"* | **three agreeing runs are a DRAW, not a verdict.** Over ten independent emissions `ranges[1]` is two-valued, 7:3, so three runs agree by chance **34 %** of the time; and `git archive cf7de13`, run unmodified today, lands on the reading iteration 13 did **not** emit, which excludes the tree | **PARTLY UPHELD** — the observation and its numbers reproduce exactly; the inference does not, and the file **is** reproducible up to a two-valued manifold |
| [`CH-0163`](CH-0163-a-worst-field-movement-is-an-emission-choice.md) | the repository-wide practice of reporting a re-run difference as *"N fields moved, worst `X`"* — `C-0131` §6, and the same shape in `C-0101`, `C-0110`, `C-0129` | **the size is set by the emitted field list.** One change of winning start reads `5.9522e−3` on `max_i k_i`, `7.5482e−4` on a peak support force at the **same** argmin and `2.6197e−3` on the objective; and the block carrying the corpus's widest VALUE movement (`subsets[*]`, `4.5745e−3`) was never quoted, because it emits no argmin functional at all | **OPEN — a methodological correction.** No named number is wrong; report a count **by kind** beside the scalar |
