# Challenges

A result that contradicts a standing claim is filed here as a challenge, with methodological grounds.
Claims in `../claims/` are never silently overwritten — §5 of the problem definition is explicit about this,
and it is the difference between a record and a rolling opinion.

A challenge names: the claim it contradicts, the result that contradicts it, the methodological grounds
(not merely a different number), and the resolution — which may be that the challenger is wrong.

| ID | Against | Grounds | Status |
|---|---|---|---|
| [`CH-0001`](CH-0001-semidilute-premise.md) | `C-0001`, its `Semidilute` validity bullet | The wrong crossover was checked, in reduced rather than physical volume-fraction units, using a coil-overlap criterion that is documented as insufficient **for this material** | **UPHELD in part** — `C-0001` stands, its numbers become bounds, `m = 3` is excluded |
| [`CH-0002`](CH-0002-corrections-do-not-all-soften.md) | `CH-0001`'s conclusion that every correction softens the layer | The height relation, which `CH-0001` itself named as the unrepairable part, is the larger effect and runs **stiffer**: 33–87 % higher at first contact, and the stroke bracket straddles | **UPHELD** — `C-0001`'s strokes are not lower bounds; that standing finding is withdrawn |
| [`CH-0003`](CH-0003-blob-stack-height.md) | `C-0001`, the Alexander-de Gennes height relation under it | The blob stack is `(Σ/π)^(5/6)` tall — 1.47 blobs at the `Σ = 5` convention — so the blob picture is being applied to a stack of one and a half, and strong stretching is outside its own premise at `L₀/R_F = 1.17–1.25` | **RESOLVED** by `C-0003`: the height relation is replaced, and the chains carry 0.06 thermal blobs — there is no blob structure to stack |
| [`CH-0004`](CH-0004-screening-decay-length.md) | the single-Debye-length reading of §1 and §3 | "The Debye length" is three different numbers — bulk buffer, counterion-dominated gap, and inside the polymer layer — and the error runs in the non-conservative direction for pull-in | **RESOLVED** by `C-0008`: the force's decay length is a *fourth* number, 1.8–2.8 nm at the working gap, and the only bias-dependent one |
| [`CH-0005`](CH-0005-rigid-tile-assumption.md) | `C-0001`, its `Rigid tile` validity bullet | The tile is rigid *exactly* under a uniform load at any rigidity, and dishes by 26–369 % of the stroke under every departure from uniformity, including 300 K | **UPHELD** — `C-0001`'s numbers stand for the case they model; its `σ_RMS` is one degree of freedom of many |
| [`CH-0006`](CH-0006-solvent-quality-bound.md) | `C-0002`, its ≤ 0.7 % of `τ` bound on the salt effect | Arithmetic 1.5× out, evaluated at the buffer concentration rather than the layer-local one, and the sign asserted rather than established | **Upheld in conclusion, overturned in construction** — no number in the parameter sheet moves |
| [`CH-0007`](CH-0007-point-ion-boundary-in-applied-bias.md) | this queue's reading of `C-0005`'s 0.197 V point-ion boundary | 0.197 V is a **diffuse-layer drop**, not an applied bias; the compact layer takes 88 % of 2 V, so the boundary is ≈ 1.0 V applied and §3's ceiling exceeds it by 1.2×, not 10× | **UPHELD** — the error was in the queue, not in `C-0005` |
| [`CH-0008`](CH-0008-plate-conservative-about-flatness.md) | `C-0006`'s claim that the continuum plate is conservative about flatness, and its `ℓ_⊥/p` validity criterion | The plate's error **changes sign** with how the load meets the sheet — it overstates anchored and edge-taper dishing by 1–16 % while understating concentrated and thermal dishing by 12–38 %; and `ℓ_⊥/p` pairs lengths from two different directions | Raised by `C-0009`; `C-0006` annotated, **no verdict moves** |
| [`CH-0009`](CH-0009-worst-point-is-not-the-centre.md) | `C-0006`'s thermal fluctuation table, its "at a point" column | The centre is the *fixed point of both tilts* and so the quietest place on the tile; a corner is exactly `√7` noisier for a rigid plate and 2.49× here, which moves the §6 task 8 verdict from 46 % of the predicate to 114 % | Raised by `C-0010`; `C-0006` annotated, no arithmetic changed |
| [`CH-0010`](CH-0010-brush-height-is-coil-height.md) | `C-0003`'s linear `L₀(N)`, its `N(L₀)`, and its `φ/φ#` | A solved profile gives `L₀ ∝ N^(0.49–0.64)`, not `N¹`; `N(10 nm) = 62.1` against 224.8–374.3; and `φ/φ# = 0.138`, i.e. the layer sits **below** the crossover, reversing the direction `C-0003` asserted | **UPHELD in substance and split** — most of the chain-length gap is the height *convention*, but 78 pN against zero is not definitional. `C-0003`'s stroke and secant stiffness survive |
| [`CH-0011`](CH-0011-electrostatic-stiffness-changes-sign.md) | `C-0008`'s statement that `k_es < 0` everywhere | `\|F_es(h)\|` peaks and then falls toward a sign change at 0.55–1.58 nm, so past the peak the electrostatics *stiffens* the layer — `C-0008`'s sweep started at 3 nm and never saw it | Raised by `C-0012`; `C-0008` annotated. It also answers §6 task 4's second branch: the collapse **is** arrested, but electrostatically rather than osmotically |
| [`CH-0012`](CH-0012-grafted-chi-number.md) | `C-0007`'s reading of the grafted `χ ≈ 0.60`, and the `P-9` premise built on it | The number is not in the source: its fits are 0.789/0.852 on a scale whose own theta is 0.696, for an air/water Langmuir monolayer under *lateral* compression, and the paper forbids the transfer explicitly | **UPHELD** — `P-9` closes as inapplicable and `C-0002`'s bulk equation of state stands |
| [`CH-0013`](CH-0013-entropic-tether-is-not-zero.md) | `C-0010`'s dismissal of an ssDNA tether as "≈ 0 at zero tension" | The geometry stretches it to the layer height whether or not it is taut, and there `F/L` *equals* the chain's own entropic constant — 0.124 pN/nm at a 10 nm gap, 27 % of the whole `A1.1` bound from one tether. The element `C-0010` dismissed is the cheapest scheme available | Raised by `C-0014`; `C-0010` annotated, no number changed |
| [`CH-0014`](CH-0014-layout-sampled-not-swept.md) | `C-0009`'s 19 % crossover-count effect, its four-point registration sample, and its 64-attachment flatness count | The layout space was sampled, not swept: the phase has period 32 bp not 16, registration is the real lever (×1.43–1.60) while the count effect is 0.3–3.4 % **with the opposite sign**, and flatness needs 45 attachments as 3 × 15 because the square diagonal is the wrong slice of a 25.6×-anisotropic sheet | Raised by `C-0015`; `C-0009` annotated in place, nothing overwritten |
| [`CH-0015`](CH-0015-usable-bias-window-is-unloaded.md) | `C-0012`'s 0.02–0.1 V usable bias window | It is a property of the **unloaded** actuator, which snaps to near-contact; the tile *held* at the §6 target sits at a 2–7 nm gap and `φ ≤ 0.09`, inside every upstream validity range. The two readings disagree by more than 2× and the device operates at the second | Raised by `C-0016`; both readings reported rather than one chosen |
| [`CH-0016`](CH-0016-coupling-requirement-is-quoted-off-operating-point.md) | `C-0012`'s output-coupling requirement table, and the same numbers in `C-0016`'s P2 | It is a **stability** threshold quoted where a **placement** condition decides the design, and it is evaluated at two grid biases that *bracket* the device's operating bias without sampling it — an interval `C-0012` itself names as an open question | **UPHELD on both grounds**, and its own "favourable direction" claim **struck**: at 10 nm the located floor is 1.5–4.4× *higher* than the column it replaces. No number in `C-0012` moves; what changes is what the table is a requirement *for* |
| [`CH-0017`](CH-0017-collapse-is-arrested-osmotically.md) | [`CH-0011`](CH-0011-electrostatic-stiffness-changes-sign.md) — its second statement, *"the instability is removed, but not by the osmotic divergence alone"*, and specifically *"the collapse **is** arrested, but by `k_es` reversing sign at 0.55–1.58 nm, not by the osmotic divergence"* as that sentence stands in `C-0012`, in `ANSWERS.md` §6 task 4 and in `TASKS.md`'s standing findings | methodological — a mechanism named from the *existence* of a feature rather than from a comparison against the alternative mechanism, and never tested against it. Both stoppers exist; only one of them is ever reached | raised. **`CH-0011`'s FIRST statement is upheld and is now asserted as four executable tests** on the real Poisson-Boltzmann solver. Only the mechanism clause is challenged, and no number in `CH-0011` or `C-0012` is disputed |
| [`CH-0019`](CH-0019-two-mean-field-expansions.md) | The standing rationale — recorded in `TASKS.md`'s `T-1f` row and repeated in `C-0011`'s "two largest exposures" framing — that bounding the **polymer** fluctuation correction is what decides whether `C-0017`'s 10 nm coupling margin survives | There are two mean fields in this programme, and the coupling margin sits inside the other one | **Upheld. The rationale identifies two different expansions.** `C-0017`'s own text is *not* at fault — it names `C-0005` correctly and calls the correction electrostatic. What is challenged is the inference drawn from it in the queue. |
| [`CH-0020`](CH-0020-thermal-blob-coarse-graining.md) | [`C-0003`](../claims/C-0003-crossover-valid-layer-response.md) — its *"the thermal blob is 1222 Kuhn segments, 3799 monomers, 167 kDa"* and the inference *"the whole Gen-1 design space is 0.02–0.10 of one thermal blob … blob arguments do not apply to Gen-1 chains at all"*; and `PegWater.thermalBlobKuhnSegments`, the code that produces it | The thermal-blob count is a convention, its excluded volume is coarse-grained linearly, and two errors nearly cancel | **Upheld in part, and it is the *inference* rather than the number that fails.** `C-0003`'s conclusion — the chains are unswollen, so Gaussian Kuhn elasticity and not blob elasticity — **stands**. What does not stand is the margin, the code that computes it, and the reading of a blob count as a measurement. |
| [`CH-0021`](CH-0021-in-plane-factor-is-not-out-of-plane.md) | [`C-0014`](../claims/C-0014-lateral-confinement.md) — its per-anchor force table, its `L_min = δ√(Sn/(2A))` design rule, and the `T-2` window constraint it derives from them | methodological — a factor transferred between two problems that have different load-path topologies, *and* transferred between two definitions that differ by the number of paths on a contour | raised. **`C-0014`'s verdict does not move** — a scheme still exists, the anisotropy theorem is untouched, and orientation is still what decides everything. What moves is the number the footprint constraint is written on |
| [`CH-0023`](CH-0023-placement-preload-sign.md) | [`C-0017`](../claims/C-0017-output-coupling-stiffness.md) — the `placementPreload` relation `R₀ = k_c s − W(s)` and the sentence that interprets its sign, in `coupling/CouplingRequirement.kt` | methodological — a sign convention stated in one direction and implemented in the other, in the one function that is the interface between `T-16` and `T-13` | raised. **No number, table or verdict in `C-0017` moves.** `placementPreload` appears in no study and in no result file; its only call sites are three assertions in `CouplingRequirementTest`, and every one of them passes with the returned value unchanged |
| [`CH-0024`](CH-0024-stroke-is-measured-from-a-height-the-tile-never-occupies.md) | [`C-0012`](../claims/C-0012-coupled-stroke-and-blocking-force.md) and [`C-0017`](../claims/C-0017-output-coupling-stiffness.md) — the stroke coordinate `s = L₀ − h`, and every stroke quoted on it; with [`C-0016`](../claims/C-0016-design-window.md) inheriting it through the window | methodological — a displacement quoted from an origin the device does not occupy, in the one state (zero bias) that the origin was supposed to describe | raised. **No verdict in `C-0012`, `C-0016` or `C-0017` moves** — the shortfall is inside every model bracket those claims already carry. What moves is the *statement* that §3's 3 nm is delivered, which becomes "2.62–2.93 nm is delivered" |
| [`CH-0025`](CH-0025-edge-taper-is-an-edge-enhancement.md) | [`C-0006`](../claims/C-0006-tile-load-distribution-and-flatness.md) (`T-5`/`T-5b`) and [`C-0009`](../claims/C-0009-discrete-lattice-tile.md) (`T-10`), specifically their `electrostatic-edge-taper` load case | methodological — an assumed load model, flagged as assumed by its own authors, replaced by a solve | the **sign** and the **width** of the taper. **No dishing verdict in either claim moves**, and no arithmetic in either is disputed. |
| [`CH-0026`](CH-0026-forces-are-footprint-integrated-one-dimensional-pressures.md) | [`C-0008`](../claims/C-0008-electrostatic-force-and-decay-length.md) (`T-3a`) and [`C-0012`](../claims/C-0012-coupled-stroke-and-blocking-force.md) (`T-3`), and through them [`C-0016`](../claims/C-0016-design-window.md), [`C-0017`](../claims/C-0017-output-coupling-stiffness.md) and [`C-0018`](../claims/C-0018-maximum-usable-bias.md) | methodological — a stated one-dimensional limitation, now quantified and found to be non-negligible and **one-signed** | a **multiplier on every force**, not a change to any mechanism. No mechanism, decay length, stiffness sign or stability verdict is disputed. |
| [`CH-0027`](CH-0027-hold-down-requirement-is-a-force-only-for-a-one-sided-stack.md) | [`C-0021`](../claims/C-0021-zero-bias-resting-position.md) — *"The scale everything is judged against"* (`F ≥ k_BT/3.0 nm = 1.380649 pN`, the **declared acceptance**), and the sentence **"A coupling can decide where the tile sits; it cannot be the thing that holds it there."** | methodological — an acceptance *currency* derived from a property of the stack that was being evaluated, and then applied as a property of the problem | raised. **No number, table or verdict in `C-0021` moves.** Every one of its 144 solved states is a one-sided stack; its two device rows are reproduced here on its own quadrature domain (1.391 against 1.40 `k_BT`, 5.371 against 5.37, 18/18 and 0/18 confining) |
| [`CH-0029`](CH-0029-the-48-pn-allowable-is-a-30-bp-number.md) | [`C-0006`](../claims/C-0006-tile-load-distribution-and-flatness.md)'s per-path allowable table **as consumed** by [`C-0009`](../claims/C-0009-discrete-lattice-tile.md), [`C-0014`](../claims/C-0014-lateral-confinement.md) and [`C-0020`](../claims/C-0020-in-plane-shear-lag.md) — specifically `C-0020`'s `A_eff = 48.00 pN`, its `L_min` table and the 54.9 pN preload that follows from it | methodological — a **length-dependent measured quantity used as a length-free material constant**, at a length no claim in the programme has ever specified | raised. **No verdict of `C-0006`, `C-0009`, `C-0014` or `C-0020` moves** — the correction is 1.35× on one path class and is dominated by the ×2 the same claim's own entry-topology result buys back. What moves is that `A_eff` acquires a **sequence-design argument** |
| [`CH-0031`](CH-0031-a-flexible-hinge-cannot-be-anisotropic.md) | [`C-0023`](../claims/C-0023-two-sided-coupling.md)'s remedy for the axial-restraint bracket — *"**A two-nucleotide single-stranded hinge at each end absorbs it**, and it is loaded along the beam's axis, not in the normal load path"* — and, through it, its `E3a` *ends free to draw in* column as a **buildable** reading rather than an idealisation | methodological — a joint sized against **one** of the three load components it has to carry, with the other two not written down | raised. **No number in `C-0023` moves** — its four spans (24.61, 39.07, 49.41, 54.91 nm), its two tangents (33.333, 91.13 pN/nm), its 8.08/86.7 pN tensions and its 0.88 nm draw-in demand are all reproduced here to ≤ 5.9e−4 — and the design that replaces the hinge lands **inside** its `E3a`/`E3b` bracket, not outside it |
| [`CH-0033`](CH-0033-thermal-excitation-is-not-a-load-non-uniformity.md) | [`C-0015`](../claims/C-0015-crossover-phase-and-registration.md)'s validity clause *"any load non-uniformity, thermal excitation or attachment-stiffness scatter restores a finite crossover force **in proportion to the non-uniformity**"*, and [`C-0017`](../claims/C-0017-output-coupling-stiffness.md)'s second named failure route, *"`T-17` finding that a real, non-uniform load restores a per-path crossover force large enough to matter, which would put `K2` back inside `C-0009`'s 2.3–7.6× concentration and take its 2.22 pN to 5.1–16.9 pN"* | methodological — **three quantities of different kinds put in one list**, and a **concentration factor applied to a force that never crosses the interface it concentrates on** | ~~raised.~~ **UPHELD** by [`C-0017`](../claims/C-0017-output-coupling-stiffness.md) and [`C-0015`](../claims/C-0015-crossover-phase-and-registration.md), each of which carries a banner accepting it. **No verdict, number or table of `C-0015` or `C-0017` moves.** What moves is the *status* of one sentence in each |
| [`CH-0034`](CH-0034-flatness-count-saturates-under-the-solved-load.md) | [`C-0006`](../claims/C-0006-tile-load-distribution-and-flatness.md)'s *"the minimum number of load paths … dishing below 10 % of the stroke: 64"*, [`C-0009`](../claims/C-0009-discrete-lattice-tile.md)'s solved 64 and [`C-0015`](../claims/C-0015-crossover-phase-and-registration.md)'s **45 as 3 × 15**, **as design numbers** — i.e. as counts that make the Gen-1 tile flat, rather than as counts that make it flat *under a uniform load* | methodological — an **optimisation run against the one load case in which the objective is identically zero at infinite count**, then quoted against a load case in which it saturates at a non-zero floor | raised. **No count and no verdict of `C-0006`, `C-0009` or `C-0015` moves.** What moves is the sentence *"45 attachments make the tile flat"*, which becomes *"45 attachments exhaust what attachments can do"* |
| [`CH-0035`](CH-0035-the-edge-correction-cannot-reach-the-window-edge.md) | [`CH-0026`](CH-0026-forces-are-footprint-integrated-one-dimensional-pressures.md) — its two *directional* statements, not its measurement | methodological — a one-signed correction propagated to two clauses by the sign of the force rather than by which quantity each clause is a function of | raised. **No number in `C-0022` or `CH-0026` moves**, and the +14.7 % is reproduced from `T-3b`'s own file. What is challenged is the two-line consequence table |
| [`CH-0036`](CH-0036-a-correction-and-the-part-that-caused-it.md) | [`CH-0024`](CH-0024-stroke-is-measured-from-a-height-the-tile-never-occupies.md) — its **quoted bracket** and its status line, not its methodological point | methodological — a correction evaluated against a stack that a claim filed on the same day removed, and reported as a property of the device | raised. **`CH-0024`'s coordinate argument is upheld in full and is right.** What moves is the number attached to it, from 2–13 % to **0.6–0.9 %** at the design point, and the reason the number moved is worth more than the number |
| [`CH-0037`](CH-0037-the-buckling-duty-is-the-mandate-not-the-element.md) | [`C-0025`](../claims/C-0025-flexure-end-joint.md)'s standoff-window table — its `buckling margin (pinned / guided)` column, `17.0 / 68.1×` down to `1.5 / 6.1×` — and the design summary line *"end shear per joint 1.111 pN at 3 nm, **3.70 pN at 10 nm**; standoff buckling 8.87 pN (pinned head) / 35.5 pN (guided)"* | methodological — **a load read off the placement condition rather than off the element that carries it**, in a programme whose own `CLAUDE.md` records four times that a secant and a tangent are different quantities | raised. **No other number in `C-0025` moves** — its span, `c`, tangent, beam tension, both buckling loads and all eight window rows are reproduced in `C-0028` to ≤ 1.2e−9 |
| [`CH-0038`](CH-0038-a-standoff-grounded-at-infinity.md) | [`C-0025`](../claims/C-0025-flexure-end-joint.md)'s `J5` normal standoff — `k_θ = EI/ℓ`, `k_a = 3EI/ℓ³`, `k_⊥ = S/ℓ`, anisotropy `Sℓ²/(3EI)` — and, through them, its catalogue row (28.75 / 1.348 / 137.5 / 102.0 at 8 nm), its design summary (*"transverse support 137.5 pN/nm … 186×"*), its `ℓ = 7–10 nm` window and its statement that the window is *"closed from both sides by different mechanisms … below, by `C-0023`'s compliance ceiling … above, by `C-0017`'s own 10 nm standoff envelope"* | methodological — **a member's stiffness quoted without the compliance of what it stands on**, which `C-0025` itself names as its own second open question and then uses unqualified in every downstream number | raised. **No number in `C-0025` fails to reproduce** — its `J5-8` span (31.6403748), `c` (95.6390226), tangent (37.3911226), beam tension (3.82799407), its eight window rows and both its buckling loads (8.8672227 / 35.4688908) are all recovered in `C-0028` to ≤ 1.2e−9 |
| [`CH-0039`](CH-0039-the-base-couple-needs-a-lever-arm-the-standoff-does-not-have.md) | [`C-0028`](../claims/C-0028-standoff-base-joint.md) (`T-40`) | **structural** — a counting theorem about strand termini, not a numerical disagreement | **high**: it removes the recommended base outright and moves the surviving window |
| [`CH-0040`](CH-0040-e5-is-a-small-rotation-law-at-47-degrees.md) | [`C-0023`](../claims/C-0023-two-sided-coupling.md) (`T-23`), and by inheritance `C-0025`'s and `C-0028`'s *"`E5` is untouched, and still the fallback"* | **geometric** — a tip cannot rise past its arm, and the arm is capped by the mandate | **high**: `E5` is the branch the whole standoff line falls back to |
| [`CH-0041`](CH-0041-the-standoff-supplies-the-draw-in-it-was-charged-for.md) | [`C-0025`](../claims/C-0025-flexure-end-joint.md) (open question 1 and its `S_eff`) and [`C-0028`](../claims/C-0028-standoff-base-joint.md) (its validity range, its `standoffTipCompliance` bound and its stated failure mode) | **methodological and numeric.** The dropped term is not small, it is not a correction to the term that was kept, and its argued sign is wrong in both halves | **UPHELD in the bound, OVERTURNED in the consequence.** No number in either claim fails to reproduce; what fails is the sentence both wrote about what the number means |
| [`CH-0042`](CH-0042-a-strain-softening-coupling-has-no-free-stability-margin.md) | [`C-0017`](../claims/C-0017-output-coupling-stiffness.md)'s tangent/secant theorem **as consumed by** [`C-0023`](../claims/C-0023-two-sided-coupling.md), [`C-0025`](../claims/C-0025-flexure-end-joint.md) and [`C-0028`](../claims/C-0028-standoff-base-joint.md) — and against the transfer of `C-0023`'s **oddness** through all three | **methodological.** A theorem stated for a strain-*stiffening* element is being applied to one that is now strain-*softening*, and a two-sidedness argument written on an odd law is being inherited by a law that is not odd | **UPHELD and RESOLVED** by [`C-0032`](../claims/C-0032-softening-coupling-stability.md) / [`T-76`](../tasks/T-76-softening-coupling-stability.md) (2026-08-14). **Both horns hold.** At 10 nm / 2 mM the softening line's bias margin collapses from `C-0018`'s 1.007–1.032 to **1.000–1.002** and the fold's own stroke walks back from 3.41–4.13 nm to **2.80–3.17 nm**, crossing §3's 3 nm target at two of six models; `min_s k_tangent` = 22.875 is below the 23.41–27.91 floor at 6 of 6. The **adverse mounting is 1.06–1.53× past `C-0023`'s ceiling at 0 of 8 standoff lengths.** What the two-horn framing did not contain: **0.5 mM clears every predicate at 1.44–5.93×**, and `ℓ = 5 nm` at 2 mM clears five of six. `C-0032` raises [`CH-0047`](CH-0047-a-tangent-minimum-over-zero-stroke-is-not-a-requirement.md) against this challenge's own `[0, 10]` reading of `min_s k_tangent` — **no verdict here moves** |
| [`CH-0043`](CH-0043-a-reproducibility-certificate-that-certifies-the-path.md) | [`C-0019`](../claims/C-0019-mean-field-fluctuation-corrections.md) — its **provenance line**, not any of its numbers | **methodological.** A byte-for-byte diff certifies that nothing perturbed the iteration, which is a property of the *path*. It was reported as a property of the *answer*, and the two differ here by three significant digits | **UPHELD, and no number of `C-0019` fails.** Every verdict, every window edge and every quoted figure stands. What fails is the sentence about what the diff established |
| [`CH-0044`](CH-0044-c-equals-twelve-and-the-series-composition-cannot-both-be-right.md) | [`C-0029`](../claims/C-0029-perpendicular-junction-routing.md), the `E5g16` design table and the `E5g` family it recommends | **methodological** — a boundary condition changed without changing the composition the boundary condition belongs to | **the design number moves, the verdict does not.** `E5g16`'s arm is 12.24 nm from a composition that is exact only at `c = 3`; solved on the boundary-value problem `c = 12` describes, the same geometry assembles to **54.61 pN/nm against the 33.3333 pN/nm mandate** and past its own **40 pN/nm** compliance ceiling at the **secant**. The corrected arm on the *realised* anchorage is **12.50 nm**, and every acceptance verdict `C-0029` reported still holds |
| [`CH-0045`](CH-0045-the-mounting-sense-is-not-a-free-binary.md) | [`C-0030`](../claims/C-0030-coupled-standoff-joint.md) — its Deliverable 4, its *"Still open"* item 1, and its sensitivity row `mounting: favourable → adverse` | **methodological** — a design variable named without the kinematic chain that determines it, and an emptiness quoted over a swept interval rather than as a threshold | **RAISED.** `C-0030`'s numbers all reproduce; its *framing* of the variable does not survive. |
| [`CH-0046`](CH-0046-a-standoff-in-tension-does-not-buckle.md) | [`C-0030`](../claims/C-0030-coupled-standoff-joint.md) — the adverse rows of its Deliverable 4 table (`margin, CanDo / Fields` = 2.53/1.90 … 0.75/0.57) and its sensitivity row `mounting: favourable → adverse` reported as `2.18 → 0.99` and `1.64 → 0.74` | **methodological** — a stability predicate evaluated on the *magnitude* of a member force whose *sign* the same model determines, and whose sign is opposite in half the cases tabulated | **RAISED, and it does not move a verdict** — the adverse mounting fails on `P3`, which owns that column. Filed because the number is quoted and would be read. |
| [`CH-0047`](CH-0047-a-tangent-minimum-over-zero-stroke-is-not-a-requirement.md) | [`CH-0042`](CH-0042-a-strain-softening-coupling-has-no-free-stability-margin.md)'s own prescription — *"the quantity to compare is not the tangent at the working point but its **minimum over the stroke**"* — as consumed by [`C-0030`](../claims/C-0030-coupled-standoff-joint.md)'s `minimumTangent` field and by [`C-0032`](../claims/C-0032-softening-coupling-stability.md)'s `Q2` | **methodological.** A minimum is being taken over a range whose lower endpoint carries **no requirement at all**, so for one whole class of elements the reported number is a property of the interval rather than of the coupling | **OPEN, and narrow.** No verdict in `C-0030` or `C-0032` moves — the softening element's minimum is **interior** and identical on both ranges. What moves is every *comparison* against a strain-stiffening element |
| [`CH-0048`](CH-0048-the-good-solvent-premise-was-checked-on-monomers.md) | [`C-0007`](../claims/C-0007-solvent-quality-vs-salt.md) — its `φ** = v/v₀ = 0.516` row and the gate-5 statement *"the good-solvent premise checked against the actual material — `φ/φ** = 0.06–0.14`, a factor of 7 of margin"* | **methodological** — a crossover defined on a *statistical segment* evaluated on a *monomer*, which is `C-0002`'s `a`-trap in a third costume | **RAISED.** Wrong by `b⁶/(v₀ v_K n_K²) = 16.17`, a factor **independent of the excluded volume**; corrected, `φ** = 0.0105–0.0319` and the layer sits at **0.9–2.2×** it at rest, i.e. the premise fails rather than holding. `C-0007`'s headline result and every other row stand, and the correction selects the mean-field end of a bracket `C-0007` already carries |
| [`CH-0049`](CH-0049-the-cited-band-is-a-reduced-density-and-the-fit-range-is-wrong.md) | [`C-0002`](../claims/C-0002-peg-material-parameters.md) — its *"the semidilute→concentrated one at 0.2–0.3"*, its *"fitted over 0–50 wt %"*, and the DOI it carries | **methodological + provenance** — a crossover quoted in the wrong convention on the wrong segment, a fitted range narrower than the data, and a citation that does not resolve | **RAISED, on three grounds.** No derived crossover lands in `0.2–0.3`; the two that do are `v_m/v₀ = 0.203` and `1 − 2χ = 0.257`, both monomer readings, and R&C's printed `φ** ≈ v/b³` is a **reduced** density, `0.257` reduced being `0.036` physical. The real fit range is **1.5–67.5 wt %** (`φ ≤ 0.63`), recovered from the source data. The DOI is `10.1021/jp806893a`. **`C-0002`'s parameter sheet, `α`, `φ#` and `m_eff` do not move** |
| [`CH-0051`](CH-0051-the-pull-in-bias-falls-too.md) | [`C-0027`](../claims/C-0027-window-resynthesis.md) — its **pull-in propagation** (`marginLowerBound` = `≥ 1.108–1.134`, and *"the operating bias falls, unambiguously"*), not its window, its axes, its coupling margins or its decomposition | **methodological** — a **ratio** of two biases corrected in one of its two arguments, with the other held at the value the correction was applied to change | **RAISED.** `C-0027`'s 8–9 % fall in `V*` is reproduced at 8.5–9.9 % by re-solving, but the pull-in bias falls 7.1–8.0 % too, so the solved margin is **1.021–1.028**, not `≥ 1.108`. And the direction is not universal: the margin moves with the sign of `3 nm − s_fold`, so at 7 nm / 10 mM the same collar makes it **0.9–3.5 % worse**. At four of six models pull-in stops binding altogether |
| [`CH-0053`](CH-0053-both-errors-run-the-same-way-and-the-desired-stroke-does-not-survive-them.md) | [`C-0034`](../claims/C-0034-guided-arm-anchorage.md)'s placement bracket and its `E5a16` design table, and by inheritance [`C-0029`](../claims/C-0029-perpendicular-junction-routing.md)'s `E5g` row | **methodological** — a bracket asserted from two corrections that are not opposed, both of which stiffen; plus a stroke that is three quarters of the arm's own contour | **raised** by [`C-0039`](../claims/C-0039-two-spring-elastica.md) ([`T-79`](../tasks/T-79-two-spring-elastica.md)). **The file declares no `**Status**` row**, so no adjudication is recorded and none is guessed at here; the row itself was missing from this index until `T-298` |
| [`CH-0054`](CH-0054-the-sixteen-crossover-hinge-line-does-not-exist.md) | [`C-0029`](../claims/C-0029-perpendicular-junction-routing.md)'s `E5g16` and [`C-0034`](../claims/C-0034-guided-arm-anchorage.md)'s `E5a16`, and [`C-0023`](../claims/C-0023-two-sided-coupling.md)'s free `hingeCount` | **a count on the lattice** — a hinge of `n` crossovers needs `(n − 1) × 32 bp` of **collinear interface**, and none of the three claims counted | **RAISED, and it moves a verdict rather than a number.** A 40 nm tile carries **four** at every one of the 32 phases; sixteen needs **163.2 nm** (4.08 tiles) or **33 duplexes** (2.22 tiles) transversally, and the transverse line restrains the wrong axis. Every upstream number reproduces at ≤ `3.8e−9`. At the count that exists the arm places at **7.748 nm** — §3's **acceptable** stroke clears at 36.58 pN/nm and the **desired** 10 nm is out of geometric reach; at the one or two a flexure can own at 45 paths the tangent is **42.0–54.1 pN/nm**, past `C-0023`'s own ceiling |
| [`CH-0055`](CH-0055-the-forty-five-path-array-is-not-a-placement.md) | [`C-0023`](../claims/C-0023-two-sided-coupling.md), [`C-0030`](../claims/C-0030-coupled-standoff-joint.md) and [`C-0035`](../claims/C-0035-flexure-mounting-sense.md) — the shared `Conditions` premise *"45 load paths on `C-0015`'s 3 × 15 grid"*, and `C-0035`'s 326 nm² tie-aperture **floor** | **methodological** — a design point carried as a condition through four claims without a plan view, and a connectivity question priced as an area | **RAISED, and it moves a design point rather than a number.** The 45-path array is unrealisable at **0 of 720 orientations, at any level count and on any body**: the grid's across-helix pitch **is** one duplex and its along-helix pitch is under the span plus a duplex, and stacking cannot separate two vertical members that share a height range whatever levels their beams sit at. The tile carries **15**. Every upstream number reproduces (`C-0030`'s span to `2.9e−5`, `C-0035`'s three areas to ≤ `1.2e−3`). And the tie holes are collinear, so they **sever** the superstructure into **18 components** at every one of the 32 phases — cured by **8 bp** of stagger |
| [`CH-0056`](CH-0056-the-azimuthal-quantum-belongs-to-the-sheet.md) | [`C-0029`](../claims/C-0029-perpendicular-junction-routing.md)'s cheap bound 3 — the 33.74 deg/bp azimuthal quantum and its *"the phase costs at most 8.4 %"* — as inherited verbatim by [`C-0037`](../claims/C-0037-triangulated-standoff.md)'s validity range | **a bound evaluated on the wrong body** — a lattice belonging to the **sheet's** phosphate positions applied to the **standoff's** base chord | **UPHELD at the level it was written** by [`C-0042`](../claims/C-0042-paired-perpendicular-junction.md), and completed by [`C-0052`](../claims/C-0052-crossbar-junction-trio.md) and [`C-0059`](../claims/C-0059-torsion-feasible-routing.md); the transfer to a multi-junction assembly is refused by [`CH-0072`](CH-0072-the-alignment-is-not-free-on-the-torsion-feasible-set.md). **NO number in either claim moves.** A standoff is a *separate* duplex, so nothing fixes its helical phase and its rotation about its own axis is **continuous**: `C-0042`'s search lays both chords at **0.00 deg** from the flexure axis at every separation from 6 to 12 bp, on both groove conventions and at the seat duplex's own axis. Both claims were conservative and neither applied the projection, so the effect is a **design obligation** and not an arithmetic — the 8.4 % is not a reserve, and `C-0037`'s "best-phase" caveat can be struck. The quantum still binds *where a link may land*, one level down, as `C-0042`'s sheet-phase residual |
| [`CH-0059`](CH-0059-the-desired-stroke-placement-is-below-the-stability-floor.md) | the programme-wide **reading convention** for §3's *desired* stroke, carried by [`C-0023`](../claims/C-0023-two-sided-coupling.md), [`C-0029`](../claims/C-0029-perpendicular-junction-routing.md), [`C-0034`](../claims/C-0034-guided-arm-anchorage.md), [`C-0039`](../claims/C-0039-two-spring-elastica.md), [`C-0040`](../claims/C-0040-hinge-line-census.md) and [`C-0041`](../claims/C-0041-flexure-array-packing.md) | **methodological** — a design evaluated at a state it was not placed for; the sixth instance of *"quote a stiffness with its compression"*, applied to the acceptance clause | **RAISED, and NO number in any claim moves — what moves is what the numbers are OF.** §3's desired clause has never been *placed*: every claim reads it on the coupling placed for the **acceptable** stroke, `k_c = 33.3333 pN/nm`, which must then deliver **333 pN** (and `C-0039` finds the real element needs **699**). `C-0017`'s own arithmetic gives the desired clause `k_c = 100/10 = **10 pN/nm**`, a different device — which `C-0046` shows the flexure **can** build (arms 11.4–18.1 nm, **12 of 29** points clearing every element clause) and which `C-0017`'s **stability floor refuses at 2.34–2.79×**. Composing the two clauses caps the stroke at **3.58–4.27 nm at 100 pN**, naming no element. The concrete casualty is the **34-path floor**, quoted in three claims as a property of the material: it is `33.333 × 10/10`, and the same allowable gives **10** under the other reading. `T-112` |
| [`CH-0062`](CH-0062-the-buildable-hinge-count-clears-the-ceiling-on-the-elastica.md) | [`C-0040`](../claims/C-0040-hinge-line-census.md)'s verdict clause *"at the one or two a flexure can actually own, the tangent is 42.0–54.1 pN/nm and even the acceptable stroke fails `C-0023`'s own ceiling"*, and its design rows at `hingeCount` = 1–3 | **methodological** — a design graded on a placement pipeline that a sibling claim, filed in the same iteration, had already superseded; the two differ by **1.9× in the placed arm and 1.38× in the tangent**, and the difference is largest exactly where the verdict is taken | **RAISED, and it moves a verdict the FAVOURABLE way.** `C-0040`'s 4.765 nm / 54.113 pN/nm at one crossover is `C-0034`'s **series composition**, which [`CH-0053`](CH-0053-both-errors-run-the-same-way-and-the-desired-stroke-does-not-survive-them.md) supersedes; on `C-0039`'s own exact elastica the same design places at a **9.131 nm arm and 39.18 pN/nm — inside `C-0023`'s ceiling with 2.1 % of margin** — and `C-0039`'s own published table already contains that number. At 45 paths the crossover budget makes `h = 1` the only affordable count, so this is the design and not a corner. `C-0046` reaches the same conclusion independently (`(56, 1)` at **38.17 pN/nm**) without naming the disagreement. **The census, the ledgers, the fan law, the `n_eff` result and the desired-stroke verdict are untouched** |
| [`CH-0063`](CH-0063-the-collar-was-carried-onto-a-load-line-the-device-does-not-have.md) | [`C-0033`](../claims/C-0033-collar-on-the-equilibrium-path.md)'s `P4` — *"the margin rises at every one of the six models … it does not go below one at any model"* — and by inheritance [`CH-0051`](CH-0051-the-pull-in-bias-falls-too.md)'s relocation table | **methodological** — a correction carried onto the **affine mandate** load line while a sibling claim of the same iteration had established that the coupling the programme has is strain-**softening**; the second instance of `CLAUDE.md`'s *"never quote a margin corrected on one channel without checking whether the other channel's correction exists yet"*, and the first where the two are not the same size | **RAISED. Every number in `C-0033` reproduces and its scope does not survive.** At `C-0018`'s own fold the baseline coupled tangent vanishes by construction, so the three standing corrections compose **exactly**: `C-0033`'s collar `+2.605` to `+4.993`, `C-0019`'s fluctuation `−1.449` to `−5.807`, and `C-0030`'s realised softening `−9.207` to `−10.288 pN/nm`, giving **−8.398 to −11.062 pN/nm at 6 of 6 models with no straddle**. The collar recovers **27–49 %** of what the element costs. `C-0033`'s **1.021–1.028** is the affine mandate's margin; **`C-0032`'s 1.0000–1.0019 is the standing 10 nm / 2 mM statement.** `C-0033`'s `P1`, `P2` and `P3` are untouched, and 0.5 mM makes the whole composition vacuous |
| [`CH-0064`](CH-0064-the-validity-ceiling-is-read-on-a-layer-four-times-denser-than-the-window.md) | [`C-0050`](../claims/C-0050-desired-stroke-reach.md)'s **bound 3** and the validity clause that says its bounds *"inherit `C-0003`'s validity ranges in full"* | **methodological** — a licence re-checked at the design points a *previous* falsifier named rather than at the one the claim actually reads, the sixth instance of *"quote it with the state it was read at"* and the first applied to a **layer model** | **RAISED, and the verdict does not move.** At the top of `C-0027`'s own 10 nm window `C-0003`'s six trial-function models sit at `φ = 0.1505–0.2845` where `C-0011`'s solved layer — the layer every window edge in this programme is drawn on — sits at **0.0623–0.0686**, a factor of **4.15**, and the two **disagree about whether a `φ ≤ 0.2` ceiling exists at all** (2 of 6 models have none; the solved layer is 2.92× clear). `C-0050`'s answer is untouched because its **bound 2 is kinematic**, needs no crossover and is short by 1.02× on its own; what is not licensed is the number, at the one design point nobody had checked. Checking costs one multiplication: `φ = Nσv₀/h` and `T-1d` emits it |
| [`CH-0065`](CH-0065-the-hinge-arm-array-has-no-plan-view-either.md) | [`C-0050`](../claims/C-0050-desired-stroke-reach.md)'s catalogue row **`E5a1` at 45 paths** — *"none — every predicate clears"* — and its headline *"3 of 14 clear them all at the acceptable one"*; and, by inheritance, [`C-0039`](../claims/C-0039-two-spring-elastica.md)'s and [`C-0023`](../claims/C-0023-two-sided-coupling.md)'s *"45 load paths"* premise for the whole `E5` family | **methodological** — an **unevaluated** predicate reported as an absent one; the row carries `packingAssessed = false` and `C-0050`'s validity range says so, but its verdict line and its count do not | **RAISED, and `C-0050`'s bound is untouched and strengthened.** Assessed, the predicate fails twice. A hinge is a crossover, so an arm roots only where one is, and a row sees only its **two bounding interfaces** — 5.44 nm apart on an interior row, 10.88 on an edge row, against an arm needing **11.82** — so the tile places **43 of 45**, exactly (construction meets the independent per-row bound), at every one of the 32 phases and on all five swept axes. And an arm is **cut out of** its host: 43 arms take **65.4 %** of its duplex length, spend 43 crossovers and bury 13, **56 of 56**, leaving **zero** surviving crossovers and **no bonded component at all**. The count that leaves the host whole is **25**, so the threshold is **45 → 25 (1.80×)**, not 45 → 43. `C-0050`'s acceptable-stroke count becomes **2 of 14**, and the row that leaves is the only one it computed on the exact element. `C-0039`'s mechanics, `C-0040`'s census and `C-0041`'s own verdict all reproduce |
| [`CH-0066`](CH-0066-the-surviving-designs-consume-the-sheets-own-connectivity.md) | [`C-0046`](../claims/C-0046-fewer-longer-flexures.md)'s verdict clause *"the best point of the whole space is `(56, 1)`"*, its three surviving designs at 45/50/56 paths, and its *"the window's width is 1.24× in path count"* | **methodological** — a budget spent past the point at which the thing it is drawn from stops existing; the ledger `n·h ≤ 56` carries no reserve for the sheet's own connectivity | **RAISED, and NO number in `C-0046` moves — every one reproduces to ≤ `4.7e−5`. What moves is which of them is a design.** The interfaces of a single-layer sheet form a **path graph** on its 15 duplexes, so a connected sheet needs one retained crossover on each of its **14** interfaces and the hinge budget is `56 − 14 = **42**`, i.e. **75.0 %** (35 of 49 at the other 22 phases) against an admissible region of **80–100 %**. All three reported designs sever the tile — into **≥ 4, ≥ 9 and exactly 15** pieces. The best *buildable* point is **`(42, 1)`**, arm 8.882 nm, usable stroke **3.063 nm**, and the surviving window is **`39 ≤ n ≤ 42`** — which also resolves `C-0046`'s own open `34 < n ≤ 45` threshold at **39**. The correction costs **7.4 %** of the stroke and leaves a **2.1 %** margin on §3's acceptable clause. `C-0046`'s degeneracy result and its desired-stroke verdict are untouched. `T-119` is the one falsifier |
| [`CH-0067`](CH-0067-a-leg-is-one-body-and-its-two-chords-are-not-independent.md) | [`C-0048`](../claims/C-0048-truss-cap.md)'s recommended azimuth pair and leg length, and `C-0037`/`C-0042` insofar as they treat the leg length as a continuum | Raised by `C-0052` — the two ends of one rigid leg cannot present independently chosen chords: they differ by `m × 33.74°`, and 21 steps is 78.5° from the pair the design asks for, on all three twist readings | **RAISED. The design survives everywhere and the forced pair is better than the recommended one**, because the recommendation reads the plane that does not govern |
| [`CH-0073`](CH-0073-the-along-helix-scatter-rule-reverses-on-a-non-uniform-coupling.md) | [`C-0026`](../claims/C-0026-one-row-per-duplex.md)'s **build rule** — *"if they must differ, let the error be along the helix"* — and the reading of its scatter table that treats the along-helix pattern as harmless *simpliciter* | **methodological** — a direction shown to be a **symmetry of equal springs on one row per duplex**, carried as a build rule for `C-0058`'s non-uniform coupling, whose 3 × 15 grid makes the along-helix index the rim/interior index | raised by `C-0060` (`T-122`). **No number of `C-0026` moves** — its 0.883 pN per unit amplitude, its `3e−11 pN` exact zero and its 17 % break-even are reproduced or cited unchanged, and the rule survives on the **crossover-force** channel. What is added is a second channel that ranks the two patterns **oppositely**: the flatness verdict is lost at **31.6 %** along the helices against **69.8 %** across, a factor of 2.21 with `C-0017`'s total held |
| [`CH-0072`](CH-0072-the-alignment-is-not-free-on-the-torsion-feasible-set.md) | [`C-0042`](../claims/C-0042-paired-perpendicular-junction.md)'s *"the azimuth costs exactly nothing … both base chords come out at 90.0°, at every separation"* and its recommended **7 bp** row; and [`C-0029`](../claims/C-0029-perpendicular-junction-routing.md)'s *"a 90° routing exists **and it is a scaffold excursion**"* | **methodological** — a design variable priced on a search space larger than the feasible one: the alignment is free over the placements that satisfy the *distance* criterion, and the placements that also satisfy the *torsion* criterion are a sparse subset over which it is not | raised by `C-0059` (`T-124`). **The pair still closes at every separation 6–12 bp** — a stronger existence result than `C-0042` could give — but its worst chord runs **33°/69°/57°/6°/6°/33°/33°** instead of 0.00° everywhere, so its own **7 bp is the worst of the seven** at 69° (12.8 % of the couple, and *past the half right angle* where `C-0037`'s two-axis base cannot be represented at all) while **9–10 bp** delivers 6.0°. And the **scaffold excursion cannot be aligned**: 1 of 120 closing at a 39.0° chord against independent staples' 7 at **0.0°**. `CH-0056` is upheld — a free duplex's chord inherits no lattice phase — and its *consequence* is not, because feasibility is a relation between two bodies |
| [`CH-0079`](CH-0079-a-tie-grid-is-a-registration-and-an-armed-tile-has-none.md) | [`C-0035`](../claims/C-0035-flexure-mounting-sense.md)'s nominal-design row — *"what the tile carries: only the 45 tie attachments of `C-0015`'s 3 × 15 grid"* — and the premise beside it that *"the tile now carries no out-of-plane element at all"* | **conditions**, not arithmetic — the row is true of the tile `C-0035` had and false of the tile the programme now proposes. On `C-0063`'s 34-arm placement **26 of those 45 stations do not exist**, at the best of 2916 arm senses, and **no rigid translation** of the grid restores them | raised by `C-0066` (`T-126`). **No number in `C-0035` fails to reproduce** — its 325.62 nm² aperture floor, 5.31 nm clearance and 4.69 nm penetration are recomputed through its own library as this task's zero-arm limiting case. What falls is the availability of the stations, not the mounting determination |
| [`CH-0078`](CH-0078-the-base-floor-is-a-minimum-over-a-coordinate-the-array-pins.md) | [`C-0059`](../claims/C-0059-torsion-feasible-routing.md)'s base misalignment floors and [`C-0062`](../claims/C-0062-crossbar-trio-existence.md)'s design table built on them — the **6.0°** at the 9 and 10 bp rows and the *"best representable design is the 10 bp row at both ends"* verdict | **conditions**, not arithmetic — a floor is a minimum over the base's axial position on the host duplex, and an array **pins** that position at the 34 upward stations `C-0055` and `C-0063` supply. Read there, the 10 bp row is **57.0°** (9.5×) and past the 45° at which `C-0037`'s `TwoLinkBase` cannot represent a base at all; the 9 bp row is 18.0° | ~~raised by `C-0065` (`T-130`).~~ **UPHELD** by [`C-0070`](../claims/C-0070-pinned-leg-budget.md) (`T-132`), which discharges its own *what would settle it* item 2. **No number in `C-0059` or `C-0062` fails to reproduce.** `C-0062`'s design table stands as a statement about a **lone** truss and is withdrawn as one about an **array**; the row an array can build is **9 bp at 18.0°** |
| [`CH-0080`](CH-0080-the-equal-spring-advantage-belongs-to-the-ten-nanometre-layer.md) | [`C-0063`](../claims/C-0063-upward-root-placement.md)'s **Deliverable 2** — *"it needs **no distribution at all**"* — as carried forward by `C-0065` and `C-0066` | **conditions**, not arithmetic — the sentence is a statement about a **10 nm layer's** load field. Over the traversed range of all three 10 nm devices the 34 equal springs are flat (**0.0789 / 0.0853 / 0.0896**), and against §3's **5 nm** layer the same 34 roots dish **0.2000** and are **worse than no coupling at all at both** of that device's states (0.1104 against a free 0.0638; 0.2000 against 0.1648) | raised by `C-0068` (`T-129`). **No number in `C-0063` fails to reproduce** — its 0.0706 to `2.9e−10` — and its placement is *strengthened*, being the argmin of the range objective as well (0 of 198 288 better). There are two remedies: a distribution at a peak ratio of **2.32** (0.0565), or a **different phase** — no centro-symmetric placement at phase 24 clears the 5 nm range (best 0.1169) while one at **phase 8** does with equal springs (**0.0895**), and it reads 0.2416 at the 10 nm design state |
| [`CH-0082`](CH-0082-a-truss-has-two-legs-and-the-design-table-gives-it-one-base.md) | [`C-0059`](../claims/C-0059-torsion-feasible-routing.md)'s `feasibleTrussDesign` and [`C-0062`](../claims/C-0062-crossbar-trio-existence.md)'s design table built on it — both take a truss's base misalignment as a **single scalar** shared by the two legs | **methodological** — a truss has **two** legs `w` base pairs apart on one duplex, and the register pins them at two *different* azimuths (−9.0° and −18.0° at the 9 bp row, 0.0° and +57.0° at the 10 bp). One leg length rotates both cap chords by the same `m τ`, so their difference is invariant under the length and floors the worse cap at `\|fold(δ_A − δ_B)\|/2` — a bound neither pipeline can express | raised by `C-0070` (`T-132`). **Not verdict-moving on the surviving set** — 4.5°, 19.5° and 16.5° at the 9, 11 and 12 bp rows, all below `C-0062`'s own cap floors — but **28.5°** at the 6 and 10 bp rows against its composed 9.0° and 27.0°, and both of those rows already fail another clause. **No number of `C-0059`'s or `C-0062`'s fails to reproduce** |
| [`CH-0081`](CH-0081-a-rigid-root-demands-a-longer-arm-than-the-plan-admits.md) | [`C-0048`](../claims/C-0048-truss-cap.md)'s, [`C-0062`](../claims/C-0062-crossbar-trio-existence.md)'s and [`C-0065`](../claims/C-0065-crossbar-array-placement.md)'s truss branch — the premise that a triangulated standoff array is a useful **foundation** for the Gen-1 output coupling | **methodological** — a truss cap is a **root**, and a bending element's plan length `(c EI/k)^(1/3)` is monotone increasing in *both* end restraints, so the truss's own virtue (a base that does not rotate) is what pushes the element it caps outside the plan budget | raised by `C-0069` (`T-133`). **`C-0065`'s placement result stands entirely** — 44 of 44 trios place 34 times, and its 27.4119472 nm span and **12 of 34** both reproduce at departure `0.0`. What falls is the output stage: against an **8.19 nm** budget the two-support flexure family's shortest possible member is **22.41 nm (2.74×)** and a rigid-root, free-tip arm is **9.247 nm (12.9 % over, 24 of 34)**, while the element that places needs a **compliant** root — one crossover at 13.53 against a ceiling of **13.93** |
| [`CH-0084`](CH-0084-the-measured-staple-incorporation-is-past-the-flatness-threshold.md) | [`C-0060`](../claims/C-0060-buildable-stiffness-ratio.md)'s tolerance verdict — *"the tolerance is a threshold, and it is generous"* — and through it [`C-0058`](../claims/C-0058-non-uniform-coupling.md)'s flat two-level distribution and [`C-0017`](../claims/C-0017-output-coupling-stiffness.md)'s mandate as an achievable **sum** | **a measurement exists and neither claim looked for it.** `C-0060` records `T-45` as *"still unmeasured"* and reasons about *"the 5 % a staple design might plausibly hold"*, an assumption with no source. Strauss et al., *Nat. Commun.* **9**:1600 (2018) map incorporation at single-staple resolution across all **168** staples of a Rothemund rectangle — **48–95 %, mean 84 %, worst at the edges** — and a missing staple **removes** a load path rather than perturbing it, so the population is Bernoulli and `σ_rel = √(f/(1−f))` | raised by `C-0072` (`T-134`). The mean gives **43.6 %** relative per-path scatter — **1.26×** `C-0060`'s 34.6 % threshold and **2.57×** `C-0026`'s break-even — the edge sites **104 %**, and the mean alone is a **16 %** shortfall on `C-0017`'s mandate, **2.9×** the worst rounding placement error `C-0060` spends a trim to remove. **The position dependence runs the wrong way for `C-0058`**, which puts 34 of its 45 stations on the rim and gives them the *stiff* level. `C-0060`'s stiffness and buildability results are **untouched and reproduce** |
| [`CH-0090`](CH-0090-the-scaling-estimate-uses-the-exponent-of-a-different-quantity.md) | [`C-0011`](../claims/C-0011-scf-density-profile.md) and [`CH-0010`](CH-0010-brush-height-is-coil-height.md) — their *"a layer whose `2⟨z⟩` is 10 nm would need `N ≈ 190–210`"*; and [`C-0003`](../claims/C-0003-crossover-valid-layer-response.md)'s reading of its own box-to-SST spread as **profile uncertainty** | **methodological** — an extrapolation is worth what its exponent is worth, and the exponent used is measured on a quantity the extrapolation is not about | raised by [`C-0077`](../claims/C-0077-first-moment-chain-length.md) (`T-1e`). **UPHELD on both heads, and neither claim's verdict fails.** The exponent of `L₀^F` in `N` (0.455–0.468) is a consistent **10 %** below the exponent of `2⟨z⟩` in `N` (0.501–0.520), and the formula uses the first where it needs the second; the band is also narrowed from the claim's own 0.49–0.64 to 0.50–0.55 with no derivation. **The exact inversion is `N = 175.08`, 7.713 kDa — OUTSIDE `190–210`**, and every reading at a measured exponent overstates it by 13.6–34.3 %, the exponent drifting upward over an extrapolation of 2.8× in `N`. Head 2: read on **one** functional the box and strong stretching agree to **0.76 %** where their own conventions put them **28.4 %** apart, so most of `C-0003`'s bracket's internal spread is a convention difference between its own two models. `C-0011`'s `N = 62.1`, window, stroke and stiffness are reproduced and untouched |
| [`CH-0091`](CH-0091-a-first-moment-ten-nanometre-layer-is-not-a-ten-nanometre-layer.md) | [`C-0016`](../claims/C-0016-design-window.md)'s **height-convention banner**, as carried by `C-0027`, `C-0051` and `ANSWERS.md` | **methodological**, with two arithmetic corrections — the banner presents two conventions as two readings of **one** layer; they are two different layers, and only one is inside §3 | raised by [`C-0077`](../claims/C-0077-first-moment-chain-length.md) (`T-1e`). **UPHELD, and `C-0016`'s window does not move — not one edge, not one owner, not one grid step.** A `2⟨z⟩ = 10 nm` layer puts its tile at **13.20–18.05 nm**, inside §3's 5–10 nm band at **0 of 61** grid points, so the window is in the right convention. The banner's *"8–9 kDa"* is **7.713 kDa** at the design point and **4.17–8.73 kDa** across the window, and its *"about four times"* is **2.585–2.843×** — that factor was the top of one range over the bottom of the other. Its *"1.71–2.16× smaller"* is correct (measured 1.710–2.065 over the window). The banner's **point** — that a convention mismatch is the likeliest way this window is misread — is upheld intact |

### Backfilled index — `CH-0050` onward (`P-26`, iteration 28)

**The index above stops at `CH-0091` and carried 66 rows against 124 files.** Three separate claims noticed in
passing — `C-0113` measured 65 of 111, `C-0122` re-measured it, `C-0128` reported it as a process note — and
none could act, because each was in the middle of something else. **An index that is half the corpus is worse
than no index**, because a reader who finds `CH-0100` in it will read `CH-0151`'s absence as meaning it does
not exist.

These rows are **generated from each challenge's own header fields** by
[`tools/check-challenge-index.py`](../../tools/check-challenge-index.py), deliberately rather than written by
hand: the 66 rows above carry prose a generator cannot reproduce, and **inventing some would be worse than
transcribing what each file already states about itself**. A backfilled row says where to look and what the
file claims; it does not paraphrase the argument. The checker's `--backfill` regenerates them, and
`tools/verify.sh` now fails if any challenge is unindexed.

| ID | Against | Grounds / raised by | Status |
|---|---|---|---|
| [`CH-0050`](CH-0050-the-truss-cost-was-priced-on-the-wrong-axis.md) | [`C-0028`](../claims/C-0028-standoff-base-joint.md) (open question 3, and the *"the sway IS the draw-in"* identity as applied to a truss) and [`C-0029`](../claims/C-0029-perpendicular-junction-routing.md) (the closing paragraph of *"the design that results"*, and its way 2 of failing) | **logical** — a frame couple is a rank-one tensor on the leg offsets, so it acts on **one** axis and is identically zero on the orthogonal one — **plus in-silico**, the whole of `C-0030`'s pipeline re-run on an assembled truss head | **RAISED.** Both claims' *identities* are upheld to the last digit; what is challenged is a **consequence** each of them states in one sentence and neither computes |
| [`CH-0060`](CH-0060-the-stagger-is-not-free.md) | [`C-0041`](../claims/C-0041-flexure-array-packing.md)'s Deliverable 4 — *"staggering the tie column is free of every upstream claim"* and *"leaves the beams comfortably inside the 40 nm edge … and moves nothing else: rows are disjoint strips, so a stagger cannot make two beams overlap"* | methodological — **a freedom established in one model (plan-view hard-body packing) asserted as a freedom in every model**. The reasoning offered for *"moves nothing else"* is a plan-geometry argument about beam overlap; the quantity it is asserted over includes the sheet's load paths, which no plan-view model can see | raised. **No count, orientation, span, packing verdict, area, severance result or allowable of `C-0041` moves.** What moves is one clause |
| [`CH-0061`](CH-0061-the-cap-is-a-body-and-its-junctions-are-counted-too.md) | [`C-0037`](../claims/C-0037-triangulated-standoff.md) — the cap's geometry and its head tie; and [`C-0042`](../claims/C-0042-paired-perpendicular-junction.md) insofar as it inherits both | **methodological** — a stiffness quoted without the body that supplies it, and a counting theorem applied to one of a joint's two load paths | — |
| [`CH-0068`](CH-0068-the-hinge-inventory-is-not-the-sheets-own.md) | [`C-0054`](../claims/C-0054-consumed-crossover-sheet.md) — its **cheap bound** `f_max = 1 − (D−1)/N = 0.750`, the **42-crossover ceiling** that follows, the *"every one of `C-0046`'s designs severs the tile"* verdict, and the `39 ≤ n ≤ 42` window | **methodological, and named in advance by the claim it challenges** | **OPEN** |
| [`CH-0069`](CH-0069-the-plate-fails-before-the-connectivity-does.md) | [`C-0054`](../claims/C-0054-consumed-crossover-sheet.md) (`T-110`) — specifically the reading of its `39 ≤ n ≤ 42` window as a region in which its **continuum companion** numbers still describe the sheet, and its *"the step is at severance, not at consumption"* | **methodological**: a validity range that was never checked, on a claim whose own *"Still open"* item 5 named the check and did not run it | **OPEN, and CONDITIONAL on [`CH-0068`](CH-0068-the-hinge-inventory-is-not-the-sheets-own.md)** — this challenge is written against `C-0054`'s window, which assumes crossovers are spent; `C-0055` argues they need not be, and if that is upheld the window and this challenge fall together. See the closing section. |
| [`CH-0070`](CH-0070-the-reported-optima-are-in-the-torsion-infeasible-set.md) | [`C-0029`](../claims/C-0029-perpendicular-junction-routing.md)'s reported routing, as inherited by [`C-0042`](../claims/C-0042-paired-perpendicular-junction.md) and [`C-0052`](../claims/C-0052-crossbar-junction-trio.md) | **methodological** — the objective the three searches minimise does not measure the quantity that decides the answer, and it drives the optimum toward the edge of a window where a backbone cannot be built | — |
| [`CH-0071`](CH-0071-the-saturation-floor-is-a-property-of-the-equal-spring-family.md) | [`CH-0034`](CH-0034-flatness-count-saturates-under-the-solved-load.md)'s **Ground 2** — the decomposition that puts `C-0022`'s edge enhancement at *"**0.149 of the stroke, floor**"*, marks it *"bought with attachments? **no** — it is a rim collar 8.9 nm wide"*, and concludes in Ground 3 that *"the attachment count is not an axis on which it can be attacked"* — and against the remedy sentence *"the residual dishing is 0.149 of the stroke and is **a property of the tile's rim, not of the coupling**"* | methodological — a **saturation measured inside one family** (all springs equal) quoted as a property of the **object**. Every point of `CH-0034`'s 45 → 225 table shares the total stiffness equally, which no upstream claim requires; `C-0017`'s mandate is an equality on a **sum** | raised. **No count, no table, no reproduction and no remedy of `CH-0034` moves.** What moves is the word *floor*, and the design-space conclusion drawn from it |
| [`CH-0074`](CH-0074-the-flat-distribution-lives-on-stations-no-placement-supplies.md) | [`C-0058`](../claims/C-0058-non-uniform-coupling.md) (`T-113`) — its *"YES at three columns"* verdict, and the licence that reading carries for the Gen-1 device | **a premise, not a number** — every number in `C-0058` reproduces here, including its 0.0753 to `5.8e−4` | **RESOLVED** (iteration 11) by [`C-0063`](../claims/C-0063-upward-root-placement.md) (`T-125`) — **and resolved from the other side than either party expected.** Sweeping the row phases of the *same* 34 upward roots gives a placement that dishes **0.0706** of the stroke with **34 equal springs** at `C-0017`'s unchanged total: inside `T-5b`'s 0.10, **4.36× better than no coupling at all**, and better than `C-0058`'s own 0.0753. So the stations *are* supplied — they were simply not the ones `C-0055`'s greedy scheduler chose, and the pathology recorded below is a property of that scheduler (centroid `x = −8.80 nm`), not of the upward lattice: **every one of the 32 phases has a placement that beats the free tile.** Two corrections travel with the resolution: the *"same 34 roots, alternate rows reflected"* row of the table below is **not on the upward lattice at all** — 16 of its 16 reflected odd-row roots are `WEST` (downward) sites, which is [`CH-0076`](CH-0076-the-mirrored-placement-is-on-the-other-face-of-the-sheet.md) — so the best *buildable* entry of that table was 0.2902 and not 0.1649; and `C-0058`'s **rim rule reverses sign** on the swept placement (0.0706 uniform against 0.2214 at its own ×5), the 34-parameter optimum asking for a peak ratio of only **1.30**. `C-0058`'s numbers all stand; what falls is the reading that Gen-1 flatness *depends* on a station set no placement supplies. |
| [`CH-0075`](CH-0075-the-cap-floor-is-read-off-a-stage-that-failed.md) | [`C-0059`](../claims/C-0059-torsion-feasible-routing.md)'s design table — *"the mechanics at the alignment feasibility gives"* — and specifically its composition of a **9 bp** base floor with a **7 bp** cap floor of **6.0°** | **a floor taken from a stage that failed, and a shared design variable read at two values.** `C-0059`'s own validity range declares the first — *"the cap floor is taken from the trio's best reach-feasible alignment (6.0°), because no trio closed … an optimistic reading of a stage that failed"* — and this challenge supplies what it could not: the alignment a trio that **does** close actually delivers. The second is not declared anywhere | — |
| [`CH-0076`](CH-0076-the-mirrored-placement-is-on-the-other-face-of-the-sheet.md) | [`C-0061`](../claims/C-0061-stacked-arm-sheet.md) (`T-121`), Deliverable 3 item 3 and the `ROOTS-MIRRORED` rows of its distribution table — and, through it, the *"the same, mirrored"* row of [`CH-0074`](CH-0074-the-flat-distribution-lives-on-stations-no-placement-supplies.md) | **a geometry, not a number** — every number in `C-0061` reproduces here, including its 0.4156, its 0.3558 and its centroid at −8.80 nm | **OPEN** |
| [`CH-0077`](CH-0077-five-solved-states-are-four-devices.md) | [`C-0058`](../claims/C-0058-non-uniform-coupling.md)'s **Deliverable 4** — *"A distribution is tuned to a **load**, and the load is an operating state"*, and its conclusion *"a flatness count now needs a load case **and an operating state**"* as applied to those five — and against [`CH-0071`](CH-0071-the-saturation-floor-is-a-property-of-the-equal-spring-family.md)'s **overturning condition 2**, *"A requirement that one distribution be flat at every operating state. No distribution found here is."* | **a premise, not a number.** Every number in `C-0058`'s Deliverable 4 reproduces here — 0.2182, 0.0753, 0.1867, 0.0710, 0.2551 to `≤ 5.8e−4` — and its five-state minimax is *improved on* rather than contradicted. What is challenged is that those five states are the set the requirement is owed over | raised. **No count, no table, no verdict and no remedy of `C-0058` moves.** What moves is the word *"every"* |
| [`CH-0083`](CH-0083-the-pull-in-verdict-is-quoted-for-a-load-line-the-recommendation-does-not-use.md) | [`C-0018`](../claims/C-0018-maximum-usable-bias.md)'s and [`C-0032`](../claims/C-0032-softening-coupling-stability.md)'s **coverage** — not against any number either of them reports. Specifically against the reading, carried in `ANSWERS.md`'s §6 table and in `C-0071`'s own §6 row, that §6 task 4 is discharged **for the Gen-1 device** | **`C-0032` is itself the proof that a fold does not transfer between load lines.** It re-ran `C-0018`'s 162 fold searches with one substitution — the affine `R = 33.333 s` replaced by `C-0030`'s nonlinear law — and **lost 7 of 54 states**, moved the pull-in bias by 0.7–1.8 %, and walked the fold's own stroke from 3.41–4.13 nm back to **2.80–3.17 nm, across §3's 3 nm target at two of six models**. A third law is therefore a third answer, and `C-0071`'s recommended element is a third law: it is **strain-stiffening** at the placement stroke, where `C-0030`'s is softening, and its tangent minimum over the traversed range is **30.03 pN/nm** where `C-0030`'s is 22.88 | **RESOLVED** by [`C-0084`](../claims/C-0084-recommended-element-pull-in-fold.md) (`T-149`), which searches the third load line and finds **no fold at 2 mM at 6 of 6 layer models**; every number in `C-0018` and `C-0032` stands |
| [`CH-0085`](CH-0085-a-window-edge-quoted-at-a-tie.md) | [`C-0016`](../claims/C-0016-design-window.md) and [`C-0011`](../claims/C-0011-scf-density-profile.md) — their **four-figure rendering** of the 10 nm upper window edge, not any number either claim derives | **methodological.** A quantity quoted at four significant figures whose fifth figure is exactly `5` is not determined at four significant figures; which way it renders is decided by digits the file has no business printing | **UPHELD, and no number of either claim fails.** Every verdict, every window width, every constraint attribution and every five- and six-figure quotation stands unchanged |
| [`CH-0086`](CH-0086-a-plan-ceiling-is-a-property-of-a-placement-not-of-a-count.md) | [`C-0072`](../claims/C-0072-plan-tolerance-model.md)'s Deliverable 6 — its `length ceiling` column and the sentence *"Dissolve them and every row carries at most two; **the ceiling is then set by the tile edge, at 9.12 nm**"* — and, through it, [`C-0069`](../claims/C-0069-output-element-placement.md)'s own ceiling column, which is 8.19 nm in every row of a table whose subject is how the design changes with the count | **the quantity is a maximum over placements and both claims evaluate it at one placement.** A placement's plan ceiling is `min` over its rows of that row's own ceiling, and the rows are **independent**, so `max over placements of min over rows` is exactly `sup{L : Σ_r min(m, maximumRootedElementsInRow(r, L)) ≥ n}` — a bisection on a monotone capacity, using `C-0069`'s own library, exact and free of any solve. `C-0072`'s ceiling column is that quantity evaluated at the output of `rowsWithoutInteriorRoots`, a deterministic reduction rule chosen to dissolve rows of three and not to maximise a ceiling | — |
| [`CH-0087`](CH-0087-a-sensitivity-row-that-does-not-meet-the-mandate.md) | [`C-0069`](../claims/C-0069-output-element-placement.md)'s Deliverable 5 — *"the sensitivities, and the one axis that closes the margin"* — and specifically its note on the 15-path row, *"fewer paths shorten the arm; **the placement is unchanged because the count is what sets the stations**"* | **`C-0017`'s mandate is a stiffness on a SUM**, so a path count `n` sizes the element *and* counts the instances. Deliverable 5 changes the first and holds the second at 34, and it never states the total the resulting array delivers. Worse, the shortfall is not confined to the path-count rows: the delivered total is `N_placed · 33.3333/n`, so **any** row whose placed count differs from its path count misses the mandate by exactly that ratio — including the two rows the claim reports as plan failures | — |
| [`CH-0088`](CH-0088-the-weave-read-through-the-plan-margin-is-on-the-wrong-axis.md) | [`C-0072`](../claims/C-0072-plan-tolerance-model.md), Deliverable 7, *"The framing changes: 2.69 nm is a Bragg lattice constant, not a local distance"* — the table headed *"read through the plan model"* | **methodological** — a measured quantity carried into a slot that names it but does not mean it | **STANDS.** `C-0072`'s arithmetic is reproduced to `3.9e−4` (its own rounding) and its *conclusion from that arithmetic* is withdrawn |
| [`CH-0089`](CH-0089-the-collinear-clearance-is-a-girth-not-a-lattice-constant.md) | [`C-0066`](../claims/C-0066-arm-slab-tie-clearance.md) bound 4 (*"the only gap the lattice offers clears a duplex by 0.0256 nm"*) and [`C-0069`](../claims/C-0069-output-element-placement.md) `Q5` (*"the plan budget on every 34-root placement is `pitch − d` = 8.19 nm exactly"*), and through them [`C-0072`](../claims/C-0072-plan-tolerance-model.md)'s *"neither margin is quotable"* | **methodological** — one symbol used for three quantities, and the one with a measurement is not the one the number came from | **UPHELD** by [`C-0079`](../claims/C-0079-unbonded-duplex-separation.md) (`T-139`), which also closes its failure route 1. **STANDS as a statement about what the 0.0256 nm is a margin AGAINST.** It does **not** assert that 0.898 nm is the true clearance: the honest outcome is that the exclusion width in this role is **unmeasured**, with a measured **floor** of 1.8173 nm and a counter-argument pointing the other way |
| [`CH-0092`](CH-0092-the-propagation-did-not-close.md) | [`C-0073`](../claims/C-0073-determined-precision-of-a-result-file.md) Part 4, *"a result file is an INPUT"* — specifically its closing statement that the propagation **closes** | **methodological** — the audit's instrument (a grep for a literal filename) cannot see the call it needed to find, and the claim's own declared falsifier is what it missed | **UPHELD, and the claim's verdict stands.** The two stale files are re-emitted here. **No verdict, no flag, no string other than one precision-carrying parameter, and no quoted figure of `C-0027`, `C-0051` or `T-63` moves.** `C-0073`'s conclusion is untouched; its *audit* is not. |
| [`CH-0093`](CH-0093-the-collinear-clearance-is-a-stacking-allowance-not-an-exclusion.md) | [`C-0053`](../claims/C-0053-hinge-arm-array-packing.md)'s footprint convention (*"a rooted element occupies `[root, root ± L]` and the next along the same row may start at `high + d`"*) and, through it, [`C-0069`](../claims/C-0069-output-element-placement.md)'s `Q5` budget `p − d = 8.19 nm` and [`C-0072`](../claims/C-0072-plan-tolerance-model.md)'s margin identity | **methodological** — a geometry substitution: the convention charges a *transverse* girth in an *axial* slot, and the two geometries' pair energies differ by 15× | **UPHELD and closed** by [`C-0085`](../claims/C-0085-collinear-stacking-clearance.md) (`T-152`). **STANDS.** It is [`CH-0089`](CH-0089-the-collinear-clearance-is-a-girth-not-a-lattice-constant.md)'s own failure route 2 — *"a demonstration that `C-0053`'s footprint convention is charging something other than a body"* — answered, and answered in the direction that **widens** the margin. It does **not** assert that 1.3 nm is the right allowance; it asserts that the quantity is a **stacking range** and that the range is measured |
| [`CH-0094`](CH-0094-a-hard-body-width-is-a-threshold-and-no-plan-claim-states-one.md) | [`C-0041`](../claims/C-0041-flexure-array-packing.md), [`C-0053`](../claims/C-0053-hinge-arm-array-packing.md), [`C-0065`](../claims/C-0065-crossbar-array-placement.md), [`C-0066`](../claims/C-0066-arm-slab-tie-clearance.md), [`C-0069`](../claims/C-0069-output-element-placement.md) and [`C-0076`](../claims/C-0076-weave-exclusion-width.md) — every claim in the branch that writes *"a duplex in plan is a rectangle of width `d`"* | **methodological** — a quantity quoted without the state it is read at, which is this programme's own recurring failure mode; here the missing state is an **energy budget** rather than a load, a compression, a bandwidth, a loading rate or a lattice coordinate | **STANDS as a statement about what a plan width IS.** It does **not** overturn any placed count: `C-0079` finds **34 of 34** at every physically calibrated reading. What it removes is the licence to quote a plan margin without naming the budget it assumes |
| [`CH-0097`](CH-0097-the-rounding-site-table-names-the-wrong-studies.md) | [`C-0073`](../claims/C-0073-determined-precision-of-a-result-file.md)'s `roundingSites` table (`gpd/results/P-18-determined-precision.json`, field `roundingSites`), and the `P-19` ranking that quotes it | **methodological, and the same one as `CH-0092`** — the table's study sets were assembled by inspection, and inspection is what `CH-0092` established cannot produce a census of this repository by hand. The two challenges are the same defect on two different tables of one claim. | **RAISED. `C-0073`'s verdict is not affected** — direction (a) is right, the ninth digit is not worth buying, and every measured `determinedDigits` stands, because a determined precision is a property of the *solver*, not of the study list. What is corrected is **which studies each site governs**, and therefore what re-emitting one would cost. |
| [`CH-0098`](CH-0098-the-0-5-mM-requirement-is-quoted-for-a-withdrawn-coupling.md) | [`C-0032`](../claims/C-0032-softening-coupling-stability.md)'s **escalation** — *"as of iteration 5 it stopped being an improvement and became a requirement for the surviving coupling"* — as carried into [`DECISIONS-FOR-NDI.md`](../../DECISIONS-FOR-NDI.md) decision 1, into `TASKS.md`'s `T-63` row, and into `C-0071`'s conditional ledger. **Not against any number `C-0032` reports**, every one of which stands | `C-0032`'s escalation was measured on `C-0030`'s strain-**softening** coupled-standoff flexure, which [`CH-0081`](CH-0081-a-rigid-root-demands-a-longer-arm-than-the-plan-admits.md) and [`C-0069`](../claims/C-0069-output-element-placement.md) removed from the output role and which [`C-0071`](../claims/C-0071-output-element-recommendation.md) does not recommend. `C-0084` re-runs `C-0018`'s 54-state fold search with the **recommended** element's own law and finds **0 folds of 6** at the 10 nm layer in 2 mM, a bias margin of **1.3877–2.5764** against `C-0032`'s 1.0000–1.0019, and the binding ceiling changing owner from pull-in to `C-0002`'s `φ = 0.2` and the element model's own branch end | — |
| [`CH-0099`](CH-0099-a-ceiling-taxonomy-belongs-to-a-load-line-too.md) | [`C-0018`](../claims/C-0018-maximum-usable-bias.md)'s **candidate ceiling list** — `{static stability (pull-in), C-0002's φ = 0.2, CH-0007's point-ion 1.0 V}` — as re-used verbatim by [`C-0032`](../claims/C-0032-softening-coupling-stability.md) and, until it was extended, by `C-0084` itself. **Not against any number `C-0018` reports**: on `C-0018`'s own load lines the list is complete, and every one of its 162 rows stands | `C-0018` established the discipline that *"a ceiling belongs to a `(bias, load line)` pair, never to the bias alone"* (`CH-0015`) and then applied it to the **values** of the three candidates while holding the **membership** of the list fixed. The membership is load-line-dependent too. Every load line `C-0018` and `C-0032` ran — an affine mandate, a preloaded dead load, a midspan flexure — is defined at every stroke the layer admits. `C-0071`'s recommended element is an **inextensible rotating arm** whose law stops existing at 7.9197 nm of stroke, and `C-0039`'s shooting solve enumerates only its small-rotation branch. At 12 of `C-0084`'s 108 states the equilibrium path therefore ends on the **element model**, and the inherited list has no candidate for that | — |
| [`CH-0100`](CH-0100-the-collinear-margin-is-an-integer-not-a-residue.md) | [`C-0069`](../claims/C-0069-output-element-placement.md)'s `Q5` margin, [`C-0072`](../claims/C-0072-plan-tolerance-model.md)'s margin identity and its floor 1, and [`C-0071`](../claims/C-0071-output-element-recommendation.md)'s three `NONE` bands | **methodological** — a quantisation argument. The three claims compute a difference of three lengths, two of which are not lengths a design can draw | **STANDS.** It does not contradict any computation in the three claims: every number reproduces to `1.3e−7` or better. It contradicts the *reading* of the result — and in the direction that makes the margin **quotable**, which is the opposite of what `C-0072` concluded |
| [`CH-0101`](CH-0101-the-nominal-tile-width-is-not-a-buildable-raster-width.md) | `Gen1Tile.EDGE_X = 40.0` and, through it, every plan, flatness and load claim written on a 40.0 nm width — [`C-0006`](../claims/C-0006-tile-load-distribution-and-flatness.md), [`C-0015`](../claims/C-0015-crossover-phase-and-registration.md), [`C-0022`](../claims/C-0022-tile-edge-load-profile.md), [`C-0053`](../claims/C-0053-hinge-arm-array-packing.md), [`C-0063`](../claims/C-0063-upward-root-placement.md), [`C-0069`](../claims/C-0069-output-element-placement.md) | **methodological, and it is a specification gap as much as a modelling one** — the width is inherited from §3 as a nominal dimension and has never been checked against the design language that has to draw it | **DISCHARGED in the item it left unevaluated** by [`C-0090`](../claims/C-0090-buildable-raster-width.md) (`T-153`), whose sign runs the *favourable* way. **STANDS as a statement about a SEAMLESS raster**, which is what `C-0086` recommends. It does **not** assert that 40.0 nm is unbuildable in general: the seamed case is not derived, and Rothemund himself offers an escape (*"one or two scaffold bases could be left unpaired"*) whose cost he introduced it to avoid |
| [`CH-0102`](CH-0102-the-forty-eight-per-cent-is-one-corner-of-one-hundred-and-sixty-eight.md) | [`C-0072`](../claims/C-0072-plan-tolerance-model.md)'s `T-45` deliverable and the [`CH-0084`](CH-0084-the-measured-staple-incorporation-is-past-the-flatness-threshold.md) it raises — specifically the composition table's row *"**Strauss, EDGE sites** \\| **0.48** \\| **0.52** \\| **104.1 %** \\| **6.12×** \\| **3.01×**"*, and the `TASKS.md` and `CLAUDE.md` phrasing *"48 % at the edges to 95 % in the centre"* read as an edge value | **the per-staple map was never read; only the abstract was.** Strauss et al.'s Supplementary Fig. 14 prints all 168 values. Their minimum, 47.9 % incorporation, is **one cell** — `P1`, the bottom-right **corner**. The perimeter's 52 cells average **77.5 %**, the interior's 116 average **86.2 %**, six perimeter cells are *above* the interior mean, and the four corners run 60.2, 69.9, 47.9 and **87.1 %**. An "edge value" of 48 % is 29.6 percentage points below what the edges measure | — |
| [`CH-0103`](CH-0103-the-path-count-recommendation-runs-against-fabrication.md) | [`C-0072`](../claims/C-0072-plan-tolerance-model.md)'s recommendation to reduce the array to 30 arms, [`C-0074`](../claims/C-0074-two-per-row-placement.md)'s recommended 30-root phase-8 placement and [`C-0075`](../claims/C-0075-path-count-consistency.md)'s self-consistent count table — specifically the reading that a **lower** path count is a strictly better design because it buys plan margin at no cost | **the count is a robustness variable and all three claims treat it as a geometry variable.** `C-0089` sweeps the path count under the measured dropout and finds the 90th-percentile dishing **monotone decreasing** in it: 0.85219673 at 15 paths, 0.668998495 at 30, 0.614243977 at 45, 0.61234699 at 60, 0.572257776 at 75 and 0.532748246 at 90, with equal springs at `C-0017`'s unchanged total. The per-path force falls as `1/n` (6.667 → 1.111 pN against the 10 pN unzip allowable), so **nothing else in the acceptance stack opposes a higher count**. `C-0072`/`C-0074`/`C-0075` move the other way | **UPHELD as a bookkeeping correction, while the recommendation it challenges STANDS**, by [`C-0103`](../claims/C-0103-path-count-at-fixed-geometry.md) (`T-163`), which additionally withdraws its instrument ([`CH-0119`](CH-0119-a-redundancy-slope-measured-over-searched-subsets-is-not-a-count-slope.md)) |
| [`CH-0104`](CH-0104-a-reachable-floor-is-an-oracle-and-cannot-license-a-design.md) | [`C-0074`](../claims/C-0074-two-per-row-placement.md)'s *"the negative belongs to the equal springs, not to the station set: the least-squares floor over every phase-24 upward root is **0.00071**, 140× below the convention"*, and by inheritance the same construction in [`C-0058`](../claims/C-0058-non-uniform-coupling.md) (`InfluenceSurrogate.reachableDishingFloor`, *"the honest statement of how much room the search may have left"*) and in [`C-0087`](../claims/C-0087-position-dependent-staple-dropout.md)'s falsifier `F4` | **the floor optimises over force vectors, and a design chooses a STIFFNESS vector once.** `reachableDishingFloor` is an ordinary linear least squares over `ℝⁿ` at a station set: it asks *"what is the flattest field any set of support forces at these stations could leave?"*. A stiffness distribution does not choose forces — the forces are the solution of a coupled system, and under fabrication the **support set itself** is random. `C-0089` measures the gap between the two on the same station sets and the same 10 000 realisations: the p90 oracle floor is **0.00111473343** at 90 paths against a best achievable fixed distribution of **0.284537599**, and **0.00917** on `C-0063`'s own 34 roots against a best of **0.449584219** | — |
| [`CH-0105`](CH-0105-the-plan-budget-is-two-bounds-and-only-one-carries-the-tile-width.md) | [`C-0069`](../claims/C-0069-output-element-placement.md)'s *"the plan budget on **every** 34-root placement is exactly `pitch − d` = 8.19 nm"*, and through it [`C-0085`](../claims/C-0085-collinear-stacking-clearance.md)'s 8.19 → 8.84 nm widening and its `c ≤ 2.34166` → `2.94462` razor | **methodological** — a quantity quoted without the state it was read at, where the state is the **tile width**. `rootedLengthCeiling`, the bisection `C-0069` uses to support the claim, takes `edgeX` as an argument; the identification of its output with `pitch − d` is a coincidence of the width it was called at | **STANDS as a statement about the QUANTIFIER, not about the number.** Every figure in `C-0069` is correct at 40.0 nm, and on `C-0069`'s own rows the budget does not move even at 38.08 nm. What is challenged is *"on **every** 34-root placement"*, which is false at any width below 38.14 nm |
| [`CH-0106`](CH-0106-six-routes-are-three.md) | the sentence *"**Six independent routes recommend it**: `C-0012` on the force clause, `C-0016` on the bias window, `C-0017` on the stability floor, `C-0018` on the usable bias, `C-0027` on the corrected margin, and `C-0032` on the realised coupling law"*, as it stands in [`DECISIONS-FOR-NDI.md`](../../DECISIONS-FOR-NDI.md) decision 1, in [`ANSWERS.md`](../../ANSWERS.md) question 1 and in `TASKS.md`'s `T-63` row. **Not against any number any of the six claims reports**, every one of which stands | **arithmetic, and mechanically checkable.** Independence was asserted from the *names* of the clauses, not from the numbers behind them. `T-2`'s `biasClauses[].biasForHundredPiconewtonBlocking` is `T-3`'s own number at **15 of 15** `(height, buffer)` states, worst departure **0.0**, and `T-2`'s binding clause at 10 nm is literally *"force"* at every buffer. `T-25`'s `bufferComparison` carries `T-16`'s `stabilityMargin` extrema and `T-4`'s coupled `margin` extrema at **20 of 20** comparisons, worst departure **2.66e−8** — which is `T-25` printing eight significant digits where `T-16` prints nine. `C-0032`'s route was already withdrawn by [`CH-0098`](CH-0098-the-0-5-mM-requirement-is-quoted-for-a-withdrawn-coupling.md). **Six named routes are 1 withdrawn + 2 transfers + 3** | — |
| [`CH-0107`](CH-0107-the-branch-end-is-a-force-ladder-artefact.md) | [`C-0084`](../claims/C-0084-recommended-element-pull-in-fold.md) **Deliverable 2** — *"refusal 7.9197 nm, branch validity 7.9205 nm … **the branch ends by FOLDING, not by turning past a right angle**"* — and the boundary [`CH-0099`](CH-0099-a-ceiling-taxonomy-belongs-to-a-load-line-too.md) prices its **2.57–3.74×** inflation from. **Not against `C-0084`'s verdict**, which this claim strengthens, nor against any of its 108 fold rows | `C-0084` reads its ceiling off `C-0039`'s `TwoSpringElastica.forceForDisplacement`, which **doubles** a tip force from `0.5 δ k_small` until the stroke reaches its target, and off `stateAtForce`, which brackets the shooting parameter by **doubling from a seed** and runs Illinois inside whatever that produces. Both are exact while the far-end moment residual has **one** root. On this arm a second root appears at about **100 pN** of tip force — far below the right angle `C-0039`'s own KDoc warns about — and once it has, a doubling step can bracket the wrong one: the stroke reported at that force collapses, the force ladder never reaches its target, and the routine throws. **A doubling ladder does not report a branch end; it reports having lost the branch.** Continued instead by marching the near-end rotation and taking the **first** sign change in the force above the previous one, the same arm answers to **8.1610821 nm** with `max_s\\|φ\\| = 1.5707924 rad`, **0.999997 of `π/2` and still below it** | — |
| [`CH-0108`](CH-0108-the-flexure-bound-is-a-bound-on-a-path-count-not-on-a-family.md) | [`C-0069`](../claims/C-0069-output-element-placement.md) cheap bound 3, and [`CH-0081`](CH-0081-a-rigid-root-demands-a-longer-arm-than-the-plan-admits.md) statement 1 | **methodological** — a quantity quoted without the state it is read at, where the state is a **path count** | **OPEN** |
| [`CH-0109`](CH-0109-a-cancellation-and-a-robust-design-are-different-designs-only-within-one-topology.md) | [`C-0089`](../claims/C-0089-dropout-robust-placement.md) Deliverable 2 and its "unanticipated result 2", and the **amplification** column of its Bound 1 | **methodological** — a trade measured inside one family reported as a property of the object, and a ratio used where an absolute is meant | **OPEN** |
| [`CH-0111`](CH-0111-the-row-end-crossover-is-admitted-with-an-interior-crossover-s-stiffness.md) | [`C-0090`](../claims/C-0090-buildable-raster-width.md)'s headline **0.0621469105** and its framing of the end-of-row question as a **convention**; and [`C-0009`](../claims/C-0009-discrete-lattice-tile.md)'s uniform crossover element, which has one `k_θ` and one vertical link for every crossover on the sheet | **methodological** — a quantity quoted without the state it was read at, where the state is *which kind of object the element is*. `C-0009`'s `k_θ` was derived for a crossover with duplex on **both** sides of it; 14 of the 56 at this width have duplex on one side only, and the primary source says their strain is unresolved | **DISCHARGED, iteration 20, by [`C-0099`](../claims/C-0099-row-end-crossover-stiffness.md) (`T-164`) — the task this challenge itself queued, in the order this challenge itself ranked.** All four *what would settle it* items are answered: the sweep is run and is monotone (16 of 16), the vertical link taken separately **is** the whole of the reachable set, Rothemund's remedy is priced at `0.0030284749` of the stroke, and the oxDNA edge crossover is **recommended against**. Ground 2's bracket is **corrected** by [`CH-0115`](CH-0115-the-row-end-bracket-is-a-constraint-not-a-stiffness.md): only **2.85 %** of it is a stiffness. Filed as raised: **STANDS as a statement about the UNCERTAINTY, not about the verdict.** `C-0095`'s answer to *"may a crossover be drawn there"* is unaffected — that is a design rule and a folded structure. What is challenged is the **precision** of the number the admitted reading produces, and the claim that the two readings are alternatives rather than bounds |
| [`CH-0112`](CH-0112-a-lost-branch-is-recorded-as-a-fold-and-as-a-ceiling.md) | **(a)** [`C-0039`](../claims/C-0039-two-spring-elastica.md)'s placement table, at the two rows it records as *"FAILS the desired stroke: the arm folds before reaching it"*; **(b)** [`C-0084`](../claims/C-0084-recommended-element-pull-in-fold.md) Deliverable 2's census — *"at **12 of 108** states the branch ended there … and at **8** of those the branch-end bias is the binding ceiling"*; **(c)** [`C-0050`](../claims/C-0050-desired-stroke-reach.md)'s reach catalogue, at the two elastica rows whose refusal note says the arm folds; **and separately (d)** [`C-0046`](../claims/C-0046-fewer-longer-flexures.md)'s binding-constraint lists at two of its 60 trade rows. **Not** against any of those claims' verdicts, all of which survive and one of which is strengthened | `C-0092`/`CH-0107` established that `C-0039`'s **doubling** force ladder loses the small-rotation branch rather than reaching its end, and re-read the **12** states of the recommended device. `T-159` repairs the ladder in place — `forceForDisplacement` continues the branch, anchoring each force step's shooting root on the previous accepted one, and **refuses** rather than returning a root off the branch — and then re-runs **every** study that consumes the changed source. Three published records were written on the lost branch and none of them was in `C-0092`'s twelve | — |
| [`CH-0113`](CH-0113-the-fifty-three-site-ceiling-is-one-phase-s-inventory-not-the-lattice-s.md) | [`C-0093`](../claims/C-0093-shared-body-coupling.md)'s Deliverable 4 row *"ties the density fit demands, against the upward sites the lattice has — 252 demanded, 53 available"* and its **4.76× shortfall**, and by inheritance the same 53 wherever it is quoted as a lattice ceiling (`C-0066`, `ANSWERS.md` §1) | **a quantity quoted without the state it is read at** — the eighth instance in this project, and here the state is a **lattice phase**. The upward (`EAST`) inventory is a function of the crossover-column phase, and 53 is its value at one of the 32 | **OPEN** |
| [`CH-0114`](CH-0114-the-shared-body-s-tie-ceiling-is-stated-at-one-value-and-used-at-another.md) | [`C-0093`](../claims/C-0093-shared-body-coupling.md)'s **Bound 2** (*"the mandate arithmetic, which is the escape in one division"*) and the `sharedBodyPerStation` column of `mandatePlacementArithmetic` in `src/main/kotlin/coupling/SharedBodyCoupling.kt` | **an inherited bound applied outside the topology it was derived in**, and a claim whose stated cap and reported design disagree by 300× without the disagreement being named | **OPEN** |
| [`CH-0115`](CH-0115-the-row-end-bracket-is-a-constraint-not-a-stiffness.md) | [`CH-0111`](CH-0111-the-row-end-crossover-is-admitted-with-an-interior-crossover-s-stiffness.md)'s **Ground 2** — *"the two 'conventions' are a bracket, and it is a wide one"*, and its arithmetic *"`T-5b`'s 0.10 lies at 0.356 of the way from the admitted reading to the refused one … so a row-end crossover retaining less than roughly a third of an interior one's contribution would take the design out of the convention"*; and, through it, [`C-0090`](../claims/C-0090-buildable-raster-width.md)'s validity-range sentence that *"the end-of-row convention is a MODELLING CHOICE and both readings are carried"* | **methodological** — a one-parameter bracket read off a two-model comparison that moves more than one parameter. `CLAUDE.md` already records the general form of the error (*"a crossover is TWO elements and only one of them is `D_⊥`"*, and *"scaling `D_⊥` is not a model of removing crossovers"*); this is the same trap in reverse, and `CH-0111`'s own *"what would settle it"* item 2 names it without drawing the consequence | **STANDS, and it DISCHARGES the challenge it is raised against.** `CH-0111`'s question — *"is the row-end crossover as stiff as an interior one?"* — is answered in the only way that matters to the acceptance: **it does not matter**. Nothing in `C-0090`'s or `C-0095`'s verdicts moves; what moves is the *width of the exposure*, from 2.70925468× to **1.0487309×** (`0.0651753854/0.0621469105`) |
| [`CH-0116`](CH-0116-the-collar-split-is-a-mesh-node.md) | [`C-0022`](../claims/C-0022-tile-edge-load-profile.md)'s emission of the edge effect as a `(depth, width)` pair **plus** a rim residual, and specifically its treatment of the two as separately meaningful load terms — its *"solved edge effect, smooth term only"* dishing row and its `edgeForceFractionMinMargin`, which pairs the **global** zeroth moment with the **smooth-only** first moment | **methodological, and numerical** — a published quantity whose value is set by a discretisation choice that the claim does not name, does not converge, and does not report the convergence of. `C-0022`'s convergence table emits `centrelineLoad`, `taperDepth`, `taperWidth`, `chargeBalance` and `centrelineRouteSpread`. It does not emit `rimResidualPerUnitEdge` or `rimResidualDepth`, which are the two fields the split is carried in | **STANDS as a statement about the SPLIT, not about the collar.** Every dishing in this programme applies **both** terms through `coupling.edgeCollarPressure` and therefore integrates their sum, which is safe; no verdict anywhere moves. What is challenged is that either term alone is a number |
| [`CH-0118`](CH-0118-the-row-end-crossover-is-a-station-at-two-phases.md) | [`C-0090`](../claims/C-0090-buildable-raster-width.md)'s Deliverable 3, the sentences *"an end plane has an **even** index, and the upward azimuth needs `k ≡ 2b + 3 (mod 4)`, which is odd. **The row-end crossover can never be an upward site**, at any phase, so admitting it adds in-plane inventory and no stations"* | **a statement proved at the state it was read at and quoted as a theorem** — the ninth instance in this project, and here the state is again a *lattice coordinate*. The parity of the end plane's index is a function of the phase, and `C-0090` examined the two phases at which it is even | **OPEN** |
| [`CH-0119`](CH-0119-a-redundancy-slope-measured-over-searched-subsets-is-not-a-count-slope.md) | [`C-0098`](../claims/C-0098-shared-body-placement-and-distribution.md)'s **Deliverable 3** — the real-lattice redundancy fit, its slope **−0.376769756**, its *"2.08× shallower"* reading and the mechanism attributed to it | **a slope fitted through six placement-searched optima confounds the count with the tightness of the search at each count, and `C-0098` records the premise for this itself.** Its own validity range says *"the subset search is a first-improvement descent on an objective that ranks at ρ = 0.47, so the fixed-count rows are **upper bounds** on what the real lattice reaches at those counts, and the 34 → 45 non-monotonicity is a property of the search rather than of the lattice."* An upper bound whose tightness varies with the count is exactly what a slope cannot be read through. `C-0103` measures the size of the effect on an array at phase 24: **nested chains −0.740086889 and −0.704431429; the placement-searched family +0.0610348337**, i.e. **12.1×** apart in magnitude and opposite in sign | — |
| [`CH-0120`](CH-0120-an-influence-bank-assumes-a-lattice-with-no-self-load.md) | [`C-0058`](../claims/C-0058-non-uniform-coupling.md) (`InfluenceSurrogate`, `latticeInfluenceSurrogate`) and [`C-0063`](../claims/C-0063-upward-root-placement.md) (`UpwardRootInfluenceBank`) | [`C-0104`](../claims/C-0104-row-end-prestrain.md) (`T-172`) | **OPEN**, with a remedy already implemented and tested on the caller's side (`OrigamiGrillage.withoutPrestrain`), and **not** on the bank's |
| [`CH-0121`](CH-0121-two-of-the-three-phase-demand-sets-are-identical-not-disjoint.md) | [`C-0102`](../claims/C-0102-crossover-phase-selection.md)'s **title line** — *"at the buildable 38.08 nm the three sets go from **10 / 10 / 2** to **2 / 2 / 2** and stay **disjoint** — richest `{0, 16}`, eight-column and centro-symmetric `{8, 24}`"* | **a set-theoretic word used of three sets when it is true only of one against the other two.** At 38.08 nm `C-0102`'s own Deliverable 1 table gives *most columns* = `{8, 24}` and *centro-symmetric* = `{8, 24}`. Two sets that are equal are not disjoint; they have both their elements in common. The pair was never disjoint at 40.00 nm either — `C-0090` states that `C-0015`'s ten eight-column phases **collapse onto** `C-0063`'s two, i.e. `{8, 24}` was a **subset** of the ten — so the collapse takes a nested pair to an equal pair, and the only disjointness anywhere in the ledger is the richest set against the rest | **STANDS as a statement about the headline, not about the claim.** No re-run, no re-derivation, nothing withdrawn |
| [`CH-0122`](CH-0122-the-prestrain-threshold-is-a-secant-not-a-triangle-inequality.md) | [`C-0104`](../claims/C-0104-row-end-prestrain.md), Deliverable 1 and its `cheapBounds` record *"the prestrain at which C-0090's placement reaches T-5b's 0.10"* | [`C-0107`](../claims/C-0107-row-end-prestrain-value.md) (`T-182`) | **OPEN, and it does not overturn a verdict.** `C-0104`'s number remains a valid upper bound on the crossing, but by **convexity** and not by the inequality it names; the inequality it names gives **11.5188°**, which is 1.34× tighter and moves the threshold *away* from every candidate value |
| [`CH-0123`](CH-0123-a-count-term-and-a-phase-term-are-one-of-two-readings-of-one-journey.md) | [`C-0103`](../claims/C-0103-path-count-at-fixed-geometry.md)'s **Deliverable 2** and the third row of its verdict — *"the count term is +12.86 % and the phase term is −19.0 %, and the phase is the larger of the two"* — and the same sentence where it appears in its *"What this does to the standing claims"* row for `C-0074` | **a two-factor move has two splits and only one total, and `C-0103` reports one split as if it were the decomposition.** `C-0108` grades all four corners of the 2 × 2 under one ensemble per phase: count first gives **+12.8596328 %** then **−19.267547 %** (reproducing `C-0103` exactly), phase first gives **−11.4787354 %** then **+2.9293362 %**. The two orderings share a total of **−8.88564999 %** at a path disagreement of **0.0**, and differ by an interaction of **−8.79880284 %**. On the **search-free** family run at both phases the same construction gives **+10.8397678 %** / **−3.92538749 %** against **+1.92730307 %** / **+4.47532136 %** — the phase term is **adverse** in one reading and **favourable** in the other. And over the whole 32-phase × 6-count grid the **interaction carries more of the variation (9.79218189 %) than the phase main effect does (7.83610301 %)**, with a worst additive residual of **1.53234725×** the count main effect it splits | — |
| [`CH-0124`](CH-0124-the-four-layer-variant-is-a-mixed-state-not-a-bound.md) | [`C-0006`](../claims/C-0006-tile-load-distribution-and-flatness.md), the `four-layer honeycomb, rigid coupling` row of its variant table, and `structure/OrigamiSheet.kt`'s `InterlayerCoupling.RIGID` | methodological — a quantity presented as a bracket end is a mixture of the two ends, one taken in each direction | **OPEN** |
| [`CH-0125`](CH-0125-the-four-layer-brick-is-mis-specified-in-three-ways.md) | [`C-0093`](../claims/C-0093-shared-body-coupling.md), the `four-layer honeycomb brick` body of its tie ladder, and the statement *"a buildable body loses where the rigid limit wins"* | the body's three plate rigidities are each taken at a reading `T-191` shows to be wrong, and the three corrections do not share a sign | **OPEN** — the re-solve is owed and is **not** performed here |
| [`CH-0126`](CH-0126-a-tall-layer-breaks-section-3s-own-effort-point.md) | [`C-0050`](../claims/C-0050-desired-stroke-reach.md)'s escape section — the sentence *"§3's own tile-thickness row notes the effort point 'may sit ~20–25 nm above the electrode', so the geometry is not absurd"* — and the same sentence as it has been carried into `TASKS.md`, `ANSWERS.md` §6 row 2 and `DECISIONS-FOR-NDI.md` decision 2 | `ActuatorGeometry.effortPointHeight(L₀) = L₀ + tileThickness + leverAttachmentHeight`, with §3's own 10 nm tile. At §3's three layer heights and a 5 nm attachment that is **20 / 22 / 25 nm** — the §3 band reproduced at both ends, which is exactly why `CLAUDE.md` records the row as *forcing a constant attachment height and fixing it at 5 nm*. At 17–26 nm the same expression gives **32–41 nm**, and 27–36 nm at a zero-length attachment. The row `C-0050` cites as permission is the row that refuses | — |
| [`CH-0127`](CH-0127-the-tall-layer-escape-is-kinematic-not-actuated.md) | [`C-0050`](../claims/C-0050-desired-stroke-reach.md)'s *"What is established"* table, row 3: *"…unreachable **in physics**" — **NO, and it is false.** A taller layer delivers it"*, and the escape table's framing as *"the escape is a specification change, and it is priced"* | the row is established by a **root of the layer's own load**, `P(L₀ − 10 nm)·A = 100 pN` at `σ = 0.024 nm⁻²` — a statement about a **compression**, not about an **actuator**. `C-0110` reproduces all six of its heights to ≤ 2.0e−4 and then asks the question the root does not contain: the deepest gap across which the field delivers 100 pN is **13.6989 nm at 0.5 mM, 11.8724 at 1 mM and 10.1299 at 2 mM** (1.0 V; 1.23 V buys 0.05 nm more), every one of them **below** the bottom of the escape's own 16.63 nm. On the 96 tall device-B states the coupled equilibrium is admitted at **1**, and on the 96 device-A states — §3's *acceptable* clause — at **0**, all 96 refused because no bias holds the tile at a 3 nm stroke | — |
| [`CH-0128`](CH-0128-inverse-debye-length-called-with-a-bjerrum-length.md) | [`C-0021`](../claims/C-0021-zero-bias-resting-position.md) `M4` and [`C-0023`](../claims/C-0023-two-sided-coupling.md), specifically the **low end** of their van der Waals bracket | **methodological.** `MagnesiumChlorideBuffer.inverseDebyeLength`'s first parameter is a **temperature**, and both call sites pass a **Bjerrum length**. The resulting `κ` is 20.5× the physical one and `e^(−2κd)` saturates to `2e−23`, so the zero-frequency Hamaker term is not screened, it is removed. | **OPEN** — the repair is **not** performed here, because it moves two committed result files and belongs with their re-emission |
| [`CH-0129`](CH-0129-the-graded-field-is-compared-against-the-other-overall-sign.md) | [`C-0107`](../claims/C-0107-row-end-prestrain-value.md), Deliverable 4 and its *"Two readings, and they disagree about the verdict"* passage | [`C-0112`](../claims/C-0112-interior-crossover-prestrain.md) (`T-190`) | **OPEN, and it strengthens the claim it is against.** `C-0107`'s **0.0922622** is reproduced here at **1.1e−10** and its flat verdict stands; what does not stand is the pairing that explains it, and correcting the pairing makes the interior term **2.68×** larger than published |
| [`CH-0130`](CH-0130-the-overall-sign-of-the-corrugation-is-undetermined.md) | [`C-0107`](../claims/C-0107-row-end-prestrain-value.md), Deliverable 3 (*"the sign … is a lattice congruence"*) and Deliverable 4's single graded row | [`C-0112`](../claims/C-0112-interior-crossover-prestrain.md) (`T-190`) | **OPEN, and it is DISCHARGED on the number**: `T-190` reads the missing state at **0.0910197** of the free stroke, flat, so the verdict survives at both signs. What does not survive is the claim that the sign was *determined* |
| [`CH-0131`](CH-0131-t-157-was-re-emitted-before-its-own-input.md) | [`C-0092`](../claims/C-0092-large-rotation-arm-branch.md)'s margin-movement deliverable — its **`A5`** verdict clause *"the margins move by 1.0000–3.3380×"* — the `biasMarginInC0084` and `marginMovement` columns of its fold table, its findings line *"the bias margin … moves by a factor of 1.0000–3.3380×"*, and the derived readings *"1.0007–1.1620× … at the other four the point-ion boundary binds and the movement is 2.84–3.34×"*. And, as the cause, [`C-0101`](../claims/C-0101-re-emitting-what-the-repair-moved.md)'s re-emission of eleven files | **a consumer was re-emitted before its own producer, inside one commit.** `C-0101` (`399333a`) re-emitted `T-149` (284 fields moved) **and** `T-157` (14 fields moved). `LargeRotationArmBranchStudy.c0084Margins()` reads `gpd/results/T-149-recommended-element-fold.json` at run time, so the order decides the answer — and the committed `T-157` reproduces the **pre-`C-0101`** `T-149` margins **digit for digit at all twelve rows**, while a re-run today reproduces the current ones, also digit for digit. The residual has stood unread for six iterations | — |
| [`CH-0147`](CH-0147-the-recommended-tile-is-folded-from-a-different-scaffold.md) | [`C-0119`](../claims/C-0119-honeycomb-raster-width.md) §5, the row *"p8064 — designs i, iii, v, **including ours**"*, and its finding that *"the remainder is **1 344 nt** rather than 529 on the standard scaffold for this design"* | **supersession** — the statement was true of the tile `C-0119` was written on and stopped being true two claims later, without anything in `C-0119` becoming wrong on its own terms. `CLAUDE.md`'s third drift class: *"a SUPERSEDED STANDING VALUE reads `CITED`"* | **OPEN** |
| [`CH-0148`](CH-0148-the-remainder-and-the-sheet-do-not-condense-alike.md) | [`C-0086`](../claims/C-0086-seamless-scaffold-routing.md) Deliverable 3, the row *"the unpaired remainder at 112 bp … a **33.3 nm** ideal coil, carrying **1.66×** the sheet's own backbone charge"*, and the same phrase in its headline and in `gpd/results/T-151-scaffold-routing.json` | **methodological** — a ratio of two charges taken before the renormalisation that this repository applies to **both** of them, and applies at a factor that differs between them by 3.35–4.12× | **OPEN** |
| [`CH-0149`](CH-0149-the-format-defect-census-is-off-by-a-file-a-study-and-a-result.md) | [`C-0125`](../claims/C-0125-scaffold-remainder.md), its *"Spawned"* paragraph and the `T-195` row of `TASKS.md`: *"its sweep of `src/` finds **14 defects in 6 files from 5 studies, all committed**, with raw `%` conversions in **8 result files**"* | **arithmetic** — a census stated as a count, checkable in one pass, and wrong in all three of its counts. Plus **a tool defect**: one of the fourteen rows is the checker's, not the tree's | **OPEN** |
| [`CH-0150`](CH-0150-twelve-of-the-fourteen-were-visibly-broken-and-the-silent-failure-diagnosis-does-not-explain-them.md) | the diagnosis carried by [`C-0125`](../claims/C-0125-scaffold-remainder.md) and the `T-207` row of `TASKS.md`: *"**Java silently ignores extra arguments**, so the class does not throw — it emits a grammatical sentence with the wrong numbers in it"*, and `CLAUDE.md`'s own entry for this family | **generalisation from one instance** — a failure mode read off the single most alarming defect and attributed to the class. The cheap bound that separates the two costs one pass and is already a column of the checker's own output | **OPEN** |
| [`CH-0151`](CH-0151-an-oblique-helix-has-two-free-azimuths-not-one.md) | [`C-0122`](../claims/C-0122-honeycomb-station-lattice.md) — its census table (`15 × 4` → **90** stations, `10 × 6` → **60**), and the sentence that licenses it: *"**It fixes which helices carry the perpendicular root and which carry two oblique ones; it does not change the COUNT**, because every top-face helix has exactly one free direction either way"* (`HoneycombLattice.pointsDirectlyOut`'s KDoc, repeated in the claim's §5) | methodological — a **parity** correctly identified and then used to justify a **count** it does not support. The parity says *which* azimuths are free; the count needs *how many*, and the two sublattices differ in that number as well as in the direction | ~~**raised.** The correction is **FAVOURABLE** and `C-0122`'s headline findings all survive it~~ **OVERTURNED** by [`C-0141`](../claims/C-0141-honeycomb-station-lattice-and-placement.md) (`T-219`) via [`CH-0175`](CH-0175-the-face-azimuth-is-thirty-degrees-and-there-is-one-of-it.md): its 132 and 90 do not hold, and `C-0122`'s 90 and 60 are restored at departure `0.0` while every reason given for them is withdrawn |
| [`CH-0152`](CH-0152-the-defect-was-read-tested-and-misdescribed-not-unread.md) | [`CH-0150`](CH-0150-twelve-of-the-fourteen-were-visibly-broken-and-the-silent-failure-diagnosis-does-not-explain-them.md)'s diagnosis, carried into [`C-0127`](../claims/C-0127-format-string-repair.md)'s headline: *"**This defect class damages the machine-readable artifact and spares the human one**"*, and *"the reason they survived is that **nobody re-reads a result file once the claim is written**"* | **a counter-instance found by `CH-0150`'s own repair.** `T-148`'s saturated statistic was computed correctly, emitted correctly, **unit-tested**, read, and **described in a sentence written around it** — and the sentence says the opposite of what the number means. Every clause of the *"unread"* answer is false of it, and no text gate can reach the class | **OPEN** |
| [`CH-0153`](CH-0153-a-statistical-power-gate-discharged-by-a-statistic-that-is-identically-zero.md) | [`C-0087`](../claims/C-0087-position-dependent-staple-dropout.md)'s gate-4 row — *"with the binomial standard error quoted beside every probability … and the binomial error to fall as `1/√n`"*, verdict `PASS` — and its `P3` predicate *"a distribution, not a point"* | **the convergence property is demonstrated at a state the study does not occupy.** The `1/√n` fall is tested at `p̂ = 0.5`; the sweep reports `p̂ = 1.0` at **25 of 60** cells and at **5 of 5** convergence sample counts, where `√(p̂(1−p̂)/n)` is identically zero and carries no `n` at all. **302 records in 7 files** carry the same degenerate statistic | **OPEN, and it moves no verdict** — a one-sided bound *strengthens* every failure reading it replaces |
| [`CH-0154`](CH-0154-the-rule-lives-once-was-true-of-one-package.md) | [`C-0129`](../claims/C-0129-result-file-hygiene.md)'s `T-209` deliverable — *"The rule is about a **record type**, so it now lives once, by name"* — and its census of what is left, *"199 fields in 27 files"* | **the mechanism and the census are both narrower than the rule, and three measurements say so.** A bare `departure` appears under **eleven** parents and `T-193`'s is in **volts**; `roundedForActuatorResult` has **no `digitsByKey` parameter at all**, so six files could not have obeyed the rule by any edit at their own emission sites; and the census keys on **one spelling of four**, 199 fields in 27 files against **601 in 63** | **UPHELD and repaired in the same task** |
| [`CH-0155`](CH-0155-the-degenerate-discharge-is-in-five-claims.md) | [`CH-0153`](CH-0153-a-statistical-power-gate-discharged-by-a-statistic-that-is-identically-zero.md)'s scope, and the four further claims carrying the identical gate-4 discharge: [`C-0089`](../claims/C-0089-dropout-robust-placement.md), [`C-0093`](../claims/C-0093-shared-body-coupling.md), [`C-0098`](../claims/C-0098-shared-body-placement-and-distribution.md), [`C-0103`](../claims/C-0103-path-count-at-fixed-geometry.md) | **the challenge is a population too, and the population is CLAIMS.** *"A binomial standard error beside every exceedance"* discharges the **statistical power** gate in five claims, and the statistic is exactly `0.0` at 25 of 60, 16 of 24, 16 of 39, 16 of 25 and 16 of 21 cells. `C-0129` measured the population of **records** and left the population of **discharges** unmeasured | **UPHELD, and repaired for all five** — no exceedance, percentile, boolean or verdict moves |
| [`CH-0156`](CH-0156-the-rim-charge-bracket-is-a-bigger-tile.md) | [`C-0022`](../claims/C-0022-tile-edge-load-profile.md) — its **falsifier 5** paragraph (*"Taking the rim from uncharged to the face areal density moves the fitted depth from −0.2906 to −0.1575, a factor of 1.85. **The two readings are both defensible** — the tile's charge is volumetric and the surface it is smeared onto is a convention"*), the validity-range line *"The rim charge is a 1.85× bracket on the depth"*, and the *"Still open"* item *"The rim charge is unsourced"* | **arithmetic, and it costs one division** — a charge-conservation ledger, checkable without a solver | **OPEN.** No `C-0022` verdict moves; what moves is the exposure fourteen downstream claims carry |
| [`CH-0157`](CH-0157-the-rim-sweep-varied-the-smaller-of-two-terms.md) | [`C-0022`](../claims/C-0022-tile-edge-load-profile.md) — the *method* of its falsifier 5, i.e. that varying `rimChargeDensity` alone measures the sensitivity of the collar to the rim convention, and the conclusion drawn from it that *"the rim charge … is load-bearing after all"* | **methodological** — a two-term perturbation swept on one term, where the unswept term is 1.41× the swept one and runs the other way; plus a **representation** failure in the quantity the sweep was read on | **OPEN.** It strengthens `C-0022`'s headline and withdraws its bracket |
| [`CH-0158`](CH-0158-the-admissible-width-list-assumes-an-uncorrected-twist.md) | [`C-0086`](../claims/C-0086-seamless-scaffold-routing.md) Deliverable 4 — *"At the 1.5-turn (16 bp) spacing this sheet is built on, three half-turns are 16 bp, so an odd number of half-turns is an **odd multiple of 16 bp**"* — and the 112 bp row it selects, carried by [`C-0090`](../claims/C-0090-buildable-raster-width.md), [`C-0104`](../claims/C-0104-row-end-prestrain.md), [`C-0107`](../claims/C-0107-row-end-prestrain-value.md) and [`CH-0101`](CH-0101-the-nominal-tile-width-is-not-a-buildable-raster-width.md) | **methodological** — a quantisation derived with one of its two inputs held at a value the same programme records as a defect. *"Three half-turns are 16 bp"* is true only at 33.75 °/bp, which is exactly what `C-0107`'s 17.15–24.98° row-end prestrain is a complaint about; at B-DNA's twist the admissible lengths are 16, 47, 79, **110**, 142 bp and **112 is not among them** | **OPEN.** Neither the theorem nor the arithmetic of `C-0086` moves; what moves is that *"the buildable widths"* is one set **per design twist**, and the programme has never chosen which twist the Gen-1 tile is built at |
| [`CH-0159`](CH-0159-a-zero-plan-clearance-cannot-survive-a-width-change.md) | [`C-0090`](../claims/C-0090-buildable-raster-width.md)'s arm ceiling — *"`C-0085`'s quantisation to **24 rises** makes the clearance exactly zero and restores every one of those numbers bit for bit"* — and [`C-0085`](../claims/C-0085-collinear-stacking-clearance.md)'s quantisation | **methodological** — a margin reported as *exactly zero* and read as sufficient, with no tolerance stated on the variable it is zero in. At the twist-corrected 110 bp row the 34-root centro-symmetric family is **empty** at 24 rises and needs **23** | **OPEN, and it costs one base pair rather than a verdict** |
| [`CH-0160`](CH-0160-a-stratified-robustness-argument-is-quantified-over-a-widths-own-strata.md) | [`C-0108`](../claims/C-0108-count-phase-interaction.md)'s per-stratum deliverable — *"THE INTERACTION IS NOT A HOST EFFECT, BECAUSE IT SURVIVES INSIDE EVERY STRATUM"* — and the `C-0102` row of its *"What this does to the standing claims"* table | **a quantifier over a census, and the census is a property of the tile WIDTH.** At `C-0086`'s buildable 38.08 nm three of `C-0102`'s four strata have no counterpart: the eight-column set is **{8, 24}** instead of ten, the *eight-column but not centro-symmetric* set is **empty**, and the richest-inventory set is **{0, 16}**. Re-stratified there the residual ratios are 0.29785241, 0.305403211 and **2.14777246** | **OPEN, and the conclusion is UPHELD on the new ground** — no `C-0108` number moves; what moves is which lattice *"every stratum"* ranges over |
| [`CH-0161`](CH-0161-the-guard-that-went-live-is-the-existence-not-the-value.md) | [`CLAUDE.md`](../../CLAUDE.md)'s two `CrossoverLayout.EDGE_MARGIN` entries, and specifically the instruction *"Sweep the guard (here 0.05 nm / half a rise / one rise: 0.32 % spread) rather than assuming it is still inert at the geometry you are running"* | **the guard's VALUE and the guard's EXISTENCE are different variables.** Swept over exactly those three values the guard leaves **one** lattice over all 32 phases of the 38.08 nm tile (worst station displacement **`0.0` nm**) and **two** at 40.00 nm, moving the column count at phases 6, 10, 22, 26. The recommended sweep is worth `0.0` refused and 0.4442352 % of a level admitted, against the **171 %** (0.0621469105 → 0.168371808) the entry is about | **OPEN, and it makes the entry stronger** — the general lesson survives and gains `C-0100`'s binary |
| [`CH-0162`](CH-0162-three-agreeing-runs-are-a-draw-not-a-verdict.md) | [`C-0131`](../claims/C-0131-departure-and-saturation-audits.md) §6: *"Three independent runs agree on `ranges[1]` to the last digit and the committed file disagrees with all three. So `gpd/results/T-129-range-robust-placement.json` at `HEAD` **is not reproducible from `HEAD`'s own code**"* | **three agreeing runs are a DRAW, not a verdict.** Over ten independent emissions `ranges[1]` is two-valued, 7:3, so three runs agree by chance **34 %** of the time; and `git archive cf7de13`, run unmodified today, lands on the reading iteration 13 did **not** emit, which excludes the tree | **PARTLY UPHELD** — the observation and its numbers reproduce exactly; the inference does not, and the file **is** reproducible up to a two-valued manifold |
| [`CH-0163`](CH-0163-a-worst-field-movement-is-an-emission-choice.md) | the repository-wide practice of reporting a re-run difference as *"N fields moved, worst `X`"* — `C-0131` §6, and the same shape in `C-0101`, `C-0110`, `C-0129` | **the size is set by the emitted field list.** One change of winning start reads `5.9522e−3` on `max_i k_i`, `7.5482e−4` on a peak support force at the **same** argmin and `2.6197e−3` on the objective; and the block carrying the corpus's widest VALUE movement (`subsets[*]`, `4.5745e−3`) was never quoted, because it emits no argmin functional at all | **OPEN — a methodological correction.** No named number is wrong; report a count **by kind** beside the scalar |
| [`CH-0166`](CH-0166-a-level-correction-is-not-exactly-absorbed-into-the-bias.md) | [`CH-0035`](CH-0035-the-edge-correction-cannot-reach-the-window-edge.md)'s Ground 2, restated in [`C-0033`](../claims/C-0033-collar-on-the-equilibrium-path.md) and inherited by `C-0027`, `C-0091` and `TASKS.md`: *"a multiplier on the LEVEL of `\|F_es\|` is absorbed entirely into the bias and reaches `k_es` not at all … it is exactly zero at the operating point"* | **the identity has a premise that is not in it — that the decay length is bias-independent.** Solved at `C-0017`'s binding state with the bias re-solved at each level, `ℓ` runs **2.2593 → 3.3158 nm** and the residual reaches **12.585 pN/nm**, `2.88×` the gradient threshold; the slope is **−3.61 to −8.70 pN/nm per e-fold of level** | **OPEN, and the residual is FAVOURABLE** — no verdict moves, and `C-0033`'s own collar is worth `−0.633 pN/nm` of floor it recorded as exactly zero |
| [`CH-0167`](CH-0167-the-123-214-per-cent-is-a-level-and-it-is-quoted-as-an-error-bar-on-a-stiffness.md) | the corpus-wide use of [`C-0005`](../claims/C-0005-mean-field-screening-validity.md)'s 123–214 % one-loop deviation as the uncertainty a **stiffness** margin must be read against — **66 occurrences in 31 claims**, plus 8 in `ANSWERS.md`, 10 in `TASKS.md` and 6 in `DECISIONS-FOR-NDI.md` | **a ratio of a correction to a leading term is an error bar on the thing that term IS.** The deviation is a **level**; the margin is a stiffness read at a **force-pinned** point where the level is absorbed into the bias (all 54 of `C-0017`'s states, relative spread `0.0`). It is also a LIKE-charged, counterion-only, salt-free quantity, and for *oppositely* charged walls the expansion has an exponentially larger validity bound with MC at `Ξ` up to 86 | **OPEN.** No `C-0005` number is wrong — five reproduced here to 0.14–0.47 % — and no verdict reverses; the replacement qualifier is `C-0137`'s two thresholds, queued as `T-220` |
| [`CH-0168`](CH-0168-a-residue-published-without-its-own-cost.md) | [`C-0131`](../claims/C-0131-departure-and-saturation-audits.md) §7 — *"The 31-file residue is queued as `T-214` with its per-file counts"* — and the `T-214` row of [`TASKS.md`](../../TASKS.md), which prices that residue out of `C-0131`'s `costPartition` | **one `python3` over the upstream result file, run before anything else.** `gpd/results/T-212-departure-and-saturation-audits.json` → `costPartition` carries **35** tags; `departureAudit.residueByFile` carries **31**; their intersection is **empty**. The partition is a partition of what `T-212` **closed** | **UPHELD, and repaired in the same task** — the residue's own cost is measured in `tools/T-214-costs.json` and published in `gpd/results/T-214-departure-rule-scope.json` |
| [`CH-0169`](CH-0169-four-spellings-of-eleven-and-four-implementations.md) | [`C-0131`](../claims/C-0131-departure-and-saturation-audits.md) (`T-212`) — the KDoc of `DEPARTURE_SPELLINGS`, *"Every spelling the corpus uses for a departure **inside** a `DEPARTURE_RECORDS` record … all four spellings a census of `gpd/results/` finds, rather than whichever one the last repair happened to look at"* — and §3's *"the rule now lives once **including in the package it could not reach**"*, and [`CH-0154`](CH-0154-the-rule-lives-once-was-true-of-one-package.md)'s own scope | **two censuses, neither of which needs a solve.** (1) A walk of the 124 committed result files finds **seven more** leaf keys inside a `reproductions`/`convergence` record carrying the same kind of quantity — **62 fields in 6 files** above two significant digits, of which **50 in 5** are the rule's own quantity under another name. (2) A `grep` of `src/main/kotlin` finds **six** rounding entry points, of which `T-212` left **four** with no `digitsByKey` parameter at all — the identical defect `CH-0154` measured on `actuator/` and named there | **UPHELD.** Ground 2 is **repaired** in `T-214` (three of the four delegate; the fourth is named and measured clean). Ground 1 is **measured, published with a per-key judgement, and deliberately not swept** — see below |
| [`CH-0164`](CH-0164-a-seamless-row-has-a-parity-not-a-phase.md) | [`C-0133`](../claims/C-0133-twist-corrected-raster-row.md) *Still open* item 1 — *"`C-0090`'s collapse of `C-0015`'s ten eight-column phases to two is an identity in the **uniform** 16 bp pitch, and a twist-corrected row has no uniform pitch. … **That is the largest single gap here**"* — and the framing it inherits from [`C-0090`](../claims/C-0090-buildable-raster-width.md) Deliverable 2, *"the row-end scaffold crossover … is a lattice point only at the phases `b ≡ 8 (mod 16)`, i.e. **8 and 24**"* | [`C-0136`](../claims/C-0136-mixed-domain-phase-and-honeycomb-twist.md) (`T-216`) | **OPEN, and it makes `C-0133`'s own position stronger.** No number of `C-0090` or `C-0133` moves; `C-0133`'s two published 110 bp enumerations are reproduced here at relative departure `3.1e−10` and `8.5e−10`. What changes is *what a phase is* on a seamless row, and therefore what does and does not transfer to a mixed-domain one |
| [`CH-0165`](CH-0165-an-integral-scaffold-lattice-is-necessary-not-sufficient.md) | [`C-0119`](../claims/C-0119-honeycomb-raster-width.md) §2 — *"The honeycomb quantises its half turn to 5 bp, not 5.25. So the scaffold-crossover lattice is `7k ± 5`, which on a 112 bp row is `{2, 5, 9, 12, 16, 19, 23, …}` — **integral**, and the routing question is open again rather than closed"* — and the headline verdict *"The honeycomb scaffold-crossover lattice is **integral** (`7k ± 5` bp), so the four-layer tile is **drawable**"*, with the 112 bp row it carries from [`C-0109`](../claims/C-0109-four-layer-tile.md) | [`C-0136`](../claims/C-0136-mixed-domain-phase-and-honeycomb-twist.md) (`T-217`) | **OPEN, and `C-0119`'s conclusion SURVIVES conditionally.** No arithmetic of `C-0119` is wrong and its integrality result is reproduced here at departure `0.0` on all four constants and all 32 offsets. What is challenged is that *"drawable"* is stated flat where the derived condition admits **6 of 21** residues, splits into **two disjoint triples**, and puts `C-0119`'s own 112 bp row inside **one** of them |
| [`CH-0172`](CH-0172-a-honeycomb-x-raster-carries-both-turn-senses.md) | [`C-0119`](../claims/C-0119-honeycomb-raster-width.md)'s *"the four-layer tile is **drawable**"* read at a uniform 112 bp, and [`C-0136`](../claims/C-0136-mixed-domain-phase-and-honeycomb-twist.md)'s 119 bp = 40.46 nm and its `Δ = 1` nearest-width cell | The honeycomb's residue condition needs a turn sense and an x-raster does not have one: the corrugation is a **theorem** and the scaffold's axial direction alternates with it, so `Δ_eff` is constant along a row interior and the `m − 1` row turns break it — **sense 2 on 30 helices and sense 1 on 28**, and the two admissible triples are disjoint | **OPEN.** No uniform honeycomb width exists; the remedy is a **3 bp = 1.02 nm** stagger, and at **112 / 108 bp** the tile's extent is **39.44 nm, `−1.40 %`**, better than the square lattice's `−4.80 %` |
| [`CH-0173`](CH-0173-the-built-block-turns-on-loops-not-crossovers.md) | [`C-0119`](../claims/C-0119-honeycomb-raster-width.md) §5's scaffold-budget table and its *"p8064 — designs i, iii, v, including ours"*, and through it [`C-0109`](../claims/C-0109-four-layer-tile.md)'s 6 720 of 7 249 at **92.7 %** | The only folded instance of this cross-section allots **126 nt per helix of which 28 are unpaired turn loops** — 22.2 % of its scaffold — so a budget written on the paired length has no line for the turns; and `60 × 126 = 7 560` exactly, so design (i) is a **p7560** design, not a p8064 one | **OPEN.** At the built allowance the widest four-layer tile is **92 bp = 31.28 nm** from M13 and **106 bp = 36.04 nm** from p8064; `C-0119`'s uniform 112 bp × 60 needs 8 400 nt and fits **neither** |
| [`CH-0174`](CH-0174-the-four-layer-cross-section-is-not-a-honeycomb.md) | [`C-0120`](../claims/C-0120-cross-section-comparison.md)'s footprint table (*"`15 × 4` … 38.08 × 38.04, essentially §3's"*; *"`10 × 6` … **0.666666667** of it"*) and its headline *"it costs a third of the footprint"*, and through it [`C-0109`](../claims/C-0109-four-layer-tile.md)'s cross-section as carried by `C-0116`, `C-0118`, `C-0122`, `C-0123` and `C-0128` | **one multiplication.** A honeycomb spends `3√3/4 · d²` = **8.35449857 nm²** of plan per helix and the assumed cross-section spends `d²` = 6.431296 — **1.29903811×** — because the in-plane row pitch is `3d/2` and the layer pitch `d√3/2` and only their **product** is the cell. So every `edgeY` is exactly **1.5×** too small, `10 × 1.5 = 15` makes the corrected `10 × 6` carry the `edgeY` the corpus gave `15 × 4`, and the footprint ordering **reverses**: `15 × 4` is **1.40084263** of §3's 40.35 and `10 × 6` is **0.929467162**. Re-solved, `C-0116`'s threshold on `15 × 4` moves `0.0788618807 → **0.276970522**`, **inside** the measured 0.26–0.33 band | **raised** by [`C-0141`](../claims/C-0141-honeycomb-station-lattice-and-placement.md) |
| [`CH-0175`](CH-0175-the-face-azimuth-is-thirty-degrees-and-there-is-one-of-it.md) | [`C-0128`](../claims/C-0128-oblique-attachment-root.md)'s *"the honeycomb's own **60°**"* and its derivation *"a top-face helix of the second sublattice has `±60°` free"*, and [`CH-0151`](CH-0151-an-oblique-helix-has-two-free-azimuths-not-one.md)'s corrected census of **132** and **90** | methodological — **a free azimuth is a lattice neighbour that is ABSENT**, and neither document derives the site set. On a full `m × n` block the `±60°` pair points at the other sublattice's helices **in its own x-raster row**, which are present; the pair is free only in a **half-row termination** none of the seven published designs has. The real face gives every one of its `m` helices **exactly one** rooting azimuth, at **30°**, sign alternating — so `C-0122`'s **90**/**60** are restored, there is **no perpendicular root at all**, and `C-0128`'s rigid-body cost falls `6.017× → **2.67233333×**` at the same anisotropy | **raised** by [`C-0141`](../claims/C-0141-honeycomb-station-lattice-and-placement.md) |
| [`CH-0170`](CH-0170-a-common-factor-is-not-an-error-bar-either.md) | The use of [`C-0005`](../claims/C-0005-mean-field-screening-validity.md)'s 123–214 % one-loop deviation as the uncertainty a **ratio** must be read against — [`C-0022`](../claims/C-0022-tile-edge-load-profile.md), [`C-0026`](../claims/C-0026-one-row-per-duplex.md), [`C-0047`](../claims/C-0047-single-column-flatness.md), [`C-0100`](../claims/C-0100-collar-at-the-buildable-width.md) ×2 and [`C-0132`](../claims/C-0132-cut-rim-charge.md) ×2. **Not the same objection as [`CH-0167`](CH-0167-the-123-214-per-cent-is-a-level-and-it-is-quoted-as-an-error-bar-on-a-stiffness.md)**, whose mechanism is a force balance | Each of the seven quantities is a quotient of two numbers taken from the **same** field at the same state, so a level multiplier is a **common factor** and divides out. **Two of the seven state that themselves in the sentence beside the banner** — `C-0100`'s *"a common factor rather than an error on it"* and `C-0026`'s *"one common factor and no ratio at all"* | **OPEN, and it is a NARROWING**: all seven over-state their own exposure. Raised by [`C-0139`](../claims/C-0139-two-quantities-quoted-out-of-scope.md) (`T-220`), falsifier `F4` |
| [`CH-0171`](CH-0171-an-irreproducible-number-was-substituted-for-a-reproducible-one.md) | [`CLAUDE.md`](../../CLAUDE.md)'s *"0.0372–0.0619 against **0.1247** over the five"* and [`ANSWERS.md`](../../ANSWERS.md) §3's *"a real minimax reaches **0.1247**, still 1.25× outside"*, both citing [`C-0064`](../claims/C-0064-robust-distribution.md), whose number is **0.1254** | `0.1247` is [`C-0058`](../claims/C-0058-non-uniform-coupling.md)'s minimax at **one** state, not a worst-of-five — and it is the **worst-determined** number in `T-113`, 0.1247 / 0.1385 / 0.1403 over three emissions (**11.2 %**), where `C-0064`'s `0.1254` is a converged objective (`1e−4`). `1.25 × 0.10` is 0.1254 | **OPEN, no verdict moves**, both carriers corrected. Raised by [`C-0139`](../claims/C-0139-two-quantities-quoted-out-of-scope.md) (`T-226`) |
| [`CH-0180`](CH-0180-the-scaffold-pairing-contradicts-its-own-paper.md) | [`C-0125`](../claims/C-0125-scaffold-remainder.md)'s *"p8064 \| 8 064 \| **READ DIRECTLY**; designs (i), (iii), (v)"* and its **1 344 nt** remainder, and [`C-0119`](../claims/C-0119-honeycomb-raster-width.md) §5's *"p8064 — designs i, iii, v, **including ours**"* | **a SOURCE contradiction, not a reading error.** The caDNAno paper's main text states a rule — *"folded either from a **7560-base scaffold into 60 parallel helices** or from an **8064-base scaffold into 64 parallel helices**"* — and its Methods list *"i: p8064, ii: p7560, iii: p8064, iv: p7560, v: p8064, vi: p7560, vii: p7560"* **agrees with that rule at 6 of its 7 designs and disagrees at exactly one — design (i), which is `15 × 4` = 60 helices and is ours.** Under the Methods reading design (i) leaves **504 nt** the paper's own 126-bases-per-helix accounting has no line for | **raised** by [`C-0144`](../claims/C-0144-honeycomb-correction-supersession.md) (`T-234`). The scaffold is **NOT ESTABLISHED**; `C-0125`'s bound survives in form and its remainder is not the published one, and `C-0140`'s p7560 derivation is upheld on **wider** ground than it knew |
| [`CH-0181`](CH-0181-the-supersession-sweeps-own-ground-moved.md) | [`C-0126`](../claims/C-0126-four-layer-supersession.md) §2's stated ground for its eight CONTINGENT items — *"The four-layer tile is flat at **0.0577199433** of the stroke with no coupling at all … So on the thicker tile the quantity these tasks optimise is **not the binding one**"* | At the corrected honeycomb cross-section ([`C-0141`](../claims/C-0141-honeycomb-station-lattice-and-placement.md)) that reading is **`15 × 4` 0.0978155002 free and 0.101759944 at the measured band's low end, which FAILS `T-5b`**, and **`10 × 6` 0.0240648102**, flat across the whole band. The ground holds on one cross-section and fails on the other, and `C-0126` states it of *"the four-layer tile"* | **raised** by [`C-0144`](../claims/C-0144-honeycomb-correction-supersession.md) (`T-234`). **No item's class changes and no item is struck** — the eight are **REPRICED**: on a `15 × 4` tile a coupling is back on the table, so they are live on two of three readings rather than one |
| [`CH-0178`](CH-0178-a-criterion-with-no-root-returned-its-own-bracket-floor.md) | `ChargedSurface.loopExpansionValidityGap` / `meanFieldValidityGap`, and the field `gpd/results/T-6-mean-field-screening-validity.json` -> `surfaces[2].loopExpansionValidityGap = 0.9240787673730241` | `D̃/ln D̃` has a **global minimum of `e`**, so for `Ξ ≤ e` the criterion holds at every separation and there is **no root**. The bisection had no bracket, never straddled, and returned its own low endpoint `e·μ` dressed as a length — `e × 0.33995 = 0.92416` to four figures. Found by emitting the criterion's own log residual beside each root: five came back `0.0` and one came back **`0.87`** | **REPAIRED** in the same iteration by [`C-0143`](../claims/C-0143-planar-coupling-wall.md) (`T-221`). Both functions return `Double?`; `T-6` re-emitted, **exactly one field moved**, and no document in the corpus quotes the withdrawn number |
| [`CH-0179`](CH-0179-exponentially-large-is-not-a-property-of-the-branch.md) | [`C-0137`](../claims/C-0137-beyond-mean-field-gap.md) §`P4`'s use of Kanduč Eq. (65) and `BeyondMeanFieldGap.kt`'s KDoc: *"its right-hand side is 'exponentially large', so it is not implemented as a number: what it says is that the criterion below is the **conservative** one here"* | The exponential is at **fixed `ζ`**, and Eq. (65) is owed only where `p₀ < 0`, which needs `D̃ > (1+ζ)/\|ζ\|` — derived here from Eq. (18) at `α → 0`. Over that domain the bound is minimised **at the branch boundary** and sits at **`0.9447`–`1.1543`** of Eq. (64)'s over 18 readings; the conservatism claim **reverses** at the saturated wall at 5 nm | **OPEN, no verdict moves.** Raised by [`C-0143`](../claims/C-0143-planar-coupling-wall.md) (`T-221`). Eq. (65) is now implemented, with its `f(ζ)` branch continuity and its Eq. (18) boundary asserted as gates; what the branch buys is measured — an excluded sliver **0.23–1.10 %** wide |
| [`CH-0176`](CH-0176-the-first-flat-coupled-tile-is-flat-at-half-its-cells.md) | [`C-0118`](../claims/C-0118-coupled-four-layer.md)'s title (*"Of 16 graded cells, **9 are flat**; on `10 × 6` **all eight** are, best **0.0278431488** … on `15 × 4` **one of eight**, at **0.0882933461**. The cross-section is worth **3.17109774×**"*), its §2 and §3 tables and its `Verdict` row | **geometric, and conceded upstream.** [`CH-0174`](CH-0174-the-four-layer-cross-section-is-not-a-honeycomb.md) shows the cross-section those cells are graded on is not a honeycomb, and `C-0141` §9 states its dropout grading was **not** re-run. Re-run — same seed, same stream, same dropout, **only** `edgeY` and the two pitches moved — it is **4 of 16**, `10 × 6` **4 of 8**, `15 × 4` **0 of 8** at `f = 0.30` **and** at the band's low `f = 0.26`; best cell **0.0680677948**, `15 × 4`'s former flat cell **0.145354102**, cross-section worth **2.13543134×**. All sixteen standing numbers reproduce at `1.4e−10 … 4.4e−9` in the same process. The claim's design **conclusion** survives and is strengthened | **raised** by [`C-0142`](../claims/C-0142-coupled-cells-at-the-honeycomb-cross-section.md) |
| [`CH-0177`](CH-0177-the-path-count-axis-is-not-monotone.md) | [`C-0118`](../claims/C-0118-coupled-four-layer.md) §2's *"More paths are monotonically **worse** there (0.0278431488 → 0.0541089284 → 0.0461988976 → 0.0408747025 as columns go 1 → 2 → 3 → 5)"* | **logical, and internal** — the sequence printed in that sentence rises once and falls three times. Non-monotone at **both** geometries, with step ratios agreeing to two figures across a 1.5× change in attachment pitch, so the shape belongs to the coupling axis. Harmless while all four cells passed; at the corrected geometry **the peak is exactly the region that fails** (0.119502047 and 0.101905503 above `T-5b`'s 0.10, 0.0680677948 and 0.0900369 below it). Two named effects run opposite ways — a dropout **is** an increase in the attachment pitch, and a sparse coupling is a net dishing source — so a monotone axis was never available. The **value** (the sparsest cell is the best) is upheld | ~~**raised**~~ **UPHELD**, filed by [`C-0142`](../claims/C-0142-coupled-cells-at-the-honeycomb-cross-section.md) (`T-232`) and recorded in [`C-0118`](../claims/C-0118-coupled-four-layer.md)'s own banner; the *value* survives and only the *monotonicity* is withdrawn |
| [`CH-0182`](CH-0182-a-census-is-dated-by-its-premise-set.md) | [`C-0144`](../claims/C-0144-honeycomb-correction-supersession.md) §9's *"Deliverable 2 — the list `T-233` needs"*: **41 entries** … *"41 of 41 verified present at the file and line they name"* | **methodological — a completeness claim with a hidden TIME argument.** `tools/T-234-census.py`'s five premise families are built from `C-0141` and `C-0140`; `C-0142` was filed the **same iteration**, hours later, and re-graded the coupled cells, so its withdrawn premises are in **no** family. Counted in the two deliverables: **26** occurrences, **16** on lines the list already names and **10 on 6 lines it does not** — the list's 21 physical lines are really **27**. The ten are the half a reader outside the programme acts on: *"1 of 8"* where the corrected reading is **0 of 8**, and *"best 0.0278431488"* where it is **0.0680677948** | **raised** by [`C-0145`](../claims/C-0145-eighth-answers-synthesis.md) (`T-233`). **Every one of the 41 verifies in place, before and after the edit**, and the queue half of `C-0144` is untouched; the repair is a rule — a census emits the claim set its families were derived from, and a list published for a downstream editor carries the word **floor** |
| [`CH-0183`](CH-0183-a-line-census-cannot-see-a-quotation.md) | [`C-0144`](../claims/C-0144-honeycomb-correction-supersession.md) §8–§9's classification of entries 10–19 as `MOVED`/`DISCHARGED`, against its own `RECORD` class — *"a closed queue row, a synthesis pass's account, a `Conditions` row, **a verbatim source quotation** — left alone, with the reason recorded"* | **methodological — a token census reads a LINE and cannot see the FRAME it sits in.** All ten lie inside one preserved-verbatim blockquote that [`C-0130`](../claims/C-0130-seventh-answers-synthesis.md) filed and `C-0071`'s *strike, never delete* makes binding — **10 of 41, and 10 of 27** on the `ANSWERS.md` half. The discriminator is that every line of the block begins `>`, one regex on an input the tool already has; either it or a preceding-heading scan moves the ten to `RECORD` and takes the published work list from **41 to 31** | **raised** by [`C-0145`](../claims/C-0145-eighth-answers-synthesis.md) (`T-233`). **The ten are correctly located and no number moves**; the repair actually owed for all ten is a **single superseding banner above the block**, which `T-233` wrote. The `MOVED` reading would be right only for a repair policy this repository forbids |
| [`CH-0186`](CH-0186-the-twenty-eight-nucleotide-turn-is-a-choice.md) | [`CH-0173`](CH-0173-the-built-block-turns-on-loops-not-crossovers.md) item 1 — *"At the built allowance the widest four-layer honeycomb tile is **92 bp = 31.28 nm** from M13 (`−21.80 %`)"* — and through it [`C-0140`](../claims/C-0140-honeycomb-raster-turn-sense.md) §5's *"route B (uniform, loops) does not fit M13 at 112 bp at all"* | **substantive — a ceiling derived by transcribing one design's CHOICE as a requirement.** A turn's two anchoring phosphates are at most `d + 2r_P = 4.35327572 nm` apart and **6** unpaired nucleotides reach that on `T-71`'s MEASURED phosphodiester step, against the built **28** — **4.67×**. At the reach bound M13 affords **114 bp = 38.76 nm**, not 92, and a uniform 112 bp row needs `L ≤ 8` nt, which it has. The `n = 0` case checks the geometry on measured constants alone: `d − 2r_P = 0.718724283 nm` is `+1.50 σ` of the measured step | **raised** by [`C-0147`](../claims/C-0147-honeycomb-turn-slack-and-ragged-face.md) (`T-230`). `C-0140`'s route-A recommendation **stands on a better reason**: at 8 nt a worst-azimuth turn carries **6.54–12.11 pN** and stores 2.36–3.74 `k_BT`, so route B fits only **strained** |
| [`CH-0187`](CH-0187-the-two-length-recommendation-rests-on-an-unstated-filter.md) | [`C-0140`](../claims/C-0140-honeycomb-raster-turn-sense.md) §4's selection rule — *"minimum `\|extent − 40 nm\|` among pairs with **a stagger of at most 4 bp** that fit M13"* | **methodological — a selection rule with a free parameter that is neither derived nor priced.** Without the filter the width optimum among all M13-fitting pairs is **101 / 109 bp at `−0.55 %`**, which also beats 112 / 108 on the scaffold (6 308 against 6 596 nt) and on the blunt-end stacking clearance (**`+4.18` rises against `+0.18`**). 112 / 108 wins **one** axis of four — `C-0141`'s saturated outboard plan ceiling, 2.380 nm, which the wider pair's 2.72 nm exceeds | **raised** by [`C-0147`](../claims/C-0147-honeycomb-turn-slack-and-ragged-face.md) (`T-231`). **ANSWERED** by [`C-0151`](../claims/C-0151-closing-raster-selection.md) (`T-245`): the four axes are re-scored on the **closing** family, where the axis `CH-0187` could not have had — scaffold closure — settles the choice at `102 / 109`, and `CH-0187`'s own `101 / 109` does not close either |
| [`CH-0184`](CH-0184-the-inter-row-offset-stops-being-free.md) | [`C-0141`](../claims/C-0141-honeycomb-station-lattice-and-placement.md) §9's *"The inter-row ladder offset is 7 or 14 bp and this repository cannot yet say which. Both are carried and **no answer here depends on the choice**"* | **logical — a convention that is free at a UNIFORM row length and decides a COUNT at a two-length one.** `C-0140`'s 112 / 108 bp raster gives the two face parities different windows (`[−112, 0]` and `[−112, −4]`), so a shift along the ladder now moves a station **out of** one parity's window: the `10 × 6` face carries **55 of 60** stations at the 7 bp offset and **50** at the 14 bp one at the same phase, and over all 42 `(phase, offset)` pairs exactly **one** keeps the full sixty — **phase 11 at the 14 bp offset**, with `15 × 4` saturating at **90 of 90** at the same pair | ~~**raised**~~ **ANSWERED** by [`C-0148`](../claims/C-0148-face-bond-class-residues-and-row-span-columns.md) (`T-244`) — the offset is **14 bp** and the saturating pair is withdrawn as unbuildable. Filed by [`C-0146`](../claims/C-0146-coupled-cells-at-the-two-length-raster.md) (`T-235`). **Nothing standing turns on it** — every graded column count (1, 2, 3, 5) is inside the sparsest row's ladder at every pair, 0 of 48 refused — but a **six**-column placement stands at one pair of 42 and is refused at 41. `C-0141`'s §3 census, §4 ceilings, §5 family and 30° azimuth are untouched |
| [`CH-0185`](CH-0185-a-bounding-box-crossover-column.md) | `CrossoverLayout.EDGE_MARGIN`'s KDoc — *"far below the 0.28 nm closest approach any base-pair phase makes on a 40 nm tile, so it **never decides a column count that the physics does not already decide**"* — and the standing practice of deriving a four-layer tile's crossover-column count from a bounding-box `edgeX` | **logical + in-silico — the SECOND geometry at which that sentence fails**, after [`C-0134`](../claims/C-0134-buildable-width-count-phase.md)'s 38.08 nm. A 116 bp = 39.44 nm honeycomb block clears eleven 3.57 nm pitches by **0.07 nm**, one fifth of a base-pair rise, so the guard gives **12** columns at 0.05 nm and **11** at half a rise — and that column alone is **6 flat coupled cells of 8 against 3**, the two effects of the wider box running opposite ways (`+4.97 %` for the width, `−8.44 %` for the column). Deeper: **every x-raster row of that block is 112 bp**, at every one of `C-0140`'s five candidate pairs, so the twelfth column belongs to the bounding **box** and no row can carry it | ~~**raised**~~ **ANSWERED** by [`C-0148`](../claims/C-0148-face-bond-class-residues-and-row-span-columns.md) (`T-243`) — the twelfth column is a bounding-box artefact and the price `DECISIONS-FOR-NDI` decision 8 rested on is withdrawn. Filed by [`C-0146`](../claims/C-0146-coupled-cells-at-the-two-length-raster.md) (`T-235`). **Nothing at 112 bp moves** (slack 1.70–2.28 nm at all three conventions; `C-0142`'s cells reproduce at `≤ 3.9e−9`). The cheap repair is to quote `(edgeX − 2m) mod pitch` beside any four-layer verdict and sweep the guard below one rise; the real one is a per-beam axial window `OrigamiGrillage` does not have (`T-243`) |
| [`CH-0190`](CH-0190-a-synthesis-owns-the-token-it-diagnosed.md) | [`C-0115`](../claims/C-0115-fifth-answers-synthesis.md)'s *"its owning claim states at full width (`0.0344013403` …). Repaired by restoring"* and [`C-0067`](../claims/C-0067-answers-reconciliation.md) §U1's *"corrected to the claim's own figure"* — **both complete in `ANSWERS.md`, neither reaching `DECISIONS-FOR-NDI.md`** — and `tools/trace-answers.py`'s numeric pass, which cannot see either survivor | **methodological — a retained checker whose corpus CONTAINS the audits, so a token quoted in order to be diagnosed acquires an owner and reads `CITED`.** Partitioned by owner on the corpus at the start of iteration 36, exactly **3** of **2 097** numeric tokens in the two deliverables have an owner set consisting only of synthesis claims — and **2 are live, unqualified assertions in `DECISIONS-FOR-NDI.md`, and both are the tokens a synthesis claim repaired in `ANSWERS.md`**: `42.4` for `C-0032`'s **42.38–61.04**, standing 24 iterations, and `0.0344` for `C-0093`'s **0.0344013403**. A repair is applied where the checker reports, and the checker reported one file. **The census destroys itself** — quoting the two tokens here gives them a non-synthesis owner, so re-running the partition now returns zero rows | **raised** by [`C-0149`](../claims/C-0149-ninth-answers-synthesis.md) (`T-240`). **Both instances REPAIRED**; neither moves a verdict. The recommended fix is a **cross-document** pass — flag a token live in one deliverable and struck or qualified in the other — which needs no corpus convention change, only the strike-blanking already required |
| [`CH-0191`](CH-0191-a-width-advantage-across-two-conventions.md) | [`C-0140`](../claims/C-0140-honeycomb-raster-turn-sense.md) §4 — *"its `−1.40 %` **beats the square lattice's 38.08 nm at `−4.80 %`** and `C-0133`'s twist-corrected 37.40 nm at `−6.50 %`"* — and both outward-facing documents, which carried it unqualified | **methodological — a comparison between two objects taken on two different functionals of them.** 39.44 nm is a **bounding box** and 38.08 nm is a **row length**; a uniform square-lattice raster has **no stagger**, so its two readings coincide and there is no second figure to notice is missing. Read on the row length both tiles are **112 bp = 38.08 nm** and the advantage is **exactly zero**; read on the bounding box the honeycomb wins by **1.36 nm — which IS the stagger**, i.e. it wins by being ragged. The substance is downstream: every four-layer plan ceiling, packing verdict and coupled cell outside [`C-0146`](../claims/C-0146-coupled-cells-at-the-two-length-raster.md) is computed at **38.08 nm** while both deliverables reported the **footprint** at 39.44 | **raised** by [`C-0149`](../claims/C-0149-ninth-answers-synthesis.md) (`T-240`, `T-242`). **No verdict moves and no number of `C-0140`'s is wrong**; what moves is what the sentence means. Closed by a stated convention — decision **8** / `T-242` — or by a both-conventions table, which is what the deliverables now carry |
| [`CH-0188`](CH-0188-the-recommended-raster-does-not-close.md) | [`C-0140`](../claims/C-0140-honeycomb-raster-turn-sense.md) §4's recommendation — *"Read on a stated rule … the recommendation is **112 / 108 bp**"* — and through it every downstream reading of the two-length raster's **absolute** axial windows | **logical — a constraint `C-0136`'s per-helix row-length rule does not carry.** A scaffold crossover sits `±5 bp` from its pair's staple position, so **one** lattice constant `b₀` must serve every raster crossover; at 112 / 108 none does, and **10 of 59** raster crossovers on the `10 × 6` block would have to be **forced**, which caDNAno permits and warns *"may lead to folding failure"*. Of `C-0140`'s five candidate pairs exactly **one** closes, **102 / 109**, at zero forced crossovers — identical at both cross-sections and all four sign/mirror/datum conventions, and **no uniform row length closes either**, which corroborates `C-0140`'s own negative from a new direction | **raised** by [`C-0148`](../claims/C-0148-face-bond-class-residues-and-row-span-columns.md) (`T-244`); **ANSWERED** by [`C-0151`](../claims/C-0151-closing-raster-selection.md) (`T-245`), which re-selects inside the **closing family** rather than among `C-0140`'s five and returns `102 / 109` at the same 39.44 nm extent for the price of one crossover column. `C-0140`'s turn senses, class algebra and level walk are consumed unmodified and are upheld 112 / 108 and 101 / 109 |
| [`CH-0189`](CH-0189-the-ladder-phase-is-not-a-sweep.md) | [`C-0141`](../claims/C-0141-honeycomb-station-lattice-and-placement.md) §5's 21-phase enumeration read as a menu of **available** designs, and [`C-0146`](../claims/C-0146-coupled-cells-at-the-two-length-raster.md) §2 / [`CH-0184`](CH-0184-the-inter-row-offset-stops-being-free.md) §2's *"best of 42 pairs: **60**, at **phase 11 / 14 bp**"* | **logical — the same `±5 bp` rule that fixes the offset OVER-determines the phase.** Where the raster closes the ladder phase is **determined** (16 at 102 / 109, carrying **55 of 60** stations, which is also the sweep's own maximum); where it does not close, **no** phase is determined. `CH-0184`'s saturating cell is reproduced at departure `0.0` and is **neither**: it lives at a non-closing pair, whose two near-miss `b₀` readings put the phase at 2 and 13. **No six-column placement stands at any pair examined** | **raised** by [`C-0148`](../claims/C-0148-face-bond-class-residues-and-row-span-columns.md) (`T-244`); **ANSWERED** by [`C-0151`](../claims/C-0151-closing-raster-selection.md) (`T-245`), which grades the design at the **determined** phase 16 / offset 14 (55 of 60 stations, no column count refused, and a six-column placement capped out by the sparsest row's five). `C-0141` §5's structural results survive, being properties of the **offset** |
| [`CH-0192`](CH-0192-the-census-that-measured-a-stopped-census-stopped-too.md) | [`CH-0169`](CH-0169-four-spellings-of-eleven-and-four-implementations.md) Ground 1 and [`C-0138`](../claims/C-0138-departure-rule-scope.md) §7's nine-row table — *"62 fields in 6 files, of which 12 must not be swept at all"* | **logical — a list is a census that stopped, and so is the census that says so, because both search for a NAME where the rule is about a SHAPE.** A shape census (a leaf key inside a `reproductions`/`convergence` record whose name denotes a **discrepancy**) finds **14** candidate names and **75 fields in 8 files**: an exact-match list cannot see a **compound of the rule's own word**, so `T-60`'s `convergence[*].multiplierDeparture` (8 fields, 2 over) and `gradientDeparture` (8, 5) are missing, as is a third logarithm, `observedOrder` (6, 6, in `T-1d` and `T-1e`). And **two of the nine published judgements do not survive reading the emission site**: `relativeMovement` is `P-18`'s answer only *outside* a departure record — where the record qualifier already protects it — while `T-182` and `T-189` emit the same key *inside* one at two digits; `worstResidual` is a **length in nm** carrying the decision `covalent` | **raised** by [`C-0150`](../claims/C-0150-departure-spelling-set-and-the-wall-clock.md) (`T-225`). **UPHELD and repaired in the same task** — the shape census is retained as `tools/T-225-census.py` with a `--check` that exits 1 on an unclassified candidate, all fourteen are classified with a ground, and the *"12 must not be swept"* is **21 fields in 3 files**. No physical quantity and no verdict moves |
| [`CH-0193`](CH-0193-a-ceiling-the-class-exceeds.md) | [`C-0129`](../claims/C-0129-result-file-hygiene.md)'s `wide` census and its restatement in [`C-0131`](../claims/C-0131-departure-and-saturation-audits.md) and [`C-0138`](../claims/C-0138-departure-rule-scope.md) §6 — *"any leaf key containing 'departure'; **a ceiling on the class**, NOT gated"* | **arithmetic over the committed corpus — a ceiling the class exceeds by 68 of 75.** The class is *a residual between two refinements of one solve*; the predicate is *a leaf key containing the substring `departure`*. Of the **54** fields the widened gate covers, **7** contain it and **47** do not; of the **21** classified out, **none** do. It is the line a reader differences against the gate to know that a clean gate means something, and it is why `T-60`'s two fields sat inside a printed `945` for four iterations without the difference being taken | **raised** by [`C-0150`](../claims/C-0150-departure-spelling-set-and-the-wall-clock.md) (`T-225`). **UPHELD.** `wide` is retained unchanged and **re-described** as a bound on *one spelling family*; the ceiling on the class is now `tools/T-225-census.py`, which searches the shape and requires a classification |
| [`CH-0198`](CH-0198-the-floor-half-of-the-rule-never-travelled.md) | [`C-0138`](../claims/C-0138-departure-rule-scope.md) (`T-214`) and [`C-0150`](../claims/C-0150-departure-spelling-set-and-the-wall-clock.md) (`T-225`) — the two claims that moved the departure rule into `structure/ResultRounding.kt` | **numerical — a mechanism half-applied.** `C-0138` made `DEPARTURE_DIGITS_BY_KEY` a **baseline** so a study obeys the digit half *"by construction rather than by remembering to pass the map"*; the **floor** half of the same rule, stated in the same KDoc — *"the floor is a claim in the locked units and a ratio of two dishing fractions is not in them"* — is still a **default parameter**. Measured over `src/main/kotlin`: **49** call sites pass a `digitsByKey` map, **17** also pass a `floor`, **32 do not**, and every one of those 32 emits any departure below `1e−9` as exactly `0.0`. Live instance: `T-164`'s `reproductions[0].departure` is `0.0` while its own `F2` sentence states `3.4e−11` — one quantity, two readings, one file. Scope as an honest **upper** bound: **811** departure fields in **86** files are exactly `0.0` against 933 nonzero, most genuinely zero, **and the artifact cannot tell the two cases apart** | **raised** by [`C-0153`](../claims/C-0153-unrounded-prose-interpolations.md) (`T-249`). Neither `C-0138`'s nor `C-0150`'s verdicts move; the digit half is correct, is in the layer, and gates at `0 field(s) in 0 file(s)`. Queued as `T-251` — it moves a **numeric** field in every affected study, which is why it is a challenge and not a repair inside a task whose claim is that it moved prose only |
| [`CH-0199`](CH-0199-a-quoted-number-has-no-link-back-to-its-file.md) | [`C-0101`](../claims/C-0101-re-emitting-what-the-repair-moved.md) (`T-167`) — *"re-emit the downstream file and amend the claim … only where it **quotes** a moved number"* — and every claim that has since followed it | **methodological — a rule with no instrument is a rule executed as well as its author's memory.** `trace-answers.py` traces a **synthesis** against the **claim** corpus, `result-transfers.py` compares two **result files**, `result-reader-census.py` derives which **code** reads which file; **none checks a claim's number against the result file the claim names**. Measured on the one token class where it is detectable — a decimal above nine significant digits, which a rounded result file cannot contain by construction — **43 tokens in 17 documents, of which 19 are already unfindable in any committed result file**. Several of the 19 are *deliberate* historical evidence (`C-0092`'s rule that a repair must leave the defect measurable), and **no instrument can tell those from stale ones**, because the corpus carries no convention marking a number as historical | **raised** by [`C-0153`](../claims/C-0153-unrounded-prose-interpolations.md) (`T-249`), whose own re-emission would have made [`C-0099`](../claims/C-0099-row-end-crossover-stiffness.md)'s `3.3864695769825204E-11` the twentieth row had the `grep` not been thought of — `C-0153` amends it. Queued as `T-252`: the **convention** must come before the checker, and the checker must ship as an **audit** because 19 of 43 fire today |
| [`CH-0196`](CH-0196-the-eight-base-pair-yield-cost-is-an-elided-attribution.md) | [`C-0055`](../claims/C-0055-unused-junction-site.md) Deliverable 6 — *"**This is the cost of the escape and it is a folding-yield cost, not a geometric one**"* — and through it `CLAUDE.md`'s entry *"that is a published YIELD cost"* | **literature — an ellipsis that removes the paper's own scope statement.** Ke et al. observe the lower yield and then decline the attribution in the very next sentence: *"Alternatively, simply having a large number of layers with our default crossover pattern may be destabilizing, **irrespective of the position of the breaks**"*, and *"**Future systematic studies will be required** to determine the relative importance of these staple breaks"*. Both fall inside the quoted paragraph; neither survives the quotation. And the paper publishes **no percentage** for the comparison | **raised** by [`C-0152`](../claims/C-0152-forced-scaffold-crossover-price.md) (`T-246`). `C-0055`'s geometry, census and verdict are untouched; what is withdrawn is the word *"cost"*. The related sentence *"a denser crossover pattern costs yield"* **survives on the other candidate cause**, which is the one the quotation had removed |
| [`CH-0197`](CH-0197-an-allowed-scaffold-crossover-is-not-aligned.md) | [`C-0147`](../claims/C-0147-honeycomb-turn-slack-and-ragged-face.md)'s `T-230` Deliverable 1 and its `F1` row — *"`d − 2r_P` … sits at `+1.49997857 σ` … **and inside its 99th percentile**"* | **logical — a convention inside a cited rule.** caDNAno's `±5 bp` is *"five base pairs, **or half a turn**"*, and at 10.5 bp/turn the half turn is **5.25** bp — so every allowed scaffold crossover sits `8.57142857°` off the line of centres, spanning **0.787091706 nm** at **`+3.39051453 σ`**, **outside** the measured 99th percentile. `C-0147` evaluated the aligned azimuth, which is caDNAno's idealisation of its own rule rather than the rule's geometry | **raised** by [`C-0152`](../claims/C-0152-forced-scaffold-crossover-price.md) (`T-246`). **The verdict survives and its ground is the stronger one**: the lattice demonstrably absorbs `8.57142857°` at every scaffold crossover of every honeycomb origami ever folded, which is an empirical calibration the corpus was not carrying — and a *forced* crossover asks for exactly **twice** it. No number moves; `C-0147`'s reach bound and worst-azimuth energies are unaffected |
| [`CH-0194`](CH-0194-the-filter-and-closure-are-disjoint.md) | [`C-0140`](../claims/C-0140-honeycomb-raster-turn-sense.md)'s *"the remedy costs **3 base pairs = 1.02 nm**"*, its `F9`, and §4's *"stagger of at most 4 bp"* selection filter | **logical.** Every closing residue pair has `L₁ − L₂ ≡ 14 (mod 21)`, so the least stagger a **drawable** two-length honeycomb raster can carry is **7 bp = 2.38 nm** — the filter's admissible set and the closing family are **disjoint**, and 3 bp is a *per-helix* minimum no drawable pair reaches | **raised** by [`C-0151`](../claims/C-0151-closing-raster-selection.md); the turn-sense theorem, the class algebra and the level walk are upheld and consumed unmodified, and the width finding does not move |
| [`CH-0195`](CH-0195-both-graded-column-counts-belong-to-an-undrawable-raster.md) | [`C-0146`](../claims/C-0146-coupled-cells-at-the-two-length-raster.md) §3's *"a numerical guard … the difference between 6 flat cells of 8 and 3"* and its *Still open* item 4; [`C-0148`](../claims/C-0148-face-bond-class-residues-and-row-span-columns.md) §4b's selection of the 11-column reading | **logical + in-silico.** Both counts are read at `112 / 108`, which needs 10 forced scaffold crossovers; the drawable pair's interface window is 102 bp and gives **10** columns at every `EDGE_MARGIN` convention, so the guard is **inert** where a design would be built | **raised** by [`C-0151`](../claims/C-0151-closing-raster-selection.md); 32 of `C-0146`'s cells are reproduced at `≤ 4.1e−9` and its recommended coupled design survives at ten columns |
| [`CH-0200`](CH-0200-the-across-helix-rigidity-formula-does-not-describe-a-honeycomb.md) | `OrigamiSheet.acrossHelixRigidity` = `layers × k_θ d / p` and `multiLayerRigidities`'s `acrossIndependent`, **as applied to a honeycomb block** — [`C-0109`](../claims/C-0109-four-layer-tile.md), [`C-0120`](../claims/C-0120-cross-section-comparison.md), [`C-0141`](../claims/C-0141-honeycomb-station-lattice-and-placement.md) §7, [`C-0142`](../claims/C-0142-coupled-cells-at-the-honeycomb-cross-section.md), [`C-0146`](../claims/C-0146-coupled-cells-at-the-two-length-raster.md), [`C-0151`](../claims/C-0151-closing-raster-selection.md) | **logical + in-silico — a formula about a stack of independent single-layer sheets, applied to a body that contains none.** Only half a honeycomb's in-plane adjacent pairs are bonded, so ONE layer of a `10 × 6` block is **5 dimer components** and of `15 × 4` **8**: the across-helix load path necessarily traverses the thickness. Measured on a honeycomb lattice at the same `k_θ`, the same 21 bp per interface and `C-0141`'s corrected `3d/2` pitch, `D_⊥` is **`7/24 = 0.291666667`** and **0.288194444** of the formula — **`24/7 = 3.42857×`** overstated, in the **optimistic** direction. **The same formula reproduces `D_∥` EXACTLY, at `2.8e−15`**, which is what confines this to one term | **raised** by [`C-0154`](../claims/C-0154-honeycomb-grillage.md) (`T-253`). `C-0141`'s `10 × 6` free-tile dishing goes `0.0240648102 → 0.0449400126`, **1.87×**, and is **still flat**; `C-0120`'s central finding is untouched. The coupled cells of `C-0142`/`C-0146`/`C-0151` are **not** re-run and are the open half |
| [`CH-0201`](CH-0201-the-composite-fraction-is-a-length-not-a-constant.md) | `CLAUDE.md`'s *"the fraction is MEASURED … `f = 0.26–0.33` on four measured origami bundles"* **read as transferable**, and `LayerCoupling.CALIBRATED`'s use in [`C-0116`](../claims/C-0116-composite-fraction-threshold.md), [`C-0120`](../claims/C-0120-cross-section-comparison.md), [`C-0141`](../claims/C-0141-honeycomb-station-lattice-and-placement.md), [`C-0142`](../claims/C-0142-coupled-cells-at-the-honeycomb-cross-section.md), [`C-0146`](../claims/C-0146-coupled-cells-at-the-two-length-raster.md), [`C-0151`](../claims/C-0151-closing-raster-selection.md) | **methodological + in-silico — a boundary-layer measure quoted as a material constant.** In *uniform* bending an infinitely long composite is rigid whatever its connectors, so a partial composite measures the end zones and `f` carries a **length**. Measured on one lattice, one `k_s` and one load case, `f` runs **0.0717149752 → 0.737066133** over `56 → 448 bp`; the two 60-helix cross-sections the corpus applies **one** band to fall on **opposite sides** of it (**0.246803583** and **0.406535456**); and read on the dishing instead of the rigidity the same block reports **0.940471** against **0.246804**, **3.81×** | **raised** by [`C-0154`](../claims/C-0154-honeycomb-grillage.md) (`T-253`). Kauert et al.'s measurement is not disputed; what is challenged is its transfer to a 38 nm tile. At `10 × 6` the standing grading is optimistic by **17.7 %** of `f` and the cell stays flat (`0.0449400126 → 0.0477844467`); `C-0116`'s threshold **construction** is untouched, but a threshold *in `f`* must now be re-read at the geometry |
| [`CH-0202`](CH-0202-the-plan-ceiling-escape-is-quoted-at-fifty-paths.md) | [`C-0151`](../claims/C-0151-closing-raster-selection.md) §2a — *"a placement is capped at five columns (50 paths) and the plan ceiling there is **4.604 nm**, comfortably above the 2.38 nm relief"* | **logical — a margin quoted at a path count below the claim's own station census, from the claim's own result file.** `planCeilings` carries a **55**-path row at the determined lattice, `maximumPlanCeiling` = **3.06 nm** = **9 rises**, and 55 of 60 is `C-0151`'s own headline census: the reachable margin over the 7 bp = 2.38 nm relief is **exactly 2 rises = 0.68 nm**, `1.28571429×`, not `1.93445378×`. **A cap on COLUMNS is not a cap on PATHS.** And at the saturation `C-0141`'s axis is written on the margin is **exactly zero** — `7 × 0.34` against 7 bp, a lattice identity rather than a near miss | **raised** by [`C-0155`](../claims/C-0155-tenth-answers-synthesis.md) (`T-257`). **The verdict is UPHELD on every reachable count** — the plan-budget axis still does not bind — and no number in the result file moves; what moves is the number a reader is handed for how far it does not bind. Settled by one sentence quoting the 55-path row the file already emits |
| [`CH-0203`](CH-0203-a-specification-question-was-posed-on-a-threshold-its-own-iteration-withdrew.md) | [`C-0149`](../claims/C-0149-ninth-answers-synthesis.md) §5's posing of decision **8** — *"that column alone is **six flat cells of eight against three** (`CH-0185`)"* — and the decision-8 sections of both outward-facing documents | **methodological — a discharge nobody swept, landing on the PRICE of a question rather than on a value.** [`C-0148`](../claims/C-0148-face-bond-class-residues-and-row-span-columns.md) answered [`CH-0185`](CH-0185-a-bounding-box-crossover-column.md) **in the same iteration** — the twelfth column *"is a property of the bounding BOX and of no row"* — and [`C-0151`](../claims/C-0151-closing-raster-selection.md) then removed the eleventh too. So the one number giving decision 8 a **cost dimension** was withdrawn before it was asked, and a respondent weighing it is weighing a quantity that does not exist. `CLAUDE.md`'s *"a question whose admissible answers you enumerate cannot return the one you did not enumerate, and the missing shape is a PRICE"*, from the other side | **raised** by [`C-0155`](../claims/C-0155-tenth-answers-synthesis.md) (`T-257`), and **REPAIRED in both deliverables by the same pass**. The **question survives** — §3 states one number and the object has two — and is now a drafting question with **no margin on either side of it**. `C-0149`'s framing, its price line and `CH-0185` itself are all upheld. Queued as `T-261`: a synthesis must re-read the STATUS of every challenge it rests a number on, which `tools/trace-answers.py` already parses and does not yet gate |
| [`CH-0204`](CH-0204-the-guard-was-wrong-in-the-other-direction-too.md) | [`C-0153`](../claims/C-0153-unrounded-prose-interpolations.md) (`T-249`) §2 — *"the false-positive rate is a PROOF over the population, not a sample"* — and §3, its own account of repairing the same guard | **methodological — a measured false-POSITIVE rate standing in for a completeness argument.** `C-0153` repaired the trailing guard once, in the direction the corpus is made of, and shipped `(?!\w)(?!\.\d)`. `(?!\w)` refuses a decimal abutting a **unit letter**, and this corpus writes a ratio as `26.381529916714886x`: measured over the same committed corpus, `(?!\d)(?!\.\d)` finds **778 tokens against 747**, i.e. **+31 (+4.1 %) in 4 of the 47 files**, every one a genuine over-precise ratio and **not one a false positive**. The anti-mantissa property the guard was there for survives on the pattern's own greediness plus `(?!\d)`, and is now held open by a named token test. **What found it was `tools/T-250-movement.py`, which must PAIR the census's output and therefore cannot use a detector's guard** — a checker's blind spot is not found by looking at the checker | **raised** by [`C-0156`](../claims/C-0156-prose-interpolation-sweep.md) (`T-250`), which **repairs the guard and sweeps the 31**, so the gate reads `0 in 0` under the wider predicate. `C-0153`'s verdicts, its exhaustive false-positive proof and its census method are all upheld. **OPEN on the general form**: the false-NEGATIVE rate is still unmeasured, and the source-side census that would measure it needs Kotlin's own types |
| [`CH-0205`](CH-0205-a-number-typed-as-a-string-is-untyped-as-well-as-unrounded.md) | [`C-0129`](../claims/C-0129-result-file-hygiene.md) (`T-208`) and [`C-0138`](../claims/C-0138-departure-rule-scope.md) (`T-214`) — the rounding rule at the serialisation boundary — and [`C-0153`](../claims/C-0153-unrounded-prose-interpolations.md), which treats the residue as a **precision** problem | **methodological — a rule enforced on the PRECISION axis where the defect lives on the TYPE axis.** `roundedForResult` dispatches on the JSON type, so a number emitted as a string is not merely unrounded, it is unchecked: the departure gate walks numbers and cannot see it either. Measured over the committed corpus: **2 096 string leaves whose whole value is a number, in 76 of 144 files** — `/parameters` 442, `/runParameters` 216, `/convergence/*` 111, `/reproductions/*` 69, `/designPoints/*/responses/*` 549 — of which the prose gate can reach the **778** above nine significant digits and **cannot reach the other 1 318**. And **7** of the 26 keys the corpus reads back with a literal-key `.toDouble()` are a string leaf somewhere, so a decision about how a **sentence renders** is one refactor from being a decision about how many digits an **input** carries; checked at `HEAD` all seven resolve to a numeric leaf, so the channel is **latent, not live** | **raised** by [`C-0156`](../claims/C-0156-prose-interpolation-sweep.md) (`T-250`). Nothing in `C-0129` or `C-0138` is wrong; what is challenged is the **scope** their rule is believed to have. The repair is a **schema partition** (a bare number becomes a JSON number; a number carrying its unit stays a string), which moves a **numeric** field in 76 files and is therefore a queued sweep rather than an edit |
| [`CH-0206`](CH-0206-the-gate-was-red-at-the-commit-that-promoted-it.md) | [`C-0156`](../claims/C-0156-prose-interpolation-sweep.md) (`T-250`) §1 — *"This task makes the corpus clean … and turns the line into a build-failing gate"* — and the comment it committed beside the gate in [`tools/verify.sh`](../../tools/verify.sh): *"so the line reads 0 in 0 and is a GATE"* | **logical + in-silico — a gate promoted on a census taken by a code path that is not the gate's own run, and never exercised end to end.** `git archive 49b1a01 \| tar -x` and running THAT tree's own checker exits **1** on **69 tokens in 44 string fields in 8 files, of 146 scanned**, so `tools/verify.sh` has been red since the commit that made it a gate. Three separate facts: `./gradlew test` runs only `--self-test`, by an argued convention; `C-0156`'s Provenance names **no suite run** and `T-250`'s `gate` record carries `isAGate` and `selfTests` and **no reading**; and the claim's own **`F3`** row states *"615 tokens in 37 files after the sweep"*, measured by `census()` in the emitter, which **is** `check_prose_precision`. The two readings are the same function at two moments, `census(RESULTS)` reading the live working tree — the unnamed state `C-0156` §6 forbids, fixed for the *before* census and not for the *after* one. Re-emitted, **62 of the 69 were stale artifacts** and **7 are source-side in 4 files**, at four call-site **shapes** the sweep's `x.toString()` rule cannot match: a lambda in a template (`T-134` ×3), a call in a template (`T-126` ×2), a `+` concatenation (`T-50`), and a hardcoded 16-digit **literal** with no call site at all (`T-159`) | **raised and REPAIRED** by [`C-0158`](../claims/C-0158-prose-gate-red.md) (`P-27`), which re-emits the eight, repairs the four emitters and reads the gate at **0 tokens in 0 files**. `C-0156`'s census, movement classification, topological sort, both mutation tables and all 47 re-emissions are consumed unmodified and **none of them moves**; [`CH-0205`](CH-0205-a-number-typed-as-a-string-is-untyped-as-well-as-unrounded.md) is upheld and extended — **0** of the eight has a string leaf any consumer parses with `.toDouble()`. **OPEN on the general form**: a gate is discharged by `tools/verify.sh --committed` against the corpus the COMMIT will carry, and nothing enforces that a claim wiring a gate records its reading |
| [`CH-0207`](CH-0207-a-parameter-block-cannot-re-run-its-own-study.md) | [`gpd/README.md`](../README.md)'s `results/` row — *"Every parameter of the run is in the file, so the result is reproducible from it alone"* — and [`CH-0205`](CH-0205-a-number-typed-as-a-string-is-untyped-as-well-as-unrounded.md)'s classification of the input-precision channel as *"**latent**, not live"*; raised by [`C-0159`](../claims/C-0159-environment-interface.md) (`T-265`) | **in-silico + a corpus census** — the committed literal and the solved value are fed to the same solver through the same call path, and the outputs compared at the file's own declared precision. `roundedForResult` walks the whole document, so a study's **inputs** are rounded by the rule written for its **outputs**: `T-3a` commits its wall charge as `−0.398665238` against the `−0.3986652379247042` it solved with, and the committed literal misses that same file's own 2 V force by one unit in the last emitted place and its `forceDecayLength` by two. The channel is **live** rather than latent — **seven call sites in `src/main/kotlin` read a parameter block back as an input** — and which files are exposed is decided by a rendering convention with no rule behind it: **49 result files write their parameters as JSON numbers (rounded) and 59 as strings (not), zero mixed**. The rule owed is *round outputs, never inputs* | **CLOSED — REPAIRED** by [`C-0162`](../claims/C-0162-round-outputs-never-inputs.md) (`T-268`) on this challenge's own first proposed repair, *"an input is not a result"*: `PARAMETER_RECORDS` defaulted **in the layer**, so all six Kotlin rounding entry points inherit it. `T-3a`'s wall charge now reads `−0.3986652379247042`, the value it solved with; 42 files re-emitted in topological order. Its **census row** is separately challenged by [`CH-0210`](CH-0210-the-parameter-block-census-is-not-reproducible.md) |
| [`CH-0209`](CH-0209-a-crossover-drawn-as-two-strand-crossings.md) | [`C-0157`](../claims/C-0157-crossover-hinge-constant-from-oxdna.md)'s simulated design and the ground stated for it in [`tools/oxdna/gen1_tile_design.py`](../../tools/oxdna/gen1_tile_design.py) — *"A Rothemund staple crossover is a SINGLE strand crossing"* — and [`C-0160`](../claims/C-0160-scadnano-writer.md)'s `noSiteIsCrossedTwice` buildability predicate; raised by [`C-0161`](../claims/C-0161-mechanics-on-an-imported-design.md) (`T-267`) | **in-silico / logical — a census of two committed `.sc` files, one of them emitted by the reference implementation (scadnano 0.21.1) and read by this repository's own reader.** `scadnano.origami_rectangle.create`, the field's own canonical Rothemund rectangle, draws every crossover as **two** strand crossings at adjacent offsets `o` and `o+1` — `31/32, 63/64, 127/128` on the even-lower interfaces and `47/48, 79/80, 111/112` on the odd ones — where this corpus's own sheet draws **one** per 32 bp per interface. That is neither of the two cases the driver's comment reasons about (one crossing, or two at the **same** offset, which `C-0157` measured does not relax), and `noSiteIsCrossedTwice` cannot see it because it keys on the exact offset. Imported, the same rectangle is **90 lattice sites or 45** depending on the reading, moving its peak dishing over stroke by `1.0873×` (`0.258057772` against `0.28058418`) | **OPEN** — filed, not repaired. Nothing here says `C-0157`'s `k_θ` bracket is wrong; it says the bracket was measured on **one** of two motifs and the corpus has no measurement on the other. `T-267` mitigates by making the reading a **named** parameter (`AdjacentCrossingReading`) rather than a default. One oxDNA relaxation of the adjacent-offset pair settles it |
| [`CH-0210`](CH-0210-the-parameter-block-census-is-not-reproducible.md) | [`CH-0207`](CH-0207-a-parameter-block-cannot-re-run-its-own-study.md)'s *"The split nobody chose"* table — **49 numeric / 59 string / zero mixed**, and *"1 004 numeric leaves sit in the 49"* — and the sentence built on it, *"which side a study lands on is a per-study rendering convention"*; raised by [`C-0162`](../claims/C-0162-round-outputs-never-inputs.md) (`T-268`) | **in-silico** — the census re-derived over `git archive bed0197 gpd/results`, the unmodified 147-file tree at the commit that filed `CH-0207`, under every plausible key set (all subsets of size ≤ 4 of eight top-level block spellings) and both depth conventions. The corpus gives **50** files carrying at least one numeric parameter leaf, **63** carrying only strings and **14–20 MIXED**; **no reading reproduces 49/59/0, and none gives 1 004 leaves**. The thesis is upheld and strengthened — one more file is exposed than was claimed, and the exposure is decided **per FIELD** rather than per study, which is finer than an audit by study name can reach | **OPEN** — filed with the correction in hand; it changes no verdict, and `C-0162` closes `CH-0207` on grounds this makes stronger |
| [`CH-0211`](CH-0211-the-closure-does-not-need-the-turn-senses.md) | [`C-0160`](../claims/C-0160-scadnano-writer.md) §9's final validity bullet and its matching *Still open* item — *"it does **not** implement `C-0148`'s closure predicate from the file — that needs the **turn senses**, which the `.sc` format does not carry in a form this reader derives"* | **logical — a stated REASON, not a number and not a verdict.** Closure is `(level − 7·class) mod 21`, and a `.sc` states both: the **level** is the edge of the axial window the helix turns at (`end` forward, `start` reverse, and the two sides of one crossover agree), the **class** comes from `grid_position` through the inverse of the very grid map `C-0160`'s own `F4` checked at departure `0.0`. A turn sense is what a **construction** needs to place a crossover; an **import** reads where it already is. Derived from the committed block: **59 crossovers → `{4, 14}` → one `b₀` → zero forced**, matching `HoneycombRasterResidues` up to the emission's 4 bp datum shift, and refusing the withdrawn `112 / 108` at exactly `C-0148`'s **10** | **RAISED and REPAIRED in the same iteration** by [`C-0164`](../claims/C-0164-lattice-aware-buildability.md) (`T-270`), which makes the honeycomb branch **answer**. `C-0160`'s verdicts, round trip, artifacts, counts and mutation table are consumed unmodified and none of them moves; its `F2` is upheld exactly as filed |
| [`CH-0212`](CH-0212-the-recommended-block-is-drawn-without-the-seam-its-own-claim-forces.md) | [`C-0119`](../claims/C-0119-honeycomb-raster-width.md) §4 — *"A seam is forced … the tree parity applies unchanged … The 60-helix case is a theorem, not an enumeration"* — read against [`C-0160`](../claims/C-0160-scadnano-writer.md)'s committed artifact `gpd/designs/gen1-block-honeycomb-10x6-102-109.sc` | **logical + a census of a committed artifact.** The emitted `10 × 6` block carries **one scaffold strand with 60 domains on 60 helices** — one per helix, a pure boustrophedon — which is [`C-0161`](../claims/C-0161-mechanics-on-an-imported-design.md) §4's **own** seam discriminator (*"a boustrophedon on 16 helices has 16"*, against the rectangle's 31) applied to this repository's own file, and it returns **no seam**. It has a consequence because `C-0161` §4(b) establishes that a seam breaks the column-parity alternation `CrossoverLayout` imposes by construction, and `HoneycombCoupledStudy.kt:312` and `HoneycombPlacementStudy.kt:267` **both** call `CrossoverLayout.centred` — so on the seamed reading every coupled flatness cell and every placement on the recommended block is outside the family its own generator can produce | **CLOSED, iteration 40** by [`C-0168`](../claims/C-0168-recommended-block-seam.md) (`T-274`), on the **favourable** resolution. The artifact is right as drawn and `C-0119` §4's *forced* is withdrawn and annotated, its derivations reproduced at departure `0.0`. **No number moved.** The seam needs a **fully folded** circular scaffold and 919 spare nucleotides close the circle at **1.03–2.69 `k_BT`** over a **35.504 nm = 14 d** terminus separation, against the 8.0 `k_BT` per crossover column. Proposed reading (2) **could not be taken** — 0 staple crossings, 0 columns — and reading (1) is a **degree census**: both raster termini are degree one, so no honeycomb block of this family admits a Hamiltonian cycle at all. The unmeasured counterfactual is measured: **1.15701888×**, both flat |
| [`CH-0213`](CH-0213-the-recommended-coupled-design-does-not-survive-the-honeycomb-grillage.md) | [`C-0151`](../claims/C-0151-closing-raster-selection.md)'s headline — *"**2 flat cells of 8** against 3, and **the recommended cell survives at both ends of the measured band** — `0.0773373597` at `f = 0.30` and `0.0821458169` at `f = 0.26`"* — and, through the same mechanism, [`C-0146`](../claims/C-0146-coupled-cells-at-the-two-length-raster.md)'s *"6 flat cells of 8"*, [`C-0142`](../claims/C-0142-coupled-cells-at-the-honeycomb-cross-section.md)'s *"four flat cells"* and [`C-0118`](../claims/C-0118-coupled-four-layer.md)'s *"nine flat cells of sixteen"* | **in-silico, and CONTROLLED**: the same 64 cells, the same extent, the same stations, the same mandate, the same `p/k_f` stroke and the **same 4 000-realisation dropout stream**, graded on [`C-0154`](../claims/C-0154-honeycomb-grillage.md)'s honeycomb grillage instead of on an `OrigamiGrillage` over an `equivalentSheet` whose across-helix rigidity is `24/7` overstated. The smeared half **reproduces `C-0151`'s four published `p90` values at `≤ 6.3e−10` in the same process**, so the difference is the model and nothing else: **0.145086839** and **0.149852804** against 0.0773373597 and 0.0821458169, worse at **4 000 of 4 000** realisations, **0 of 64** cells flat against **15**, and **0 of 8** against `C-0151`'s published 2 | **RAISED, with the re-graded value in hand** ([`C-0167`](../claims/C-0167-coupled-cells-on-the-honeycomb-grillage.md), `T-263`). Only the **flatness** verdicts are challenged: the uncoupled block is still flat (0.0501417315 / 0.0522223659), the recommended cell is still flat at **zero defects** (0.0626407003), and `C-0151`'s selection of `102 / 109` — exhaustive arithmetic on the closure lattice — is untouched. **The correction is NOT a multiplier and is not monotone in the path count**: the per-realisation median ratio runs 1.06375481–2.47485493, and at six cells the *unpaired* ratio of 90th percentiles falls below one while the paired median does not |
| [`CH-0214`](CH-0214-the-honeycomb-lattice-stopped-at-its-last-crossover-plane.md) | [`C-0154`](../claims/C-0154-honeycomb-grillage.md) (`T-253`) — [`tile/HoneycombGrillage.kt`](../../src/main/kotlin/tile/HoneycombGrillage.kt)'s `nodeS`, and the claim's `F1` row, which certifies the uniform-load falsifier on the object rather than on the object *at the row lengths it was run at* | **in-silico — the repository's own best falsifier, fired on a correct-looking solve.** `nodeS` is built from `(0..rowBasePairs step 7)`, so the beams reach the end of the tile **only if `rowBasePairs` is a multiple of 7** — which every row `C-0154` ran is (112, 56, 224, 448) and which is stated in no `require`, no KDoc and no Conditions row. At `C-0151`'s **116 bp** block extent the outer **1.36 nm** carried no beam element, no foundation and no load, and a uniform pressure — whose exact answer is **zero** — dished **0.15 of the stroke** | **RAISED and REPAIRED in the same iteration** ([`C-0167`](../claims/C-0167-coupled-cells-on-the-honeycomb-grillage.md), `T-263`). The repair adds a trailing node where the row has a remainder and **nothing** where it does not, asserted as a test at 42 / 56 / 112 / 116 bp; a control re-run of `tile.HoneycombGrillageStudyKt` moves **not one numeric field** of `gpd/results/T-253-honeycomb-grillage.json` (its one **prose** field that does move was already stale against `HEAD`'s own source and is re-emitted here) |
| [`CH-0215`](CH-0215-the-artifact-does-not-state-its-scaffold-topology.md) | [`C-0160`](../claims/C-0160-scadnano-writer.md) — its §6 states three omissions (no staple set, no turn loopouts, no sequence); the scaffold's **topology** is a fourth and is not among them | **a census of a committed artifact.** The emitted scaffold strand carries `domains`, `is_scaffold` and `color` and **no `circular` field**, whose scadnano default is `False` — so the file asserts a **LINEAR** scaffold while the budget it was drawn to is **M13's circle**, 6 330 of 7 249 nt with **919 spare**. The 919 nt remainder is not drawn either, which is the same heterogeneous-`domains` schema step `C-0160` §6 declines for the raster turns | **OPEN** — filed, not repaired. **No number moves and no verdict moves**: a linear scaffold and a remainder-closed circular one both supply the Hamiltonian path the block needs, which is why `C-0168` settles `CH-0212` without it. What is missing is the **statement** — and `DECISIONS-FOR-NDI.md` decision 5 records NDI's answer as *circular M13, remainder the default*, which is the reading the file does not carry |
| [`CH-0216`](CH-0216-the-recommended-block-is-graded-at-neither-of-its-widths.md) | [`C-0141`](../claims/C-0141-honeycomb-station-lattice-and-placement.md) (`T-219`) and [`C-0142`](../claims/C-0142-coupled-cells-at-the-honeycomb-cross-section.md) (`T-232`) — both set `edgeX` from a 112 bp row | **in-silico, priced.** `112 bp = 38.08 nm` is the **square** sheet's own row length and the **withdrawn** `112 / 108` honeycomb pair's; the drawable `102 / 109` raster's rows span **109 bp = 37.06 nm** and its box is **116 bp = 39.44 nm**, and 38.08 is **neither** — while decision 8 puts exactly those two readings to NDI as the choice | **RAISED AND PRICED — no verdict moves.** Swept on the studies' own tile the free-tile collar dishing reads **0.0231880196 / 0.0240648102 / 0.0231299291** at 109 / 112 / 116 bp — at most **3.9 %**, both alternatives *better* than the value in print, all three flat at `T-5b`'s 0.10. The repair is a re-emission and it belongs with [`T-263`](../tasks/T-263-honeycomb-grillage-regrade.md) |
| [`CH-0217`](CH-0217-the-penalty-was-never-compared-against-the-crossovers-own-displacement-stiffness.md) | [`C-0009`](../claims/C-0009-discrete-lattice-tile.md) — the justification committed beside `OrigamiGrillage.RIGID_LINK_STIFFNESS`: *"`10⁴ pN/nm` is roughly ten times the duplex stretch modulus per nm and about 5000× the hinge's own equivalent vertical stiffness `k_θ/d²`, and gate 4 shows the transmitted force has stopped moving by then"* — and the `linkStiffness` convergence sweep in `structure/DiscreteLatticeTileStudy.kt` that is *gate 4*, whose rungs are `1e2, 1e3, 1e4, 1e5, 1e6` | **logical** — a penalty is justified as *large compared with* three things, and the one thing it has to be large compared with is the stiffness of the element it stands in for. That element's stiffness is `Gen1Tile.crossoverInPlaneStiffness`, in the same package, and it is not among the three. **In-silico** for the consequence: the interval between the sweep's lowest rung and the derived value is where the physical answer lives | **RAISED. No number of `C-0009` moves and the verdict is upheld** — see `C-0169`'s Deliverable 3, which resolves the interval rather than disputing it |
| [`CH-0218`](CH-0218-a-pruned-trajectory-forecloses-every-question-nobody-asked-it.md) | [`C-0157`](../claims/C-0157-crossover-hinge-constant-from-oxdna.md) §7 — *"The raw trajectories (649 MB) were pruned after analysis"* — read against its own §6, *"`T-9` therefore stays open, on two of three counts"*, and against the `T-9` row of [`TASKS.md`](../../TASKS.md), whose title reads *"and, **at no extra cost**, the crossover's vertical/axial compliance"* | **methodological** — a run's raw output is the only artifact from which a question **nobody thought to ask while it existed** can be answered, so pruning it converts every unasked question from *cheap* to *a whole re-run*, silently, and in a claim whose own section says two of them are still open. `SESSION-PROMPT.md`: *"Everything built on behalf of this project stays in this project. No throwaway scratch analysis that vanishes with the session."* | **RAISED. Not one number of `C-0157` is disputed** — its bracket, its sawtooth, its non-converged rigidities and its DERIVED/RECORDED partition are consumed unchanged. What is challenged is a **cost**, and the queue row that carries it |
| [`CH-0219`](CH-0219-the-simulated-tile-is-not-the-tile-the-corpus-grades-on.md) | [`C-0157`](../claims/C-0157-crossover-hinge-constant-from-oxdna.md)'s stated ground — its `Consumes` row cites [`C-0063`](../claims/C-0063-upward-root-placement.md) for *"crossover phase 8"* — and [`C-0160`](../claims/C-0160-scadnano-writer.md)'s headline *"15 duplexes, 112 bp, **phase 8**, seven columns, 49 crossovers"* | **logical** — an integer census over the committed `gpd/designs/gen1-sheet-square-15x112.sc` against `anchoring.rasterColumnLayout`, the function every placement study calls. The design file's phase datum is the **row start** and the placement corpus's is the **tile centre**; on a 112 bp row (`7 × 16 bp`, seven **odd**) they differ by exactly 8 bp, so the simulated tile is tile-centre phase **16** — 7 columns, **49** staple crossovers, **63** ties — where the graded tile is phase **8** — 8 columns, **42** staple crossovers plus **14** raster turns, **56** ties. Their staple column sets are **disjoint** | **RAISED. No number of `C-0157` is disputed** — `k_θ = k_BT/Var(roll at a site)` is a per-site equipartition and reads no count. What is challenged is which object the variance was measured on, priced at `1.125×` in ties and **2.3 %** in peak dishing |
| [`CH-0220`](CH-0220-a-validation-range-that-spans-two-lattices-cannot-say-which-was-drawn.md) | [`C-0157`](../claims/C-0157-crossover-hinge-constant-from-oxdna.md)'s *Verification type* row — *"the design generator asserts the corpus's own lattice counts before anything is simulated"* — and the three checks in [`tools/oxdna/test_gen1_tile_design.py`](../../tools/oxdna/test_gen1_tile_design.py) that discharge it | **logical** — the generator's docstring accepts 49 as *"inside the 49–56 the corpus reports the tile builds"*, and 49 and 56 are not a tolerance on one number but **two different lattices with no staple crossover column in common**. The band admits **32 of 32** phases of this tile, so it is a tautology that reads as a check; and all three assertions compare the generator against its own literals rather than against `rasterColumnLayout` | **RAISED. The generator is internally consistent and its three checks pass**; the charge is about what they are checks *against*. The discriminator is four lines and it returns a **phase** where a band returns a boolean |
| [`CH-0221`](CH-0221-the-mechanism-of-a-reversal-had-no-carrier-for-two-syntheses.md) | [`C-0155`](../claims/C-0155-tenth-answers-synthesis.md) and [`C-0165`](../claims/C-0165-eleventh-answers-synthesis.md) — the tenth and eleventh synthesis passes, neither of which consumes [`C-0154`](../claims/C-0154-honeycomb-grillage.md) nor cites it in either deliverable | **a census, one `grep`.** `C-0154` was filed in iteration 38, in the same commit as `C-0155`, and measures that `OrigamiSheet.acrossHelixRigidity` overstates a honeycomb block's `D_⊥` by `24/7 = 3.42857×` and that its `10 × 6` block is outside `T-5b` at enhancement 1.0 (**0.127358454**). Both are premises of statements standing in both deliverables — every coupled-cell count, and `10 × 6`'s *"no coupling-fraction threshold at all"*. `grep` for `C-0154`, `24/7`, `dimer`, `acrossHelixRigidity` and `0.0449400126` over both documents at `9e35853` returns **0 hits**. The cause is structural: **a claim about a MODEL states no verdict about the device**, so a synthesis keyed on verdicts has no passage to attach it to until a later claim converts it into one — which `C-0167` did, two iterations later | **RAISED and REPAIRED in the same pass** ([`C-0171`](../claims/C-0171-twelfth-answers-synthesis.md), `T-276`). **No number of `C-0155` or `C-0165` is disputed**; every reading either pass recorded was correct when taken. What is challenged is the **search** |
| [`CH-0222`](CH-0222-a-self-describing-count-can-be-right-and-its-predicate-wrong.md) | [`C-0165`](../claims/C-0165-eleventh-answers-synthesis.md) §3 and the passage it wrote into [`DECISIONS-FOR-NDI.md`](../../DECISIONS-FOR-NDI.md) — *"there are now SEVEN, and every one of them reads this file … All seven are wired into `tools/verify.sh`"* | **derivation, three commands.** `ls tools/check-*.py tools/trace-answers.py` returns **8**; `grep` of `tools/verify.sh` shows **7** invoked — all but `trace-answers.py`, whose *self-test* is wired in `build.gradle.kts` and whose checker is wired nowhere; and by scope exactly **4** read the deliverables. One sentence carries three different predicates and a single number was written for all three | **RAISED and REPAIRED in the same pass** ([`C-0171`](../claims/C-0171-twelfth-answers-synthesis.md), `T-276`), by striking the two false clauses and printing the three derived counts with the commands that produce them. **`C-0165`'s finding is upheld** — the count had been stale and deriving it is the repair; what is added is that **a count is only as true as the predicate it is a count of** |
| [`CH-0223`](CH-0223-seven-emitters-call-no-rounding-function.md) | [`C-0162`](../claims/C-0162-round-outputs-never-inputs.md) §5(a) — *"`P1` was already discharged. All six Kotlin rounding entry points already delegated to `structure/ResultRounding.kt` before this task opened"* | **a census over the wrong population.** The six *implementations* were delegated and the statement is true; what nobody counted is the *emitters*. Seven studies write `output.writeText(json.encodeToString(result) + "\n")` with no rounding call anywhere in the source, in a shape identical to the character in all seven — and on the emission rule's own predicate (`roundForResult(v, 9, floor = 0) != v`) they carry **41 297** of the corpus's **41 369** over-precise numeric result leaves, **99.83 %**. `T-1c` emits **25 774 of its 27 272** | **OPEN — measured, not repaired.** The digit count is a judgement per study (`T-1c` and `T-1` are downstream of a solved SCF height and determined to six or fewer, `P-18`/`CH-0043`), so choosing in bulk would replace one unfalsifiable number with another. `T-272` gives all seven the emission header **without** rounding them, so the defect stays measurable |
| [`CH-0224`](CH-0224-a-regime-cannot-name-a-swept-buffer.md) | [`C-0159`](../claims/C-0159-environment-interface.md)'s `Regime`, and [`ARCHITECTURE.md`](../../ARCHITECTURE.md)'s step 6 — *"a regime block (buffer, valency, gap, bandwidth) — so consuming a result outside the range it was solved in is a gate rather than a reading"* | **methodological — the block is a property of a RECORD and was designed for a FILE.** `Regime.bufferMillimolar` is a scalar `Double?` with two states, one molarity or none at all; of the **21** studies naming `MagnesiumChlorideBuffer`, **14** declare a list of 2–5 molarities and solve every state at each. So at file granularity the block is `null` on exactly the studies whose results the gate exists to refuse, and non-null nowhere | ~~**OPEN — the key is emitted on every record and is `null` on all of them.** Three repairs are priced (per-record regime, a set-valued buffer, a `RegimeFamily`); none is cheap and none is `T-272`. **No number moves**~~ **RESOLVED** (iteration 43) — [`T-286`](../tasks/T-286-a-regime-is-a-set.md), [`C-0181`](../claims/C-0181-a-regime-is-a-set.md). Grounds upheld and the census corrected to **17 sweeping, 5 fixing one, of 22**; `emission.regime` is a `RegimeSet` — a JSON array — with **three** values (`null` not stated, `[]` no environment coordinate, `[…]` the states solved). **Repair 2 falsified by the corpus**; **repair 1, the per-record regime, is NOT discharged and is named as the residue** — a file-granular set cannot refuse a consumer picking the wrong record, and **50 of the 71 addressable read edges land on `T-3b`**, where `CLAUDE.md` records exactly that defect |
| [`CH-0231`](CH-0231-a-gate-that-cannot-fail.md) | [`C-0173`](../claims/C-0173-trace-answers-wired.md) (`T-277`) — *"it already returned its defect count, so wiring cost one line each"*, and *"it exits with `ABSENT + stale challenges + self-contradictions`, so it is already gate-shaped"* | **methodological** — a gate is a claim about a corpus and it is discharged by *running* it; this one asserts a property of the tool being wired and never ran it | **UPHELD and REPAIRED** ([`C-0177`](../claims/C-0177-queue-status-vocabulary.md)). `C-0173`'s verdict stands as a description of intent and is false as a description of the artifact |
| [`CH-0229`](CH-0229-a-census-assumes-a-premise-is-withdrawn-once.md) | [`C-0144`](../claims/C-0144-honeycomb-correction-supersession.md) §9 and the tool it retains, [`tools/T-234-census.py`](../../tools/T-234-census.py): five premise families, **one** `POINTERS` tuple, and a gate that demands a pointer from that one tuple for every `MOVED` or `DISCHARGED` occurrence | [`C-0176`](../claims/C-0176-partial-discharge-and-restatement-predicates.md) / [`T-260`](../tasks/T-260-partial-discharge-predicate.md) | **ANSWERED.** Filed and repaired in the same claim by [`C-0176`](../claims/C-0176-partial-discharge-and-restatement-predicates.md) (`T-260`) — the repair is `DISCHARGES` plus a per-family discharge — and ~~what stays open is the general question below, which is about every census this repository writes and not only this one~~ **the general question too is ANSWERED** by [`C-0182`](../claims/C-0182-name-the-discharge.md) (`T-281`) |
| [`CH-0230`](CH-0230-the-debt-line-grows-when-the-documents-are-corrected.md) | the `T-233 debt` line of [`tools/T-234-census.py`](../../tools/T-234-census.py) `--check`, and the reading every synthesis claim since [`C-0144`](../claims/C-0144-honeycomb-correction-supersession.md) has put on it — a work list of what the two deliverables still owe | [`C-0176`](../claims/C-0176-partial-discharge-and-restatement-predicates.md) / [`T-262`](../tasks/T-262-width-restatement-predicate.md), whose own `F3` fired on it | **raised** by [`C-0176`](../claims/C-0176-partial-discharge-and-restatement-predicates.md) (`T-262`), whose own `F3` fired on it. **ANSWERED** by [`C-0179`](../claims/C-0179-the-debt-line-as-a-ratio.md) (`T-280`) in all three candidates — and it **refutes the challenge's own statement of candidate 2**: the denominator it names, *all occurrences of the **same** families*, fell at **0** of the 4 non-saturated passes and rose at **4**, while a denominator over **every** occurrence the census finds fell at **3** and rose at **1**, the one rise being a pass that added two unpointed assertions and no repair. Candidate 3 is **priced and declined**: the line count rose at 11 of 11, and 7 of 51 deliverable lines carry more than one class. ~~What is not repaired is the metric~~ — the **count**'s growth is unrepaired and unrepairable, which is the challenge's real content; the **metric** is repaired, because a ratio can fall where a count cannot |
| [`CH-0225`](CH-0225-the-scf-hazard-does-not-reach-the-two-studies-it-names.md) | [`CH-0223`](CH-0223-seven-emitters-call-no-rounding-function.md) §4 — *"the digit count is a **judgement per study**: `T-1c` and `T-1` are downstream of a solved SCF height and are determined to `SOLVED_HEIGHT_SIGNIFICANT_DIGITS = 6` or fewer (`P-18`, `CH-0043`), not to nine"* | **methodological** — a rule invoked against a solver the studies do not call, and a per-study judgement located on the wrong axis | **CLOSED by the same task.** The count `CH-0223` publishes is right and is untouched; its ground and its axis are both replaced, and the replacement is what the repair was made on |
| [`CH-0226`](CH-0226-a-flat-nine-digit-corpus-predicate-has-false-positives.md) | [`CH-0223`](CH-0223-seven-emitters-call-no-rounding-function.md) §2 — *"The predicate is `roundForResult(v, 9, floor = 0) != v` — the emission rule applied to the committed value"*, and the **corpus** figure it produces, *"41 369, in 17 of 152 files"* | **methodological** — a gate that cannot come clean: its predicate is wider than the rule in one direction (it ignores a study's own declared per-key precision, 49 leaves) and its own arithmetic over-counts in another (`10.0 ** 23` is one ulp from `1e23`, and Python's `**` and Java's `Math.pow` disagree at exactly that exponent, 8 more) | **OPEN as to the residue, CLOSED as to the instrument.** The predicate is repaired in `tools/T-278-emitter-rounding-census.py`, which reads each study's declared precision from its own source and scales by the decimal literal rather than by `10.0 ** k`; the residue it then reports is **15 leaves in 5 files, every one written by a Python emitter** — not one Kotlin-written result file in the corpus is over-precise at its own declared precision |
| [`CH-0227`](CH-0227-the-honeycomb-lattice-omits-the-rasters-own-turn-ties.md) | [`C-0154`](../claims/C-0154-honeycomb-grillage.md) (`T-253`) — `HoneycombGrillage.bonds`, the **staple** crossover ladder, and every free-tile dishing built on it; and through it [`C-0167`](../claims/C-0167-coupled-cells-on-the-honeycomb-grillage.md)'s uncoupled references and 64 re-graded cells | **logical + in-silico** — a raster carrying zero unpaired nucleotides ties its `H − 1` turns covalently at `s = ±L/2`, past the last 7 bp plane, and none of the 59 is in the bond list; raised by [`C-0175`](../claims/C-0175-drawable-raster-rim.md) / [`T-254`](../tasks/T-254-raster-turn-prestrain.md) | **RAISED.** Sign favourable, size `1.12×` at `10 × 6` and `1.017×` at `15 × 4`; **no verdict reverses** and the untied readings reproduce `C-0167`'s own at `1e−9`. What is owed is the coupled re-grade on the tied lattice |
| [`CH-0228`](CH-0228-every-allowed-scaffold-crossover-is-a-prestrain.md) | [`C-0152`](../claims/C-0152-forced-scaffold-crossover-price.md) (`T-246`) §7 and §11, which name the flatness channel as a question about the **ten forced** crossovers; and [`C-0154`](../claims/C-0154-honeycomb-grillage.md) §5, whose ceiling is *"over every choice of **ten** sites"* | **logical + in-silico** — `C-0152` §5's own calibration read as a load: an **allowed** scaffold crossover carries `8.57142857°`, and there are **59** of them on every raster; raised by [`C-0175`](../claims/C-0175-drawable-raster-rim.md) / [`T-254`](../tasks/T-254-raster-turn-prestrain.md) | **RAISED.** Neither claim is wrong; both are incomplete in the direction that **understates** the load, and at the design this programme recommends the forcing is **zero** while all 59 turns carry the allowed departure. `59 × 8.57°` moves within **9 %** of what `10 × 17.14°` does |
| [`CH-0232`](CH-0232-the-agreement-check-agreed-about-words.md) | [`C-0177`](../claims/C-0177-queue-status-vocabulary.md) (`P-29`) — *"THE PART THAT HAS CONTENT is not the vocabulary list, it is the AGREEMENT"* | **methodological** — the agreement was taken over the vocabulary's MEMBERS and not over the predicate that FINDS them, so the gate's *leading, short, bold* run and the reader's *whole-row scan* stayed different predicates; the unguarded residue held **four open rows the register read closed**, and the reader could not see **2 of 273** rows at all | **UPHELD and REPAIRED** ([`C-0178`](../claims/C-0178-leading-verdict-and-row-coverage.md)) |
| [`CH-0234`](CH-0234-no-verdict-reverses-was-a-free-tile-statement.md) | [`C-0175`](../claims/C-0175-drawable-raster-rim.md) (`T-254`) and the [`CH-0227`](CH-0227-the-honeycomb-lattice-omits-the-rasters-own-turn-ties.md) **Status** field it wrote — *"**No verdict of `C-0154` or `C-0167` reverses**"* — together with §5's first bullet, whose evidence is a table of six **free-tile** dishings | **methodological + in-silico.** The statement's evidence is a free tile and `CH-0227` §6 says in as many words that the coupled case was not run; run, on the same 64 cells, the same extent, the same stations, the same mandate, the same `p/k_f` stroke and the same 4 000-realisation stream, `C-0167`'s *"`0` of `64`"* becomes **`2` of `64`** — `0.106041029 → 0.0995744767` and `0.101931622 → 0.0998791032`, both at `f = 0.30` and both rim-graded. The favourable direction is exactly what reverses a cell sitting **1.93 %** over the tolerance | **RAISED and ANSWERED in the same iteration** ([`C-0180`](../claims/C-0180-tied-honeycomb-coupled-regrade.md), `T-279`). **Not one number of `C-0175` is disputed** — its six free-tile readings reproduce at `≤ 1.5e−9` and its `435 + 59` census exactly; what is challenged is the **scope**. The recovery is thin (**0.426 %** of the tolerance, converged at `4.57e−4`) and becomes **`1` of `64`** at either sign of the `8.57142857°` `CH-0228` establishes every allowed scaffold crossover carries, a *different* cell at each |
| [`CH-0236`](CH-0236-a-line-left-advisory-on-a-ground-about-its-own-predicate.md) | [`C-0178`](../claims/C-0178-leading-verdict-and-row-coverage.md) §5 — *"The residue is printed and NOT gated. A row whose prose carries a closing word that is not its verdict **cannot be made clean**"* | **methodological** — an impossibility asserted of a QUESTION and demonstrated only of an INSTRUMENT. `T-261`'s three criterion words, the standing counter-example, are already **backticked** in the row as committed, so blanking inline code spans before the whole-row scan clears it in one line. Over **139** revisions of `TASKS.md` the blanked predicate fires **115** row-instances on **7** distinct rows against **118** on **8**, and all seven are genuine idiom violations with repairs that falsify nothing | **RAISED and REPAIRED in the same iteration** ([`C-0183`](../claims/C-0183-residue-as-a-gate.md), `T-283`). **No number of `C-0178` is disputed** — its *"4 rows before the document repair and 1 after"* is reproduced exactly. The residue is now a gate, reading **0** rows, at a measured false-positive rate of **0** |
| [`CH-0237`](CH-0237-a-mutation-harness-layout-is-a-premise-of-its-own-measurement.md) | [`C-0178`](../claims/C-0178-leading-verdict-and-row-coverage.md)'s Verdict and §6 — *"**24 mutations, 0 survivors**"* — and the harness that produces it, [`tools/P-30-mutation-test.py`](../../tools/P-30-mutation-test.py) | **methodological** — the harness copies `tools/` **flat**, and the gate resolves its queue as `dirname(dirname(__file__))/TASKS.md`. Harmless while no self-test read the queue; the moment `T-283` added ones that do, **every one of the 24 rows** was `killed` by one and the same `FileNotFoundError: /tmp/TASKS.md`, the headline unchanged and no test failing. `C-0177`'s *full and empty* trap reached from the SUBJECT's side rather than from the table's | **RAISED and REPAIRED in the same iteration** ([`C-0183`](../claims/C-0183-residue-as-a-gate.md), `T-283`). The layout is now `<tmp>/tools/*.py` beside `<tmp>/TASKS.md` and the failures of an **unmutated** copy are measured and subtracted; the reading is **24 mutations, 0 survivors** again, at a baseline of **0**. `T-283`'s own first run read **12 of 12 survivors** on the same defect in the loud direction |
| [`CH-0238`](CH-0238-a-tools-own-instrument-was-wrong-about-which-of-its-tests-are-load-bearing.md) | [`tools/T-234-mutation-test.py`](../../tools/T-234-mutation-test.py)'s `_named_tests`, and through it every `UNREACHED` list [`C-0176`](../claims/C-0176-partial-discharge-and-restatement-predicates.md) and [`C-0182`](../claims/C-0182-name-the-discharge.md) quote from that tool | **methodological** — a mutation table reports two things by two instruments, and the second one MATCHED where it should have PARSED. `_named_tests` captured only the FIRST string literal after `ok(`, so every test whose name is written as adjacent literals across source lines was listed **truncated** while the self-test reports the whole concatenated name; the two never compare equal, so a test is recorded `UNREACHED` however many mutations kill it. **182 names against 184** over an unchanged test set, and on `T-285`'s own rule **7** tests were listed unreached of which **3** had been killed by a mutation in the same run. The failure direction manufactures work and hides the real gap in one list | **RAISED and REPAIRED in the same iteration** ([`C-0184`](../claims/C-0184-a-slug-is-not-a-statement.md), `T-285`). Parsed with `ast`, which folds implicit concatenation exactly as the interpreter does at the call, so the list and the `FAIL` lines are the same strings by construction; the extractor carries its own self-check, which refuses the run rather than reporting a table taken with a broken instrument. All **18** of `T-285`'s named tests are reached |
| [`CH-0239`](CH-0239-a-refactor-orphaned-a-wired-gates-anchors.md) | [`C-0178`](../claims/C-0178-leading-verdict-and-row-coverage.md) (`P-30`) §4 and §6 — the lift of the verdict predicate into [`tools/queue_verdicts.py`](../../tools/queue_verdicts.py), and a verification table listing thirteen gate readings and **not** the wired mutation test of the predicate it moved; and [`C-0180`](../claims/C-0180-tied-honeycomb-coupled-regrade.md) and [`C-0181`](../claims/C-0181-a-regime-is-a-set.md), whose Provenance rows quote a suite count taken with the failing task **excluded** | **methodological** — a mutation anchor is a reference into somebody else's source, and a refactor orphans it. Five of six anchors of `tools/test-check-queue-vocabulary.py` went looking for text `P-30` had moved one file across, and `testQueueVocabularyMutations` went red at **`9620d3e`**, which is `P-30`'s own commit. Both exclusions blamed *"a concurrent agent's in-flight file"*, and the second did so having verified the red in a `git archive HEAD` tree, which contains no such file by construction. **A red gate survived a whole iteration because two authors each believed the other owned it** | **RAISED and REPAIRED in the same iteration** ([`C-0185`](../claims/C-0185-orphaned-mutation-anchors.md), [`P-31`](../tasks/P-31-orphaned-mutation-anchors.md)). **No number of `C-0178`, `C-0180` or `C-0181` is disputed.** Five anchors re-anchored, none retired; the sixth row's `SURVIVES` was a **fixture artifact** and is killed by two named tests under a correct layout — [`CH-0237`](CH-0237-a-mutation-harness-layout-is-a-premise-of-its-own-measurement.md) in the quiet direction. The class is silent in principle, so [`tools/P-31-harness-census.py`](../../tools/P-31-harness-census.py) `--check` now resolves every anchor and subject symbol of **all ten** harnesses and fails the build on any orphan: **0 of 155 anchors and 33 symbols unresolved**, **10 of 10 wired** against 3 of 8 at the ref, **10 of 10 measuring a baseline** against 4 of 8 |
| [`CH-0240`](CH-0240-the-allowed-departure-is-common-mode.md) | [`CH-0228`](CH-0228-every-allowed-scaffold-crossover-is-a-prestrain.md), whose load is *"a built-in **relative** roll"* at all 59 raster turns; [`C-0175`](../claims/C-0175-drawable-raster-rim.md) (`T-254`) §8, whose unit-response bank and triangle-inequality ceiling are built on `HoneycombGrillage`'s dihedral spring; and [`C-0180`](../claims/C-0180-tied-honeycomb-coupled-regrade.md) (`T-279`) §4, which carries the same term into a flatness verdict | **logical** — one line of algebra on the challenged claims' own azimuth convention. Both helices of a bond are parallel, same-handed and at one design twist, so `∂(ψ_P − ψ_Q)/∂ζ = 0` **identically**: a level displacement produces **no** relative azimuth, only a **common-mode** one. [`tile/ForcedCrossoverPrice.kt`](../../src/main/kotlin/tile/ForcedCrossoverPrice.kt)'s own header says it verbatim — *"rotates **both** backbones … in the **same** sense"* — and the tie prestrain is applied as an equal-and-opposite couple pair, the work conjugate of `Φ_upper − Φ_lower`, on which a common-mode azimuth has coefficient **exactly zero** | **UPHELD** by [`C-0190`](../claims/C-0190-the-departure-is-common-mode-and-what-replaces-it.md) / [`T-291`](../tasks/T-291-common-mode-departure-and-beam-twist.md), on three checks and no solve, having been raised by [`C-0187`](../claims/C-0187-the-turn-prestrain-sign-is-derived.md) / [`T-284`](../tasks/T-284-turn-prestrain-sign.md). **No number of `C-0175` or `C-0180` is disputed** and `C-0152` §5's calibration is untouched; what is challenged is which mechanical channel the departure loads. What replaces it is not zero: the derived assignment **alternates**, so each interior helix is asked for `17.1428571°` of **twist** over its own row — a per-beam torsional eigenstrain `HoneycombGrillage` has no term for, whose **sign relative to the applied term is unknown**. Priced by `T-291`, and it is **not small**: on the twist channel the free tile reads **0.296735462** of the stroke against the relative roll's **0.0457993778**, and §4's parenthetical that *"a relative roll does reduce the phosphate span, just less efficiently"* is **withdrawn** — `u* = 1.37990892 > 1`, so it does not reduce it at all |
| [`CH-0241`](CH-0241-the-preserved-priority-idiom-is-a-dropped-cell.md) | [`C-0178`](../claims/C-0178-leading-verdict-and-row-coverage.md) §2, *"which verdict WINS"*, and the same sentence in [`tools/queue_verdicts.py`](../../tools/queue_verdicts.py)'s own header comment | **methodological** — a correct rule justified by a reading of the corpus the corpus does not support. `C-0178` reads nine rows as *"the live verdict in an EARLIER cell and the original note in a LATER one — `C-0071`'s strike, never delete applied to a whole column"*; read at `C-0178`'s **own** baseline ref with a predicate that locates each table's status column from that table's own header, **9 of those 9** carry their verdict outside it, because the rows have **dropped the `Leaf` cell**. There is no column for *strike, never delete* to have been applied to, and `tools/check-markdown-tables.py` is clean on every one of them because their cell **count** matches the header — only the **meaning** does not | **RAISED and ANNOTATED in the same iteration** by [`C-0188`](../claims/C-0188-a-verdict-in-the-wrong-column.md) / [`T-289`](../tasks/T-289-a-verdict-in-the-wrong-column.md). **The rule is UPHELD and its GROUND is withdrawn**, and nothing `C-0178` computed moves. The ground is what says when the rule fails: `T-276` is the **tenth** row of the same shape with the two contents exchanged, and there the leftmost verdict is the **superseded** one and a live **HIGH** row left the register. Three rows of the family (`P-12`, `P-20`, `T-276`) have already been repaired into the status cell by three hands in three iterations, with no rule written down anywhere |
| [`CH-0242`](CH-0242-the-tie-carries-no-common-mode-stiffness.md) | [`C-0154`](../claims/C-0154-honeycomb-grillage.md) (`T-253`) — `HoneycombGrillage`'s element set, whose bond and tie carry a dihedral spring on the **relative** roll and nothing else on the azimuthal coordinates; and through it [`C-0175`](../claims/C-0175-drawable-raster-rim.md) §8, [`C-0180`](../claims/C-0180-tied-honeycomb-coupled-regrade.md) §4, [`C-0187`](../claims/C-0187-the-turn-prestrain-sign-is-derived.md) and [`C-0190`](../claims/C-0190-the-departure-is-common-mode-and-what-replaces-it.md) itself | **logical** — a second-order expansion of `turnPhosphateSpan`, which is `C-0147`'s model and this corpus's own definition of a crossover's geometric cost, about the line of centres. The quadratic form in the two backbone azimuths is **rank two**, `(r_P/2)(θ_u² + θ_l²) + [r_P²/(2(d − 2r_P))](θ_u + θ_l)²`, and the mode the model keeps is the **cheaper** one: at `d = 2.536 nm` and `T-71`'s measured `r_P` the common mode costs **`3.52810239×`** the relative one. Exactly rather than to second order, `u* = d cos δ / (2 r_P) = 1.37990892 > 1`, so a relative roll of **any** amplitude makes a honeycomb crossover's span **worse** | **RAISED** by [`C-0190`](../claims/C-0190-the-departure-is-common-mode-and-what-replaces-it.md) / [`T-291`](../tasks/T-291-common-mode-departure-and-beam-twist.md). **No number of any of them is disputed** — what is challenged is the element set. Three consequences: a common-mode azimuthal demand has **no work conjugate at the bond at all** and reaches the structure only through the duplex's own `GJ`; `C-0175`'s and `C-0180`'s tie **stiffness** deliverables are **lower** bounds; and `C-0190`'s answer has a derived **shape** and an unpinned **magnitude**, because that magnitude is `k_common · δ`. No value is claimed for `k_common`: the ratio is exact given `g′(span) > 0`, which `T-71`'s `+1.50 σ` supplies, and the absolute stiffness needs a force-extension law for a phosphodiester step that this repository does not carry |
| [`CH-0243`](CH-0243-a-checker-census-keyed-on-a-filename-prefix.md) | [`CH-0222`](CH-0222-a-self-describing-count-can-be-right-and-its-predicate-wrong.md) and the passage [`C-0171`](../claims/C-0171-twelfth-answers-synthesis.md) wrote into [`DECISIONS-FOR-NDI.md`](../../DECISIONS-FOR-NDI.md) — *"`ls tools/check-*.py tools/trace-answers.py` returns **eight** retained checkers; … **seven** of the eight wired into it — every one **except** `trace-answers.py`"* | **derivation, four commands, no solve** — the census's predicate is a **filename prefix**, so all three of its counts are counts of `ls tools/check-*.py` and none is a count of gates. Re-derived at iteration 45: the command returns **10**, not 8; **all ten** are wired, `trace-answers.py` having been wired by `T-277` ([`C-0173`](../claims/C-0173-trace-answers-wired.md)) in the **same iteration** the challenged sentence was written; and `tools/verify.sh` runs **four further tools** under `set -euo pipefail` that the command cannot see — `result-reader-census.py`, `T-278-emitter-rounding-census.py`, `T-272-emit-result-inputs.py` and `T-272-header-census.py` — so the gate count is **14**. Which tools read the two deliverables is derived by importing each and asking it for its own file list: **4**, unchanged | **RAISED and REPAIRED in the same pass** by [`C-0191`](../claims/C-0191-thirteenth-answers-synthesis.md) (`T-276`). **`CH-0222` is upheld and generalised**: it found a count right in its number and wrong in its predicate; the predicate here is a **naming convention**, so the sentence cannot be made true by re-deriving the number, and the repaired passage quotes both counts with the command that produces each |
| [`CH-0245`](CH-0245-the-leaf-cell-was-never-dropped.md) | [`C-0188`](../claims/C-0188-a-verdict-in-the-wrong-column.md) §4 and §9 — *"the five-column science table's rows **omit their Leaf cell**"*, *"the nine rows have **dropped a cell**"* — and [`CH-0241`](CH-0241-the-preserved-priority-idiom-is-a-dropped-cell.md), whose title says the same | **methodological, and the check is one `rsplit(None, 1)` per row** — no cell was dropped. Every one of the eleven Leaf cells **ends in its own leaf token** (`A8.2` on seven, `new` on two, `—` on two), and each agrees with the newest revision of `TASKS.md` in which that cell was a bare leaf: **11 of 11**. An iteration's record was written **in front of** a leaf that is still standing behind it, which is why moving the record out of `T-276`'s cell in iteration 43 left `A8.2` in it. The mechanism is what prices the repair: on the withdrawn reading eleven leaf values must be supplied from **outside** the row and the repair adds content; on the true one it supplies **none** and moves **not one token**. `C-0188`'s own `TASKS.md` row states it correctly — *"written into the **Leaf** cell, **ending `A8.2`**"* — and its claim and its challenge do not | **RAISED and ANNOTATED in the same iteration** by [`C-0192`](../claims/C-0192-the-column-repair.md) / [`T-292`](../tasks/T-292-the-column-repair.md). **The finding is UPHELD and its stated mechanism is withdrawn**, in `C-0188` §4 and §9 and in `CH-0241`'s title and body, struck in place. **Nothing `C-0188` computed moves** — the 21, the 11/10 partition, the 615 row-instances over 140 revisions, the 0 of 24 and the 16/0 mutation table are all reproduced unchanged. The eleven rows are repaired at **0 tokens added and 0 removed** |
| [`CH-0246`](CH-0246-a-corpus-subject-result-file-cannot-be-re-run-as-a-control.md) | [`C-0110`](../claims/C-0110-device-b-tall-gap.md) — *"a PROOF that a shared-source change is invisible is not a substitute for re-running the consumers, because the run also checks everything the proof was not about"* — as a general rule, applied to the class of result file whose **subject is the corpus** | **methodological, and it was found by obeying the rule.** `T-293` changed one expression in [`tools/T-234-census.py`](../../tools/T-234-census.py) and ran its six emitting consumers; **all seven committed files moved**, and not one movement is the change under test. Each names a `baselineRef` older than `HEAD` and each emitter's `--ref` **defaults to `HEAD`**, so the obvious command re-bases the measurement onto today's corpus and overwrites the record — sharpest at `T-287-a-filename-cannot-supply-a-context.json`, whose subject is *the ten occurrences a filename admitted* and which now reports **`removed` 10 → 0**, because `T-287` itself removed them. And even at its own ref such a file is not reproducible, because the **after** side still reads the working tree | **RAISED** by [`C-0196`](../claims/C-0196-a-name-cannot-govern-a-token.md) / [`T-293`](../tasks/T-293-a-name-cannot-govern-a-token.md). **No number of `C-0110` is disputed** and its rule stands for every study of a physical model; what is disputed is its **scope**. The seven files were restored unmoved and the run was replaced by a **containment** argument about the consumers' *inputs* rather than their outputs — the predicate moves exactly one occurrence over every tracked markdown file, that occurrence is in `gpd/challenges/`, and `in_scope` is one line that excludes it. Three remedies are named, of which **(b)** *refuse to overwrite a file whose committed `baselineRef` differs from the resolved one unless `--rebase` is passed* is the cheapest |
| [`CH-0247`](CH-0247-the-tie-set-is-a-route-not-a-lattice.md) | [`CH-0227`](CH-0227-the-honeycomb-lattice-omits-the-rasters-own-turn-ties.md)'s title and Status sentence — *"the honeycomb lattice **omits** the raster's own turn ties"* — and [`C-0175`](../claims/C-0175-drawable-raster-rim.md) §9's ground, *"a raster turn sits at `s = ±L/2` … and it **is a covalent crossover like any other**"*; and through them `C-0180` §4 and the whole of `C-0190` | **substantive scope, and the evidence is the built block's OWN strand diagram.** Read off *Nature* 459:414 Supplementary Figure S4 — the monolith, whose cross-section inset is **ten rows of six**, i.e. the `10 × 6` — the scaffold occupies bases **14 → 140** of every helix (126, the paper's own allotment) and the staples **28 → 126** (98, its own paired count). So the scaffold **does** turn at the rim with no topological loopout, and the two **DUPLEX** ends it joins are **28 nt apart, 14 on each side**: the covalent link sits **`14 bp = 4.76 nm` OUTBOARD** of the duplex end and what stands between the two rim nodes is **ssDNA**. A tie between two duplex ends at `s = ±L/2` is a property of one **routing**, not of the lattice — and the 28 nt is a **purchase**, because an unpaired base has no azimuth and caDNAno's `±5 bp` condition therefore cannot bind that turn at all, which is how a lattice carrying both turn senses folds at ONE uniform length and exactly why `C-0151`'s zero-loop raster must be two-length | **RAISED** by [`C-0193`](../claims/C-0193-the-built-turn-is-a-tether.md) / [`T-296`](../tasks/T-296-zero-loop-raster-turn.md). **No number is disputed** — `C-0175` §9's `0.0501417316 / 0.0446459684 / 0.890395426`, `C-0180` §4's `2 of 64`, `CH-0228`'s `0.0764244991` and `C-0190`'s `0.296735462` all reproduce and are all correct **about route A**. Nor is route A refused: it is reachable at `+1.49997857 σ` of `T-71`'s measured step, it closes on `±5 bp` with zero forced crossovers, and this repository's own committed `.sc` draws it with **zero loopouts**. What is challenged is **which object the numbers may be attached to**, and the settling move is either one folded honeycomb origami whose raster turns carry no unpaired scaffold — `0` of `186` unique records over 30 queries reports one — or the re-grade `T-299` opens |
| [`CH-0248`](CH-0248-the-common-mode-is-the-link.md) | [`C-0190`](../claims/C-0190-the-departure-is-common-mode-and-what-replaces-it.md) (`T-291`) §6 — *"what scales it is the tie's own **common-mode** stiffness, and `HoneycombGrillage` does not carry one: its bond and tie have `½ k_θ(Φ_u − Φ_l)²` and **nothing else on the azimuthal coordinates**"* — and its headline `3.52810239`; and, inherited from it, [`CH-0242`](CH-0242-the-tie-carries-no-common-mode-stiffness.md) §3, this index's `CH-0242` row and two prose strings of `gpd/results/T-291-common-mode-departure-and-beam-twist.json` | **logical, and measured on the assembled matrix.** Four lines of `HoneycombGrillage.assemble` build the normal link with the gradient `(1, armY, −1, armY)` over `(W_a, Φ_a, W_b, Φ_b)`, `armY = (d/2)·unitY`, so its residual is a function of `Φ_a + Φ_b` and a common-mode roll **has** a work conjugate at the bond. And `d/2` is the **only** arm annihilating the linearised rigid roll `Φ ≡ α`, `W = α y` — the residual an arm `a` leaves is `α·unitY·(2a − d)` — so no other is admissible in a linear element. Probed on the recommended `10 × 6` lattice a common roll of 1 mrad at every beam stores **`0.0` pN·nm** in the hinges and **`6.7528608`** in the links, and a rigid roll stores `0.0` in all three element families | **RAISED** by [`C-0194`](../claims/C-0194-the-common-mode-is-the-link.md) / [`T-297`](../tasks/T-297-the-common-mode-is-the-link.md). **No number of `C-0190` is disputed and no verdict of it moves** — its 58 twisted beams, its `17.1428571°`, its `8.31368089 k_BT`, its `0.296735462` free tile and its `0 of 64` are all right about the load it assembles. What is disputed is the **premise the threshold rests on**: the lattice does not miss the common-mode spring, it carries it as a **constraint** at `336.800449×` the physical value in plane and `84.2001122×` through the thickness, so the departure is an **offset in the link's own residual** and its magnitude is **derivable** rather than a threshold — on that coordinate the free tile reads `0.0931890716` against the per-beam twist's `0.296735462`, **inside** `T-5b` where the twist is outside, while `0 of 24` coupled readings are flat either way. And separately, `1 + 2r_P/(d − 2r_P) = d/(d − 2r_P) = ` **`3.52847408`**, not `3.52810239` — 0.0105 % low in four documents, while `T-291`'s own `openQuestions` block emitted the right one from the same expression |
| [`CH-0250`](CH-0250-the-survivor-had-two-causes.md) | [`C-0192`](../claims/C-0192-the-column-repair.md) §8 — *"the committed queue **was itself the fixture** that made `verdicts[0]` and `verdicts[-1]` different objects … what vanished was **the** discriminating input"* — and the same singular account in `TASKS.md`'s `T-292` and `T-295` rows | **methodological — a correct observation with an incomplete causal account.** Run against `git archive 7f7957d`, the last commit BEFORE the repair, [`T-295`](../tasks/T-295-the-discriminating-input.md)'s census classifies that mutation **FIXTURE**: **2** killers in a faithful copy and **1** with every committed artifact emptied, and the survivor is `check-queue-vocabulary.py`'s own **constructed** two-verdict `_leaf_row`, fed through `_gate_on` and asserted to exit **0**. `T-292` promoted that arm, which required **inverting** the assertion to *a miscolumned verdict FAILS the gate* — and an assertion that an exit code is **1** cannot detect an ADDITIONAL defect, because exit codes saturate. So the corpus-only state existed at **no committed state of this repository**: it was made inside `T-292`'s own working tree by its two halves acting together. What is asked is the annotation, the two queue rows, and the general rule, which nothing in the corpus carries | **RAISED** by [`C-0195`](../claims/C-0195-the-discriminating-input.md) / [`T-295`](../tasks/T-295-the-discriminating-input.md). Everything `C-0192` computed is **UPHELD** and none of it moves — the 21 rows, the token-preservation proof, the repair and its `12 mutations, 0 survivors` re-reading |
| [`CH-0251`](CH-0251-the-deposited-block-has-no-loops.md) | [`C-0193`](../claims/C-0193-the-built-turn-is-a-tether.md) §3, §4 and its headline `7 of 7` | `C-0193` rests *"what the built block ACTUALLY does"* on a Methods **sentence** (126 allotted, 98 paired, 28 unpaired) and a figure reading; the **seven deposited design files for those same seven cross-sections**, retrieved from the gallery the caDNAno paper names as its own record, say **126 of 126 paired, `0` unpaired at any helix end, and `0` scaffold crossings off the `±5` lattice**. The `126` allotment and the paper's own scaffold-pairing list are reproduced by the files at 7 of 7 — the **split** is not. The `10 × 6` block is deposited twice, by both papers, **bit-identical**, and its raster turns carry zero unpaired nucleotides, which is `C-0193`'s own route A drawn. Two primary artifacts of the same authors disagree and only one was consulted; the decisive experiment (the SI staple **sequence tables**: `5 880` nt against the file's `7 560`) is named and not run | **RAISED** by [`C-0199`](../claims/C-0199-the-gallery-opened.md) / [`T-255`](../tasks/T-255-cadnano-gallery-forced-crossovers.md). `C-0193`'s quotation, arithmetic, FJC pricing, reach bound and scaffold budget are **UPHELD** and none of them moves; what is challenged is which artifact settles the built form, and therefore which way `C-0193` §7's fork points |
| [`CH-0252`](CH-0252-two-rows-one-identifier.md) | [`TASKS.md`](../../TASKS.md) as committed at `b04a675` — **two rows opening `\| T-300 \|`**, at lines 867 and 871, filed by two agents in one iteration | **register defect, and the failure direction is the unsafe one.** `queue_verdicts` keys a row on its **leading identifier cell** and `queue_status` returns **one verdict per identifier**, so two rows under one identifier collapse to one entry: measured at that commit, `queue_status("T-300") == "OPEN"`, one answer for two questions, and a closing verdict on either row would have been read against **both**. No gate here can see it — `check-queue-vocabulary.py` reads verdict WORDS and both rows are individually well-formed, `check-corpus-identifiers.py` resolves identifiers that are **cited** rather than ones **defined twice**, and `trace-answers.py` compares against a `queue_status` that has already collapsed them. `CLAUDE.md` records the mechanism (*a reserved task ID can be taken by a sibling between the reservation and the work*) and not this consequence | **RAISED and REPAIRED in the same commit** by [`C-0202`](../claims/C-0202-a-length-is-not-a-provenance.md) / [`T-300`](../tasks/T-300-a-length-is-not-a-provenance.md). The **later** row is renumbered `T-303` and keeps its old number inside its own note, so a `grep` still lands — `C-0071`'s *strike, never delete* on an identifier. The direction is recorded and disputable: the register's own reservation block and `JOURNAL.md` bind `T-300` to the census family, and the renumbered row's three outside references ([`C-0194`](../claims/C-0194-the-common-mode-is-the-link.md) twice, `CLAUDE.md` once) are none of them links. **The gate is deliberately NOT written here** — one line, *no two rows may open with the same identifier* — because an agent who has just repaired the only instance would ship a gate the corpus cannot exercise (`C-0161`) |
| [`CH-0253`](CH-0253-a-register-census-of-a-drawing.md) | [`C-0199`](../claims/C-0199-the-gallery-opened.md) §1 — *"measured on the deposited files, `10` of the 26 designs carry **zero** unpaired scaffold — including all seven caDNAno blocks and the Nature monolith — **so the exemption does not apply to them at all**"* — its `F5` firing, and the `zeroLoopTurns` block of `gpd/results/T-255-cadnano-gallery-forced-crossovers.json` | **an artifact the census could not consult, now read.** The Nature paper's own **`monolith staple sequences`** table orders `144` strands and `5 880` nucleotides against the deposited file's `214` and `7 560`; the `70` strands it omits total exactly `60 × 28` and lie in the helix ends. So under the ORDER every one of the block's 60 helices carries `28` unpaired scaffold bases, and **`60` of the `118` scaffold crossings `C-0199`'s register test scores sit in single-stranded scaffold** — where there are no base pairs, `C-0193` §4's *an unpaired base has no azimuth* applies exactly. `C-0199` §10 states the validity range itself (*"this is a census of DESIGN FILES"*) and §1 draws a folded-object conclusion from it | **RAISED** by [`C-0200`](../claims/C-0200-the-file-draws-and-the-table-orders.md) / [`T-302`](../tasks/T-302-si-staple-order.md). **Nothing `C-0199` measured is disputed** — the retrieval, the 26-design census, the three tests, the `28` one-base-pair departures, the digitised yields, the `0 of 15` conjunction and the bit-identity finding all stand, and `C-0200` rests on two of them. What is disputed is one inference and the artifact class its `F5` was read on |
| [`CH-0254`](CH-0254-the-tether-was-priced-as-a-stiffness.md) | [`C-0193`](../claims/C-0193-the-built-turn-is-a-tether.md) §11 — *"it is not a tie and it is not nothing: it is a one-sided entropic element"* — [`CH-0247`](CH-0247-the-tie-set-is-a-route-not-a-lattice.md)'s *What would settle it*, and the `T-299` row of `TASKS.md` as it was written, all three of which describe the missing raster-turn element by its **stiffness** | **substantive scope — a question posed on the wrong quantity.** Measured, the tether's tangent stiffness is `0.22377084–0.919840405 pN/nm`, at most `0.109313066` of `k_θ` on the rim node's own `d/2` arm and `9.19840405E−5` of the link penalty the lattice uses — so the cheap bound the question asks for returns *"arithmetically no element at all"*, and with the preload removed the free tile moves `0.0501417315 → 0.0496660245`, `0.95 %` and **toward** flatness. What moves the tile is the chain's **PRELOAD**, `0.160569993–3.03288672 pN`: a self-equilibrated internal load in `C-0104`'s exact sense, which takes the free tile to `0.11296458` past `T-5b`'s `0.10`. The number was already on the page — `C-0193` §8 publishes those tensions as stored **energy** — and no statement of the open question reads it as a **load on the lattice** | **RAISED** by [`C-0201`](../claims/C-0201-the-tether-is-a-load-not-a-spring.md) / [`T-299`](../tasks/T-299-tethered-raster-turn.md). **Nothing `C-0193` or `CH-0247` computed is disputed** — the fork, the scaffold budget, the FJC pricing and the energy table all stand and `C-0201` reproduces them. What is disputed is the **statement of what is open**, and the general rule it asks for: an element can be overwhelmingly a stiffness and still be decided by an initial stress, so a cheap bound taken on the wrong one of the two is not conservative — it is silent |
| [`CH-0255`](CH-0255-a-cross-reference-is-not-an-index-row.md) | [`tools/check-challenge-index.py`](../../tools/check-challenge-index.py)'s `indexed()`, and the standing reading that this index is complete | **a gate that cannot see** — its row pattern is unanchored and applied over the whole file, so a challenge merely LINKED from another row's cell counts as indexed | **RAISED** by [`C-0203`](../claims/C-0203-a-challenges-status-row-is-the-authority.md) ([`T-298`](../tasks/T-298-a-challenges-status-row-is-the-authority.md)). `CH-0053` had no row of its own for **38 revisions** of this file while the gate reported `0 unindexed` on every one; the row is added and the predicate is not repaired here |
| [`CH-0256`](CH-0256-the-azimuth-is-not-a-convention.md) | [`C-0201`](../claims/C-0201-the-tether-is-a-load-not-a-spring.md)'s headline and its §3 table, and the `span` row of [`T-299`](../tasks/T-299-tethered-raster-turn.md)'s own conventions block — all three of which read the tether's free tile at the **worst** azimuth, *"an AZIMUTH convention nobody has measured"* | **quote it with the state it is read at.** The span is not a convention: a chain leaves a helix at the phosphate of its **last paired base**, and on the drawable `102 / 109` raster `C-0187`'s `b₀ = 5` puts **every one** of the 59 turns at an *allowed* scaffold crossover, so both phosphates sit `8.57142857°` off the line of centres and the span is `0.787091706 nm` — `C-0152`'s own allowed-crossover span, reproduced out of `gpd/results/T-246-…json` at `4.2e−10`, at all 59 turns, all eight datum readings and every anchor offset. There the free tile is flat at **`16 of 16`** surviving corners, `0.0569497052–0.0689014279`, where the headline's `0.11296458` is the `d + 2r_P` corner the raster reaches at **no turn** | **RAISED** by [`C-0204`](../claims/C-0204-the-anchor-azimuth-is-determined.md) / [`T-304`](../tasks/T-304-raster-turn-anchor-azimuth.md). **`C-0201`'s method, its element, its `0 of 64` coupled verdict and its *load, not a spring* finding all stand and are reproduced here**; `C-0201` §9 and §10 name this gap in as many words, so what is challenged is the **headline**, which does not carry that qualifier. And the alarm is **relocated, not withdrawn**: route B's own uniform rasters close at no lattice phase, so their worst turn spans `3.93454333–4.35327572 nm` over all 21 — reaching that very corner at some of them (`T-307`) |
| [`CH-0260`](CH-0260-the-third-repair-made-the-fourth-collision.md) | [`C-0203`](../claims/C-0203-a-challenges-status-row-is-the-authority.md) / [`T-298`](../tasks/T-298-a-challenges-status-row-is-the-authority.md), and the `T-306` queue row's own account of its third collision — *"repaired by making that harness print `killed by N named test(s)` … **the harness moved, not the parser**, deliberately"* | **methodological — a correct repair, in the direction this corpus recommends, that made the next instance of the class it was repairing.** `T-298`'s row printed its killers after the name and the `killed-by` shape captures everything after the count, so the parsed LABEL was `name + padding + killers`; the killers differ once the census empties the corpus, the two arms' labels disagreed, and `tools/T-295-mutation-input-census.py` refused the harness and printed **ten uncensused mutations as a clean row of zeros**. The refusal message truncated both labels to forty characters and they are identical for their first forty, so the report printed two strings that look the same and said they differ. Found by `tools/verify.sh --committed` at `342d7ad`, which is the run no agent's own run is | **RAISED and REPAIRED in the same iteration** by [`C-0206`](../claims/C-0206-a-harness-output-format-is-an-interface.md) / [`T-306`](../tasks/T-306-a-harness-output-format-is-an-interface.md). `C-0203`'s 17 annotations, its gate, its mutation table and the DIRECTION of its repair all stand. What is landed is a rule saying what a row may carry: a parsed label must be a **prefix** of one of the harness's own mutation names — which `P-31`'s adapter already reads — so padding and truncation are fine and anything printed after the name is not, and it runs on the **control arm alone**, catching the class at authoring time with no drift needed. Plus a drift report that names the first differing character, and a **third state** in the table so a refused harness prints `REFUSED` and never `0` |
