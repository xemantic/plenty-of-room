# C-0149 — **The ninth synthesis carries two REMOVALS and raises one QUESTION, and the removals were both invisible because they run the favourable way.** The honeycomb turn slack is a **choice** — 6 nt by reach against the built 28, `4.66666667×` — and the 4 bp ragged face costs §3's flatness **exactly zero**, being on the tile's **rim**. What it does cost is **plan budget**, 0.571 of a saturated outboard ceiling. The question is decision **8**: a two-length raster has **two true widths**, `38.08` and `39.44 nm`, and §3 states one number — posed with its **price line open**, because when six decisions were offered as yes/no all three with a cost dimension came back in a shape the document had not offered. Cheap bound: a grep found **0** carriers to strike for `C-0147` and **4** for `C-0146`, which is the reverse of what was predicted

| | |
|---|---|
| **Task** | [`T-240`](../tasks/T-240-deliverables-owe-turn-slack-and-ragged-face.md) — the two outward-facing documents owe `C-0147`'s two results — and [`T-242`](../tasks/T-242-which-width-section-three-names.md) — which of `C-0146`'s two width readings §3 is owed |
| **Leaf** | — (`T-240` is a process claim on the documents that report every leaf; `T-242` is `A8.2`, and its deliverable is a **question**) |
| **Verification type** | **logical** — a mechanical grep-located reconciliation of two documents against three claims, plus one reading of the problem definition. **No physics is derived here**, no study is run and no result file is emitted |
| **Verdict** | **PASS on all eleven predicates.** `T-240` `P1`–`P6` and `T-242` `P1`–`P5`. **Of the eight declared falsifiers, five did not fire and three FIRED — `T-240`'s `F1` and `F4`, and neither in the direction declared.** `F1` predicted no strikes were owed and **four** were, all against `C-0146` rather than `C-0147`; `F4` was declared as *"the sweep finds nothing the corpus has answered that a deliverable still calls open"* and the sweep found **two**, plus **two** stale-precision items and **one** withdrawn ground still cited as live. Raises [`CH-0190`](../challenges/CH-0190-a-synthesis-owns-the-token-it-diagnosed.md) and [`CH-0191`](../challenges/CH-0191-a-width-advantage-across-two-conventions.md) |
| **Maturity** | **Below TRL 1–3: nothing here is physics.** Every quantity written into either document is read from [`C-0146`](C-0146-coupled-cells-at-the-two-length-raster.md), [`C-0147`](C-0147-honeycomb-turn-slack-and-ragged-face.md), [`C-0140`](C-0140-honeycomb-raster-turn-sense.md), [`C-0141`](C-0141-honeycomb-station-lattice-and-placement.md), [`C-0032`](C-0032-softening-coupling-stability.md), [`C-0093`](C-0093-shared-body-coupling.md) or [`C-0116`](C-0116-composite-fraction-threshold.md), at that claim's own precision |
| **Provenance** | No study, no result file. The five retained document checkers plus `tools/T-233-reconcile.py` and `tools/T-234-census.py --check`, run before and after; the owner partition of `tools/trace-answers.py`'s output that `CH-0190` rests on is one `awk` over its own emitted rows |
| **Conditions** | The corpus at iteration 36. **A snapshot**: two sibling agents (`T-244`/`T-243`, `T-225`/`T-227`) were writing `TASKS.md`, `JOURNAL.md` and `CLAUDE.md` while this pass ran, and this claim edits neither their tasks nor their sources |
| **Consumes** | [`C-0147`](C-0147-honeycomb-turn-slack-and-ragged-face.md) (`T-230`, `T-231` — the turn slack and the ragged face), [`C-0146`](C-0146-coupled-cells-at-the-two-length-raster.md) (`T-235` — the two width readings and the stagger), [`C-0145`](C-0145-eighth-answers-synthesis.md) (the previous pass, whose conventions and whose 7a/7b re-posing are preserved), [`C-0140`](C-0140-honeycomb-raster-turn-sense.md), [`C-0141`](C-0141-honeycomb-station-lattice-and-placement.md), [`CH-0185`](../challenges/CH-0185-a-bounding-box-crossover-column.md), [`CH-0186`](../challenges/CH-0186-the-twenty-eight-nucleotide-turn-is-a-choice.md), [`CH-0187`](../challenges/CH-0187-the-two-length-recommendation-rests-on-an-unstated-filter.md), and — for the three repairs of §4 — [`C-0067`](C-0067-answers-reconciliation.md), [`C-0115`](C-0115-fifth-answers-synthesis.md), [`C-0032`](C-0032-softening-coupling-stability.md), [`C-0035`](C-0035-flexure-mounting-sense.md), [`C-0049`](C-0049-compliance-ceiling-stroke.md), [`C-0093`](C-0093-shared-body-coupling.md), [`C-0116`](C-0116-composite-fraction-threshold.md). **Zero fetches** |
| **Constrains** | [`ANSWERS.md`](../../ANSWERS.md), [`DECISIONS-FOR-NDI.md`](../../DECISIONS-FOR-NDI.md) and [`TASKS.md`](../../TASKS.md)'s register only. **No claim, number or verdict is contradicted, and none is re-derived** |

---

## 1. The cheap bound, and it inverted the task

The expensive half of a document pass is prose; the cheap half is **locating**.
So before a word was written, both documents were grepped for every token `C-0147` and `C-0146`
could bear on — `92 bp`, `31.28`, `28 nt`, `ragged`, `turn loop`, `116 bp`, `39.44`, `38.08`,
`two-length`, `row length`.

| | predicted | measured |
|---|---|---|
| passages owed a **strike** because they contradict `C-0147` | some | **0** |
| passages owed an **addition** because `C-0147` supplies what was never there | few | **6** (three per document) |
| passages owed a **strike** because they contradict `C-0146` | *not asked* | **4** |

> **`T-240`'s `F1` was declared as *"a passage contradicts `C-0147`"* and fired against the wrong
> claim.** Nothing in either document contradicts `C-0147` — its two results are simply absent, and
> `92 bp`, `31.28`, `28 nt` and `ragged` appear **zero** times between them.
> What *is* contradicted is `C-0146`, filed by a different agent in the same iteration as `C-0147`:
> **four live passages assert that the honeycomb has no derived row length and that `38.08 nm` is a
> withdrawn width, and it is neither.**

That is `CH-0182`'s *a census is dated by its premise set* one more time, and this pass was the
census. The task named one claim; the documents had drifted against its sibling.

## 2. `T-240` — the turn slack, and the minimum is a CRITERION

Both documents now carry, in `DECISIONS-FOR-NDI.md` §7b and `ANSWERS.md` §1 and §5:

| criterion | ground | nt per turn | widest uniform row on M13 |
|---|---|---|---|
| **reach** | `T-71`'s **measured** phosphodiester step; below it no conformation closes | **6** | **114 bp = 38.76 nm** |
| the 10 pN unzip allowable | `Gen1Tile.DUPLEX_UNZIP_ALLOWABLE` | 8–9 | 111–112 bp |
| **one `k_BT`** of stored free energy | the fold's own currency | **16–22** | 98–104 bp |
| one pN of turn tension | below every per-path allowable here | 29–41 | 79–91 bp |

The built caDNAno blocks spend **28**, which is **`4.66666667×`** the reach bound and therefore a
**choice**; read at the worst azimuth it carries **1.00195245–1.46667915 pN** and stores
**0.518481856–0.7570064 `k_BT`** — sub-thermal, about one piconewton, **inside** the bracket the
four criteria span. So *"the widest four-layer tile is 92 bp = 31.28 nm"* (`CH-0173`) is a ceiling
on **one published allowance** and not on the route (`CH-0186`).

**M13 affords exactly eight**, from `60 × (112 + L) ≤ 7 249`. A uniform 112 bp row therefore fits
**by two nucleotides** and fits only **strained** — **0.777–0.837** of the turn's own contour,
**6.54349121–12.112167 pN**, at or past the 10 pN unzip allowable at the tight end of the ssDNA
Kuhn bracket, for **139–220 `k_BT`** over 59 turns. `C-0140`'s two-length recommendation therefore
stands **on a better reason than it had**, and p8064 — decision 7b's other reading — affords
**22 nt** and removes the question.

**The yield half is declared unpriceable and the threshold is quoted instead: 8 nt.**
That is now a bullet of `ANSWERS.md` §5, *What we cannot answer, and why*, and it is the first
entry there whose reason is that **no published measurement is on the axis at all** rather than
that the measurement is paywalled or expensive.

## 3. `T-240` — the ragged face, and the answer is an AXIS

A four-layer block's **gap-facing** surface is the outermost layer's **sidewalls**; a row length
moves where a helix **ends**, in the tile plane, at right angles to the gap.

> **So the two ragged faces are the tile's RIM, the coefficient of the raggedness on §3's
> normal-direction flatness field is EXACTLY ZERO, and `T-5b`'s convention cannot read it.**

The residual rim channel is bounded rather than zero, and the bound is not close:

| cross-section | `ℓ_across` [nm] | modulation `λ` [nm] | bounded move | headroom | margin |
|---|---|---|---|---|---|
| `15 × 4` | **17.2310927** | 7.608 | **5.54399427e−05** | 0.0274976866 | 496× |
| `10 × 6` | **23.2114857** | 7.608 | **1.68371917e−05** | 0.0274976866 | — |

**What it does cost is plan budget**, and there it is not small: 4 bp = **1.36 nm** of axial extent
outboard of a fixed root plane, against a `C-0141` outboard ceiling that **saturates at 2.380 nm**
at 90 demanded paths — **0.571 of it**, against 0.036 at 10–15 paths.
And what it **buys** is a published anti-stacking geometry, cleared by **0.18 of a base-pair rise**
— which by this project's own rule is not a quotable margin, and which is why `CH-0187` travels
with every 112 / 108 quotation both documents now make.

## 4. What the sweep found that neither task asked for — `F4`, and it fired five times

`C-0067`'s standing finding is that a deliverable that **under-claims** is as wrong as one that
over-claims, and `C-0115`'s is that `DECISIONS-FOR-NDI.md`'s failure mode has a **sign**.
Both held.

| # | where | what the deliverable said | what the corpus says | class |
|---|---|---|---|---|
| 1 | `DECISIONS` §7a and §7b; `ANSWERS` row (g) | `10 × 6`'s *"own two row lengths are not derived here"* | **the same 112 / 108 pair** — `C-0146` grades the whole `10 × 6` coupled family at it and `C-0147` finds the identical 4 / 8 bp faces and 116 bp extent on it | **under-claim** |
| 2 | `DECISIONS` §7b; `ANSWERS` row (g), decision-6 row | *"every `38.08 × …` is a withdrawn width"*, and *"what remains is the **row length**"* | `38.08 nm` is a **live reading** — every x-raster row spans it exactly — and what remains is a **convention**, not a length | **under-claim** |
| 3 | `DECISIONS` §1 | *"the adverse mounting is **42.4–61.0** pN/nm"* | `C-0032`: **42.38–61.04**. `C-0067` found and repaired this exact token in `ANSWERS.md` **in iteration 12** | **stale precision**, 24 iterations |
| 4 | `DECISIONS` §6 | *"the rigid limit reads **0.0344**"* | `C-0093`, `C-0116`: **0.0344013403**. `C-0115` repaired this exact token in `ANSWERS.md` | **stale precision** |
| 5 | `DECISIONS` §1 | *"past `C-0023`'s **40 pN/nm** compliance ceiling"* | `C-0049` **withdrew** that ceiling; the verdict survives on `C-0035`'s ground — both adverse mountings cannot place §3's own effort point | **withdrawn ground, surviving verdict** |

Items 3, 4 and 5 are the reason `CH-0190` exists, and item 5 is `CLAUDE.md`'s *a verdict that
survives can survive on a different reason* caught in the act: the sentence is still true and every
word of its justification is withdrawn.

## 5. `T-242` — how the question was posed, and the price line

**The two readings, and the difference is not a length:**

| reading | value | what it is |
|---|---|---|
| **row length** | **112 bp = 38.08 nm** | the span **every one** of the block's x-raster rows has — all ten rows, at **every one** of `C-0140`'s five candidate pairs |
| **bounding box** | **116 bp = 39.44 nm** | the axial extent the folded object occupies |

`3.57 %` apart, and the difference is a **4 bp = 1.36 nm inter-row STAGGER** which no plate or
grillage model in this repository can represent, because all of them take a single `lengthX`.

**It is not cosmetic, and the channel is one nobody would guess.** A 116 bp box clears eleven
honeycomb crossover pitches by **0.07 nm** — one fifth of a rise — so `CrossoverLayout.EDGE_MARGIN`
admits a **twelfth** column at its standing 0.05 nm and refuses it at half a rise, and that column
alone is **six flat cells of eight against three** (`CH-0185`); the row-length reading gives four.
**No recommendation turns on the answer and a margin does**: the recommended cell is flat at every
reading — **0.0662801686**, **0.0708759349**, and **0.0708859619** / **0.0754995025** at the
measured band's adverse end.

**Posed with its price line open.** `CLAUDE.md` records that a question whose admissible answers
are enumerated cannot return the one that was not enumerated, and that when the first six decisions
were offered as yes/no **all three with a cost dimension came back in a shape the document had not
offered** — a price, a contingency on another decision, and an answer volunteered inside a
different question. So decision 8 states four options **and then does not close on them**:

> **Which width is the Gen-1 tile specified to — the 112 bp = 38.08 nm row span, the 116 bp =
> 39.44 nm block extent, or both with a stated stagger? And if this costs something — in metrology,
> in how a folded object is accepted at the bench, or in a downstream drawing this programme cannot
> see — what?**

**And the same framing was withdrawn where it stood.** `DECISIONS-FOR-NDI.md`'s *"a one-sentence
answer is enough for all six; three of them need a single word"* is struck and replaced by the
measurement of what actually happened: the three that came back in the offered shape are exactly
the three with **no** cost dimension — a material, a device count and a scaffold.

**One reading of the specification came out of this and it is worth recording.** §3's parameter
table says *"Tile footprint — 40 × 40 nm (test tiles up to ~70 × 100 nm)"*: **the tilde is on the
test tiles and not on the nominal.** This corpus has written that row as *"~40 × 40 nm"*
throughout, quietly supplying a tolerance §3 does not state — which is what makes a 3.57 %
ambiguity a question rather than a rounding, and which slightly strengthens `C-0145`'s own
validity note that a reader might hold 7a open because the nominal is not a bound.

## 6. The two challenges

**`CH-0190` — a synthesis owns the token it diagnosed.** `tools/trace-answers.py` searches a corpus
that **contains the audits**, so a token quoted in order to be repaired acquires an owner and reads
`CITED`. Partitioned by owner on the corpus as it stood at the start of this iteration, **exactly
three** of 2 097 numeric tokens in the two documents have an owner set consisting only of synthesis
claims — and **two of them are live, unqualified assertions in `DECISIONS-FOR-NDI.md`, and both are
the tokens a synthesis claim repaired in `ANSWERS.md`.** A repair is applied where the checker
reports, and the checker reported one file. **The census destroys itself**: quoting the two tokens
in the challenge gives them a non-synthesis owner, so re-running the partition now returns zero
rows.

**`CH-0191` — a width advantage across two conventions.** `C-0140`'s *"`−1.40 %` beats the square
lattice's `−4.80 %`"* compares a **bounding box** against a **row length**. A uniform square-lattice
raster has **no stagger**, so its two readings coincide and there is no second figure to notice is
missing; read on the row length both tiles are **112 bp = 38.08 nm** and the advantage is **exactly
zero**, and read on the bounding box the honeycomb wins by **1.36 nm, which is the stagger itself**.
The substance is downstream: **every four-layer plan ceiling, packing verdict and coupled cell
outside `C-0146` is computed at 38.08 nm**, and both deliverables were quoting the **footprint** at
39.44 with nothing saying so.

## 6b. `CH-0182` for the third consecutive iteration, and this time it fired on a RESTORED premise

`tools/T-234-census.py`'s `WIDTH` family is *"a honeycomb row length asserted as a uniform tile
width"*, built from `C-0140`, which withdrew `112 bp` in exactly that role.
**`C-0146` restores `112 bp` in a different role — a ROW SPAN — and the family cannot tell the two
apart**, so this pass's own correct sentences read as debt: the advisory `T-233 debt` line went
**18 → 23**, and **five of the five new occurrences are this claim's own restatement**.
The gate itself is at **0 defects** (the debt line is advisory by design and does not fail it), and
`tools/T-233-reconcile.py` reads **40 RESTATED, 1 LEFT, 0 DEFECT** as it did before — but only
after two superseding cues were added to sentences that needed them.

Two tool changes were made and both are the minimum:

- `C-0149` added to `tools/T-234-emit-classification.py`'s `SYNTHESIS` set and the classification
  regenerated, for the same reason `C-0145` added itself — a synthesis quotes every withdrawn
  premise beside the value that replaced it. **The set went stale against the claim that is
  extending it, in the same iteration, for the third time running.**
- **No change to the `WIDTH` family**, which belongs to `T-234`. The observation is recorded here
  instead: *a census family built on a withdrawal is invalidated by a RESTORATION as surely as by a
  new premise, and the restored statement is the one that looks most like the withdrawn one.*

## 7. The five gates

| gate | how it was discharged |
|---|---|
| **dimensional consistency** | none — no quantity is derived here. Every number written is grepped out of the claim that owns it at that claim's own precision, and the two **ratios** (`0.571`, the `496×` margin) are `C-0147`'s own, quoted with their construction: `1.36 / 2.380` and `0.0274976866 / 5.54399427e−05` |
| **limiting cases** | the grep-located cheap bound reports **0** carriers for `92 bp`, `31.28`, `28 nt` and `ragged` in either document before the edit, which is the degenerate case `T-240`'s `F3` is written on and is why the owed repair is an addition rather than a strike |
| **symmetry / conservation** | the two documents are reconciled against **each other** as well as against the corpus, which is what found items 3–5 of §4: `ANSWERS.md` carries both repairs and `DECISIONS-FOR-NDI.md` carried neither. That cross-document comparison is **not** a retained check and `CH-0190` recommends it as one |
| **numerical convergence** | none; the reconciliation is an exact enumeration. In its place, the owner partition of `CH-0190` is derived from the tracer's own emitted rows rather than re-implemented, and it is reported **with the date it was taken**, because it is not repeatable |
| **literature cross-check** | **zero fetches.** The one primary reading is `third-party/2026-08-ndi-gen1-problem-definition.md` §3's parameter table, read directly for `T-242`'s `F1` and `F2` |

### The declared falsifiers

| # | task | falsifier | fired? | outcome |
|---|---|---|---|---|
| **F1** | `T-240` | a passage already in either document **contradicts** `C-0147`, so the owed edit is a strike | **FIRED, against the wrong claim** | **0** carriers contradict `C-0147`; **4** contradict `C-0146`, its same-iteration sibling. The prediction was right about `C-0147` and the task was wrong about which claim the documents had drifted against |
| **F2** | `T-240` | a number written here cannot be grepped out of a claim — **declared open** | **no** | every token greps; the two ratios are `C-0147`'s own and are quoted with their construction |
| **F3** | `T-240` | `CH-0186`'s challenge is already carried under a different form of words | **no** | `92 bp`, `31.28`, `28 nt` and `ragged` appear **0** times in either document before the edit |
| **F4** | `T-240` | the sweep finds **nothing** the corpus has answered that a deliverable still calls open | **FIRED** | **five** items, §4 — two under-claims, two stale precisions and one withdrawn ground under a surviving verdict |
| **F5** | `T-240` | a deliverable contradicts itself after the edit | **no** | `tools/trace-answers.py` reads **0** self-contradicted tasks on both files, before and after |
| **F1** | `T-242` | §3 already fixes the convention, so the question does not exist | **no** | §3's parameter table states a footprint and no convention |
| **F2** | `T-242` | the two readings are inside the precision §3 states — **declared open** | **no**, and the check ran the other way | the tilde in that row is on the **test tiles**, not on the nominal; §3 states the footprint **without** a tolerance |
| **F3** | `T-242` | a claim in the corpus already answers it | **no** | `C-0146` §9 declines it in as many words: *"Which one §3 is owed is a specification question this claim does not settle"* |

## 8. Validity range, and what this does NOT establish

- **Nothing here is physics and nothing is re-derived.** A restatement is only as good as the claim
  it reads, and every claim read is `PASS` at TRL 1–3 — model-consistent, never measured.
- **The reconciliation is complete against `C-0147` and `C-0146` and NOT against the corpus.**
  It was scoped by a token list, so a `C-0146` or `C-0147` premise a deliverable states **in words**
  and not in figures is not found by it — `C-0145`'s own validity note, unchanged.
- **`CH-0191` does not move `C-0140`'s recommendation**, which is selected within a family read on
  one convention; only the sentence comparing the winner to a *square* lattice is cross-convention.
  The recommendation is separately conditional under `CH-0187`.
- **Decision 8 is a question and this claim does not answer it.** Both readings are true of the same
  object; the four options are stated with their costs and **no preference is expressed**, because
  this programme has no column for what an option costs.
- **The `4 bp` ragged face's flatness cost is BOUNDED, not measured** (`C-0147` §*Still open* 1):
  no lattice model here has a per-row row length, and the bound carries `CLAUDE.md`'s 50× free-edge
  penalty because the transfer function is an infinite-plate result.
- **`CH-0190`'s census is over the two deliverables only** and is dated. Nothing here says how many
  diagnosed tokens stand elsewhere in the corpus.
- **This task edits no code, runs no study and emits no result file.** `tools/verify.sh` is run as
  the gate on the tree it edits, not on work it produced: **`BUILD SUCCESSFUL in 22m 4s`**, with the
  result-reader census (47 checks, 119 studies), the table check (**0** over 478 files), the link
  check (**0** over 471), the format-string check (**0** over `src`), the challenge index
  (**162 of 162**) and the raw-conversion gate (**0** over 138 result files) all clean.
  **The run then stopped at the departure-precision gate, and the failure is a concurrent agent's
  work in flight and not this task's**: `T-225` is widening `DEPARTURE_SPELLINGS` from four names to
  twelve, so the working tree's `tools/check-result-file-hygiene.py --departures` reports **54
  fields in 6 files** where `HEAD`'s reports **0 in 0** — isolated by running `HEAD`'s copy of the
  tool against the same result files, one command. The six files are exactly the ones `T-225`'s own
  queue row is chartered to re-emit, and **no result file is touched by this claim.**
