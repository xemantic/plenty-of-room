# C-0220 — **§3 ROW (g)'s ANSWER MOVED TWICE MORE AND NEITHER DELIVERABLE KNEW IT: A COUPLED CELL IS NOW FLAT *AND* ADMISSIBLE, AT `7 of 48` CELLS ON ROUTE B's OWN TILE, WHICH NO PREVIOUS SYNTHESIS COULD SAY.** Twenty-eight passages moved over **six of seven** declared kinds. `SUPERSEDED_RATIO` is prosecuted and **EMPTY for the second pass running**; `STALE_PRICE` is **6** after being 0 and then 7, so the two classes have now swapped places twice, and that is visible only because both are declared. Six of the six stale prices are cells of the two decision tables NDI reads, five of them pricing deferral on a coupled count `C-0212` and `C-0215` show is a reading on **two transferred distributions** and, at decision 9, **on a tile the programme does not recommend** — searched, and on route B's own tile, it is `27 of 48` flat and `7` flat *and* inside `C-0023`'s per-path allowable. **`C-0186`'s deliberate leave is DISCHARGED**: `C-0218` grades `15 × 4` coupled on the tied lattice at last (`0 of 64`, `0 of 128`, tightest `0.160538609`), and its free stroke is exactly `2/3` of `10 × 6`'s, so `T-5b` is `1.5×` tighter in nm there and **both columns must now travel together**. **The challenge-and-claim census is stale for the EIGHTH time in NINE passes** — `231 / 204` against a derived `247 / 214` — and the **checker** census is dated a third time and in a new way (**`CH-0286`**): its third derivation is a regular expression over a `commandLine("$projectDir/tools/…")` **literal**, and since iteration 46 twelve Kotlin-subject mutation harnesses are wired through a **helper**, so `37` was an undercount of its own sentence's quantity by six **at its own ref** and the honest figure is **`51`**. **The *"what we cannot answer"* list carries no entry the programme has answered** — the sign-carrying class is empty for the third pass — and one bullet gains a **measured price** for the first time. And of the `27` corpus occurrences of the triple `C-0219` moved, **exactly one non-owner still states it live**, in a synthesis claim's own correction table, where **no gate in this repository can see it** (**`CH-0287`**)

| | |
|---|---|
| **Task** | [`T-332`](../tasks/T-332-fifteenth-answers-synthesis.md) — the fifteenth synthesis, the first owed after a coupled cell went flat **and** admissible and after three published readings were corrected |
| **Leaf** | `A8.2` |
| **Verification type** | **logical** — a reconciliation of two outward-facing documents against the claim corpus, the challenge corpus and the task queue, with every mechanised half run by a retained checker **enumerated at the moment of running**, every self-describing count **derived at a named state**, and every unmechanised half stated as a named drift class |
| **Verdict** | **PASS on `P1`–`P7`.** Of the eight declared falsifiers, **`F3` FIRED** — the challenge-and-claim census is stale again, eighth of nine. **`F4` FIRED, and in a way no previous pass has seen**: the checker census's *number* moved at two of its three predicates **and** a fourth predicate was found that none of the three can see, so the published `37` was wrong about its own quantity at its own ref. **`F6` FIRED** — the stale-price class, `0` at the thirteenth pass and `7` at the fourteenth, is **6** at the fifteenth. **`F5` did NOT fire** — no entry of the *cannot answer* list names something the programme has answered — and the neighbouring reading did: one bullet gains a price. **`F1`, `F2`, `F7` and `F8` did not fire** |
| **Maturity** | **TRL 1–3, and below it: NO PHYSICS CHANGED.** No study was run, no Kotlin source was touched, no committed result file of any other task was moved. Every quantity carried is read out of the claim or the result file that owns it, and §5 names the counts this claim owns itself |
| **Provenance** | Edits to [`ANSWERS.md`](../../ANSWERS.md) (12 passages) and [`DECISIONS-FOR-NDI.md`](../../DECISIONS-FOR-NDI.md) (16 passages), two new challenges and their index rows, two new queue rows. Result [`gpd/results/T-332-fifteenth-answers-synthesis.json`](../results/T-332-fifteenth-answers-synthesis.json), emitted by [`tools/T-332-emit-result.py`](../../tools/T-332-emit-result.py) (**15 self-tests**), which **imports `T-319`'s emitter** — which imports `T-276`'s — rather than copying it, and therefore inherits the anchor refusal: it will not build unless every declared passage's *before* anchor occurs at the ref and its *after* anchor occurs its declared **multiplicity** of times in the working tree and **not at all** at the ref. What is new is the multiplicity itself (two of this pass's edits are one substitution applied to two identical table cells) and the fourth Gradle predicate `CH-0286` names. Result file **byte-identical across two independent runs**. The classification registry [`tools/T-234-classification.json`](../../tools/T-234-classification.json) is regenerated as the close-out and **two** hand overrides are added, each built from `census(root)`'s own record rather than typed (`C-0202`); `tools/T-234-census.py --check` goes **8 → 22 → 0**. Gates before and after in §6 |
| **Conditions** | Documents, one new emitter, two challenges, the challenge index and the generated classification registry only. `baselineRef` is `HEAD` as this pass began, **`d7b7074a2be0367429bd63762fd2c8082bbd498d`**, resolved by the emitter and recorded in the result file; each deliverable's *before* reading is taken from `git show <ref>:<path>`. **That ref carries iteration 52's own `T-330` work UNCOMMITTED**, so the ref counts exclude `C-0219` and `CH-0284` and the working-tree counts include them; **both are emitted and every sentence names which it quotes**. Units unchanged and untouched: nm, pN, pN/nm, pN/nm² = 1 MPa exactly, `k_BT` = 4.142 pN·nm at 300 K |
| **Consumes** | [`C-0215`](C-0215-route-b-coupled-on-its-own-stations.md) (`T-322`), [`CH-0276`](../challenges/CH-0276-a-coupled-census-inherited-a-station-set.md), [`CH-0277`](../challenges/CH-0277-a-ratio-transferred-between-two-lattices.md) — the flat-and-admissible cell; [`C-0216`](C-0216-the-placement-and-the-distribution-together.md) (`T-323`) — the interaction and the reversed ordering; [`C-0212`](C-0212-a-searched-distribution-at-the-resolved-link.md) (`T-316`), [`CH-0272`](../challenges/CH-0272-a-flat-count-is-not-an-admissible-design.md), [`CH-0273`](../challenges/CH-0273-the-buildable-ratio-window-is-a-flatness-window.md) — the searched distribution; [`C-0211`](C-0211-the-uniform-raster-at-the-resolved-link.md) (`T-315`), [`CH-0265`](../challenges/CH-0265-756-of-756-is-a-reading-at-the-penalty.md) — the resolved link; [`C-0218`](C-0218-the-tied-regrade-at-the-other-cross-section.md) (`T-294`), [`CH-0283`](../challenges/CH-0283-the-uncoupled-tile-is-not-always-the-floor.md) — the tied `15 × 4` re-grade and the dropped scope clause; [`C-0219`](C-0219-a-dishing-fit-and-the-parity-of-its-basis.md) (`T-330`), [`CH-0282`](../challenges/CH-0282-a-dishing-fit-assumes-an-even-raster-row-count.md) — the corrected triple; [`C-0210`](C-0210-fourteenth-answers-synthesis.md) — the pass this one is owed over, and the source of the `37`; [`C-0191`](C-0191-thirteenth-answers-synthesis.md), [`C-0080`](C-0080-third-answers-synthesis.md), [`C-0088`](C-0088-does-the-deliverable-agree-with-itself.md), [`C-0115`](C-0115-fifth-answers-synthesis.md), [`C-0197`](C-0197-the-challenge-halfs-own-open-word.md), [`C-0202`](C-0202-a-length-is-not-a-provenance.md), [`C-0071`](C-0071-output-element-recommendation.md) (*strike, never delete*) |
| **Constrains** | **`T-332` is DONE.** Opens [`T-333`](../../TASKS.md) (the reconciliation of a synthesis claim's own correction table, `CH-0287`) and [`T-334`](../../TASKS.md) (the checker census's derivation, `CH-0286`) |
| **Raises** | [`CH-0286`](../challenges/CH-0286-a-gate-wired-through-a-helper-is-invisible-to-the-census.md), [`CH-0287`](../challenges/CH-0287-a-synthesis-claims-correction-table-is-outside-every-pass.md) |

---

## 1. The census, by kind

Every number in this section is a field of
[`gpd/results/T-332-fifteenth-answers-synthesis.json`](../results/T-332-fifteenth-answers-synthesis.json).

| kind | moved | what it is |
|---|---|---|
| `SCOPE` | **11** | the passage exists, is correct, traces clean, and is *about* the wrong state |
| `STALE_PRICE` | **6** | a decision or a verdict cell priced on a number a later claim moved |
| `SUPERSEDED_VALUE` | **4** | a value a later claim moved, whose owner still states it |
| `STALE_STATUS` | **3** | an assertion the queue or a later claim has falsified |
| `SELF_DESCRIBING_COUNT` | **3** | a fact about the corpus, belonging to no claim |
| `OTHER` | **1** | a *cannot answer* bullet gaining a measured price |
| `SUPERSEDED_RATIO` | **0** | **prosecuted and empty**, and reported empty — second pass running |
| **total** | **28** | **12** in `ANSWERS.md`, **16** in `DECISIONS-FOR-NDI.md` |

**The two classes that swapped places at the fourteenth pass have swapped back and forth again.**
`C-0191` reported `SUPERSEDED_RATIO` 5 and `STALE_PRICE` **0**;
`C-0210` reported 0 and **7**;
this pass reports **0 and 6**.
Neither reading is available to a partition that drops a kind because it came back empty,
which is why `SUPERSEDED_RATIO` is prosecuted at every pass and reported at every pass.

## 2. What row (g)'s answer now is, in the order the claims arrived

The coupled half of §3 row (g) has been *"`0 of 64`, at every link a crossover connector can
supply"* since iteration 48. Three claims move it, and the order matters because a document that
carried only the last of them would be wrong about what the first two settled.

**(a) `C-0212` (`T-316`) — the `0 of 64` is a reading on two TRANSFERRED rules.**
Every coupled census in this corpus grades on `C-0058`'s equal springs and its rim-graded 5:1,
both *rules transferred onto* the lattice and neither an *optimum of* it.
Searched — `C-0135`'s smoothed minimax with a multi-start descent on the true training percentile,
graded **out of sample** on `C-0208`'s own 4 000-realisation stream, which it reproduces at
`4.0E-9` — the same 32 cells read **`22 of 32` flat**, tightest **`0.0647024141`** against the
transferred `0.100198485`, i.e. **`1.45251772×`** at that very cell.
Both deliverables already carried this, in **one** passage each, filed by its own author;
what neither carried is that **six further cells price a customer decision on the count it moves**.

**(b) `C-0215` (`T-322`) — and that reading is on a tile the programme does not recommend.**
Route B is the only turn topology anybody has folded, its rows are `92 / 98 / 106 bp`,
and `C-0211` shows its free tile flat at `756 of 756`.
Graded coupled on **that** tile, at stations **derived at each row length**:

| | transferred | searched |
|---|---|---|
| cells flat at the 90th percentile | **`0 of 48`** (tightest `0.100227515`) | **`27 of 48`** (tightest **`0.0687939715`**) |
| flat **and** inside `C-0023`'s per-path allowable | — | **`7 of 48`** |
| cells beating the **uncoupled** tile (`0.0521565503`–`0.0576976711`) | — | **`0 of 48`** |

**`7 of 48` is the first non-empty *flat AND admissible* conjunction this corpus has had**, against
`3 of 32` on the `116 bp` block and `0 of 32` on `C-0212`'s own two flags — one of which,
`C-0060`'s `3.5 ≤ R ≤ 20`, is not the threshold it is named after (`CH-0273`).

**The sentence most easily made wrong is the last row of that table**, and both deliverables now
carry it beside the count: `0 of 48` beat the uncoupled tile, which is **already flat**, so on this
tile the coupling is **not a flatness remedy**. It is not an argument for removing it either —
`C-0017`'s mandate is a **placement and stability** requirement and an uncoupled tile delivers no
output at all. **It is a mandate the flatness survives**, at 27 cells and admissibly at 7.

**(c) `C-0216` (`T-323`) — and the two design freedoms are synergistic.**
`0.106508519` / `0.105528854` / `0.078544978` / **`0.0677344328`** at the four corners, total
**`−0.452630313`** log units = **`1.57244277×`**, interaction **`−0.1388` = `−12.96 %`, NEGATIVE**.
`C-0063`'s ordering **reverses**: `0.009241` for the placement against `0.3046` for the
distribution, quotient **`32.9583`** (`0.304553543 / 0.00924055963`).

## 3. `C-0186`'s deliberate leave, discharged — and a convention that must travel with it

`C-0180` re-graded `10 × 6` only, and `C-0186` §1 left one comparison passage of
`DECISIONS-FOR-NDI.md` **un-annotated on purpose**, because giving one cross-section its tied
numbers while the other could be given none would read as a measured ordering across two lattice
states when only one side had moved. `C-0191` stated that leave in the document, which was the
repair available without a run.

[`C-0218`](C-0218-the-tied-regrade-at-the-other-cross-section.md) (`T-294`) runs it.

| | `10 × 6` | `15 × 4` |
|---|---|---|
| tied cells flat at the penalty link | 2 of 64 | **`0 of 64`** |
| tied cells flat at the resolved link | 0 of 64 | **`0 of 128`** |
| tightest coupled cell | `0.0995744767`, clears by `0.426 %` | **`0.160538609`** = `0.565012343 nm`, `1.60538609×` `T-5b` |
| the **uncoupled** tied block, `f = 0.30 / 0.26` | `0.0446459684` / `0.0467367262`, flat | **`0.157228044` / `0.160659712`**, not flat |
| staple bonds | 435 = 135 + 300 | **`410`** = 140 + 270 |
| turn ties, through / in plane | 50 / 9 | **`45 / 14`** |
| free stroke | `5.27921926 nm` | **`3.5194795 nm`** — exactly `2/3` |

**The last row is a convention, not a value, and it is why both columns must now travel together.**
`edgeX` is shared and `edgeY = m·3d/2`, so the free stroke goes as `1/m`; `T-5b`'s dimensionless
`0.10` is therefore `0.527921926 nm` at `10 × 6` and **`0.35194795 nm`** at `15 × 4`, **`1.5×`
tighter**. Read in nm the ordering **reverses at `10` of `128`** matched rows — every one of them a
row where **both** blocks are far outside the tolerance, and **none** of them a row with either side
flat — so **decision 7 is untouched** and the ordering is unchanged. Both deliverables now carry the
nm reading beside the dimensionless one.

## 4. The three self-describing counts, derived

### (a) Challenges and claims — stale for the eighth time in nine

| count | as written | at the ref `d7b7074` | at this pass's working tree, before its own files |
|---|---|---|---|
| challenges | **231** | 246 | **247** |
| claims | **204** | 213 | **214** |
| sum | **435** | 459 | **461** |

Cross-checked by two gates that print the numbers on every run without being asked:
`tools/check-challenge-index.py` reported *247 challenge file(s), 247 indexed* and
`tools/check-corpus-identifiers.py` reported *461 claims and challenges exist*, which is
`247 + 214` exactly. The commands are `ls gpd/challenges/CH-*.md \| wc -l` and
`ls gpd/claims/C-*.md \| wc -l`. **What is missing has never been the number and is still not: it
is the COMPARISON**, and the deliverable now carries the derivation beside it.

### (b) The checkers — the number held at one predicate and a FOURTH predicate was found

| predicate | at `71d126e` (`C-0210`'s ref) | at this pass's tree |
|---|---|---|
| `ls tools/check-*.py tools/trace-answers.py` | 11 | **11** — unchanged for a third consecutive pass |
| tools `tools/verify.sh` invokes with no `--self-test` flag | 17 (16 gates + 1 fixture) | **18** (17 + 1) |
| `commandLine("$projectDir/tools/…")` in `build.gradle.kts`, no `--self-test` | 20 | **21** |
| **`commandLine(mutationSnapshotArguments("…"))` — a fourth predicate, seen by none of the three** | **6** | **12** |
| **distinct tools that can fail `tools/verify.sh`** | **43** | **`51`** |

`C-0210` published **`16 + 1 + 20` = 37** at the first column. Under its own stated predicate that
is right; the quantity its sentence names was **43** there and is **51** here.
The twelve are the mutation harnesses of `T-294`, `T-297`, `T-299`, `T-303`, `T-304`, `T-307`,
`T-310`, `T-315`, `T-316`, `T-322`, `T-323` and `T-330`. They are wired through a helper because
`CLAUDE.md` records that a harness mutating **Kotlin** sources must be handed a snapshot directory
and cannot be run bare — the wiring is correct and the census cannot see it.
**`CH-0243` found a predicate that was a filename prefix; `C-0210` replaced it with one about a
FILE where the question is about a RUN; this is one about a LITERAL where the question is about an
INVOCATION.** [`CH-0286`](../challenges/CH-0286-a-gate-wired-through-a-helper-is-invisible-to-the-census.md).

### (c) This claim's own numbers

`ANSWERS.md`'s rule is that every number in it belongs to a claim, and a self-describing count
belongs to none. **This claim is the owner of `247`, `214`, `461`, `459`, `11`, `18`, `21`, `12`,
`51`, `50` and `43`**, derived at the states the result file records. And the census destroys
itself: this pass files one claim and two challenges, so its own finished tree reads **248 / 215**,
which is why every sentence names the state it was read at.

## 5. The decisions, re-priced — and the *cannot answer* list, re-read

**All nine decisions were prosecuted for a stale price and six cells carry one.**

| cell | what it priced on | what moved |
|---|---|---|
| at-a-glance decision 7, cost of deferring | `0 of 64` coupled | a reading on two **transferred** rules (`C-0212`) and on the wrong tile (`C-0215`) |
| decision 7's comparison table 1, `10 × 6` coupled row | the same | the same |
| decision 7's comparison table 2, `10 × 6` coupled row | the same | the same |
| decision 7's comparison table 1, `15 × 4` coupled row | *"0 of 8, at both ends of the band"* — a **smeared** reading | `C-0218` grades it on the tied grillage |
| decision 7's comparison table 2, `15 × 4` coupled row | the same | the same |
| decision 9, cost of deferring | `756 of 756` at `C-0207` | `CH-0265` challenged it and `C-0211` **upholds** it at the resolved link, and `C-0215` adds the coupled reading |

**Every challenge a decision prices on was re-read for its status**, which is `C-0197`'s standing
residue: `tools/trace-answers.py` reports **12** numbers in `ANSWERS.md` and **1** in
`DECISIONS-FOR-NDI.md` priced on an adjudicated challenge with no claim named, **unchanged by this
pass** — the same 12 and the same 1 at the ref. It is **up from the 10 `C-0210` reported**, and the
growth is not drift: `CH-0265` was **adjudicated** by `C-0211` in iteration 49, so every number
beside it became residue. That is `C-0197`'s mechanism, and it is the reason the line is a residue
rather than a gate.

**The *"what we cannot answer"* list carries no entry the programme has answered.** Twenty-five
top-level bullets; the section names 25 task identifiers, of which the queue reads **two** open
(`T-9`, `T-31`) and both are correctly described. The sign-carrying class — a document listing what
the programme *cannot* do can only go stale by **under-claiming** — is empty for the third pass
running.

**One bullet gains a measured price for the first time.** *A per-site staple-incorporation
measurement on a coupling-bearing tile* has been in this list since iteration 25 with no number on
it. `C-0212` and `C-0215` supply one: with the distribution **searched**, `16 of 32` cells beat the
uncoupled tile at **zero defects** (best `0.0298112409` against `0.0448134881`) and **`0 of 32`**
beat it at the 90th percentile of `C-0087`'s measured dropout — `0 of 48` on route B. **The gap this
bullet names is the term that decides the sign**, and it is still a bench measurement and not a
solve. The bullet's own *"no longer the largest single gap"* stands, because what a per-site map
would settle is a **coupled** cell and the verdict stands on the **uncoupled** body.

## 6. The gates, before and after

Enumerated at the moment of running. The *before* column is read against the baseline ref for the
two deliverables and against the checkout for the corpus-wide tools.

| gate | before | after |
|---|---|---|
| `trace-answers.py` (`ANSWERS.md`) | 2 230 tokens: 2 066 `CITED`, 164 `ELSEWHERE`, **0 `ABSENT`**; 1 open assertion, 0 contradicted; **0** self-contradictions | 2 360 tokens: 2 194 `CITED`, 166 `ELSEWHERE`, **0 `ABSENT`**; 1 open assertion, 0 contradicted; **0** self-contradictions |
| `trace-answers.py` (`DECISIONS-FOR-NDI.md`) | 1 302 tokens: 975 `CITED`, 327 `ELSEWHERE`, **0 `ABSENT`**; 0 open assertions | 1 429 tokens: 1 103 `CITED`, 326 `ELSEWHERE`, **0 `ABSENT`**; 0 open assertions |
| `trace-answers.py` priced-on-adjudicated **residue** (not gated) | 12 in `ANSWERS.md`, 1 in `DECISIONS-FOR-NDI.md` | **12 and 1** — unchanged, same identifiers |
| `check-markdown-tables.py` | 0 defects in 721 files | **0** in 725 |
| `check-corpus-links.py` | 0 broken in 718 files | **0** in 722 |
| `check-corpus-identifiers.py` | 0 dangling in 690 files, **461** claims and challenges | **0** in 694, **464** |
| `check-challenge-index.py` | 247 files, 247 indexed | **249 / 249** |
| `check-cold-start-note.py` | 0 defects, heading 51, journal 51 | **0**, heading 51, journal 51 |
| `check-queue-vocabulary.py` | 0 defects, 307 leading verdicts over 317 rows; residue 0; miscolumned 0 | **0**, unchanged — this pass edits no queue row; residue **0**; miscolumned **0** |
| `check-result-path-references.py` | 0 missing in 686 documents | **0** in 690 |
| `check-entry-points.py` | — | **0** defects, 145 of 147 studies, 145 rows. Read **after only**, and unchanged by construction: it reads `src/` and `TASKS.md`, and this pass touches neither |
| `check-kotlin-format-strings.py` | 0 defects over `src` | **0** — no Kotlin was touched |
| `check-result-file-hygiene.py` (gate / `--prose` / `--departures` / `--saturated`) | 0 / 0 / clean / 0 | **0 / 0 / clean / 0** |
| `result-reader-census.py --check` | exit 0 | **exit 0** |
| `T-278-emitter-rounding-census.py --check` | exit 0 | **exit 0** |
| `T-272-header-census.py --check` | exit 0 | **exit 0** |
| `T-295-mutation-input-census.py --check` | clean | **clean** |
| `cli_guard.py --check` | 45 writers, 45 refusing, 0 not | **46 / 46 / 0** — this pass's emitter is the forty-sixth, and it refuses |
| `T-332-emit-result.py --self-test` | — | **15 self-tests, 0 failures** |
| `T-234-census.py --check` | **GATE 8 defects** at the ref | **GATE 0** — this pass's own corrections took it to **22** first, which is `CH-0230`'s mechanism exactly; regenerating the classification took it to **2** and two hand overrides took it to **0** |

**ONE gate is red at `HEAD`, it is not this pass's, and it is named rather than repaired — and a second prints an advisory line that is not a defect.**
`tools/T-272-emit-result-inputs.py --check` reads *stale* because iteration 52's result files —
`T-330`'s and this pass's — are not yet committed, and `CLAUDE.md` records that this generator reads
the **git index** and drops handles for files it cannot see, so the regeneration belongs to the
coordinator's close-out **after** every result file is committed. And `tools/T-278-emitter-rounding-census.py`
**exits 0** while printing an advisory line about **15 over-precise numeric leaves in 5 files** — `T-225`,
`T-198`, `T-194`, `T-200` and `T-211`, all long-standing, **none of them iteration 52's and none of them this
pass's**. Neither is a document defect and neither is `T-332`'s to fix.

**One gate went red because of this pass and was repaired by it**, and the route is the finding:
`tools/T-234-census.py --check` was **8** at the ref, **22** after this pass's corrections — because
a correcting sentence has to **name** the withdrawn premise in order to withdraw it, so a synthesis
grows that census by construction (`CH-0230`) — **2** after regenerating the classification, and
**0** after two hand overrides. Both overrides are built from `census(root)`'s own record rather
than typed (`C-0202`), and both survive a second regeneration: **23 of 23 carried over**.

## 7. Two challenges

- **[`CH-0286`](../challenges/CH-0286-a-gate-wired-through-a-helper-is-invisible-to-the-census.md)** —
  §4(b) above. The census's third derivation is a regular expression over a **literal**, and twelve
  build-failing tools are wired through a **helper**. The number in print, `37`, was an undercount
  of its own sentence's quantity by **six at its own ref**. The remedy is in the deliverable and the
  residue is `T-334`: the derivation should be taken from
  [`tools/P-31-harness-census.py`](../../tools/P-31-harness-census.py), which already **resolves**
  every harness's wiring instead of inferring it from a shape.
- **[`CH-0287`](../challenges/CH-0287-a-synthesis-claims-correction-table-is-outside-every-pass.md)** —
  of the **27** corpus line-occurrences of the triple `C-0219` moved (excluding this pass's own
  three, which `CH-0182` makes unavoidable), **19** are the owners', **3** are `C-0154`'s own struck
  rows, **4** are the deliverables' — repaired here — and **exactly one** is live and unannotated:
  `C-0191` §2(b), the **thirteenth synthesis's** own *"what moved"* table, which quotes `C-0154` as
  reading what `C-0154` no longer reads. **No gate reaches it**: pointed at that file
  `tools/trace-answers.py` reports `111 tokens, 0 ABSENT, 0 self-contradictions`, because the
  withdrawn value is still `CITED` — by the very claims that withdrew it. That is `C-0080`'s third
  drift class, unmechanisable by construction, met on the one document class that **generates** it:
  a synthesis claim's correction table exists in order to copy other claims' values into a second
  file, and *strike, never delete* guarantees the copy survives. `T-332` is forbidden by its own
  brief from editing `C-0191`, so the instance is **reported and left**, which is the challenge's
  own evidence. The pass-level remedy is free — a synthesis re-reads its predecessors' correction
  tables — and the gate is **priced rather than proposed**, because a *before* column is the
  commonest shape in the population and would be its commonest false positive. `T-333`.

## 8. What is still owed

- **`T-333`** — the pass-level reconciliation `CH-0287` asks for, and the one live instance it names.
- **`T-334`** — the checker census taken from `P-31`'s resolved wiring rather than from a shape.
- **`T-320`** — decision 9's answer, which only NDI can give.
- **`T-321`** — closed by `C-0214`, which **refuses** the dynamic arm as a gate on measurement:
  of the sixteen writers the static predicate flags, only `7` ever write, and the sixteenth is not a
  write at all. Recorded here because a refusal is a result and a synthesis is where a reader looks
  for one.
- **The sixteenth synthesis**, whose cheapest first act is written down: run
  `tools/T-332-emit-result.py --ref <this pass's commit>` and read what the counts did — and then
  re-read **this claim's own §2 and §3 tables** before anything else, which is `CH-0287`'s free half.
