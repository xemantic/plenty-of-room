# C-0145 — **The eighth synthesis is the first whose largest item is a QUESTION rather than an answer: *decision 7* asks NDI to buy a smaller tile with a footprint it does not cost, and corrected, the tile offered as the price is the one that fits §3 while the default overruns it by 40 % and stops being flat.** `C-0144`'s 41-entry work list reconciles at **40 RESTATED, 1 LEFT, 0 DEFECT** — and it is a **floor**: **10 further occurrences over 6 lines** carry a coupled-flatness premise `C-0142` withdrew hours after the census's families were written, and **10 of the 41 sit inside a preserved-verbatim quotation this repository forbids editing.** Cheap bound **7 of 44** cited before the pass, **20** after

| | |
|---|---|
| **Task** | [`T-233`](../tasks/T-233-deliverable-restatement-and-eighth-synthesis.md) — restate the four-layer numbers in the two outward-facing documents, together with the eighth `ANSWERS.md` synthesis |
| **Leaf** | — (a process claim; it audits the two documents that report every leaf) |
| **Verification type** | **logical** — a mechanical reconciliation of a published work list, a partition of the corpus filed since the seventh pass, and the five retained document checkers run before and after |
| **Verdict** | **PASS on all five predicates.** `P1` all **41** entries located by exact line text and dispositioned — **40 RESTATED, 1 LEFT, 0 DEFECT** — by a retained gate with **28 self-tests**; `P2` every number written is grepped out of the claim that owns it, and the one derived ratio is quoted **with its construction**; `P3` decision 7 is **superseded as posed** and re-posed as two questions in both documents; `P4` the pass ran on **both** files and found three items the corpus had answered and a deliverable still called open; `P5` all five retained document checkers clean, and `C-0144`'s own `tools/T-234-census.py --check` reads **GATE 0 defects**. **`F1`, `F3`, `F4` and `F5` did not fire; `F2` FIRED once, favourably** — a ratio the deliverable had carried for ten iterations is stated by no claim at its corrected value |
| **Maturity** | **Below TRL 1–3: nothing here is physics.** No number is derived here. Every quantity written into either document is read from `C-0141`, `C-0142`, `C-0140`, `C-0144`, `C-0137`, `C-0143`, `C-0136`, `C-0132` or `CH-0179`, at that claim's own precision |
| **Provenance** | [`tools/T-233-reconcile.py`](../../tools/T-233-reconcile.py) (**28 self-tests**, exit 1 on any defect), reading `C-0144`'s published list in [`gpd/results/T-234-honeycomb-correction-supersession.json`](../results/T-234-honeycomb-correction-supersession.json) under `deliverableListForT233`. No study was run and no result file is emitted: this task edits documents |
| **Conditions** | The corpus at iteration 35. **A snapshot**: two sibling agents (`T-235`, `T-230`/`T-231`) were writing while the pass ran |
| **Consumes** | [`C-0144`](C-0144-honeycomb-correction-supersession.md) (the work list), [`C-0141`](C-0141-honeycomb-station-lattice-and-placement.md), [`C-0142`](C-0142-coupled-cells-at-the-honeycomb-cross-section.md), [`C-0140`](C-0140-honeycomb-raster-turn-sense.md), [`C-0137`](C-0137-beyond-mean-field-gap.md), [`C-0143`](C-0143-planar-coupling-wall.md), [`C-0136`](C-0136-mixed-domain-phase-and-honeycomb-twist.md), [`C-0132`](C-0132-cut-rim-charge.md), [`CH-0179`](../challenges/CH-0179-exponentially-large-is-not-a-property-of-the-branch.md), and [`C-0130`](C-0130-seventh-answers-synthesis.md) as the previous pass. **Zero fetches** |
| **Raises** | [`CH-0182`](../challenges/CH-0182-a-census-is-dated-by-its-premise-set.md) against `C-0144` §9's completeness; [`CH-0183`](../challenges/CH-0183-a-line-census-cannot-see-a-quotation.md) against its classification of ten entries |
| **Constrains** | [`ANSWERS.md`](../../ANSWERS.md) and [`DECISIONS-FOR-NDI.md`](../../DECISIONS-FOR-NDI.md) only. **No claim, number or verdict is contradicted, and none is re-derived** |

---

## 1. The cheap bound, and it is a reconciliation rather than an arithmetic

The expensive half of a synthesis is reading; the mechanical half is **locating**.
So `C-0144`'s 41 entries were located **by exact line text, never by line number** before a word of prose was
written — which converted *"find the moved passages"* into *"edit 21 physical lines"*.

| | |
|---|---|
| entries published by `C-0144` §9 | **41** — 27 in `ANSWERS.md`, 14 in `DECISIONS-FOR-NDI.md` |
| **distinct physical lines** they occupy | **21** |
| located by exact text before the edit | **41 of 41** |
| **after** the edit: RESTATED / LEFT / DEFECT | **40 / 1 / 0** |

The one `LEFT` is `ANSWERS.md`'s *"A CENSUS OF THE HONEYCOMB'S ATTACHMENT LATTICE (the entry as written at
iteration 25)"* — an explicitly retained historical bullet whose discharge is stated in the bullet immediately
above it. Leaving it is the repository's own rule, and the gate requires a superseding cue within 40 lines
above before it will accept that disposition.

`tools/T-233-reconcile.py` is retained and re-runnable. It decides `RESTATED` when the withdrawn token
survives only inside a `~~struck~~` span or under a superseding cue, and `DEFECT` otherwise — so **it is the
audit of this task's own edit**, not a description of it.

**Its first draft produced four classes of false positive, and every one of them is a way a checker written
for one document is always wrong.** It read strike spans **per line**, so a strike opened on one line and
closed on the next read as live. It demanded a cue on the **occurrence's own line**, so the four sentences of
this task's own scaffold correction — which necessarily *say* `p8064` in order to explain the contradiction —
read as unqualified. It matched the bare token `132` inside the identifier **`C-0132`**, which
`tools/T-234-census.py` blanks length-preservingly for exactly that reason. And it fired the `WIDTH` family on
`C-0086`'s **square-lattice** 112 bp row, which `C-0140` does not touch — it overturns a **honeycomb** row
length — which is why the census carries a per-family **context** requirement and so must anything reading its
list. All four are now self-tested by name. `CLAUDE.md`: *a drift checker's false positives cost more than its
true ones.*
`CLAUDE.md`: *a drift checker's false positives cost more than its true ones.*

## 2. Decision 7 — superseded as posed, and re-posed

> *"**Is a 38 × 25 nm tile acceptable, in exchange for removing the last unmeasured dependency in the flatness
> verdict?**"*

| | as posed | corrected (`C-0141`, `C-0142`) |
|---|---|---|
| `15 × 4` — the **default** the question offers | 38.08 × 38.04 nm, *"essentially §3's"* | **38.08 × 56.524 nm — 1.40084263× §3's 40.35** |
| `10 × 6` — the tile the question **charges** for | 38.08 × 25.36 nm, *"a third of the footprint"* | **38.08 × 37.504 nm — 0.929467162 of §3's** |
| `15 × 4` free tile | 0.0577199433 | **0.0978155002** |
| `15 × 4` coupling-fraction threshold | `f` = 0.0788618807, cleared 3.29690337× | **0.276970522 — INSIDE the measured 0.26–0.33 band** |
| `15 × 4` at the band's adverse end | 0.0612595739, flat | **0.101759944 — FAILS `T-5b`** |
| `15 × 4` coupled, under the measured dropout | 1 of 8 cells flat | **0 of 8, at BOTH ends of the band** |
| `10 × 6` coupled | 8 of 8, best 0.0278431488 | **4 of 8, best 0.0680677948** |

**The trade does not exist.** The tile offered as the cost is the one that fits §3; the default overruns it by
40 % and stops being flat. All three criteria this programme can rank — folding yield, flatness, footprint —
now point the same way.

**Re-posed as two questions**, in `DECISIONS-FOR-NDI.md` §7 and in `ANSWERS.md`'s NDI table:

- **7a — confirm `10 × 6`.** There is nothing left to weigh. A *yes* costs nothing this programme can see;
  a *no* re-opens a flatness verdict that now **fails** at one end of the only measured band there is.
- **7b — which scaffold?** The caDNAno paper's Methods list says **p8064** and its own main-text rule says
  **p7560**, agreeing at **six of its seven designs and disagreeing at exactly ours**, and under the Methods
  reading design (i) would leave **504 nucleotides** the paper's own *"126 bases per helix"* accounting has no
  line for (`CH-0180`). **A source contradiction, not a reading error**, and no calculation closes it.

**And the cost of deferring has reversed sign.** It used to be *"carrying the interlayer-coupling calibration
as a live dependency"*; it is now *"carrying a default that overruns §3's footprint by 40 % and fails `T-5b`"*.
Both files said *"deferring costs little, the four-layer result stands either way"*. It does not.

## 3. What the corpus had ANSWERED that a deliverable still called open

`C-0067`'s standing finding is that **a deliverable that under-claims is as wrong as one that over-claims and
is far harder to catch.** Three instances, all in `ANSWERS.md` §5 and §2:

| | the deliverable said | the corpus says |
|---|---|---|
| **the oppositely-charged correlation correction** | *"Every published coupling criterion is a like-charge result. **This is the largest uncertainty on every electrostatic force here.**"* | The premise is false: Kanduč et al. publish the branch, and their Monte Carlo at `Ξ` up to **86** finds PB and strong coupling *"nearly coincide"*. `C-0137` (iteration 32) closes `T-50`; `C-0143` (iteration 34) settles which **input** the criterion is owed — the disputed `16.5×` is **100 %** the bare/renormalised axis and **0 %** the geometry it was framed as, worth **half a per cent** of an asymmetry range on this device's branch |
| **Task 6, mean-field screening** | *"Mean field is **uncontrolled** across the whole 5–10 nm working range"*, with no mention of `T-50` | All **54** of `C-0017`'s states are **force-pinned** — the pinned force identical across all three buffers at every `(model, height)`, relative spread **`0.0`** — so a correction to the **level** contributes **exactly zero** to the stiffness. Thresholds: the true force **1.48–2.22× smaller**, or the decay length **9.73 % shorter** |
| **the row-end crossover register** | *"an independent value for the register the boundary layer relieves"* is still wanted | `C-0136`: the honeycomb's design twist is `720/21 = 34.2857 °/bp`, **which is B-DNA's `360/10.5`**, so `Δω` is **exactly zero**. Every register number in `C-0104`, `C-0107` and `C-0133` is a **square-lattice** number, and on the tile §3 specifies **the axis is empty rather than unmeasured** |

A fourth is a *discharge* rather than an under-claim: `ANSWERS.md`'s honeycomb-lattice bullet said the corpus
has **no** plan ceiling and no placement family for a honeycomb face. `C-0141` supplies both — an exact
bisection at ten counts reproducing `C-0072`'s **9.535 nm** on the square lattice at departure `0.0`, and a
centro-symmetric family that `10 × 6` admits at the **full 60 stations** and `15 × 4` admits at **none** of 21
phases, at either offset and either row length.

## 4. The two challenges, and why they are about the same blind spot from opposite sides

**`CH-0182` — a census is dated by its premise set.** `tools/T-234-census.py`'s families are built from
`C-0141` and `C-0140`; `C-0142` was filed the **same iteration** by a sibling, hours later, and re-graded the
coupled cells. So **26** occurrences of a `C-0142`-withdrawn premise stand in the two deliverables, **16** of
them on lines the 41-entry list already names and **10 on 6 lines it does not**. The list's 21 lines are
really **27**. `CLAUDE.md`'s *a discharge is invisible to whoever files the removal*, with the arrow reversed:
here it is the census that cannot see the claim.

**`CH-0183` — a line census cannot see a quotation.** Ten of the 41 — entries 10–19, `10 of 27` on the
`ANSWERS.md` half — lie inside one preserved-verbatim blockquote that `C-0130` filed and `C-0071`'s *strike,
never delete* makes binding. `C-0144`'s own `RECORD` class is defined to include *"a verbatim source
quotation"*; the discriminator is that every line of the block begins `> `. The repair owed for all ten is
**one superseding banner above the block**, which is what `T-233` wrote.

**Together they say the same thing about a token census: it sees strings and not structure — neither the date
of its own vocabulary nor the frame the string sits in.** The list is 41; the moved set is 51 occurrences over
27 lines, of which 10 must not be edited at all.

**Measured after the edit, by `C-0144`'s own gate.** `tools/T-234-census.py --check` reads **GATE 0 defects**
and a residual `T-233 debt` of **18** occurrences in the two deliverables, against 41 before. Every one is
accounted for and none is a repair still owed:

| residual | count | what it is |
|---|---|---|
| the two preserved-verbatim blockquotes, `ANSWERS.md` 957 and 972 | **13** | `CH-0183` — text `C-0130` and `C-0071` forbid editing; both now carry a superseding banner **above** them |
| `single-layer square-lattice` at `ANSWERS.md` 1044 and 1071 | **2** | one is a **token collision** in this pass's own new sentence, *"every register number … is a single-layer square-lattice number"*, which is `C-0136` quoted correctly; the other is the retained iteration-25 bullet, the reconciler's one `LEFT` |
| `p8064` at `DECISIONS-FOR-NDI.md` 29–31 | **3** | this pass's own scaffold correction, which must name the token in order to withdraw it |

The gate's classification was regenerated by `tools/T-234-emit-classification.py` from its stated rules, with
this claim added to its `SYNTHESIS` set — **which is `CH-0182` in miniature**: a rules file naming the claims
that may quote a withdrawn premise goes stale the moment the next such claim is written, and it went stale
against the very claim that raised the challenge.

## 5. The partition — 13 newly carried in, 3 materially extended, 4 claims and 20 challenges not

Universe: `C-0131` … `C-0144` (**14**) and `CH-0152` … `CH-0181` (**30**) — **44** items filed since the
seventh pass (`C-0130`).

**Cheap bound: 7 of 44 cited by ID before the pass, 20 after.**

| | |
|---|---|
| **NEWLY CARRIED IN** | **13** — `C-0132`, `C-0136`, `C-0140`, `C-0142`, `C-0143`, `C-0144`, `CH-0172`, `CH-0175`, `CH-0176`, `CH-0177`, `CH-0179`, `CH-0180`, `CH-0181` |
| **already cited and MATERIALLY EXTENDED here** | **3** — `C-0137` (Task 6 and §5, where its closure of `T-50` had not reached), `C-0141` and `CH-0174` (restated in place rather than banner-flagged) |
| **DELIBERATELY NOT CARRIED** | **4 claims** — `C-0131`, `C-0134`, `C-0135`, `C-0138` — **and 20 challenges** |

The four, with their reasons, because a synthesis that does not say why it dropped something is asserting
rather than deciding:

- **`C-0131` and `C-0138`** are result-file hygiene: a departure-precision gate closed at `0` fields in `0`
  files over 121 result files, and a saturated-proportion statistic repaired at its shared source in 31 more.
  **They move no physical number and this deliverable reports no result-file field**, so carrying them would
  put a serialisation rule in a document about a device. They belong to `§7`'s process row, which already
  names the discipline generically.
- **`C-0134`** — the buildable width's count/phase grid, whose argmin moves onto **phase 8** — is a
  **single-layer square-lattice** result, and §3's tile is now a four-layer honeycomb block whose lattice
  `CLAUDE.md` records as not inheriting any of that machinery. Carrying it would attach a design-internal fact
  to a body the programme no longer proposes. **It is not withdrawn**; it is simply not this document's axis.
- **`C-0135`** — `T-129` reproduces, and what does not is a two-valued **descent manifold**: the VALUE spans
  `2.62e−3` and the POINT `5.95e−3`, and read on another functional of the same argmin the POINT spans
  `7.55e−4`, so the headline **0.60 %** is a property of *which field the file emits*. That is a statement
  about a search, and no number in either deliverable rests on the point rather than the value.

**Twenty challenges are not carried**, and they fall in three groups: those resolved inside the claim
that raised them, those against a claim whose numbers neither deliverable quotes, and those about tooling.
`CH-0177` and `CH-0181` **are** carried, because their subjects — the path-count axis and *"the four-layer tile
is flat with no coupling"* — are sentences these documents state.

## 6. The five gates

| gate | how it was discharged |
|---|---|
| **dimensional consistency** | none — no quantity is derived here. Every number written is grepped out of the claim that owns it, at that claim's precision; the check is mechanical and was run over **50** tokens across eight claims and one challenge |
| **limiting cases** | the reconciler on an empty document reports every entry `RESTATED` with *"no live unqualified occurrence remains"*, which is correct and is why the gate also requires the entry to have been **locatable before** the edit — 41 of 41 were |
| **symmetry / conservation** | the work list is reconciled **twice by different routes**: `C-0144`'s token census (41 entries) and an independent grep of `C-0142`'s own headline numbers (26 occurrences). The two overlap on 16 and differ on 10, and that difference **is** `CH-0182` |
| **numerical convergence** | none; the reconciliation is an exact enumeration. In its place: the gate carries **28 self-tests**, six of which name a false positive its own first draft produced |
| **literature cross-check** | none new — **zero fetches**. §2's scaffold contradiction is read from `C-0144` §6, itself read from `gpd/data/T-151-sources/PMC2731887-fullTextXML.xml` |

### The declared falsifiers

| # | falsifier | fired? | outcome |
|---|---|---|---|
| **F1** | an entry of the 41 cannot be located by its exact line text — the list is stale and must be rebuilt | **no** | 41 of 41 located, on stripped-and-prefix text; the line **numbers** had all drifted, which is why the tool matches on text |
| **F2** | a number written here cannot be grepped out of a claim — the synthesis has become a source | **FIRED, once, and favourably** | *"`10 × 6` is **6.6×** flatter"* is `C-0120`'s own number and its corrected value, **4.06×**, is stated by no claim. Struck, restated, and quoted **with its construction** — the ratio of 0.0978155002 to 0.0240648102. It stood in both documents through four synthesis passes because both of its arguments had owners |
| **F3** | decision 7 survives as posed — the footprint trade still exists somewhere in the corpus | **no** | `C-0141` reverses the ordering; the trade's cost column is empty |
| **F4** | the sweep finds nothing the deliverables call open that the corpus has answered — `C-0067`'s failure mode has stopped operating | **no** | three, §3 |
| **F5** | a deliverable contradicts itself, and the contradiction is one a previous pass introduced | **no** | `tools/trace-answers.py` reads **0** self-contradicted tasks on both files, before and after |

## 7. Validity range, and what this does NOT establish

- **Nothing here is physics and nothing is re-derived.** A restatement is only as good as the claim it reads,
  and every claim read is `PASS` at TRL 1–3 — model-consistent, never measured.
- **The reconciliation is complete against `C-0144`'s list and NOT against the corpus.** `CH-0182` measures
  exactly how incomplete: **10 occurrences over 6 lines** found by an independent grep. There is no reason to
  believe that grep is complete either — it was built from `C-0142`'s headline numbers, so a `C-0142` premise
  the deliverables state **in words** rather than in figures is not found by it.
- **The `RESTATED` disposition is a statement about a token, not about a sentence.** A withdrawn premise
  expressed without any of the censused tokens survives this pass, and `C-0115`'s finding — that the largest
  synthesis defects are **scope** corrections, invisible to every check that compares a document to a corpus —
  applies here unchanged.
- **`4.06×` is this file's arithmetic**, quoted with its construction and owned by no claim. The same is true
  of any min/max a reader assembles from the §7 comparison table.
- **Decision 7's re-posing is a judgement.** A reader may hold that 7a is still a real question because §3's
  *~40 × 40 nm* is a nominal rather than a bound; the re-posing says so, and asks for the bound.
- **This task edits no code, runs no study and emits no result file.** `tools/verify.sh` is run as the gate on
  the tree it edits, not on work it produced: **BUILD SUCCESSFUL in 22 m 15 s**, with the result-reader census,
  the table, link, format-string, challenge-index, raw-conversion, departure-precision and saturated-statistic
  checks all clean. Its one `note` is a sibling's brand-new study not yet in the census, which that check is
  explicitly written not to fail on.
