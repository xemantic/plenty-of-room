# C-0155 — **The tenth synthesis carries a RECOMMENDATION CHANGE the deliverables had not heard of, and the cheap bound measured the work list before a word was written: `112 / 108` stood in **10** live places across the two documents and `102 / 109` in **1**.** The drawable honeycomb raster is **`102 / 109 bp`** at the **same** 116 bp = 39.44 nm extent, so §7's width finding does not move and the whole cost of closure is **one crossover column**; the recommended coupled cell survives at both ends of the measured band, **`0.0773373597`** and **`0.0821458169`** against `T-5b`'s 0.10. The distinction the pass exists to preserve is that closure is a reason to **PREFER** `102 / 109` and **not** a proof that `112 / 108` is unbuildable — `C-0152` prices the elastic half of forcing at `0.350894669 k_BT`, **sub-thermal** — and that distinction is now explicit in **both** documents. `F2` fired: **two** things the corpus had answered were still called open, one of them a **price** put to NDI inside a question (`CH-0203`); and `C-0151`'s own result file carries a plan-ceiling row the claim does not quote (`CH-0202`)

| | |
|---|---|
| **Task** | [`T-257`](../tasks/T-257-deliverables-owe-the-closing-raster.md) — `ANSWERS.md` and `DECISIONS-FOR-NDI.md` owe the closing honeycomb raster |
| **Leaf** | — (a process claim on the two documents that report every leaf) |
| **Verification type** | **logical** — a grep-located reconciliation of two documents against four claims, two challenges and one queue. **No physics is derived here**, no study is run and no result file is emitted |
| **Maturity** | **Below TRL 1–3: nothing here is physics.** Every quantity written into either document is read from [`C-0151`](C-0151-closing-raster-selection.md), [`C-0152`](C-0152-forced-scaffold-crossover-price.md), [`C-0148`](C-0148-face-bond-class-residues-and-row-span-columns.md), [`C-0147`](C-0147-honeycomb-turn-slack-and-ragged-face.md), [`C-0146`](C-0146-coupled-cells-at-the-two-length-raster.md), [`C-0141`](C-0141-honeycomb-station-lattice-and-placement.md) or [`C-0140`](C-0140-honeycomb-raster-turn-sense.md), at that claim's own precision |
| **Verdict** | **PASS on all six predicates. Of the six declared falsifiers, three did not fire and three FIRED — `F1`, `F2` and `F5`.** `F1` (a passage *contradicts* the new claims, so the owed edit is a strike) fired at **11 live passages**; `F2` (the sweep finds **nothing** the corpus has answered that a deliverable still calls open) fired **twice**; `F5` (a deliverable contradicts **itself**) fired **twice**, both in `DECISIONS-FOR-NDI.md` §8 — a live *"3.57 % ambiguity"* sentence and a live *"the tile is 38.08 nm along the helices"* opener, each surviving beside a restated table or block a few lines away — and both were repaired in the same pass. `F3`, `F4` and `F6` did not fire. Raises [`CH-0202`](../challenges/CH-0202-the-plan-ceiling-escape-is-quoted-at-fifty-paths.md) and [`CH-0203`](../challenges/CH-0203-a-specification-question-was-posed-on-a-threshold-its-own-iteration-withdrew.md) |
| **Provenance** | No study, no result file. The five retained document checkers plus `tools/T-233-reconcile.py` and `tools/T-234-census.py --check`, run before and after; the before/after census is one `re.findall` over the `HEAD` and working copies of both files, with the struck spans partitioned out |
| **Conditions** | The corpus at iteration 38. **A snapshot**: two sibling agents (`T-253`, `T-250`) were writing `TASKS.md`, `JOURNAL.md`, `CLAUDE.md` and `src/` while this pass ran, and this claim edits neither their tasks nor their sources |
| **Consumes** | [`C-0151`](C-0151-closing-raster-selection.md) (`T-245` — the closing family, the selection, the determined phase, the re-graded cells), [`C-0152`](C-0152-forced-scaffold-crossover-price.md) (`T-246` — the forced-crossover price and the negative existence result), [`C-0148`](C-0148-face-bond-class-residues-and-row-span-columns.md) (`T-244`/`T-243` — the residues, the offset and the row-derived column count), [`C-0149`](C-0149-ninth-answers-synthesis.md) (the previous pass, whose conventions are preserved), [`C-0147`](C-0147-honeycomb-turn-slack-and-ragged-face.md), [`C-0146`](C-0146-coupled-cells-at-the-two-length-raster.md), [`C-0141`](C-0141-honeycomb-station-lattice-and-placement.md), [`C-0140`](C-0140-honeycomb-raster-turn-sense.md), [`CH-0194`](../challenges/CH-0194-the-filter-and-closure-are-disjoint.md), [`CH-0195`](../challenges/CH-0195-both-graded-column-counts-belong-to-an-undrawable-raster.md), [`CH-0187`](../challenges/CH-0187-the-two-length-recommendation-rests-on-an-unstated-filter.md), [`CH-0185`](../challenges/CH-0185-a-bounding-box-crossover-column.md). **Zero fetches** |
| **Constrains** | [`ANSWERS.md`](../../ANSWERS.md), [`DECISIONS-FOR-NDI.md`](../../DECISIONS-FOR-NDI.md) and [`TASKS.md`](../../TASKS.md)'s register only. **No claim, number or verdict is contradicted, and none is re-derived.** `T-256` is discharged by the repair it asked for |

---

## 1. The cheap bound, and it ran before a word was written

The expensive half of a document pass is prose; the cheap half is **locating**. Both documents were
counted at `HEAD` for every token the two new claims bear on, with a `re.findall` rather than a
memory:

| token | `ANSWERS.md` | `DECISIONS-FOR-NDI.md` |
|---|---|---|
| `112 / 108` — the superseded pair | **4** | **6** |
| `101 / 109` — `CH-0187`'s width optimum, which also does not close | 1 | 3 |
| **`102 / 109`** — the drawable pair | **0** | **1** |
| `39.44` — the extent, which does **not** move | 7 | 10 |
| `40.46` — a width no claim states | **0** | **0** |
| `3.57 %` / `0.07 nm` — decision 8's withdrawn price | 1 / 3 | 5 / 4 |
| `C-0148` / `C-0151` / `C-0152` | 0 / 0 / 0 | 0 / **2** / 0 |

> **`112 / 108` stood in 10 places against `102 / 109`'s 1, and the one was a repair made hours
> earlier by the coordinator** (`T-256`, from `C-0153`'s standing-checker hit on the stale
> *"`CH-0187` is open"* sentence). **`C-0148` — the claim that found the raster does not close — is
> named nowhere in either document**, and `C-0152` likewise.

The `40.46` row is the one worth recording as a **null**: it was on the census list because a
withdrawn width of that value was suspected, and there is none. A census that only ever confirms is
not a census.

## 2. What the two documents now say, and it is the same thing

Both carry, in `ANSWERS.md` §1, §3 row (g), §5 and the NDI decision table, and in
`DECISIONS-FOR-NDI.md`'s head block, *At a glance*, §5, §7a, §7b and §8:

| | `102 / 109` — recommended | `112 / 108` — superseded |
|---|---|---|
| forced raster crossovers, of 59 | **0** | **10** |
| axial extent | **116 bp = 39.44 nm**, `−1.40 %` | **116 bp = 39.44 nm**, `−1.40 %` — **a tie** |
| row span | **109 bp** | 112 bp = 38.08 nm |
| inter-row stagger | **7 bp = 2.38 nm** | 4 bp = 1.36 nm |
| interface window → crossover columns | **102 bp → 10** | 108 bp → 11 |
| stations at the determined phase 16 | **55 of 60** | none — no phase is determined |
| scaffold on `10 × 6` (M13 spare) | **6 330 nt** (919) | 6 600 nt (649) |
| blunt-end stacking relief | **3.18 rises** | 0.18 — below the design quantum, so not quotable |
| recommended cell, `f` = 0.30 / 0.26 | **0.0773373597 / 0.0821458169** | 0.0708759349 / 0.0754995025 |
| flat cells of eight, `f` = 0.30 | **2** | 3 |

**The width finding does not move**, which is the single most important thing for a document that
has told NDI a honeycomb tile is `−1.40 %` of the nominal three times. **The whole cost of closure
is one crossover column** — a uniform `1.0567397`–`1.09611647×` of the dishing — because the two
pairs give the **same tile** and differ only in the interface window.

## 3. The preference/prohibition distinction, stated in both documents

`C-0151`'s §2a axes table calls closure *"a **rule**, not a preference"* and its own §7a narrows
that to *"the word **free**, not the word **impossible**"*. **A reader takes the table row.** Both
deliverables now carry the narrowing explicitly, in a set-off block in `DECISIONS-FOR-NDI.md` §7b
and in `ANSWERS.md` §1, §3 row (g) and §5:

- caDNAno's `±5 bp` scaffold rule is that software's **default**; the same paper supplies the tool
  to force a crossover and warns only that departure *"may lead to folding failure if too much
  deviation from canonical DNA geometry is implied"*.
- A forced crossover is an **azimuth**: `17.1428571°` at the minimal rung, exactly **twice** the
  `8.57142857°` an **allowed** scaffold crossover already carries at the exact 10.5 bp/turn
  geometry, which every honeycomb origami ever folded absorbs.
- Its ceiling is **`0.350894669 k_BT`** — **sub-thermal**, `2.84985806×` below one `k_BT` — and all
  ten cost **`0.438634952`** of **one** crossover column of a host sheet that folds.
- **So `112 / 108` is off-rule and cannot be argued out of a fold on elastic grounds.** The
  **kinetic** half is unpriced, over `C-0152`'s **68** queries in **7** declared families.

**Nothing NDI has to decide turns on it**, and the documents say so: `102 / 109` is on-rule, ties on
width, and wins scaffold and stacking relief, so the trade exists only if the eleventh crossover
column is worth a kinetic risk nobody can quote.

## 4. `F2` fired twice, and one of them is a PRICE inside a question

`C-0067`'s standing finding is that a deliverable that under-claims is as wrong as one that
over-claims, and `C-0115`'s is that `DECISIONS-FOR-NDI.md`'s failure mode has a **sign**. Both held
again.

| # | where | what the deliverable said | what the corpus says | class |
|---|---|---|---|---|
| 1 | `DECISIONS` §8, *What deferring costs* | *"`T-243` **is queued** to derive the crossover-column count from the row spans"* | `T-243` is **DONE**, iteration 36 (`C-0148`) — and it did remove the guard's vote | **stale open**, 2 iterations |
| 2 | `DECISIONS` §8 and *At a glance* row 8; `ANSWERS` §1 and NDI row 8 | decision 8's threshold: *"six flat coupled cells of eight against three, through a crossover column decided by a **0.07 nm** slack (`CH-0185`)"* | `C-0148` **answered `CH-0185` in the same iteration** — the twelfth column is a box artefact — and `C-0151` removed the eleventh too. **The question was put to NDI carrying a price the corpus had already withdrawn** | **stale price in an outward-facing QUESTION** — `CH-0203` |

Item 2 is the one worth a challenge rather than an edit. `C-0144` had already had to withdraw
decision 7 *as posed*; this is the same failure one level in — the question is real and its **cost
dimension** was not — and it lands on the axis `CLAUDE.md` records this programme has no column for.
A respondent weighing *"is it worth six flat cells of eight against three"* is weighing a quantity
that does not exist.

## 5. `CH-0202` — a margin quoted at a count below the claim's own census

`C-0151` §2a discharges the plan-budget axis with *"a placement is capped at five columns (50 paths)
and the plan ceiling there is **4.604 nm**, comfortably above the 2.38 nm relief"*. Its own result
file's `planCeilings` block carries a **55**-path row at the same determined lattice — and 55 is
the claim's own headline station census.

| demanded paths | ceiling | in rises | margin over the 7 bp relief | ratio |
|---|---|---|---|---|
| 50 — five full columns | 4.604 nm | 13.54 | 2.224 nm | **1.93445378×** |
| **55 — the whole census** | **3.06 nm** | **9** | **0.68 nm = exactly 2 rises** | **1.28571429×** |
| 60 — saturation, unreachable | 2.38 nm | **7** | **0.00 nm = ZERO rises** | **1.00000000×** |

**The verdict is upheld on every reachable count** and the number handed to a reader moves 1.93× →
1.29×. The zero at saturation is a **lattice identity** rather than a near miss: `C-0141`'s
saturated `10 × 6` ceiling is `7 × 0.34` exactly and the drawable raster's relief is 7 bp, the same
integer on the same quantum. That is `CLAUDE.md`'s *"a margin below 0.34 nm cannot be corrected,
only removed"*, at its limit.

**A cap on COLUMNS is not a cap on PATHS**, and the axis is written on paths.

**Both deliverables carry the corrected reading rather than the challenged one** — `ANSWERS.md`
§3 row (g) and `DECISIONS-FOR-NDI.md` §7b state the 55-path ceiling and the 2-rise margin, with
`C-0151`'s own 50-path 4.604 nm named beside it. A synthesis that repeats a sentence it is
simultaneously challenging has published the defect twice.

## 6. The five gates

| gate | how it was discharged |
|---|---|
| **dimensional consistency** | none — no quantity is derived here. Every number written is grepped out of the claim that owns it at that claim's own precision. The **derived** quantities are quoted with their construction: §5's three ratios are `ceiling / 2.38` and `(ceiling − 2.38) / 0.34`, and the `109 bp` row span is quoted as *"the 116 bp box less the 7 bp = 2.38 nm stagger"* rather than converted, so that no token enters either document that the corpus does not state |
| **limiting cases** | the census's **null** rows are reported, not dropped: `40.46` appears **0** times in either document and `123 / 109` **0** times, which is what makes the 10-against-1 count a measurement |
| **symmetry / conservation** | the two documents are reconciled against **each other** as well as against the corpus, and the §7a coupled-flatness row of `DECISIONS` was found to state `C-0142`'s abstract-grid cells beside `ANSWERS`' — both correct, and neither carrying `C-0151`'s raster re-grade; a pointer was added to both so the two cannot be read as one number. `tools/trace-answers.py` reads **0 task(s) the document contradicts itself about** on both files |
| **numerical convergence** | none; the reconciliation is an exact enumeration. In its place the census is reported **before and after** with the struck spans partitioned out, so a *"live"* count is a count of assertions the documents still make |
| **literature cross-check** | **zero fetches.** The one primary reading is caDNAno's own `±5 bp` sentence and its forced-crossover warning, both quoted through `C-0148` and `C-0151` rather than re-fetched |

### The declared falsifiers

| # | falsifier | fired? | outcome |
|---|---|---|---|
| **F1** | a passage already in either document **contradicts** `C-0151` or `C-0152`, so the owed edit is a strike | **FIRED** | **11** live passages — 4 in `ANSWERS.md`, 7 in `DECISIONS-FOR-NDI.md`. Unlike `C-0149`'s `F1` it fired against the claims the task named, because this task's claims **replace a recommendation** rather than supply a missing one |
| **F2** | the sweep finds **nothing** the corpus has answered that a deliverable still calls open | **FIRED** | **two** items, §4 — a two-iteration stale *"is queued"*, and a **price** carried into a question NDI is being asked to answer |
| **F3** | a derived ratio, percentage or min/max already standing cannot be reconstructed from the claim owning its arguments | **no** | all five checked reconstruct exactly: `0.2016 %` = `5.54399427e−05 / 0.0274976866`, `496×` its inverse, `0.571` = `1.36 / 2.380`, `4.06×` = `0.0978155002 / 0.0240648102` (and the document already says it is stated by no claim), `3.57 %` = `4 / 112` and is `C-0146`'s own |
| **F4** | a number written here cannot be grepped out of a claim | **no** | `tools/trace-answers.py`: **0 ABSENT** on both documents, before and after. Two numbers were deliberately **not** written for this reason — the row span in nm and the new stagger as a percentage — and are given as `109 bp` and as the stagger itself |
| **F5** | a deliverable contradicts **itself** after the edit | **FIRED TWICE, and repaired in the pass** | §8's prose still read *"which is what makes a 3.57 % ambiguity a question rather than a rounding"* two paragraphs above the table that had just restated the stagger at 7 bp, and its opening sentence still named *"the tile is 38.08 nm along the helices"* as one of the two true readings four lines above the block that restates the row span at `109 bp`. The tracer cannot see either — neither half cites a task — which is `C-0080`'s class exactly, and both were found by re-reading the edited section end to end rather than by any check |
| **F6** | the preference/prohibition distinction collapses somewhere — a passage reads closure as a proof that `112 / 108` is unbuildable | **no** | both documents state it as a preference, in a set-off block, with `C-0152`'s ceiling and its unpriced kinetic half beside it |

## 7. What this pass did NOT do

- **`C-0147`'s ragged-face cost is not re-run at 7 bp.** Its coefficient on §3's flatness is exactly
  zero either way — the raggedness is on the tile's **rim** — and the residual rim bound scales with
  the relief, so the 496× margin is not in question; but the number is `T-258`'s and both documents
  say so rather than interpolating one.
- **`T-234`'s `WIDTH` family was not touched.** Its advisory debt line went **23 → 32** and every
  one of the nine new occurrences is **this pass's own correcting sentence** — `CH-0182` for the
  sixth consecutive iteration, and the second time it has fired on a *restoration* rather than on a
  new premise. The gate itself is at **0 defects** after regenerating
  `tools/T-234-classification.json` from its stated rules, which is what `C-0149` did for the same
  reason. Recorded here and queued as `T-262`; the family belongs to `T-234`.
- **No result file is re-emitted and no study is run.** `CH-0202` is read out of `C-0151`'s existing
  `planCeilings` block and moves no number in it.
- **`tools/verify.sh --drop-file src/main/kotlin/tile/HoneycombGrillageStudy.kt`: BUILD SUCCESSFUL
  in 22 m 3 s**, with every wired gate clean — the challenge index, the corpus links, the
  deliverable tracer's self-tests, the format-string checker (19 of 19), the harness (19 checks),
  the markdown tables and the result-file hygiene checker (128 of 128). The drop is a **sibling's**
  in-flight study main (`T-253`), whose first run of the same command failed `compileKotlin` on it;
  nothing under `src/` is touched by this claim. The markdown-only edits made after that snapshot
  were re-checked by hand against the same gate commands on the live tree: **0** broken links,
  **0** table defects, the challenge index complete, and the tracer at **0 ABSENT / 0 contradicted /
  0 self-contradiction** on both documents. Two gate failures standing in the live tree at the close
  of this pass are **siblings' work in flight and not this task's** — `check-result-file-hygiene.py
  --prose` on `T-3b` (`T-250`), and `T-234-census.py --check` at 3 defects, all three of them
  `C-0154`'s occurrences filed after the classification was regenerated (`T-253`, and `T-260`'s own
  subject).

## 8. Validity range

- **Nothing here is physics and nothing is re-derived.** A restatement is only as good as the claim
  it reads, and every claim read is `PASS` at TRL 1–3 — model-consistent, never measured.
- **The reconciliation is complete against `C-0151`, `C-0152` and `C-0148` and NOT against the
  corpus.** It was scoped by a token list, so a premise a deliverable states **in words** and not in
  figures is not found by it — `C-0145`'s and `C-0149`'s validity note, unchanged.
- **The iteration-35 and iteration-36 restatement paragraphs of `ANSWERS.md` §1 are retained
  unstruck except for the one withdrawn price**, per the corpus's layered-restatement convention:
  each is superseded by the next and the iteration-38 paragraph states what moved. A reader who
  stops at the first paragraph reads a superseded pair, which is the standing cost of that
  convention and is not new here.
- **`CH-0202` does not move `C-0151`'s recommendation**, which is selected on axes the plan ceiling
  does not decide; only the size of the margin on that one axis moves.
- **`CH-0203` does not withdraw decision 8.** Both readings are still true of the same object and
  §3 still states one number; what is withdrawn is the threshold that gave the question a cost.
